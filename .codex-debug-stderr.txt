OpenAI Codex v0.130.0
--------
workdir: C:\Users\sutec\IdeaProjects\untitled
model: gpt-5.5
provider: openai
approval: never
sandbox: workspace-write [workdir, C:\Users\sutec\.codex\memories]
reasoning effort: high
reasoning summaries: none
session id: 019e15e6-25b6-76a0-ab49-dad2ba7b0f38
--------
user
Read .codex-current-prompt.md and follow its instructions exactly. Output only the requested result.
exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Raw .codex-current-prompt.md' in C:\Users\sutec\IdeaProjects\untitled
 succeeded in 645ms:
あなたはWorkerです。
次のGitHubサブIssueだけを実装してください。

絶対ルール:
- このサブIssueに書かれている範囲だけ実装する
- 親Issue全体を一気に実装しない
- ついで修正をしない
- 大きな設計変更、DB変更、認証/権限変更、外部API仕様変更が必要なら、実装せず理由を書いて止まる
- 変更後に可能な範囲でテスト、lint、build、または動作確認コマンドを実行する
- 最後に、変更内容・確認結果・残課題を短くまとめる

GitHubサブIssue:
Number: #3
URL: https://github.com/Onlysuuji/BBBB/issues/3
Title: [#1-2] アイテムA/B切り替え制御の基盤実装
Body:
Parent: #1
Order: 2
Depends-on: sequential

## Goal
指定したアイテムAとアイテムBを検出し、手動または設定に基づいて安全に入れ替える基盤を作る

## Scope
インベントリ内のアイテムA/B検出、ホットバー切り替え、基本設定の読み込みまでを実装する。エンチャント効果の維持判定、効果切れ時の自動ループ、詳細UIは含めない

## Acceptance Criteria
- 設定されたアイテムAとアイテムBをインベントリまたはホットバーから検出できる
- プレイヤー操作を壊さずにアイテムA/Bを切り替えられる
- 対象アイテムが存在しない場合に何もしない、または安全に失敗する

## Risk
プレイヤーの通常操作と競合すると誤切り替えが発生する可能性がある。クライアントとサーバーの同期ずれにも注意が必要

## Test Hint
ローカル環境でアイテムA/Bの有無、ホットバー位置の違い、インベントリ満杯時の挙動を確認する

---
Generated by local Codex auto worker.


exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-ChildItem -Force' in C:\Users\sutec\IdeaProjects\untitled
exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'rg --files' in C:\Users\sutec\IdeaProjects\untitled
exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'git status --short' in C:\Users\sutec\IdeaProjects\untitled
 succeeded in 567ms:
AM codex_auto_worker.ps1
?? .codex-current-prompt.md
?? .codex-debug-stderr.txt
?? .codex-debug-stdout.txt
?? .codex-plan-output.txt
?? codex_auto_worker_fixed.ps1

 succeeded in 784ms:


    ディレクトリ: C:\Users\sutec\IdeaProjects\untitled


Mode                 LastWriteTime         Length Name                                                                 
----                 -------------         ------ ----                                                                 
d--h--        2026/05/11     16:17                .git                                                                 
d-----        2026/05/11     14:55                .gradle                                                              
d-----        2026/05/11     16:13                .idea                                                                
d-----        2026/05/11     14:47                build                                                                
d-----        2026/05/11     14:32                gradle                                                               
d-----        2026/05/11     14:49                run                                                                  
d-----        2026/05/11     14:32                src                                                                  
-a----        2026/05/11     16:17           1822 .codex-current-prompt.md                                             
-a----        2026/05/11     16:17           2411 .codex-debug-stderr.txt                                              
-a----        2026/05/11     16:17              0 .codex-debug-stdout.txt                                              
-a----        2026/05/11     16:14           3700 .codex-plan-output.txt                                               
-a----        2026/05/11     14:58            633 .gitignore                                                           
-a----        2026/05/11     14:46           3265 build.gradle                                                         
-a----        2026/05/11     16:17          26380 codex_auto_worker.ps1                                                
-a----        2026/05/11     15:16          16345 codex_auto_worker_fixed.ps1                                          
-a----        2026/05/11     14:33            410 gradle.properties                                                    
-a----        2026/05/11     14:32             41 LICENSE.txt                                                          
-a----        2026/05/11     14:56             18 README.md                                                            
-a----        2026/05/11     14:32            177 settings.gradle                                                      



2026-05-11T07:17:58.934109Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 3.5 seconds
Output:
rg : 用語 'rg' は、コマンドレット、関数、スクリプト ファイル、または操作可能なプログラムの名前として認識されません。名前が正しく記述されていることを確認し、パスが含まれている場合はそのパスが正しいことを確認してから、再試行して
ください。
発生場所 行:2 文字:1
+ rg --files
+ ~~
    + CategoryInfo          : ObjectNotFound: (rg:String) [], CommandNotFoundException
    + FullyQualifiedErrorId : CommandNotFoundException
 

 exited 1 in 3455ms:
rg : 用語 'rg' は、コマンドレット、関数、スクリプト ファイル、または操作可能なプログラムの名前として認識されません。名前が正しく記述されていることを確認し、パスが含まれている場合はそのパスが正しいことを確認してから、再試行して
ください。
発生場所 行:2 文字:1
+ rg --files
+ ~~
    + CategoryInfo          : ObjectNotFound: (rg:String) [], CommandNotFoundException
    + FullyQualifiedErrorId : CommandNotFoundException
 

exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-ChildItem -Recurse -File | Select-Object -ExpandProperty FullName' in C:\Users\sutec\IdeaProjects\untitled
 succeeded in 746ms:
C:\Users\sutec\IdeaProjects\untitled\.codex-current-prompt.md
C:\Users\sutec\IdeaProjects\untitled\.codex-debug-stderr.txt
C:\Users\sutec\IdeaProjects\untitled\.codex-debug-stdout.txt
C:\Users\sutec\IdeaProjects\untitled\.codex-plan-output.txt
C:\Users\sutec\IdeaProjects\untitled\.gitignore
C:\Users\sutec\IdeaProjects\untitled\build.gradle
C:\Users\sutec\IdeaProjects\untitled\codex_auto_worker.ps1
C:\Users\sutec\IdeaProjects\untitled\codex_auto_worker_fixed.ps1
C:\Users\sutec\IdeaProjects\untitled\gradle.properties
C:\Users\sutec\IdeaProjects\untitled\LICENSE.txt
C:\Users\sutec\IdeaProjects\untitled\README.md
C:\Users\sutec\IdeaProjects\untitled\settings.gradle
C:\Users\sutec\IdeaProjects\untitled\.gradle\file-system.probe
C:\Users\sutec\IdeaProjects\untitled\.gradle\8.10.2\gc.properties
C:\Users\sutec\IdeaProjects\untitled\.gradle\8.10.2\checksums\checksums.lock
C:\Users\sutec\IdeaProjects\untitled\.gradle\8.10.2\checksums\md5-checksums.bin
C:\Users\sutec\IdeaProjects\untitled\.gradle\8.10.2\checksums\sha1-checksums.bin
C:\Users\sutec\IdeaProjects\untitled\.gradle\8.10.2\dependencies-accessors\gc.properties
C:\Users\sutec\IdeaProjects\untitled\.gradle\8.10.2\executionHistory\executionHistory.bin
C:\Users\sutec\IdeaProjects\untitled\.gradle\8.10.2\executionHistory\executionHistory.lock
C:\Users\sutec\IdeaProjects\untitled\.gradle\8.10.2\fileChanges\last-build.bin
C:\Users\sutec\IdeaProjects\untitled\.gradle\8.10.2\fileHashes\fileHashes.bin
C:\Users\sutec\IdeaProjects\untitled\.gradle\8.10.2\fileHashes\fileHashes.lock
C:\Users\sutec\IdeaProjects\untitled\.gradle\buildOutputCleanup\buildOutputCleanup.lock
C:\Users\sutec\IdeaProjects\untitled\.gradle\buildOutputCleanup\cache.properties
C:\Users\sutec\IdeaProjects\untitled\.gradle\buildOutputCleanup\outputFiles.bin
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\launch.cfg
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\log4j.xml
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapClasspath.txt
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\minecraftMaven\net\minecraft\minecraft-clientOnly-8bc85877dd\1.21.1-net.fabricmc.yarn.1_21_1.1.21.1+build.3-v2\minecraft-clientOnly-8bc85877dd-1.21.1-net.fabricmc.yarn.1_21_1.1.21.1+build.3-v2.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\minecraftMaven\net\minecraft\minecraft-clientOnly-8bc85877dd\1.21.1-net.fabricmc.yarn.1_21_1.1.21.1+build.3-v2\minecraft-clientOnly-8bc85877dd-1.21.1-net.fabricmc.yarn.1_21_1.1.21.1+build.3-v2.jar.backup
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\minecraftMaven\net\minecraft\minecraft-clientOnly-8bc85877dd\1.21.1-net.fabricmc.yarn.1_21_1.1.21.1+build.3-v2\minecraft-clientOnly-8bc85877dd-1.21.1-net.fabricmc.yarn.1_21_1.1.21.1+build.3-v2.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\minecraftMaven\net\minecraft\minecraft-common-8bc85877dd\1.21.1-net.fabricmc.yarn.1_21_1.1.21.1+build.3-v2\minecraft-common-8bc85877dd-1.21.1-net.fabricmc.yarn.1_21_1.1.21.1+build.3-v2.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\minecraftMaven\net\minecraft\minecraft-common-8bc85877dd\1.21.1-net.fabricmc.yarn.1_21_1.1.21.1+build.3-v2\minecraft-common-8bc85877dd-1.21.1-net.fabricmc.yarn.1_21_1.1.21.1+build.3-v2.jar.backup
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\minecraftMaven\net\minecraft\minecraft-common-8bc85877dd\1.21.1-net.fabricmc.yarn.1_21_1.1.21.1+build.3-v2\minecraft-common-8bc85877dd-1.21.1-net.fabricmc.yarn.1_21_1.1.21.1+build.3-v2.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-api-base-common\0.4.42+6573ed8c19\fabric-api-base-common-0.4.42+6573ed8c19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-api-base-common\0.4.42+6573ed8c19\fabric-api-base-common-0.4.42+6573ed8c19.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-api-base-common\0.4.42+6573ed8c19\fabric-api-base-common-0.4.42+6573ed8c19.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-api-common\0.116.11+1.21.1\fabric-api-common-0.116.11+1.21.1-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-api-common\0.116.11+1.21.1\fabric-api-common-0.116.11+1.21.1.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-api-common\0.116.11+1.21.1\fabric-api-common-0.116.11+1.21.1.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-api-lookup-api-v1-common\1.6.72+7b3d111d19\fabric-api-lookup-api-v1-common-1.6.72+7b3d111d19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-api-lookup-api-v1-common\1.6.72+7b3d111d19\fabric-api-lookup-api-v1-common-1.6.72+7b3d111d19.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-api-lookup-api-v1-common\1.6.72+7b3d111d19\fabric-api-lookup-api-v1-common-1.6.72+7b3d111d19.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-biome-api-v1-common\13.0.31+d527f9fd19\fabric-biome-api-v1-common-13.0.31+d527f9fd19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-biome-api-v1-common\13.0.31+d527f9fd19\fabric-biome-api-v1-common-13.0.31+d527f9fd19.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-biome-api-v1-common\13.0.31+d527f9fd19\fabric-biome-api-v1-common-13.0.31+d527f9fd19.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-block-api-v1-common\1.1.0+0bc3503219\fabric-block-api-v1-common-1.1.0+0bc3503219-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-block-api-v1-common\1.1.0+0bc3503219\fabric-block-api-v1-common-1.1.0+0bc3503219.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-block-api-v1-common\1.1.0+0bc3503219\fabric-block-api-v1-common-1.1.0+0bc3503219.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-block-view-api-v2-client\1.0.11+ebb2264e19\fabric-block-view-api-v2-client-1.0.11+ebb2264e19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-block-view-api-v2-client\1.0.11+ebb2264e19\fabric-block-view-api-v2-client-1.0.11+ebb2264e19.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-block-view-api-v2-client\1.0.11+ebb2264e19\fabric-block-view-api-v2-client-1.0.11+ebb2264e19.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-block-view-api-v2-common\1.0.11+ebb2264e19\fabric-block-view-api-v2-common-1.0.11+ebb2264e19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-block-view-api-v2-common\1.0.11+ebb2264e19\fabric-block-view-api-v2-common-1.0.11+ebb2264e19.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-block-view-api-v2-common\1.0.11+ebb2264e19\fabric-block-view-api-v2-common-1.0.11+ebb2264e19.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-blockrenderlayer-v1-client\1.1.52+0af3f5a719\fabric-blockrenderlayer-v1-client-1.1.52+0af3f5a719-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-blockrenderlayer-v1-client\1.1.52+0af3f5a719\fabric-blockrenderlayer-v1-client-1.1.52+0af3f5a719.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-blockrenderlayer-v1-client\1.1.52+0af3f5a719\fabric-blockrenderlayer-v1-client-1.1.52+0af3f5a719.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-client-tags-api-v1-client\1.1.15+6573ed8c19\fabric-client-tags-api-v1-client-1.1.15+6573ed8c19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-client-tags-api-v1-client\1.1.15+6573ed8c19\fabric-client-tags-api-v1-client-1.1.15+6573ed8c19.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-client-tags-api-v1-client\1.1.15+6573ed8c19\fabric-client-tags-api-v1-client-1.1.15+6573ed8c19.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-command-api-v1-common\1.2.49+f71b366f19\fabric-command-api-v1-common-1.2.49+f71b366f19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-command-api-v1-common\1.2.49+f71b366f19\fabric-command-api-v1-common-1.2.49+f71b366f19.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-command-api-v1-common\1.2.49+f71b366f19\fabric-command-api-v1-common-1.2.49+f71b366f19.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-command-api-v2-client\2.2.28+6ced4dd919\fabric-command-api-v2-client-2.2.28+6ced4dd919-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-command-api-v2-client\2.2.28+6ced4dd919\fabric-command-api-v2-client-2.2.28+6ced4dd919.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-command-api-v2-client\2.2.28+6ced4dd919\fabric-command-api-v2-client-2.2.28+6ced4dd919.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-command-api-v2-common\2.2.28+6ced4dd919\fabric-command-api-v2-common-2.2.28+6ced4dd919-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-command-api-v2-common\2.2.28+6ced4dd919\fabric-command-api-v2-common-2.2.28+6ced4dd919.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-command-api-v2-common\2.2.28+6ced4dd919\fabric-command-api-v2-common-2.2.28+6ced4dd919.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-commands-v0-common\0.2.66+df3654b319\fabric-commands-v0-common-0.2.66+df3654b319-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-commands-v0-common\0.2.66+df3654b319\fabric-commands-v0-common-0.2.66+df3654b319.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-commands-v0-common\0.2.66+df3654b319\fabric-commands-v0-common-0.2.66+df3654b319.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-content-registries-v0-common\8.0.19+b559734419\fabric-content-registries-v0-common-8.0.19+b559734419-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-content-registries-v0-common\8.0.19+b559734419\fabric-content-registries-v0-common-8.0.19+b559734419.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-content-registries-v0-common\8.0.19+b559734419\fabric-content-registries-v0-common-8.0.19+b559734419.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-convention-tags-v1-common\2.1.7+7f945d5b19\fabric-convention-tags-v1-common-2.1.7+7f945d5b19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-convention-tags-v1-common\2.1.7+7f945d5b19\fabric-convention-tags-v1-common-2.1.7+7f945d5b19.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-convention-tags-v1-common\2.1.7+7f945d5b19\fabric-convention-tags-v1-common-2.1.7+7f945d5b19.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-convention-tags-v2-common\2.12.0+c3656daa19\fabric-convention-tags-v2-common-2.12.0+c3656daa19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-convention-tags-v2-common\2.12.0+c3656daa19\fabric-convention-tags-v2-common-2.12.0+c3656daa19.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-convention-tags-v2-common\2.12.0+c3656daa19\fabric-convention-tags-v2-common-2.12.0+c3656daa19.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-crash-report-info-v1-common\0.2.29+0af3f5a719\fabric-crash-report-info-v1-common-0.2.29+0af3f5a719-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-crash-report-info-v1-common\0.2.29+0af3f5a719\fabric-crash-report-info-v1-common-0.2.29+0af3f5a719.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-crash-report-info-v1-common\0.2.29+0af3f5a719\fabric-crash-report-info-v1-common-0.2.29+0af3f5a719.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-data-attachment-api-v1-client\1.4.6+5b36e0f719\fabric-data-attachment-api-v1-client-1.4.6+5b36e0f719-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-data-attachment-api-v1-client\1.4.6+5b36e0f719\fabric-data-attachment-api-v1-client-1.4.6+5b36e0f719.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-data-attachment-api-v1-client\1.4.6+5b36e0f719\fabric-data-attachment-api-v1-client-1.4.6+5b36e0f719.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-data-attachment-api-v1-common\1.4.6+5b36e0f719\fabric-data-attachment-api-v1-common-1.4.6+5b36e0f719-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-data-attachment-api-v1-common\1.4.6+5b36e0f719\fabric-data-attachment-api-v1-common-1.4.6+5b36e0f719.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-data-attachment-api-v1-common\1.4.6+5b36e0f719\fabric-data-attachment-api-v1-common-1.4.6+5b36e0f719.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-data-generation-api-v1-client\20.2.37+60a6976219\fabric-data-generation-api-v1-client-20.2.37+60a6976219-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-data-generation-api-v1-client\20.2.37+60a6976219\fabric-data-generation-api-v1-client-20.2.37+60a6976219.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-data-generation-api-v1-client\20.2.37+60a6976219\fabric-data-generation-api-v1-client-20.2.37+60a6976219.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-data-generation-api-v1-common\20.2.37+60a6976219\fabric-data-generation-api-v1-common-20.2.37+60a6976219-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-data-generation-api-v1-common\20.2.37+60a6976219\fabric-data-generation-api-v1-common-20.2.37+60a6976219.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-data-generation-api-v1-common\20.2.37+60a6976219\fabric-data-generation-api-v1-common-20.2.37+60a6976219.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-dimensions-v1-common\4.0.1+65213ef819\fabric-dimensions-v1-common-4.0.1+65213ef819-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-dimensions-v1-common\4.0.1+65213ef819\fabric-dimensions-v1-common-4.0.1+65213ef819.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-dimensions-v1-common\4.0.1+65213ef819\fabric-dimensions-v1-common-4.0.1+65213ef819.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-entity-events-v1-client\1.8.0+2b27e0a419\fabric-entity-events-v1-client-1.8.0+2b27e0a419-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-entity-events-v1-client\1.8.0+2b27e0a419\fabric-entity-events-v1-client-1.8.0+2b27e0a419.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-entity-events-v1-client\1.8.0+2b27e0a419\fabric-entity-events-v1-client-1.8.0+2b27e0a419.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-entity-events-v1-common\1.8.0+2b27e0a419\fabric-entity-events-v1-common-1.8.0+2b27e0a419-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-entity-events-v1-common\1.8.0+2b27e0a419\fabric-entity-events-v1-common-1.8.0+2b27e0a419.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-entity-events-v1-common\1.8.0+2b27e0a419\fabric-entity-events-v1-common-1.8.0+2b27e0a419.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-events-interaction-v0-client\0.7.13+ba9dae0619\fabric-events-interaction-v0-client-0.7.13+ba9dae0619-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-events-interaction-v0-client\0.7.13+ba9dae0619\fabric-events-interaction-v0-client-0.7.13+ba9dae0619.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-events-interaction-v0-client\0.7.13+ba9dae0619\fabric-events-interaction-v0-client-0.7.13+ba9dae0619.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-events-interaction-v0-common\0.7.13+ba9dae0619\fabric-events-interaction-v0-common-0.7.13+ba9dae0619-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-events-interaction-v0-common\0.7.13+ba9dae0619\fabric-events-interaction-v0-common-0.7.13+ba9dae0619.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-events-interaction-v0-common\0.7.13+ba9dae0619\fabric-events-interaction-v0-common-0.7.13+ba9dae0619.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-game-rule-api-v1-client\1.0.53+6ced4dd919\fabric-game-rule-api-v1-client-1.0.53+6ced4dd919-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-game-rule-api-v1-client\1.0.53+6ced4dd919\fabric-game-rule-api-v1-client-1.0.53+6ced4dd919.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-game-rule-api-v1-client\1.0.53+6ced4dd919\fabric-game-rule-api-v1-client-1.0.53+6ced4dd919.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-game-rule-api-v1-common\1.0.53+6ced4dd919\fabric-game-rule-api-v1-common-1.0.53+6ced4dd919-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-game-rule-api-v1-common\1.0.53+6ced4dd919\fabric-game-rule-api-v1-common-1.0.53+6ced4dd919.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-game-rule-api-v1-common\1.0.53+6ced4dd919\fabric-game-rule-api-v1-common-1.0.53+6ced4dd919.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-gametest-api-v1-common\2.0.5+6fc22b9919\fabric-gametest-api-v1-common-2.0.5+6fc22b9919-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-gametest-api-v1-common\2.0.5+6fc22b9919\fabric-gametest-api-v1-common-2.0.5+6fc22b9919.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-gametest-api-v1-common\2.0.5+6fc22b9919\fabric-gametest-api-v1-common-2.0.5+6fc22b9919.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-item-api-v1-client\11.2.0+3b3cb2e819\fabric-item-api-v1-client-11.2.0+3b3cb2e819-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-item-api-v1-client\11.2.0+3b3cb2e819\fabric-item-api-v1-client-11.2.0+3b3cb2e819.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-item-api-v1-client\11.2.0+3b3cb2e819\fabric-item-api-v1-client-11.2.0+3b3cb2e819.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-item-api-v1-common\11.2.0+3b3cb2e819\fabric-item-api-v1-common-11.2.0+3b3cb2e819-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-item-api-v1-common\11.2.0+3b3cb2e819\fabric-item-api-v1-common-11.2.0+3b3cb2e819.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-item-api-v1-common\11.2.0+3b3cb2e819\fabric-item-api-v1-common-11.2.0+3b3cb2e819.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-item-group-api-v1-client\4.1.7+def88e3a19\fabric-item-group-api-v1-client-4.1.7+def88e3a19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-item-group-api-v1-client\4.1.7+def88e3a19\fabric-item-group-api-v1-client-4.1.7+def88e3a19.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-item-group-api-v1-client\4.1.7+def88e3a19\fabric-item-group-api-v1-client-4.1.7+def88e3a19.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-item-group-api-v1-common\4.1.7+def88e3a19\fabric-item-group-api-v1-common-4.1.7+def88e3a19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-item-group-api-v1-common\4.1.7+def88e3a19\fabric-item-group-api-v1-common-4.1.7+def88e3a19.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-item-group-api-v1-common\4.1.7+def88e3a19\fabric-item-group-api-v1-common-4.1.7+def88e3a19.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-key-binding-api-v1-client\1.0.47+0af3f5a719\fabric-key-binding-api-v1-client-1.0.47+0af3f5a719-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-key-binding-api-v1-client\1.0.47+0af3f5a719\fabric-key-binding-api-v1-client-1.0.47+0af3f5a719.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-key-binding-api-v1-client\1.0.47+0af3f5a719\fabric-key-binding-api-v1-client-1.0.47+0af3f5a719.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-keybindings-v0-client\0.2.45+df3654b319\fabric-keybindings-v0-client-0.2.45+df3654b319-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-keybindings-v0-client\0.2.45+df3654b319\fabric-keybindings-v0-client-0.2.45+df3654b319.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-keybindings-v0-client\0.2.45+df3654b319\fabric-keybindings-v0-client-0.2.45+df3654b319.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-lifecycle-events-v1-client\2.6.0+0865547519\fabric-lifecycle-events-v1-client-2.6.0+0865547519-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-lifecycle-events-v1-client\2.6.0+0865547519\fabric-lifecycle-events-v1-client-2.6.0+0865547519.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-lifecycle-events-v1-client\2.6.0+0865547519\fabric-lifecycle-events-v1-client-2.6.0+0865547519.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-lifecycle-events-v1-common\2.6.0+0865547519\fabric-lifecycle-events-v1-common-2.6.0+0865547519-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-lifecycle-events-v1-common\2.6.0+0865547519\fabric-lifecycle-events-v1-common-2.6.0+0865547519.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-lifecycle-events-v1-common\2.6.0+0865547519\fabric-lifecycle-events-v1-common-2.6.0+0865547519.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-loot-api-v2-common\3.0.15+3f89f5a519\fabric-loot-api-v2-common-3.0.15+3f89f5a519-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-loot-api-v2-common\3.0.15+3f89f5a519\fabric-loot-api-v2-common-3.0.15+3f89f5a519.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-loot-api-v2-common\3.0.15+3f89f5a519\fabric-loot-api-v2-common-3.0.15+3f89f5a519.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-loot-api-v3-common\1.0.3+3f89f5a519\fabric-loot-api-v3-common-1.0.3+3f89f5a519-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-loot-api-v3-common\1.0.3+3f89f5a519\fabric-loot-api-v3-common-1.0.3+3f89f5a519.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-loot-api-v3-common\1.0.3+3f89f5a519\fabric-loot-api-v3-common-1.0.3+3f89f5a519.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-message-api-v1-client\6.0.14+8aaf3aca19\fabric-message-api-v1-client-6.0.14+8aaf3aca19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-message-api-v1-client\6.0.14+8aaf3aca19\fabric-message-api-v1-client-6.0.14+8aaf3aca19.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-message-api-v1-client\6.0.14+8aaf3aca19\fabric-message-api-v1-client-6.0.14+8aaf3aca19.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-message-api-v1-common\6.0.14+8aaf3aca19\fabric-message-api-v1-common-6.0.14+8aaf3aca19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-message-api-v1-common\6.0.14+8aaf3aca19\fabric-message-api-v1-common-6.0.14+8aaf3aca19.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-message-api-v1-common\6.0.14+8aaf3aca19\fabric-message-api-v1-common-6.0.14+8aaf3aca19.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-model-loading-api-v1-client\2.1.0+b4d813fc19\fabric-model-loading-api-v1-client-2.1.0+b4d813fc19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-model-loading-api-v1-client\2.1.0+b4d813fc19\fabric-model-loading-api-v1-client-2.1.0+b4d813fc19.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-model-loading-api-v1-client\2.1.0+b4d813fc19\fabric-model-loading-api-v1-client-2.1.0+b4d813fc19.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-networking-api-v1-client\4.3.0+c7469b2119\fabric-networking-api-v1-client-4.3.0+c7469b2119-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-networking-api-v1-client\4.3.0+c7469b2119\fabric-networking-api-v1-client-4.3.0+c7469b2119.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-networking-api-v1-client\4.3.0+c7469b2119\fabric-networking-api-v1-client-4.3.0+c7469b2119.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-networking-api-v1-common\4.3.0+c7469b2119\fabric-networking-api-v1-common-4.3.0+c7469b2119-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-networking-api-v1-common\4.3.0+c7469b2119\fabric-networking-api-v1-common-4.3.0+c7469b2119.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-networking-api-v1-common\4.3.0+c7469b2119\fabric-networking-api-v1-common-4.3.0+c7469b2119.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-object-builder-api-v1-client\15.2.1+40875a9319\fabric-object-builder-api-v1-client-15.2.1+40875a9319-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-object-builder-api-v1-client\15.2.1+40875a9319\fabric-object-builder-api-v1-client-15.2.1+40875a9319.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-object-builder-api-v1-client\15.2.1+40875a9319\fabric-object-builder-api-v1-client-15.2.1+40875a9319.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-object-builder-api-v1-common\15.2.1+40875a9319\fabric-object-builder-api-v1-common-15.2.1+40875a9319-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-object-builder-api-v1-common\15.2.1+40875a9319\fabric-object-builder-api-v1-common-15.2.1+40875a9319.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-object-builder-api-v1-common\15.2.1+40875a9319\fabric-object-builder-api-v1-common-15.2.1+40875a9319.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-particles-v1-client\4.0.2+6573ed8c19\fabric-particles-v1-client-4.0.2+6573ed8c19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-particles-v1-client\4.0.2+6573ed8c19\fabric-particles-v1-client-4.0.2+6573ed8c19.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-particles-v1-client\4.0.2+6573ed8c19\fabric-particles-v1-client-4.0.2+6573ed8c19.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-particles-v1-common\4.0.2+6573ed8c19\fabric-particles-v1-common-4.0.2+6573ed8c19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-particles-v1-common\4.0.2+6573ed8c19\fabric-particles-v1-common-4.0.2+6573ed8c19.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-particles-v1-common\4.0.2+6573ed8c19\fabric-particles-v1-common-4.0.2+6573ed8c19.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-recipe-api-v1-client\5.0.15+2475392c19\fabric-recipe-api-v1-client-5.0.15+2475392c19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-recipe-api-v1-client\5.0.15+2475392c19\fabric-recipe-api-v1-client-5.0.15+2475392c19.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-recipe-api-v1-client\5.0.15+2475392c19\fabric-recipe-api-v1-client-5.0.15+2475392c19.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-recipe-api-v1-common\5.0.15+2475392c19\fabric-recipe-api-v1-common-5.0.15+2475392c19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-recipe-api-v1-common\5.0.15+2475392c19\fabric-recipe-api-v1-common-5.0.15+2475392c19.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-recipe-api-v1-common\5.0.15+2475392c19\fabric-recipe-api-v1-common-5.0.15+2475392c19.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-registry-sync-v0-client\5.3.1+e3eddc2119\fabric-registry-sync-v0-client-5.3.1+e3eddc2119-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-registry-sync-v0-client\5.3.1+e3eddc2119\fabric-registry-sync-v0-client-5.3.1+e3eddc2119.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-registry-sync-v0-client\5.3.1+e3eddc2119\fabric-registry-sync-v0-client-5.3.1+e3eddc2119.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-registry-sync-v0-common\5.3.1+e3eddc2119\fabric-registry-sync-v0-common-5.3.1+e3eddc2119-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-registry-sync-v0-common\5.3.1+e3eddc2119\fabric-registry-sync-v0-common-5.3.1+e3eddc2119.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-registry-sync-v0-common\5.3.1+e3eddc2119\fabric-registry-sync-v0-common-5.3.1+e3eddc2119.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-renderer-api-v1-client\3.4.1+b4d813fc19\fabric-renderer-api-v1-client-3.4.1+b4d813fc19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-renderer-api-v1-client\3.4.1+b4d813fc19\fabric-renderer-api-v1-client-3.4.1+b4d813fc19.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-renderer-api-v1-client\3.4.1+b4d813fc19\fabric-renderer-api-v1-client-3.4.1+b4d813fc19.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-renderer-indigo-client\1.7.1+c705a49c19\fabric-renderer-indigo-client-1.7.1+c705a49c19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-renderer-indigo-client\1.7.1+c705a49c19\fabric-renderer-indigo-client-1.7.1+c705a49c19.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-renderer-indigo-client\1.7.1+c705a49c19\fabric-renderer-indigo-client-1.7.1+c705a49c19.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-renderer-registries-v1-client\3.2.69+df3654b319\fabric-renderer-registries-v1-client-3.2.69+df3654b319-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-renderer-registries-v1-client\3.2.69+df3654b319\fabric-renderer-registries-v1-client-3.2.69+df3654b319.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-renderer-registries-v1-client\3.2.69+df3654b319\fabric-renderer-registries-v1-client-3.2.69+df3654b319.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-rendering-data-attachment-v1-client\0.3.49+73761d2e19\fabric-rendering-data-attachment-v1-client-0.3.49+73761d2e19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-rendering-data-attachment-v1-client\0.3.49+73761d2e19\fabric-rendering-data-attachment-v1-client-0.3.49+73761d2e19.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-rendering-data-attachment-v1-client\0.3.49+73761d2e19\fabric-rendering-data-attachment-v1-client-0.3.49+73761d2e19.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-rendering-data-attachment-v1-common\0.3.49+73761d2e19\fabric-rendering-data-attachment-v1-common-0.3.49+73761d2e19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-rendering-data-attachment-v1-common\0.3.49+73761d2e19\fabric-rendering-data-attachment-v1-common-0.3.49+73761d2e19.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-rendering-data-attachment-v1-common\0.3.49+73761d2e19\fabric-rendering-data-attachment-v1-common-0.3.49+73761d2e19.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-rendering-fluids-v1-client\3.1.6+1daea21519\fabric-rendering-fluids-v1-client-3.1.6+1daea21519-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-rendering-fluids-v1-client\3.1.6+1daea21519\fabric-rendering-fluids-v1-client-3.1.6+1daea21519.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-rendering-fluids-v1-client\3.1.6+1daea21519\fabric-rendering-fluids-v1-client-3.1.6+1daea21519.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-rendering-fluids-v1-common\3.1.6+1daea21519\fabric-rendering-fluids-v1-common-3.1.6+1daea21519-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-rendering-fluids-v1-common\3.1.6+1daea21519\fabric-rendering-fluids-v1-common-3.1.6+1daea21519.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-rendering-fluids-v1-common\3.1.6+1daea21519\fabric-rendering-fluids-v1-common-3.1.6+1daea21519.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-rendering-v0-client\1.1.72+df3654b319\fabric-rendering-v0-client-1.1.72+df3654b319-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-rendering-v0-client\1.1.72+df3654b319\fabric-rendering-v0-client-1.1.72+df3654b319.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-rendering-v0-client\1.1.72+df3654b319\fabric-rendering-v0-client-1.1.72+df3654b319.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-rendering-v1-client\5.1.0+ab4c25a019\fabric-rendering-v1-client-5.1.0+ab4c25a019-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-rendering-v1-client\5.1.0+ab4c25a019\fabric-rendering-v1-client-5.1.0+ab4c25a019.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-rendering-v1-client\5.1.0+ab4c25a019\fabric-rendering-v1-client-5.1.0+ab4c25a019.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-resource-conditions-api-v1-common\4.3.0+8dc279b119\fabric-resource-conditions-api-v1-common-4.3.0+8dc279b119-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-resource-conditions-api-v1-common\4.3.0+8dc279b119\fabric-resource-conditions-api-v1-common-4.3.0+8dc279b119.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-resource-conditions-api-v1-common\4.3.0+8dc279b119\fabric-resource-conditions-api-v1-common-4.3.0+8dc279b119.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-resource-loader-v0-client\1.3.1+5b5275af19\fabric-resource-loader-v0-client-1.3.1+5b5275af19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-resource-loader-v0-client\1.3.1+5b5275af19\fabric-resource-loader-v0-client-1.3.1+5b5275af19.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-resource-loader-v0-client\1.3.1+5b5275af19\fabric-resource-loader-v0-client-1.3.1+5b5275af19.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-resource-loader-v0-common\1.3.1+5b5275af19\fabric-resource-loader-v0-common-1.3.1+5b5275af19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-resource-loader-v0-common\1.3.1+5b5275af19\fabric-resource-loader-v0-common-1.3.1+5b5275af19.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-resource-loader-v0-common\1.3.1+5b5275af19\fabric-resource-loader-v0-common-1.3.1+5b5275af19.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-screen-api-v1-client\2.0.25+8b68f1c719\fabric-screen-api-v1-client-2.0.25+8b68f1c719-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-screen-api-v1-client\2.0.25+8b68f1c719\fabric-screen-api-v1-client-2.0.25+8b68f1c719.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-screen-api-v1-client\2.0.25+8b68f1c719\fabric-screen-api-v1-client-2.0.25+8b68f1c719.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-screen-handler-api-v1-client\1.3.90+b559734419\fabric-screen-handler-api-v1-client-1.3.90+b559734419-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-screen-handler-api-v1-client\1.3.90+b559734419\fabric-screen-handler-api-v1-client-1.3.90+b559734419.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-screen-handler-api-v1-client\1.3.90+b559734419\fabric-screen-handler-api-v1-client-1.3.90+b559734419.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-screen-handler-api-v1-common\1.3.90+b559734419\fabric-screen-handler-api-v1-common-1.3.90+b559734419-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-screen-handler-api-v1-common\1.3.90+b559734419\fabric-screen-handler-api-v1-common-1.3.90+b559734419.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-screen-handler-api-v1-common\1.3.90+b559734419\fabric-screen-handler-api-v1-common-1.3.90+b559734419.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-sound-api-v1-client\1.0.23+6573ed8c19\fabric-sound-api-v1-client-1.0.23+6573ed8c19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-sound-api-v1-client\1.0.23+6573ed8c19\fabric-sound-api-v1-client-1.0.23+6573ed8c19.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-sound-api-v1-client\1.0.23+6573ed8c19\fabric-sound-api-v1-client-1.0.23+6573ed8c19.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-transfer-api-v1-client\5.4.4+7b3d111d19\fabric-transfer-api-v1-client-5.4.4+7b3d111d19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-transfer-api-v1-client\5.4.4+7b3d111d19\fabric-transfer-api-v1-client-5.4.4+7b3d111d19.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-transfer-api-v1-client\5.4.4+7b3d111d19\fabric-transfer-api-v1-client-5.4.4+7b3d111d19.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-transfer-api-v1-common\5.4.4+7b3d111d19\fabric-transfer-api-v1-common-5.4.4+7b3d111d19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-transfer-api-v1-common\5.4.4+7b3d111d19\fabric-transfer-api-v1-common-5.4.4+7b3d111d19.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-transfer-api-v1-common\5.4.4+7b3d111d19\fabric-transfer-api-v1-common-5.4.4+7b3d111d19.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-transitive-access-wideners-v1-common\6.2.0+45b9699719\fabric-transitive-access-wideners-v1-common-6.2.0+45b9699719-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-transitive-access-wideners-v1-common\6.2.0+45b9699719\fabric-transitive-access-wideners-v1-common-6.2.0+45b9699719.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-transitive-access-wideners-v1-common\6.2.0+45b9699719\fabric-transitive-access-wideners-v1-common-6.2.0+45b9699719.pom
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\source_mappings\8bc85877dd8498fa18631647b020528caffd6df8.tiny
C:\Users\sutec\IdeaProjects\untitled\.gradle\vcs-1\gc.properties
C:\Users\sutec\IdeaProjects\untitled\.idea\.gitignore
C:\Users\sutec\IdeaProjects\untitled\.idea\compiler.xml
C:\Users\sutec\IdeaProjects\untitled\.idea\gradle.xml
C:\Users\sutec\IdeaProjects\untitled\.idea\jarRepositories.xml
C:\Users\sutec\IdeaProjects\untitled\.idea\misc.xml
C:\Users\sutec\IdeaProjects\untitled\.idea\modules.xml
C:\Users\sutec\IdeaProjects\untitled\.idea\vcs.xml
C:\Users\sutec\IdeaProjects\untitled\.idea\workspace.xml
C:\Users\sutec\IdeaProjects\untitled\.idea\modules\untitled.client.iml
C:\Users\sutec\IdeaProjects\untitled\.idea\modules\untitled.main.iml
C:\Users\sutec\IdeaProjects\untitled\.idea\modules\untitled.test.iml
C:\Users\sutec\IdeaProjects\untitled\.idea\runConfigurations\Minecraft_Client.xml
C:\Users\sutec\IdeaProjects\untitled\.idea\runConfigurations\Minecraft_Server.xml
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-api-0.116.11+1.21.1-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-api-0.116.11+1.21.1.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-api-base-0.4.42+6573ed8c19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-api-base-0.4.42+6573ed8c19.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-api-lookup-api-v1-1.6.72+7b3d111d19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-api-lookup-api-v1-1.6.72+7b3d111d19.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-biome-api-v1-13.0.31+d527f9fd19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-biome-api-v1-13.0.31+d527f9fd19.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-block-api-v1-1.1.0+0bc3503219-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-block-api-v1-1.1.0+0bc3503219.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-block-view-api-v2-1.0.11+ebb2264e19-client-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-block-view-api-v2-1.0.11+ebb2264e19-client.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-block-view-api-v2-1.0.11+ebb2264e19-common-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-block-view-api-v2-1.0.11+ebb2264e19-common.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-block-view-api-v2-1.0.11+ebb2264e19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-block-view-api-v2-1.0.11+ebb2264e19.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-blockrenderlayer-v1-1.1.52+0af3f5a719-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-blockrenderlayer-v1-1.1.52+0af3f5a719.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-client-tags-api-v1-1.1.15+6573ed8c19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-client-tags-api-v1-1.1.15+6573ed8c19.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-command-api-v1-1.2.49+f71b366f19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-command-api-v1-1.2.49+f71b366f19.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-command-api-v2-2.2.28+6ced4dd919-client-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-command-api-v2-2.2.28+6ced4dd919-client.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-command-api-v2-2.2.28+6ced4dd919-common-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-command-api-v2-2.2.28+6ced4dd919-common.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-command-api-v2-2.2.28+6ced4dd919-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-command-api-v2-2.2.28+6ced4dd919.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-commands-v0-0.2.66+df3654b319-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-commands-v0-0.2.66+df3654b319.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-content-registries-v0-8.0.19+b559734419-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-content-registries-v0-8.0.19+b559734419.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-convention-tags-v1-2.1.7+7f945d5b19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-convention-tags-v1-2.1.7+7f945d5b19.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-convention-tags-v2-2.12.0+c3656daa19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-convention-tags-v2-2.12.0+c3656daa19.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-crash-report-info-v1-0.2.29+0af3f5a719-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-crash-report-info-v1-0.2.29+0af3f5a719.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-data-attachment-api-v1-1.4.6+5b36e0f719-client-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-data-attachment-api-v1-1.4.6+5b36e0f719-client.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-data-attachment-api-v1-1.4.6+5b36e0f719-common-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-data-attachment-api-v1-1.4.6+5b36e0f719-common.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-data-attachment-api-v1-1.4.6+5b36e0f719-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-data-attachment-api-v1-1.4.6+5b36e0f719.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-data-generation-api-v1-20.2.37+60a6976219-client-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-data-generation-api-v1-20.2.37+60a6976219-client.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-data-generation-api-v1-20.2.37+60a6976219-common-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-data-generation-api-v1-20.2.37+60a6976219-common.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-data-generation-api-v1-20.2.37+60a6976219-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-data-generation-api-v1-20.2.37+60a6976219.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-dimensions-v1-4.0.1+65213ef819-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-dimensions-v1-4.0.1+65213ef819.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-entity-events-v1-1.8.0+2b27e0a419-client-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-entity-events-v1-1.8.0+2b27e0a419-client.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-entity-events-v1-1.8.0+2b27e0a419-common-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-entity-events-v1-1.8.0+2b27e0a419-common.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-entity-events-v1-1.8.0+2b27e0a419-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-entity-events-v1-1.8.0+2b27e0a419.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-events-interaction-v0-0.7.13+ba9dae0619-client-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-events-interaction-v0-0.7.13+ba9dae0619-client.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-events-interaction-v0-0.7.13+ba9dae0619-common-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-events-interaction-v0-0.7.13+ba9dae0619-common.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-events-interaction-v0-0.7.13+ba9dae0619-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-events-interaction-v0-0.7.13+ba9dae0619.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-game-rule-api-v1-1.0.53+6ced4dd919-client-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-game-rule-api-v1-1.0.53+6ced4dd919-client.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-game-rule-api-v1-1.0.53+6ced4dd919-common-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-game-rule-api-v1-1.0.53+6ced4dd919-common.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-game-rule-api-v1-1.0.53+6ced4dd919-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-game-rule-api-v1-1.0.53+6ced4dd919.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-gametest-api-v1-2.0.5+6fc22b9919-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-gametest-api-v1-2.0.5+6fc22b9919.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-item-api-v1-11.2.0+3b3cb2e819-client-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-item-api-v1-11.2.0+3b3cb2e819-client.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-item-api-v1-11.2.0+3b3cb2e819-common-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-item-api-v1-11.2.0+3b3cb2e819-common.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-item-api-v1-11.2.0+3b3cb2e819-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-item-api-v1-11.2.0+3b3cb2e819.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-item-group-api-v1-4.1.7+def88e3a19-client-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-item-group-api-v1-4.1.7+def88e3a19-client.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-item-group-api-v1-4.1.7+def88e3a19-common-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-item-group-api-v1-4.1.7+def88e3a19-common.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-item-group-api-v1-4.1.7+def88e3a19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-item-group-api-v1-4.1.7+def88e3a19.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-key-binding-api-v1-1.0.47+0af3f5a719-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-key-binding-api-v1-1.0.47+0af3f5a719.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-keybindings-v0-0.2.45+df3654b319-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-keybindings-v0-0.2.45+df3654b319.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-lifecycle-events-v1-2.6.0+0865547519-client-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-lifecycle-events-v1-2.6.0+0865547519-client.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-lifecycle-events-v1-2.6.0+0865547519-common-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-lifecycle-events-v1-2.6.0+0865547519-common.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-lifecycle-events-v1-2.6.0+0865547519-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-lifecycle-events-v1-2.6.0+0865547519.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-loot-api-v2-3.0.15+3f89f5a519-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-loot-api-v2-3.0.15+3f89f5a519.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-loot-api-v3-1.0.3+3f89f5a519-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-loot-api-v3-1.0.3+3f89f5a519.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-message-api-v1-6.0.14+8aaf3aca19-client-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-message-api-v1-6.0.14+8aaf3aca19-client.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-message-api-v1-6.0.14+8aaf3aca19-common-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-message-api-v1-6.0.14+8aaf3aca19-common.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-message-api-v1-6.0.14+8aaf3aca19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-message-api-v1-6.0.14+8aaf3aca19.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-model-loading-api-v1-2.1.0+b4d813fc19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-model-loading-api-v1-2.1.0+b4d813fc19.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-networking-api-v1-4.3.0+c7469b2119-client-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-networking-api-v1-4.3.0+c7469b2119-client.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-networking-api-v1-4.3.0+c7469b2119-common-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-networking-api-v1-4.3.0+c7469b2119-common.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-networking-api-v1-4.3.0+c7469b2119-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-networking-api-v1-4.3.0+c7469b2119.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-object-builder-api-v1-15.2.1+40875a9319-client-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-object-builder-api-v1-15.2.1+40875a9319-client.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-object-builder-api-v1-15.2.1+40875a9319-common-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-object-builder-api-v1-15.2.1+40875a9319-common.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-object-builder-api-v1-15.2.1+40875a9319-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-object-builder-api-v1-15.2.1+40875a9319.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-particles-v1-4.0.2+6573ed8c19-client-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-particles-v1-4.0.2+6573ed8c19-client.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-particles-v1-4.0.2+6573ed8c19-common-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-particles-v1-4.0.2+6573ed8c19-common.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-particles-v1-4.0.2+6573ed8c19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-particles-v1-4.0.2+6573ed8c19.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-recipe-api-v1-5.0.15+2475392c19-client-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-recipe-api-v1-5.0.15+2475392c19-client.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-recipe-api-v1-5.0.15+2475392c19-common-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-recipe-api-v1-5.0.15+2475392c19-common.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-recipe-api-v1-5.0.15+2475392c19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-recipe-api-v1-5.0.15+2475392c19.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-registry-sync-v0-5.3.1+e3eddc2119-client-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-registry-sync-v0-5.3.1+e3eddc2119-client.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-registry-sync-v0-5.3.1+e3eddc2119-common-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-registry-sync-v0-5.3.1+e3eddc2119-common.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-registry-sync-v0-5.3.1+e3eddc2119-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-registry-sync-v0-5.3.1+e3eddc2119.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-renderer-api-v1-3.4.1+b4d813fc19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-renderer-api-v1-3.4.1+b4d813fc19.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-renderer-indigo-1.7.1+c705a49c19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-renderer-indigo-1.7.1+c705a49c19.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-renderer-registries-v1-3.2.69+df3654b319-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-renderer-registries-v1-3.2.69+df3654b319.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-rendering-data-attachment-v1-0.3.49+73761d2e19-client-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-rendering-data-attachment-v1-0.3.49+73761d2e19-client.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-rendering-data-attachment-v1-0.3.49+73761d2e19-common-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-rendering-data-attachment-v1-0.3.49+73761d2e19-common.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-rendering-data-attachment-v1-0.3.49+73761d2e19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-rendering-data-attachment-v1-0.3.49+73761d2e19.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-rendering-fluids-v1-3.1.6+1daea21519-client-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-rendering-fluids-v1-3.1.6+1daea21519-client.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-rendering-fluids-v1-3.1.6+1daea21519-common-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-rendering-fluids-v1-3.1.6+1daea21519-common.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-rendering-fluids-v1-3.1.6+1daea21519-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-rendering-fluids-v1-3.1.6+1daea21519.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-rendering-v0-1.1.72+df3654b319-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-rendering-v0-1.1.72+df3654b319.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-rendering-v1-5.1.0+ab4c25a019-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-rendering-v1-5.1.0+ab4c25a019.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-resource-conditions-api-v1-4.3.0+8dc279b119-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-resource-conditions-api-v1-4.3.0+8dc279b119.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-resource-loader-v0-1.3.1+5b5275af19-client-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-resource-loader-v0-1.3.1+5b5275af19-client.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-resource-loader-v0-1.3.1+5b5275af19-common-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-resource-loader-v0-1.3.1+5b5275af19-common.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-resource-loader-v0-1.3.1+5b5275af19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-resource-loader-v0-1.3.1+5b5275af19.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-screen-api-v1-2.0.25+8b68f1c719-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-screen-api-v1-2.0.25+8b68f1c719.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-screen-handler-api-v1-1.3.90+b559734419-client-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-screen-handler-api-v1-1.3.90+b559734419-client.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-screen-handler-api-v1-1.3.90+b559734419-common-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-screen-handler-api-v1-1.3.90+b559734419-common.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-screen-handler-api-v1-1.3.90+b559734419-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-screen-handler-api-v1-1.3.90+b559734419.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-sound-api-v1-1.0.23+6573ed8c19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-sound-api-v1-1.0.23+6573ed8c19.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-transfer-api-v1-5.4.4+7b3d111d19-client-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-transfer-api-v1-5.4.4+7b3d111d19-client.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-transfer-api-v1-5.4.4+7b3d111d19-common-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-transfer-api-v1-5.4.4+7b3d111d19-common.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-transfer-api-v1-5.4.4+7b3d111d19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-transfer-api-v1-5.4.4+7b3d111d19.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-transitive-access-wideners-v1-6.2.0+45b9699719-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-transitive-access-wideners-v1-6.2.0+45b9699719.jar
C:\Users\sutec\IdeaProjects\untitled\build\reports\problems\problems-report.html
C:\Users\sutec\IdeaProjects\untitled\gradle\wrapper\gradle-wrapper.properties
C:\Users\sutec\IdeaProjects\untitled\src\client\java\org\ahaha\untitled\client\UntitledClient.java
C:\Users\sutec\IdeaProjects\untitled\src\client\java\org\ahaha\untitled\client\UntitledDataGenerator.java
C:\Users\sutec\IdeaProjects\untitled\src\client\resources\untitled.client.mixins.json
C:\Users\sutec\IdeaProjects\untitled\src\main\java\org\ahaha\untitled\Untitled.java
C:\Users\sutec\IdeaProjects\untitled\src\main\resources\fabric.mod.json
C:\Users\sutec\IdeaProjects\untitled\src\main\resources\untitled.mixins.json

exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw src\\client\\java\\org\\ahaha\\untitled\\client\\UntitledClient.java" in C:\Users\sutec\IdeaProjects\untitled
exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw src\\main\\java\\org\\ahaha\\untitled\\Untitled.java" in C:\Users\sutec\IdeaProjects\untitled
exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Raw build.gradle' in C:\Users\sutec\IdeaProjects\untitled
exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw src\\main\\resources\\fabric.mod.json" in C:\Users\sutec\IdeaProjects\untitled
exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Raw gradle.properties' in C:\Users\sutec\IdeaProjects\untitled
 succeeded in 1030ms:
package org.ahaha.untitled.client;

import net.fabricmc.api.ClientModInitializer;

public class UntitledClient implements ClientModInitializer {

    @Override
    public void onInitializeClient() {
    }
}


 succeeded in 1035ms:
package org.ahaha.untitled;

import net.fabricmc.api.ModInitializer;

public class Untitled implements ModInitializer {

    @Override
    public void onInitialize() {
    }
}


 succeeded in 1073ms:
{
  "schemaVersion": 1,
  "id": "untitled",
  "version": "${version}",
  "name": "untitled",
  "description": "",
  "authors": [],
  "contact": {},
  "license": "All-Rights-Reserved",
  "icon": "assets/untitled/icon.png",
  "environment": "*",
  "entrypoints": {
    "fabric-datagen": [
      "org.ahaha.untitled.client.UntitledDataGenerator"
    ],
    "client": [
      "org.ahaha.untitled.client.UntitledClient"
    ],
    "main": [
      "org.ahaha.untitled.Untitled"
    ]
  },
  "mixins": [
    "untitled.mixins.json",
    {
      "config": "untitled.client.mixins.json",
      "environment": "client"
    }
  ],
  "depends": {
    "fabricloader": ">=${loader_version}",
    "fabric-api": "*",
    "minecraft": "${minecraft_version}"
  }
}


 succeeded in 1084ms:
# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx1G
# Fabric Properties
# check these on https://modmuss50.me/fabric.html
minecraft_version=1.21.1
yarn_mappings=1.21.1+build.3
loader_version=0.19.2
# Mod Properties
mod_version=1.0-SNAPSHOT
maven_group=org.ahaha
archives_base_name=untitled
# Dependencies
# check this on https://modmuss50.me/fabric.html
fabric_version=0.116.11+1.21.1


 succeeded in 1113ms:
plugins {
    id 'fabric-loom' version '1.8-SNAPSHOT'
    id 'maven-publish'
}

version = project.mod_version
group = project.maven_group

base {
    archivesName = project.archives_base_name
}

loom {
    splitEnvironmentSourceSets()

    mods {
        "untitled" {
            sourceSet sourceSets.main
            sourceSet sourceSets.client
        }
    }
}

repositories {
    // Add repositories to retrieve artifacts from in here.
    // You should only use this when depending on other mods because
    // Loom adds the essential maven repositories to download Minecraft and libraries from automatically.
    // See https://docs.gradle.org/current/userguide/declaring_repositories.html
    // for more information about repositories.
}

dependencies {
    // To change the versions see the gradle.properties file
    minecraft "com.mojang:minecraft:${project.minecraft_version}"
    mappings "net.fabricmc:yarn:${project.yarn_mappings}:v2"
    modImplementation "net.fabricmc:fabric-loader:${project.loader_version}"

    modImplementation "net.fabricmc.fabric-api:fabric-api:${project.fabric_version}"
}

processResources {
    inputs.property "version", project.version
    inputs.property "minecraft_version", project.minecraft_version
    inputs.property "loader_version", project.loader_version
    filteringCharset "UTF-8"

    filesMatching("fabric.mod.json") {
        expand "version": project.version,
                "minecraft_version": project.minecraft_version,
                "loader_version": project.loader_version
    }
}

def targetJavaVersion = 21
tasks.withType(JavaCompile).configureEach {
    // ensure that the encoding is set to UTF-8, no matter what the system default is
    // this fixes some edge cases with special characters not displaying correctly
    // see http://yodaconditions.net/blog/fix-for-java-file-encoding-problems-with-gradle.html
    // If Javadoc is generated, this must be specified in that task too.
    it.options.encoding = "UTF-8"
    if (targetJavaVersion >= 10 || JavaVersion.current().isJava10Compatible()) {
        it.options.release.set(targetJavaVersion)
    }
}

java {
    def javaVersion = JavaVersion.toVersion(targetJavaVersion)
    if (JavaVersion.current() < javaVersion) {
        toolchain.languageVersion = JavaLanguageVersion.of(targetJavaVersion)
    }
    // Loom will automatically attach sourcesJar to a RemapSourcesJar task and to the "build" task
    // if it is present.
    // If you remove this line, sources will not be generated.
    withSourcesJar()
}

jar {
    from("LICENSE") {
        rename { "${it}_${project.archives_base_name}" }
    }
}

// configure the maven publication
publishing {
    publications {
        create("mavenJava", MavenPublication) {
            artifactId = project.archives_base_name
            from components.java
        }
    }

    // See https://docs.gradle.org/current/userguide/publishing_maven.html for information on how to set up publishing.
    repositories {
        // Add repositories to publish to here.
        // Notice: This block does NOT have the same function as the block in the top level.
        // The repositories here will be used for publishing your artifact, not for
        // retrieving dependencies.
    }
}


codex
実装範囲を空のFabricクライアントModに限定して、設定読み込み、A/B検出、ホットバーへの安全な切り替えの最小基盤を追加します。対象が見つからない場合や画面操作中は何もしない形にして、通常操作との競合を避けます。
exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-ChildItem -Recurse -File "$env:USERPROFILE'"\\.gradle\\caches\\modules-2\\files-2.1\" -Filter '*1.21.1+build.3*.jar' | Select-Object -First 20 -ExpandProperty FullName" in C:\Users\sutec\IdeaProjects\untitled
exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-ChildItem -Recurse -File .gradle\\loom-cache -Filter '*sources.jar' | Select-Object -First 20 -ExpandProperty FullName" in C:\Users\sutec\IdeaProjects\untitled
 succeeded in 778ms:
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-api-base-common\0.4.42+6573ed8c19\fabric-api-base-common-0.4.42+6573ed8c19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-api-common\0.116.11+1.21.1\fabric-api-common-0.116.11+1.21.1-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-api-lookup-api-v1-common\1.6.72+7b3d111d19\fabric-api-lookup-api-v1-common-1.6.72+7b3d111d19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-biome-api-v1-common\13.0.31+d527f9fd19\fabric-biome-api-v1-common-13.0.31+d527f9fd19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-block-api-v1-common\1.1.0+0bc3503219\fabric-block-api-v1-common-1.1.0+0bc3503219-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-block-view-api-v2-client\1.0.11+ebb2264e19\fabric-block-view-api-v2-client-1.0.11+ebb2264e19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-block-view-api-v2-common\1.0.11+ebb2264e19\fabric-block-view-api-v2-common-1.0.11+ebb2264e19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-blockrenderlayer-v1-client\1.1.52+0af3f5a719\fabric-blockrenderlayer-v1-client-1.1.52+0af3f5a719-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-client-tags-api-v1-client\1.1.15+6573ed8c19\fabric-client-tags-api-v1-client-1.1.15+6573ed8c19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-command-api-v1-common\1.2.49+f71b366f19\fabric-command-api-v1-common-1.2.49+f71b366f19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-command-api-v2-client\2.2.28+6ced4dd919\fabric-command-api-v2-client-2.2.28+6ced4dd919-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-command-api-v2-common\2.2.28+6ced4dd919\fabric-command-api-v2-common-2.2.28+6ced4dd919-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-commands-v0-common\0.2.66+df3654b319\fabric-commands-v0-common-0.2.66+df3654b319-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-content-registries-v0-common\8.0.19+b559734419\fabric-content-registries-v0-common-8.0.19+b559734419-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-convention-tags-v1-common\2.1.7+7f945d5b19\fabric-convention-tags-v1-common-2.1.7+7f945d5b19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-convention-tags-v2-common\2.12.0+c3656daa19\fabric-convention-tags-v2-common-2.12.0+c3656daa19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-crash-report-info-v1-common\0.2.29+0af3f5a719\fabric-crash-report-info-v1-common-0.2.29+0af3f5a719-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-data-attachment-api-v1-client\1.4.6+5b36e0f719\fabric-data-attachment-api-v1-client-1.4.6+5b36e0f719-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-data-attachment-api-v1-common\1.4.6+5b36e0f719\fabric-data-attachment-api-v1-common-1.4.6+5b36e0f719-sources.jar
C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-data-generation-api-v1-client\20.2.37+60a6976219\fabric-data-generation-api-v1-client-20.2.37+60a6976219-sources.jar

 succeeded in 1081ms:
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc\yarn\1.21.1+build.3\2ad14aed781c4a73ed4dbb421966d408a0a06686\yarn-1.21.1+build.3-constants.jar
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc\yarn\1.21.1+build.3\53c8c9729c0dd52e3549c04f16eca89a15818822\yarn-1.21.1+build.3-v2.jar
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc\yarn\1.21.1+build.3\ce51544996d8a3cd4f6a1a4ba3a54dc142c92ea7\yarn-1.21.1+build.3-sources.jar

exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'jar tf "$env:USERPROFILE'"\\.gradle\\caches\\modules-2\\files-2.1\\net.fabricmc\\yarn\\1.21.1+build.3\\ce51544996d8a3cd4f6a1a4ba3a54dc142c92ea7\\yarn-1.21.1+build.3-sources.jar\" | Select-String 'PlayerInventory|ClientPlayerInteractionManager|UpdateSelectedSlotC2SPacket|ScreenHandler'" in C:\Users\sutec\IdeaProjects\untitled
2026-05-11T07:18:38.314610Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 2.4 seconds
Output:
jar : 用語 'jar' は、コマンドレット、関数、スクリプト ファイル、または操作可能なプログラムの名前として認識されません。名前が正しく記述されていることを確認し、パスが含まれている場合はそのパスが正しいことを確認してから、再試行
してください。
発生場所 行:2 文字:1
+ jar tf "$env:USERPROFILE\.gradle\caches\modules-2\files-2.1\net.fabri ...
+ ~~~
    + CategoryInfo          : ObjectNotFound: (jar:String) [], CommandNotFoundException
    + FullyQualifiedErrorId : CommandNotFoundException
 

 exited 1 in 2395ms:
jar : 用語 'jar' は、コマンドレット、関数、スクリプト ファイル、または操作可能なプログラムの名前として認識されません。名前が正しく記述されていることを確認し、パスが含まれている場合はそのパスが正しいことを確認してから、再試行
してください。
発生場所 行:2 文字:1
+ jar tf "$env:USERPROFILE\.gradle\caches\modules-2\files-2.1\net.fabri ...
+ ~~~
    + CategoryInfo          : ObjectNotFound: (jar:String) [], CommandNotFoundException
    + FullyQualifiedErrorId : CommandNotFoundException
 

exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$zip = [System.IO.Compression.ZipFile]::OpenRead("$env:USERPROFILE'"\\.gradle\\caches\\modules-2\\files-2.1\\net.fabricmc\\yarn\\1.21.1+build.3\\ce51544996d8a3cd4f6a1a4ba3a54dc142c92ea7\\yarn-1.21.1+build.3-sources.jar\"); "'$zip.Entries | Where-Object { $_.FullName -match '"'PlayerInventory|ClientPlayerInteractionManager|UpdateSelectedSlotC2SPacket|SlotActionType|KeyBinding' } | Select-Object -First 40 -ExpandProperty FullName; "'$zip.Dispose()' in C:\Users\sutec\IdeaProjects\untitled
2026-05-11T07:18:45.743192Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 0.8 seconds
Output:
型 [System.IO.Compression.ZipFile] が見つかりません。
発生場所 行:2 文字:8
+ $zip = [System.IO.Compression.ZipFile]::OpenRead("$env:USERPROFILE\.g ...
+        ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    + CategoryInfo          : InvalidOperation: (System.IO.Compression.ZipFile:TypeName) []、RuntimeException
    + FullyQualifiedErrorId : TypeNotFound
 
null 値の式ではメソッドを呼び出せません。
発生場所 行:2 文字:418
+ ...  } | Select-Object -First 40 -ExpandProperty FullName; $zip.Dispose()
+                                                            ~~~~~~~~~~~~~~
    + CategoryInfo          : InvalidOperation: (:) []、RuntimeException
    + FullyQualifiedErrorId : InvokeMethodOnNull
 

 exited 1 in 753ms:
型 [System.IO.Compression.ZipFile] が見つかりません。
発生場所 行:2 文字:8
+ $zip = [System.IO.Compression.ZipFile]::OpenRead("$env:USERPROFILE\.g ...
+        ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    + CategoryInfo          : InvalidOperation: (System.IO.Compression.ZipFile:TypeName) []、RuntimeException
    + FullyQualifiedErrorId : TypeNotFound
 
null 値の式ではメソッドを呼び出せません。
発生場所 行:2 文字:418
+ ...  } | Select-Object -First 40 -ExpandProperty FullName; $zip.Dispose()
+                                                            ~~~~~~~~~~~~~~
    + CategoryInfo          : InvalidOperation: (:) []、RuntimeException
    + FullyQualifiedErrorId : InvokeMethodOnNull
 

exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Add-Type -AssemblyName System.IO.Compression.FileSystem; $zip = [System.IO.Compression.ZipFile]::OpenRead("$env:USERPROFILE'"\\.gradle\\caches\\modules-2\\files-2.1\\net.fabricmc\\yarn\\1.21.1+build.3\\ce51544996d8a3cd4f6a1a4ba3a54dc142c92ea7\\yarn-1.21.1+build.3-sources.jar\"); "'$zip.Entries | Where-Object { $_.FullName -match '"'PlayerInventory|ClientPlayerInteractionManager|UpdateSelectedSlotC2SPacket|SlotActionType|KeyBinding' } | Select-Object -First 80 -ExpandProperty FullName; "'$zip.Dispose()' in C:\Users\sutec\IdeaProjects\untitled
 succeeded in 894ms:
exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-ChildItem -Recurse -File .gradle build "$env:USERPROFILE'"\\.gradle\\caches\" -Filter '*minecraft*source*.jar' -ErrorAction SilentlyContinue | Select-Object -First 40 -ExpandProperty FullName" in C:\Users\sutec\IdeaProjects\untitled
2026-05-11T07:18:57.919701Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 0.8 seconds
Output:
Get-ChildItem : 引数 '.gradle' を受け入れる位置指定パラメーターが見つかりません。
発生場所 行:2 文字:1
+ Get-ChildItem -Recurse -File .gradle build "$env:USERPROFILE\.gradle\ ...
+ ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    + CategoryInfo          : InvalidArgument: (:) [Get-ChildItem]、ParameterBindingException
    + FullyQualifiedErrorId : PositionalParameterNotFound,Microsoft.PowerShell.Commands.GetChildItemCommand
 

 exited 1 in 821ms:
Get-ChildItem : 引数 '.gradle' を受け入れる位置指定パラメーターが見つかりません。
発生場所 行:2 文字:1
+ Get-ChildItem -Recurse -File .gradle build "$env:USERPROFILE\.gradle\ ...
+ ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    + CategoryInfo          : InvalidArgument: (:) [Get-ChildItem]、ParameterBindingException
    + FullyQualifiedErrorId : PositionalParameterNotFound,Microsoft.PowerShell.Commands.GetChildItemCommand
 

exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-ChildItem -Path .gradle,build,"$env:USERPROFILE'"\\.gradle\\caches\" -Recurse -File -Filter '*minecraft*source*.jar' -ErrorAction SilentlyContinue | Select-Object -First 40 -ExpandProperty FullName" in C:\Users\sutec\IdeaProjects\untitled
 succeeded in 2509ms:
exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-ChildItem -Path .gradle,build -Recurse -File -Include '*PlayerInventory*','*ClientPlayerInteractionManager*','*UpdateSelectedSlotC2SPacket*' -ErrorAction SilentlyContinue | Select-Object -First 40 -ExpandProperty FullName" in C:\Users\sutec\IdeaProjects\untitled
 succeeded in 899ms:
exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-ChildItem -Force gradlew*; Get-Command gradle -ErrorAction SilentlyContinue | Select-Object -ExpandProperty Source' in C:\Users\sutec\IdeaProjects\untitled
2026-05-11T07:19:16.928540Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 2.5 seconds
Output:

 exited 1 in 2532ms:
exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Command java -ErrorAction SilentlyContinue | Select-Object -ExpandProperty Source; Get-Command javap -ErrorAction SilentlyContinue | Select-Object -ExpandProperty Source' in C:\Users\sutec\IdeaProjects\untitled
2026-05-11T07:19:25.784083Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 2.4 seconds
Output:

 exited 1 in 2409ms:
codex
切り替えは「現在選択中がAならB、BならA、どちらでもなければAを選ぶ」にします。ホットバー外のアイテムは、選択スロットがA/Bまたは空のときだけSWAPクリックで入れ替え、無関係な持ち物を動かさないようにします。
apply patch
patch: completed
C:\Users\sutec\IdeaProjects\untitled\src\client\java\org\ahaha\untitled\client\itemswap\InventoryItemFinder.java
C:\Users\sutec\IdeaProjects\untitled\src\client\java\org\ahaha\untitled\client\itemswap\ItemSwapConfig.java
C:\Users\sutec\IdeaProjects\untitled\src\client\java\org\ahaha\untitled\client\itemswap\ItemSwapController.java
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
new file mode 100644
index 0000000000000000000000000000000000000000..3214363102de082962b3401df416e73144012217
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
@@ -0,0 +1,44 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+
+import java.util.OptionalInt;
+
+public final class InventoryItemFinder {
+    public static final int HOTBAR_SIZE = 9;
+    public static final int MAIN_INVENTORY_SIZE = 36;
+
+    public OptionalInt find(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < MAIN_INVENTORY_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findInHotbar(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findEmptyHotbarSlot(PlayerInventory inventory) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            if (inventory.getStack(slot).isEmpty()) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
new file mode 100644
index 0000000000000000000000000000000000000000..e3f80e5c19e04354bc7646bb6acd358895963c32
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
@@ -0,0 +1,94 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.fabricmc.loader.api.FabricLoader;
+import net.minecraft.item.Item;
+import net.minecraft.registry.Registries;
+import net.minecraft.util.Identifier;
+
+import java.io.IOException;
+import java.io.Reader;
+import java.io.Writer;
+import java.nio.file.Files;
+import java.nio.file.Path;
+import java.util.Optional;
+import java.util.Properties;
+
+public final class ItemSwapConfig {
+    private static final String CONFIG_FILE_NAME = "untitled-item-swap.properties";
+    private static final String DEFAULT_ITEM_A = "minecraft:golden_apple";
+    private static final String DEFAULT_ITEM_B = "minecraft:totem_of_undying";
+
+    private final String itemAId;
+    private final String itemBId;
+    private final boolean allowInventorySwap;
+
+    private ItemSwapConfig(String itemAId, String itemBId, boolean allowInventorySwap) {
+        this.itemAId = itemAId;
+        this.itemBId = itemBId;
+        this.allowInventorySwap = allowInventorySwap;
+    }
+
+    public static ItemSwapConfig load() {
+        Path path = FabricLoader.getInstance().getConfigDir().resolve(CONFIG_FILE_NAME);
+        Properties properties = defaultProperties();
+
+        if (Files.exists(path)) {
+            try (Reader reader = Files.newBufferedReader(path)) {
+                properties.load(reader);
+            } catch (IOException ignored) {
+                return fromProperties(properties);
+            }
+        } else {
+            writeDefaults(path, properties);
+        }
+
+        return fromProperties(properties);
+    }
+
+    public Optional<Item> itemA() {
+        return resolveItem(itemAId);
+    }
+
+    public Optional<Item> itemB() {
+        return resolveItem(itemBId);
+    }
+
+    public boolean allowInventorySwap() {
+        return allowInventorySwap;
+    }
+
+    private static Properties defaultProperties() {
+        Properties properties = new Properties();
+        properties.setProperty("itemA", DEFAULT_ITEM_A);
+        properties.setProperty("itemB", DEFAULT_ITEM_B);
+        properties.setProperty("allowInventorySwap", "true");
+        return properties;
+    }
+
+    private static ItemSwapConfig fromProperties(Properties properties) {
+        return new ItemSwapConfig(
+                properties.getProperty("itemA", DEFAULT_ITEM_A).trim(),
+                properties.getProperty("itemB", DEFAULT_ITEM_B).trim(),
+                Boolean.parseBoolean(properties.getProperty("allowInventorySwap", "true").trim())
+        );
+    }
+
+    private static void writeDefaults(Path path, Properties properties) {
+        try {
+            Files.createDirectories(path.getParent());
+            try (Writer writer = Files.newBufferedWriter(path)) {
+                properties.store(writer, "Untitled item A/B swap settings");
+            }
+        } catch (IOException ignored) {
+        }
+    }
+
+    private static Optional<Item> resolveItem(String rawId) {
+        Identifier id = Identifier.tryParse(rawId);
+        if (id == null || !Registries.ITEM.containsId(id)) {
+            return Optional.empty();
+        }
+
+        return Optional.of(Registries.ITEM.get(id));
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
new file mode 100644
index 0000000000000000000000000000000000000000..1627b1c02312751fdcee2e7b2abe108fb036a763
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
@@ -0,0 +1,107 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.client.MinecraftClient;
+import net.minecraft.client.network.ClientPlayerEntity;
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+import net.minecraft.screen.slot.SlotActionType;
+
+import java.util.Optional;
+import java.util.OptionalInt;
+
+public final class ItemSwapController {
+    private static final int PLAYER_SCREEN_HOTBAR_SLOT_OFFSET = 36;
+
+    private final ItemSwapConfig config;
+    private final InventoryItemFinder finder;
+
+    public ItemSwapController(ItemSwapConfig config) {
+        this.config = config;
+        this.finder = new InventoryItemFinder();
+    }
+
+    public boolean toggle(MinecraftClient client) {
+        if (client.player == null || client.interactionManager == null || client.currentScreen != null) {
+            return false;
+        }
+
+        Optional<Item> itemA = config.itemA();
+        Optional<Item> itemB = config.itemB();
+        if (itemA.isEmpty() || itemB.isEmpty()) {
+            return false;
+        }
+
+        ClientPlayerEntity player = client.player;
+        PlayerInventory inventory = player.getInventory();
+        Item target = targetItem(inventory.getStack(inventory.selectedSlot), itemA.get(), itemB.get());
+        OptionalInt targetSlot = finder.find(inventory, target);
+        if (targetSlot.isEmpty()) {
+            return false;
+        }
+
+        return switchToSlot(client, player, inventory, targetSlot.getAsInt(), itemA.get(), itemB.get());
+    }
+
+    private Item targetItem(ItemStack selectedStack, Item itemA, Item itemB) {
+        if (selectedStack.isOf(itemA)) {
+            return itemB;
+        }
+        if (selectedStack.isOf(itemB)) {
+            return itemA;
+        }
+
+        return itemA;
+    }
+
+    private boolean switchToSlot(
+            MinecraftClient client,
+            ClientPlayerEntity player,
+            PlayerInventory inventory,
+            int inventorySlot,
+            Item itemA,
+            Item itemB
+    ) {
+        if (inventorySlot < InventoryItemFinder.HOTBAR_SIZE) {
+            selectHotbarSlot(client, inventory, inventorySlot);
+            return true;
+        }
+
+        if (!config.allowInventorySwap()) {
+            return false;
+        }
+
+        OptionalInt destinationSlot = safeHotbarDestination(inventory, itemA, itemB);
+        if (destinationSlot.isEmpty()) {
+            return false;
+        }
+
+        client.interactionManager.clickSlot(
+                player.playerScreenHandler.syncId,
+                inventorySlot,
+                destinationSlot.getAsInt(),
+                SlotActionType.SWAP,
+                player
+        );
+        selectHotbarSlot(client, inventory, destinationSlot.getAsInt());
+        return true;
+    }
+
+    private OptionalInt safeHotbarDestination(PlayerInventory inventory, Item itemA, Item itemB) {
+        ItemStack selectedStack = inventory.getStack(inventory.selectedSlot);
+        if (selectedStack.isEmpty() || selectedStack.isOf(itemA) || selectedStack.isOf(itemB)) {
+            return OptionalInt.of(inventory.selectedSlot);
+        }
+
+        return finder.findEmptyHotbarSlot(inventory);
+    }
+
+    private void selectHotbarSlot(MinecraftClient client, PlayerInventory inventory, int slot) {
+        if (inventory.selectedSlot == slot) {
+            return;
+        }
+
+        inventory.selectedSlot = slot;
+        client.interactionManager.syncSelectedSlot();
+    }
+}

diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
new file mode 100644
index 0000000000000000000000000000000000000000..3214363102de082962b3401df416e73144012217
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
@@ -0,0 +1,44 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+
+import java.util.OptionalInt;
+
+public final class InventoryItemFinder {
+    public static final int HOTBAR_SIZE = 9;
+    public static final int MAIN_INVENTORY_SIZE = 36;
+
+    public OptionalInt find(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < MAIN_INVENTORY_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findInHotbar(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findEmptyHotbarSlot(PlayerInventory inventory) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            if (inventory.getStack(slot).isEmpty()) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
new file mode 100644
index 0000000000000000000000000000000000000000..e3f80e5c19e04354bc7646bb6acd358895963c32
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
@@ -0,0 +1,94 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.fabricmc.loader.api.FabricLoader;
+import net.minecraft.item.Item;
+import net.minecraft.registry.Registries;
+import net.minecraft.util.Identifier;
+
+import java.io.IOException;
+import java.io.Reader;
+import java.io.Writer;
+import java.nio.file.Files;
+import java.nio.file.Path;
+import java.util.Optional;
+import java.util.Properties;
+
+public final class ItemSwapConfig {
+    private static final String CONFIG_FILE_NAME = "untitled-item-swap.properties";
+    private static final String DEFAULT_ITEM_A = "minecraft:golden_apple";
+    private static final String DEFAULT_ITEM_B = "minecraft:totem_of_undying";
+
+    private final String itemAId;
+    private final String itemBId;
+    private final boolean allowInventorySwap;
+
+    private ItemSwapConfig(String itemAId, String itemBId, boolean allowInventorySwap) {
+        this.itemAId = itemAId;
+        this.itemBId = itemBId;
+        this.allowInventorySwap = allowInventorySwap;
+    }
+
+    public static ItemSwapConfig load() {
+        Path path = FabricLoader.getInstance().getConfigDir().resolve(CONFIG_FILE_NAME);
+        Properties properties = defaultProperties();
+
+        if (Files.exists(path)) {
+            try (Reader reader = Files.newBufferedReader(path)) {
+                properties.load(reader);
+            } catch (IOException ignored) {
+                return fromProperties(properties);
+            }
+        } else {
+            writeDefaults(path, properties);
+        }
+
+        return fromProperties(properties);
+    }
+
+    public Optional<Item> itemA() {
+        return resolveItem(itemAId);
+    }
+
+    public Optional<Item> itemB() {
+        return resolveItem(itemBId);
+    }
+
+    public boolean allowInventorySwap() {
+        return allowInventorySwap;
+    }
+
+    private static Properties defaultProperties() {
+        Properties properties = new Properties();
+        properties.setProperty("itemA", DEFAULT_ITEM_A);
+        properties.setProperty("itemB", DEFAULT_ITEM_B);
+        properties.setProperty("allowInventorySwap", "true");
+        return properties;
+    }
+
+    private static ItemSwapConfig fromProperties(Properties properties) {
+        return new ItemSwapConfig(
+                properties.getProperty("itemA", DEFAULT_ITEM_A).trim(),
+                properties.getProperty("itemB", DEFAULT_ITEM_B).trim(),
+                Boolean.parseBoolean(properties.getProperty("allowInventorySwap", "true").trim())
+        );
+    }
+
+    private static void writeDefaults(Path path, Properties properties) {
+        try {
+            Files.createDirectories(path.getParent());
+            try (Writer writer = Files.newBufferedWriter(path)) {
+                properties.store(writer, "Untitled item A/B swap settings");
+            }
+        } catch (IOException ignored) {
+        }
+    }
+
+    private static Optional<Item> resolveItem(String rawId) {
+        Identifier id = Identifier.tryParse(rawId);
+        if (id == null || !Registries.ITEM.containsId(id)) {
+            return Optional.empty();
+        }
+
+        return Optional.of(Registries.ITEM.get(id));
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
new file mode 100644
index 0000000000000000000000000000000000000000..1627b1c02312751fdcee2e7b2abe108fb036a763
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
@@ -0,0 +1,107 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.client.MinecraftClient;
+import net.minecraft.client.network.ClientPlayerEntity;
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+import net.minecraft.screen.slot.SlotActionType;
+
+import java.util.Optional;
+import java.util.OptionalInt;
+
+public final class ItemSwapController {
+    private static final int PLAYER_SCREEN_HOTBAR_SLOT_OFFSET = 36;
+
+    private final ItemSwapConfig config;
+    private final InventoryItemFinder finder;
+
+    public ItemSwapController(ItemSwapConfig config) {
+        this.config = config;
+        this.finder = new InventoryItemFinder();
+    }
+
+    public boolean toggle(MinecraftClient client) {
+        if (client.player == null || client.interactionManager == null || client.currentScreen != null) {
+            return false;
+        }
+
+        Optional<Item> itemA = config.itemA();
+        Optional<Item> itemB = config.itemB();
+        if (itemA.isEmpty() || itemB.isEmpty()) {
+            return false;
+        }
+
+        ClientPlayerEntity player = client.player;
+        PlayerInventory inventory = player.getInventory();
+        Item target = targetItem(inventory.getStack(inventory.selectedSlot), itemA.get(), itemB.get());
+        OptionalInt targetSlot = finder.find(inventory, target);
+        if (targetSlot.isEmpty()) {
+            return false;
+        }
+
+        return switchToSlot(client, player, inventory, targetSlot.getAsInt(), itemA.get(), itemB.get());
+    }
+
+    private Item targetItem(ItemStack selectedStack, Item itemA, Item itemB) {
+        if (selectedStack.isOf(itemA)) {
+            return itemB;
+        }
+        if (selectedStack.isOf(itemB)) {
+            return itemA;
+        }
+
+        return itemA;
+    }
+
+    private boolean switchToSlot(
+            MinecraftClient client,
+            ClientPlayerEntity player,
+            PlayerInventory inventory,
+            int inventorySlot,
+            Item itemA,
+            Item itemB
+    ) {
+        if (inventorySlot < InventoryItemFinder.HOTBAR_SIZE) {
+            selectHotbarSlot(client, inventory, inventorySlot);
+            return true;
+        }
+
+        if (!config.allowInventorySwap()) {
+            return false;
+        }
+
+        OptionalInt destinationSlot = safeHotbarDestination(inventory, itemA, itemB);
+        if (destinationSlot.isEmpty()) {
+            return false;
+        }
+
+        client.interactionManager.clickSlot(
+                player.playerScreenHandler.syncId,
+                inventorySlot,
+                destinationSlot.getAsInt(),
+                SlotActionType.SWAP,
+                player
+        );
+        selectHotbarSlot(client, inventory, destinationSlot.getAsInt());
+        return true;
+    }
+
+    private OptionalInt safeHotbarDestination(PlayerInventory inventory, Item itemA, Item itemB) {
+        ItemStack selectedStack = inventory.getStack(inventory.selectedSlot);
+        if (selectedStack.isEmpty() || selectedStack.isOf(itemA) || selectedStack.isOf(itemB)) {
+            return OptionalInt.of(inventory.selectedSlot);
+        }
+
+        return finder.findEmptyHotbarSlot(inventory);
+    }
+
+    private void selectHotbarSlot(MinecraftClient client, PlayerInventory inventory, int slot) {
+        if (inventory.selectedSlot == slot) {
+            return;
+        }
+
+        inventory.selectedSlot = slot;
+        client.interactionManager.syncSelectedSlot();
+    }
+}

apply patch
patch: completed
C:\Users\sutec\IdeaProjects\untitled\src\client\java\org\ahaha\untitled\client\UntitledClient.java
diff --git a/src/client/java/org/ahaha/untitled/client/UntitledClient.java b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
index 96f2fa5caf72eb792f660392a6ef7bab77503eb9..760523f1a4590b451ae9a35dd4bed823b44da6a1
--- a/src/client/java/org/ahaha/untitled/client/UntitledClient.java
+++ b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
@@ -1,10 +1,32 @@
 package org.ahaha.untitled.client;
 
 import net.fabricmc.api.ClientModInitializer;
+import net.fabricmc.fabric.api.client.event.lifecycle.v1.ClientTickEvents;
+import net.fabricmc.fabric.api.client.keybinding.v1.KeyBindingHelper;
+import net.minecraft.client.option.KeyBinding;
+import net.minecraft.client.util.InputUtil;
+import org.ahaha.untitled.client.itemswap.ItemSwapConfig;
+import org.ahaha.untitled.client.itemswap.ItemSwapController;
+import org.lwjgl.glfw.GLFW;
 
 public class UntitledClient implements ClientModInitializer {
+    private static final String CATEGORY = "key.category.untitled";
+    private static final String SWAP_KEY = "key.untitled.swap_item_a_b";
 
     @Override
     public void onInitializeClient() {
+        ItemSwapController controller = new ItemSwapController(ItemSwapConfig.load());
+        KeyBinding swapKey = KeyBindingHelper.registerKeyBinding(new KeyBinding(
+                SWAP_KEY,
+                InputUtil.Type.KEYSYM,
+                GLFW.GLFW_KEY_UNKNOWN,
+                CATEGORY
+        ));
+
+        ClientTickEvents.END_CLIENT_TICK.register(client -> {
+            while (swapKey.wasPressed()) {
+                controller.toggle(client);
+            }
+        });
     }
 }
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
new file mode 100644
index 0000000000000000000000000000000000000000..3214363102de082962b3401df416e73144012217
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
@@ -0,0 +1,44 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+
+import java.util.OptionalInt;
+
+public final class InventoryItemFinder {
+    public static final int HOTBAR_SIZE = 9;
+    public static final int MAIN_INVENTORY_SIZE = 36;
+
+    public OptionalInt find(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < MAIN_INVENTORY_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findInHotbar(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findEmptyHotbarSlot(PlayerInventory inventory) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            if (inventory.getStack(slot).isEmpty()) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
new file mode 100644
index 0000000000000000000000000000000000000000..e3f80e5c19e04354bc7646bb6acd358895963c32
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
@@ -0,0 +1,94 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.fabricmc.loader.api.FabricLoader;
+import net.minecraft.item.Item;
+import net.minecraft.registry.Registries;
+import net.minecraft.util.Identifier;
+
+import java.io.IOException;
+import java.io.Reader;
+import java.io.Writer;
+import java.nio.file.Files;
+import java.nio.file.Path;
+import java.util.Optional;
+import java.util.Properties;
+
+public final class ItemSwapConfig {
+    private static final String CONFIG_FILE_NAME = "untitled-item-swap.properties";
+    private static final String DEFAULT_ITEM_A = "minecraft:golden_apple";
+    private static final String DEFAULT_ITEM_B = "minecraft:totem_of_undying";
+
+    private final String itemAId;
+    private final String itemBId;
+    private final boolean allowInventorySwap;
+
+    private ItemSwapConfig(String itemAId, String itemBId, boolean allowInventorySwap) {
+        this.itemAId = itemAId;
+        this.itemBId = itemBId;
+        this.allowInventorySwap = allowInventorySwap;
+    }
+
+    public static ItemSwapConfig load() {
+        Path path = FabricLoader.getInstance().getConfigDir().resolve(CONFIG_FILE_NAME);
+        Properties properties = defaultProperties();
+
+        if (Files.exists(path)) {
+            try (Reader reader = Files.newBufferedReader(path)) {
+                properties.load(reader);
+            } catch (IOException ignored) {
+                return fromProperties(properties);
+            }
+        } else {
+            writeDefaults(path, properties);
+        }
+
+        return fromProperties(properties);
+    }
+
+    public Optional<Item> itemA() {
+        return resolveItem(itemAId);
+    }
+
+    public Optional<Item> itemB() {
+        return resolveItem(itemBId);
+    }
+
+    public boolean allowInventorySwap() {
+        return allowInventorySwap;
+    }
+
+    private static Properties defaultProperties() {
+        Properties properties = new Properties();
+        properties.setProperty("itemA", DEFAULT_ITEM_A);
+        properties.setProperty("itemB", DEFAULT_ITEM_B);
+        properties.setProperty("allowInventorySwap", "true");
+        return properties;
+    }
+
+    private static ItemSwapConfig fromProperties(Properties properties) {
+        return new ItemSwapConfig(
+                properties.getProperty("itemA", DEFAULT_ITEM_A).trim(),
+                properties.getProperty("itemB", DEFAULT_ITEM_B).trim(),
+                Boolean.parseBoolean(properties.getProperty("allowInventorySwap", "true").trim())
+        );
+    }
+
+    private static void writeDefaults(Path path, Properties properties) {
+        try {
+            Files.createDirectories(path.getParent());
+            try (Writer writer = Files.newBufferedWriter(path)) {
+                properties.store(writer, "Untitled item A/B swap settings");
+            }
+        } catch (IOException ignored) {
+        }
+    }
+
+    private static Optional<Item> resolveItem(String rawId) {
+        Identifier id = Identifier.tryParse(rawId);
+        if (id == null || !Registries.ITEM.containsId(id)) {
+            return Optional.empty();
+        }
+
+        return Optional.of(Registries.ITEM.get(id));
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
new file mode 100644
index 0000000000000000000000000000000000000000..1627b1c02312751fdcee2e7b2abe108fb036a763
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
@@ -0,0 +1,107 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.client.MinecraftClient;
+import net.minecraft.client.network.ClientPlayerEntity;
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+import net.minecraft.screen.slot.SlotActionType;
+
+import java.util.Optional;
+import java.util.OptionalInt;
+
+public final class ItemSwapController {
+    private static final int PLAYER_SCREEN_HOTBAR_SLOT_OFFSET = 36;
+
+    private final ItemSwapConfig config;
+    private final InventoryItemFinder finder;
+
+    public ItemSwapController(ItemSwapConfig config) {
+        this.config = config;
+        this.finder = new InventoryItemFinder();
+    }
+
+    public boolean toggle(MinecraftClient client) {
+        if (client.player == null || client.interactionManager == null || client.currentScreen != null) {
+            return false;
+        }
+
+        Optional<Item> itemA = config.itemA();
+        Optional<Item> itemB = config.itemB();
+        if (itemA.isEmpty() || itemB.isEmpty()) {
+            return false;
+        }
+
+        ClientPlayerEntity player = client.player;
+        PlayerInventory inventory = player.getInventory();
+        Item target = targetItem(inventory.getStack(inventory.selectedSlot), itemA.get(), itemB.get());
+        OptionalInt targetSlot = finder.find(inventory, target);
+        if (targetSlot.isEmpty()) {
+            return false;
+        }
+
+        return switchToSlot(client, player, inventory, targetSlot.getAsInt(), itemA.get(), itemB.get());
+    }
+
+    private Item targetItem(ItemStack selectedStack, Item itemA, Item itemB) {
+        if (selectedStack.isOf(itemA)) {
+            return itemB;
+        }
+        if (selectedStack.isOf(itemB)) {
+            return itemA;
+        }
+
+        return itemA;
+    }
+
+    private boolean switchToSlot(
+            MinecraftClient client,
+            ClientPlayerEntity player,
+            PlayerInventory inventory,
+            int inventorySlot,
+            Item itemA,
+            Item itemB
+    ) {
+        if (inventorySlot < InventoryItemFinder.HOTBAR_SIZE) {
+            selectHotbarSlot(client, inventory, inventorySlot);
+            return true;
+        }
+
+        if (!config.allowInventorySwap()) {
+            return false;
+        }
+
+        OptionalInt destinationSlot = safeHotbarDestination(inventory, itemA, itemB);
+        if (destinationSlot.isEmpty()) {
+            return false;
+        }
+
+        client.interactionManager.clickSlot(
+                player.playerScreenHandler.syncId,
+                inventorySlot,
+                destinationSlot.getAsInt(),
+                SlotActionType.SWAP,
+                player
+        );
+        selectHotbarSlot(client, inventory, destinationSlot.getAsInt());
+        return true;
+    }
+
+    private OptionalInt safeHotbarDestination(PlayerInventory inventory, Item itemA, Item itemB) {
+        ItemStack selectedStack = inventory.getStack(inventory.selectedSlot);
+        if (selectedStack.isEmpty() || selectedStack.isOf(itemA) || selectedStack.isOf(itemB)) {
+            return OptionalInt.of(inventory.selectedSlot);
+        }
+
+        return finder.findEmptyHotbarSlot(inventory);
+    }
+
+    private void selectHotbarSlot(MinecraftClient client, PlayerInventory inventory, int slot) {
+        if (inventory.selectedSlot == slot) {
+            return;
+        }
+
+        inventory.selectedSlot = slot;
+        client.interactionManager.syncSelectedSlot();
+    }
+}

diff --git a/src/client/java/org/ahaha/untitled/client/UntitledClient.java b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
index 96f2fa5caf72eb792f660392a6ef7bab77503eb9..760523f1a4590b451ae9a35dd4bed823b44da6a1
--- a/src/client/java/org/ahaha/untitled/client/UntitledClient.java
+++ b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
@@ -1,10 +1,32 @@
 package org.ahaha.untitled.client;
 
 import net.fabricmc.api.ClientModInitializer;
+import net.fabricmc.fabric.api.client.event.lifecycle.v1.ClientTickEvents;
+import net.fabricmc.fabric.api.client.keybinding.v1.KeyBindingHelper;
+import net.minecraft.client.option.KeyBinding;
+import net.minecraft.client.util.InputUtil;
+import org.ahaha.untitled.client.itemswap.ItemSwapConfig;
+import org.ahaha.untitled.client.itemswap.ItemSwapController;
+import org.lwjgl.glfw.GLFW;
 
 public class UntitledClient implements ClientModInitializer {
+    private static final String CATEGORY = "key.category.untitled";
+    private static final String SWAP_KEY = "key.untitled.swap_item_a_b";
 
     @Override
     public void onInitializeClient() {
+        ItemSwapController controller = new ItemSwapController(ItemSwapConfig.load());
+        KeyBinding swapKey = KeyBindingHelper.registerKeyBinding(new KeyBinding(
+                SWAP_KEY,
+                InputUtil.Type.KEYSYM,
+                GLFW.GLFW_KEY_UNKNOWN,
+                CATEGORY
+        ));
+
+        ClientTickEvents.END_CLIENT_TICK.register(client -> {
+            while (swapKey.wasPressed()) {
+                controller.toggle(client);
+            }
+        });
     }
 }
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
new file mode 100644
index 0000000000000000000000000000000000000000..3214363102de082962b3401df416e73144012217
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
@@ -0,0 +1,44 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+
+import java.util.OptionalInt;
+
+public final class InventoryItemFinder {
+    public static final int HOTBAR_SIZE = 9;
+    public static final int MAIN_INVENTORY_SIZE = 36;
+
+    public OptionalInt find(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < MAIN_INVENTORY_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findInHotbar(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findEmptyHotbarSlot(PlayerInventory inventory) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            if (inventory.getStack(slot).isEmpty()) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
new file mode 100644
index 0000000000000000000000000000000000000000..e3f80e5c19e04354bc7646bb6acd358895963c32
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
@@ -0,0 +1,94 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.fabricmc.loader.api.FabricLoader;
+import net.minecraft.item.Item;
+import net.minecraft.registry.Registries;
+import net.minecraft.util.Identifier;
+
+import java.io.IOException;
+import java.io.Reader;
+import java.io.Writer;
+import java.nio.file.Files;
+import java.nio.file.Path;
+import java.util.Optional;
+import java.util.Properties;
+
+public final class ItemSwapConfig {
+    private static final String CONFIG_FILE_NAME = "untitled-item-swap.properties";
+    private static final String DEFAULT_ITEM_A = "minecraft:golden_apple";
+    private static final String DEFAULT_ITEM_B = "minecraft:totem_of_undying";
+
+    private final String itemAId;
+    private final String itemBId;
+    private final boolean allowInventorySwap;
+
+    private ItemSwapConfig(String itemAId, String itemBId, boolean allowInventorySwap) {
+        this.itemAId = itemAId;
+        this.itemBId = itemBId;
+        this.allowInventorySwap = allowInventorySwap;
+    }
+
+    public static ItemSwapConfig load() {
+        Path path = FabricLoader.getInstance().getConfigDir().resolve(CONFIG_FILE_NAME);
+        Properties properties = defaultProperties();
+
+        if (Files.exists(path)) {
+            try (Reader reader = Files.newBufferedReader(path)) {
+                properties.load(reader);
+            } catch (IOException ignored) {
+                return fromProperties(properties);
+            }
+        } else {
+            writeDefaults(path, properties);
+        }
+
+        return fromProperties(properties);
+    }
+
+    public Optional<Item> itemA() {
+        return resolveItem(itemAId);
+    }
+
+    public Optional<Item> itemB() {
+        return resolveItem(itemBId);
+    }
+
+    public boolean allowInventorySwap() {
+        return allowInventorySwap;
+    }
+
+    private static Properties defaultProperties() {
+        Properties properties = new Properties();
+        properties.setProperty("itemA", DEFAULT_ITEM_A);
+        properties.setProperty("itemB", DEFAULT_ITEM_B);
+        properties.setProperty("allowInventorySwap", "true");
+        return properties;
+    }
+
+    private static ItemSwapConfig fromProperties(Properties properties) {
+        return new ItemSwapConfig(
+                properties.getProperty("itemA", DEFAULT_ITEM_A).trim(),
+                properties.getProperty("itemB", DEFAULT_ITEM_B).trim(),
+                Boolean.parseBoolean(properties.getProperty("allowInventorySwap", "true").trim())
+        );
+    }
+
+    private static void writeDefaults(Path path, Properties properties) {
+        try {
+            Files.createDirectories(path.getParent());
+            try (Writer writer = Files.newBufferedWriter(path)) {
+                properties.store(writer, "Untitled item A/B swap settings");
+            }
+        } catch (IOException ignored) {
+        }
+    }
+
+    private static Optional<Item> resolveItem(String rawId) {
+        Identifier id = Identifier.tryParse(rawId);
+        if (id == null || !Registries.ITEM.containsId(id)) {
+            return Optional.empty();
+        }
+
+        return Optional.of(Registries.ITEM.get(id));
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
new file mode 100644
index 0000000000000000000000000000000000000000..1627b1c02312751fdcee2e7b2abe108fb036a763
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
@@ -0,0 +1,107 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.client.MinecraftClient;
+import net.minecraft.client.network.ClientPlayerEntity;
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+import net.minecraft.screen.slot.SlotActionType;
+
+import java.util.Optional;
+import java.util.OptionalInt;
+
+public final class ItemSwapController {
+    private static final int PLAYER_SCREEN_HOTBAR_SLOT_OFFSET = 36;
+
+    private final ItemSwapConfig config;
+    private final InventoryItemFinder finder;
+
+    public ItemSwapController(ItemSwapConfig config) {
+        this.config = config;
+        this.finder = new InventoryItemFinder();
+    }
+
+    public boolean toggle(MinecraftClient client) {
+        if (client.player == null || client.interactionManager == null || client.currentScreen != null) {
+            return false;
+        }
+
+        Optional<Item> itemA = config.itemA();
+        Optional<Item> itemB = config.itemB();
+        if (itemA.isEmpty() || itemB.isEmpty()) {
+            return false;
+        }
+
+        ClientPlayerEntity player = client.player;
+        PlayerInventory inventory = player.getInventory();
+        Item target = targetItem(inventory.getStack(inventory.selectedSlot), itemA.get(), itemB.get());
+        OptionalInt targetSlot = finder.find(inventory, target);
+        if (targetSlot.isEmpty()) {
+            return false;
+        }
+
+        return switchToSlot(client, player, inventory, targetSlot.getAsInt(), itemA.get(), itemB.get());
+    }
+
+    private Item targetItem(ItemStack selectedStack, Item itemA, Item itemB) {
+        if (selectedStack.isOf(itemA)) {
+            return itemB;
+        }
+        if (selectedStack.isOf(itemB)) {
+            return itemA;
+        }
+
+        return itemA;
+    }
+
+    private boolean switchToSlot(
+            MinecraftClient client,
+            ClientPlayerEntity player,
+            PlayerInventory inventory,
+            int inventorySlot,
+            Item itemA,
+            Item itemB
+    ) {
+        if (inventorySlot < InventoryItemFinder.HOTBAR_SIZE) {
+            selectHotbarSlot(client, inventory, inventorySlot);
+            return true;
+        }
+
+        if (!config.allowInventorySwap()) {
+            return false;
+        }
+
+        OptionalInt destinationSlot = safeHotbarDestination(inventory, itemA, itemB);
+        if (destinationSlot.isEmpty()) {
+            return false;
+        }
+
+        client.interactionManager.clickSlot(
+                player.playerScreenHandler.syncId,
+                inventorySlot,
+                destinationSlot.getAsInt(),
+                SlotActionType.SWAP,
+                player
+        );
+        selectHotbarSlot(client, inventory, destinationSlot.getAsInt());
+        return true;
+    }
+
+    private OptionalInt safeHotbarDestination(PlayerInventory inventory, Item itemA, Item itemB) {
+        ItemStack selectedStack = inventory.getStack(inventory.selectedSlot);
+        if (selectedStack.isEmpty() || selectedStack.isOf(itemA) || selectedStack.isOf(itemB)) {
+            return OptionalInt.of(inventory.selectedSlot);
+        }
+
+        return finder.findEmptyHotbarSlot(inventory);
+    }
+
+    private void selectHotbarSlot(MinecraftClient client, PlayerInventory inventory, int slot) {
+        if (inventory.selectedSlot == slot) {
+            return;
+        }
+
+        inventory.selectedSlot = slot;
+        client.interactionManager.syncSelectedSlot();
+    }
+}

exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Add-Type -AssemblyName System.IO.Compression.FileSystem; $jar = Resolve-Path '"'.gradle\\loom-cache\\minecraftMaven\\net\\minecraft\\minecraft-clientOnly-*\\1.21.1-*\\minecraft-clientOnly-*.jar' | Select-Object -First 1 -ExpandProperty Path; "'$zip=[IO.Compression.ZipFile]::OpenRead($jar); $zip.Entries | Where-Object { $_.FullName -in @('"'net/minecraft/client/network/ClientPlayerInteractionManager.class','net/minecraft/entity/player/PlayerInventory.class','net/minecraft/client/option/KeyBinding.class','net/minecraft/client/util/InputUtil.class') } | Select-Object -ExpandProperty FullName; "'$zip.Dispose()' in C:\Users\sutec\IdeaProjects\untitled
 succeeded in 1116ms:
net/minecraft/client/util/InputUtil.class
net/minecraft/client/option/KeyBinding.class
net/minecraft/client/network/ClientPlayerInteractionManager.class

diff --git a/src/client/java/org/ahaha/untitled/client/UntitledClient.java b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
index 96f2fa5caf72eb792f660392a6ef7bab77503eb9..760523f1a4590b451ae9a35dd4bed823b44da6a1
--- a/src/client/java/org/ahaha/untitled/client/UntitledClient.java
+++ b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
@@ -1,10 +1,32 @@
 package org.ahaha.untitled.client;
 
 import net.fabricmc.api.ClientModInitializer;
+import net.fabricmc.fabric.api.client.event.lifecycle.v1.ClientTickEvents;
+import net.fabricmc.fabric.api.client.keybinding.v1.KeyBindingHelper;
+import net.minecraft.client.option.KeyBinding;
+import net.minecraft.client.util.InputUtil;
+import org.ahaha.untitled.client.itemswap.ItemSwapConfig;
+import org.ahaha.untitled.client.itemswap.ItemSwapController;
+import org.lwjgl.glfw.GLFW;
 
 public class UntitledClient implements ClientModInitializer {
+    private static final String CATEGORY = "key.category.untitled";
+    private static final String SWAP_KEY = "key.untitled.swap_item_a_b";
 
     @Override
     public void onInitializeClient() {
+        ItemSwapController controller = new ItemSwapController(ItemSwapConfig.load());
+        KeyBinding swapKey = KeyBindingHelper.registerKeyBinding(new KeyBinding(
+                SWAP_KEY,
+                InputUtil.Type.KEYSYM,
+                GLFW.GLFW_KEY_UNKNOWN,
+                CATEGORY
+        ));
+
+        ClientTickEvents.END_CLIENT_TICK.register(client -> {
+            while (swapKey.wasPressed()) {
+                controller.toggle(client);
+            }
+        });
     }
 }
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
new file mode 100644
index 0000000000000000000000000000000000000000..3214363102de082962b3401df416e73144012217
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
@@ -0,0 +1,44 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+
+import java.util.OptionalInt;
+
+public final class InventoryItemFinder {
+    public static final int HOTBAR_SIZE = 9;
+    public static final int MAIN_INVENTORY_SIZE = 36;
+
+    public OptionalInt find(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < MAIN_INVENTORY_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findInHotbar(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findEmptyHotbarSlot(PlayerInventory inventory) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            if (inventory.getStack(slot).isEmpty()) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
new file mode 100644
index 0000000000000000000000000000000000000000..e3f80e5c19e04354bc7646bb6acd358895963c32
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
@@ -0,0 +1,94 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.fabricmc.loader.api.FabricLoader;
+import net.minecraft.item.Item;
+import net.minecraft.registry.Registries;
+import net.minecraft.util.Identifier;
+
+import java.io.IOException;
+import java.io.Reader;
+import java.io.Writer;
+import java.nio.file.Files;
+import java.nio.file.Path;
+import java.util.Optional;
+import java.util.Properties;
+
+public final class ItemSwapConfig {
+    private static final String CONFIG_FILE_NAME = "untitled-item-swap.properties";
+    private static final String DEFAULT_ITEM_A = "minecraft:golden_apple";
+    private static final String DEFAULT_ITEM_B = "minecraft:totem_of_undying";
+
+    private final String itemAId;
+    private final String itemBId;
+    private final boolean allowInventorySwap;
+
+    private ItemSwapConfig(String itemAId, String itemBId, boolean allowInventorySwap) {
+        this.itemAId = itemAId;
+        this.itemBId = itemBId;
+        this.allowInventorySwap = allowInventorySwap;
+    }
+
+    public static ItemSwapConfig load() {
+        Path path = FabricLoader.getInstance().getConfigDir().resolve(CONFIG_FILE_NAME);
+        Properties properties = defaultProperties();
+
+        if (Files.exists(path)) {
+            try (Reader reader = Files.newBufferedReader(path)) {
+                properties.load(reader);
+            } catch (IOException ignored) {
+                return fromProperties(properties);
+            }
+        } else {
+            writeDefaults(path, properties);
+        }
+
+        return fromProperties(properties);
+    }
+
+    public Optional<Item> itemA() {
+        return resolveItem(itemAId);
+    }
+
+    public Optional<Item> itemB() {
+        return resolveItem(itemBId);
+    }
+
+    public boolean allowInventorySwap() {
+        return allowInventorySwap;
+    }
+
+    private static Properties defaultProperties() {
+        Properties properties = new Properties();
+        properties.setProperty("itemA", DEFAULT_ITEM_A);
+        properties.setProperty("itemB", DEFAULT_ITEM_B);
+        properties.setProperty("allowInventorySwap", "true");
+        return properties;
+    }
+
+    private static ItemSwapConfig fromProperties(Properties properties) {
+        return new ItemSwapConfig(
+                properties.getProperty("itemA", DEFAULT_ITEM_A).trim(),
+                properties.getProperty("itemB", DEFAULT_ITEM_B).trim(),
+                Boolean.parseBoolean(properties.getProperty("allowInventorySwap", "true").trim())
+        );
+    }
+
+    private static void writeDefaults(Path path, Properties properties) {
+        try {
+            Files.createDirectories(path.getParent());
+            try (Writer writer = Files.newBufferedWriter(path)) {
+                properties.store(writer, "Untitled item A/B swap settings");
+            }
+        } catch (IOException ignored) {
+        }
+    }
+
+    private static Optional<Item> resolveItem(String rawId) {
+        Identifier id = Identifier.tryParse(rawId);
+        if (id == null || !Registries.ITEM.containsId(id)) {
+            return Optional.empty();
+        }
+
+        return Optional.of(Registries.ITEM.get(id));
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
new file mode 100644
index 0000000000000000000000000000000000000000..1627b1c02312751fdcee2e7b2abe108fb036a763
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
@@ -0,0 +1,107 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.client.MinecraftClient;
+import net.minecraft.client.network.ClientPlayerEntity;
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+import net.minecraft.screen.slot.SlotActionType;
+
+import java.util.Optional;
+import java.util.OptionalInt;
+
+public final class ItemSwapController {
+    private static final int PLAYER_SCREEN_HOTBAR_SLOT_OFFSET = 36;
+
+    private final ItemSwapConfig config;
+    private final InventoryItemFinder finder;
+
+    public ItemSwapController(ItemSwapConfig config) {
+        this.config = config;
+        this.finder = new InventoryItemFinder();
+    }
+
+    public boolean toggle(MinecraftClient client) {
+        if (client.player == null || client.interactionManager == null || client.currentScreen != null) {
+            return false;
+        }
+
+        Optional<Item> itemA = config.itemA();
+        Optional<Item> itemB = config.itemB();
+        if (itemA.isEmpty() || itemB.isEmpty()) {
+            return false;
+        }
+
+        ClientPlayerEntity player = client.player;
+        PlayerInventory inventory = player.getInventory();
+        Item target = targetItem(inventory.getStack(inventory.selectedSlot), itemA.get(), itemB.get());
+        OptionalInt targetSlot = finder.find(inventory, target);
+        if (targetSlot.isEmpty()) {
+            return false;
+        }
+
+        return switchToSlot(client, player, inventory, targetSlot.getAsInt(), itemA.get(), itemB.get());
+    }
+
+    private Item targetItem(ItemStack selectedStack, Item itemA, Item itemB) {
+        if (selectedStack.isOf(itemA)) {
+            return itemB;
+        }
+        if (selectedStack.isOf(itemB)) {
+            return itemA;
+        }
+
+        return itemA;
+    }
+
+    private boolean switchToSlot(
+            MinecraftClient client,
+            ClientPlayerEntity player,
+            PlayerInventory inventory,
+            int inventorySlot,
+            Item itemA,
+            Item itemB
+    ) {
+        if (inventorySlot < InventoryItemFinder.HOTBAR_SIZE) {
+            selectHotbarSlot(client, inventory, inventorySlot);
+            return true;
+        }
+
+        if (!config.allowInventorySwap()) {
+            return false;
+        }
+
+        OptionalInt destinationSlot = safeHotbarDestination(inventory, itemA, itemB);
+        if (destinationSlot.isEmpty()) {
+            return false;
+        }
+
+        client.interactionManager.clickSlot(
+                player.playerScreenHandler.syncId,
+                inventorySlot,
+                destinationSlot.getAsInt(),
+                SlotActionType.SWAP,
+                player
+        );
+        selectHotbarSlot(client, inventory, destinationSlot.getAsInt());
+        return true;
+    }
+
+    private OptionalInt safeHotbarDestination(PlayerInventory inventory, Item itemA, Item itemB) {
+        ItemStack selectedStack = inventory.getStack(inventory.selectedSlot);
+        if (selectedStack.isEmpty() || selectedStack.isOf(itemA) || selectedStack.isOf(itemB)) {
+            return OptionalInt.of(inventory.selectedSlot);
+        }
+
+        return finder.findEmptyHotbarSlot(inventory);
+    }
+
+    private void selectHotbarSlot(MinecraftClient client, PlayerInventory inventory, int slot) {
+        if (inventory.selectedSlot == slot) {
+            return;
+        }
+
+        inventory.selectedSlot = slot;
+        client.interactionManager.syncSelectedSlot();
+    }
+}

exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Add-Type -AssemblyName System.IO.Compression.FileSystem
function Get-ClassStrings([string]$jar, [string]$entryName) {
  $zip=[IO.Compression.ZipFile]::OpenRead($jar)
  try {
    $entry=$zip.GetEntry($entryName)
    $stream=$entry.Open()
    $ms=New-Object IO.MemoryStream
    $stream.CopyTo($ms)
    $bytes=$ms.ToArray()
    $text=[Text.Encoding]::Latin1.GetString($bytes)
    [regex]::Matches($text, '"'[A-Za-z_"'$][A-Za-z0-9_$]{2,}'"') | ForEach-Object { "'$_.Value } | Sort-Object -Unique
  } finally { if ($stream) { $stream.Dispose() }; $zip.Dispose() }
}
$clientJar = Resolve-Path '"'.gradle\\loom-cache\\minecraftMaven\\net\\minecraft\\minecraft-clientOnly-*\\1.21.1-*\\minecraft-clientOnly-*.jar' | Select-Object -First 1 -ExpandProperty Path
Get-ClassStrings "'$clientJar '"'net/minecraft/client/network/ClientPlayerInteractionManager.class' | Select-String 'sync|click|Slot|selected|interact'" in C:\Users\sutec\IdeaProjects\untitled
 succeeded in 871ms:
null 値の式ではメソッドを呼び出せません。
発生場所 行:11 文字:5
+     $text=[Text.Encoding]::Latin1.GetString($bytes)
+     ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    + CategoryInfo          : InvalidOperation: (:) []、RuntimeException
    + FullyQualifiedErrorId : InvokeMethodOnNull
 

diff --git a/src/client/java/org/ahaha/untitled/client/UntitledClient.java b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
index 96f2fa5caf72eb792f660392a6ef7bab77503eb9..760523f1a4590b451ae9a35dd4bed823b44da6a1
--- a/src/client/java/org/ahaha/untitled/client/UntitledClient.java
+++ b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
@@ -1,10 +1,32 @@
 package org.ahaha.untitled.client;
 
 import net.fabricmc.api.ClientModInitializer;
+import net.fabricmc.fabric.api.client.event.lifecycle.v1.ClientTickEvents;
+import net.fabricmc.fabric.api.client.keybinding.v1.KeyBindingHelper;
+import net.minecraft.client.option.KeyBinding;
+import net.minecraft.client.util.InputUtil;
+import org.ahaha.untitled.client.itemswap.ItemSwapConfig;
+import org.ahaha.untitled.client.itemswap.ItemSwapController;
+import org.lwjgl.glfw.GLFW;
 
 public class UntitledClient implements ClientModInitializer {
+    private static final String CATEGORY = "key.category.untitled";
+    private static final String SWAP_KEY = "key.untitled.swap_item_a_b";
 
     @Override
     public void onInitializeClient() {
+        ItemSwapController controller = new ItemSwapController(ItemSwapConfig.load());
+        KeyBinding swapKey = KeyBindingHelper.registerKeyBinding(new KeyBinding(
+                SWAP_KEY,
+                InputUtil.Type.KEYSYM,
+                GLFW.GLFW_KEY_UNKNOWN,
+                CATEGORY
+        ));
+
+        ClientTickEvents.END_CLIENT_TICK.register(client -> {
+            while (swapKey.wasPressed()) {
+                controller.toggle(client);
+            }
+        });
     }
 }
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
new file mode 100644
index 0000000000000000000000000000000000000000..3214363102de082962b3401df416e73144012217
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
@@ -0,0 +1,44 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+
+import java.util.OptionalInt;
+
+public final class InventoryItemFinder {
+    public static final int HOTBAR_SIZE = 9;
+    public static final int MAIN_INVENTORY_SIZE = 36;
+
+    public OptionalInt find(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < MAIN_INVENTORY_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findInHotbar(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findEmptyHotbarSlot(PlayerInventory inventory) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            if (inventory.getStack(slot).isEmpty()) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
new file mode 100644
index 0000000000000000000000000000000000000000..e3f80e5c19e04354bc7646bb6acd358895963c32
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
@@ -0,0 +1,94 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.fabricmc.loader.api.FabricLoader;
+import net.minecraft.item.Item;
+import net.minecraft.registry.Registries;
+import net.minecraft.util.Identifier;
+
+import java.io.IOException;
+import java.io.Reader;
+import java.io.Writer;
+import java.nio.file.Files;
+import java.nio.file.Path;
+import java.util.Optional;
+import java.util.Properties;
+
+public final class ItemSwapConfig {
+    private static final String CONFIG_FILE_NAME = "untitled-item-swap.properties";
+    private static final String DEFAULT_ITEM_A = "minecraft:golden_apple";
+    private static final String DEFAULT_ITEM_B = "minecraft:totem_of_undying";
+
+    private final String itemAId;
+    private final String itemBId;
+    private final boolean allowInventorySwap;
+
+    private ItemSwapConfig(String itemAId, String itemBId, boolean allowInventorySwap) {
+        this.itemAId = itemAId;
+        this.itemBId = itemBId;
+        this.allowInventorySwap = allowInventorySwap;
+    }
+
+    public static ItemSwapConfig load() {
+        Path path = FabricLoader.getInstance().getConfigDir().resolve(CONFIG_FILE_NAME);
+        Properties properties = defaultProperties();
+
+        if (Files.exists(path)) {
+            try (Reader reader = Files.newBufferedReader(path)) {
+                properties.load(reader);
+            } catch (IOException ignored) {
+                return fromProperties(properties);
+            }
+        } else {
+            writeDefaults(path, properties);
+        }
+
+        return fromProperties(properties);
+    }
+
+    public Optional<Item> itemA() {
+        return resolveItem(itemAId);
+    }
+
+    public Optional<Item> itemB() {
+        return resolveItem(itemBId);
+    }
+
+    public boolean allowInventorySwap() {
+        return allowInventorySwap;
+    }
+
+    private static Properties defaultProperties() {
+        Properties properties = new Properties();
+        properties.setProperty("itemA", DEFAULT_ITEM_A);
+        properties.setProperty("itemB", DEFAULT_ITEM_B);
+        properties.setProperty("allowInventorySwap", "true");
+        return properties;
+    }
+
+    private static ItemSwapConfig fromProperties(Properties properties) {
+        return new ItemSwapConfig(
+                properties.getProperty("itemA", DEFAULT_ITEM_A).trim(),
+                properties.getProperty("itemB", DEFAULT_ITEM_B).trim(),
+                Boolean.parseBoolean(properties.getProperty("allowInventorySwap", "true").trim())
+        );
+    }
+
+    private static void writeDefaults(Path path, Properties properties) {
+        try {
+            Files.createDirectories(path.getParent());
+            try (Writer writer = Files.newBufferedWriter(path)) {
+                properties.store(writer, "Untitled item A/B swap settings");
+            }
+        } catch (IOException ignored) {
+        }
+    }
+
+    private static Optional<Item> resolveItem(String rawId) {
+        Identifier id = Identifier.tryParse(rawId);
+        if (id == null || !Registries.ITEM.containsId(id)) {
+            return Optional.empty();
+        }
+
+        return Optional.of(Registries.ITEM.get(id));
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
new file mode 100644
index 0000000000000000000000000000000000000000..1627b1c02312751fdcee2e7b2abe108fb036a763
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
@@ -0,0 +1,107 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.client.MinecraftClient;
+import net.minecraft.client.network.ClientPlayerEntity;
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+import net.minecraft.screen.slot.SlotActionType;
+
+import java.util.Optional;
+import java.util.OptionalInt;
+
+public final class ItemSwapController {
+    private static final int PLAYER_SCREEN_HOTBAR_SLOT_OFFSET = 36;
+
+    private final ItemSwapConfig config;
+    private final InventoryItemFinder finder;
+
+    public ItemSwapController(ItemSwapConfig config) {
+        this.config = config;
+        this.finder = new InventoryItemFinder();
+    }
+
+    public boolean toggle(MinecraftClient client) {
+        if (client.player == null || client.interactionManager == null || client.currentScreen != null) {
+            return false;
+        }
+
+        Optional<Item> itemA = config.itemA();
+        Optional<Item> itemB = config.itemB();
+        if (itemA.isEmpty() || itemB.isEmpty()) {
+            return false;
+        }
+
+        ClientPlayerEntity player = client.player;
+        PlayerInventory inventory = player.getInventory();
+        Item target = targetItem(inventory.getStack(inventory.selectedSlot), itemA.get(), itemB.get());
+        OptionalInt targetSlot = finder.find(inventory, target);
+        if (targetSlot.isEmpty()) {
+            return false;
+        }
+
+        return switchToSlot(client, player, inventory, targetSlot.getAsInt(), itemA.get(), itemB.get());
+    }
+
+    private Item targetItem(ItemStack selectedStack, Item itemA, Item itemB) {
+        if (selectedStack.isOf(itemA)) {
+            return itemB;
+        }
+        if (selectedStack.isOf(itemB)) {
+            return itemA;
+        }
+
+        return itemA;
+    }
+
+    private boolean switchToSlot(
+            MinecraftClient client,
+            ClientPlayerEntity player,
+            PlayerInventory inventory,
+            int inventorySlot,
+            Item itemA,
+            Item itemB
+    ) {
+        if (inventorySlot < InventoryItemFinder.HOTBAR_SIZE) {
+            selectHotbarSlot(client, inventory, inventorySlot);
+            return true;
+        }
+
+        if (!config.allowInventorySwap()) {
+            return false;
+        }
+
+        OptionalInt destinationSlot = safeHotbarDestination(inventory, itemA, itemB);
+        if (destinationSlot.isEmpty()) {
+            return false;
+        }
+
+        client.interactionManager.clickSlot(
+                player.playerScreenHandler.syncId,
+                inventorySlot,
+                destinationSlot.getAsInt(),
+                SlotActionType.SWAP,
+                player
+        );
+        selectHotbarSlot(client, inventory, destinationSlot.getAsInt());
+        return true;
+    }
+
+    private OptionalInt safeHotbarDestination(PlayerInventory inventory, Item itemA, Item itemB) {
+        ItemStack selectedStack = inventory.getStack(inventory.selectedSlot);
+        if (selectedStack.isEmpty() || selectedStack.isOf(itemA) || selectedStack.isOf(itemB)) {
+            return OptionalInt.of(inventory.selectedSlot);
+        }
+
+        return finder.findEmptyHotbarSlot(inventory);
+    }
+
+    private void selectHotbarSlot(MinecraftClient client, PlayerInventory inventory, int slot) {
+        if (inventory.selectedSlot == slot) {
+            return;
+        }
+
+        inventory.selectedSlot = slot;
+        client.interactionManager.syncSelectedSlot();
+    }
+}

exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Add-Type -AssemblyName System.IO.Compression.FileSystem
function Get-ClassStrings([string]$jar, [string]$entryName) {
  $zip=[IO.Compression.ZipFile]::OpenRead($jar)
  try {
    $entry=$zip.GetEntry($entryName)
    if ($null -eq $entry) { Write-Output "MISSING:$entryName"; return }
    $stream=$entry.Open()
    $ms=New-Object IO.MemoryStream
    $stream.CopyTo($ms)
    $bytes=$ms.ToArray()
    $text=[Text.Encoding]::GetEncoding('"'ISO-8859-1').GetString("'$bytes)
    [regex]::Matches($text, '"'[A-Za-z_"'$][A-Za-z0-9_$]{2,}'"') | ForEach-Object { "'$_.Value } | Sort-Object -Unique
  } finally { if ($stream) { $stream.Dispose() }; $zip.Dispose() }
}
$clientJar = Resolve-Path '"'.gradle\\loom-cache\\minecraftMaven\\net\\minecraft\\minecraft-clientOnly-*\\1.21.1-*\\minecraft-clientOnly-*.jar' | Select-Object -First 1 -ExpandProperty Path
Get-ClassStrings "'$clientJar '"'net/minecraft/client/network/ClientPlayerInteractionManager.class' | Select-String 'sync|click|Slot|selected|interact'" in C:\Users\sutec\IdeaProjects\untitled
 succeeded in 970ms:

ButtonClickC2SPacket
Click
clickButton
clickCreativeStack
clickRecipe
clickSlot
ClickSlotC2SPacket
ClientPlayerInteractionManager
interact
interactAt
interactBlock
interactBlockInternal
interactEntity
interactEntityAtLocation
interactItem
lastSelectedSlot
onSlotClick
PASS_TO_DEFAULT_BLOCK_INTERACTION
PlayerInteractBlockC2SPacket
PlayerInteractEntityC2SPacket
PlayerInteractItemC2SPacket
selectedSlot
selectedStack
shouldCancelInteraction
slot
SlotActionType
slotChangedState
SlotChangedStateC2SPacket
slotId
slots
syncId
syncSelectedSlot
UpdateSelectedSlotC2SPacket



diff --git a/src/client/java/org/ahaha/untitled/client/UntitledClient.java b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
index 96f2fa5caf72eb792f660392a6ef7bab77503eb9..760523f1a4590b451ae9a35dd4bed823b44da6a1
--- a/src/client/java/org/ahaha/untitled/client/UntitledClient.java
+++ b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
@@ -1,10 +1,32 @@
 package org.ahaha.untitled.client;
 
 import net.fabricmc.api.ClientModInitializer;
+import net.fabricmc.fabric.api.client.event.lifecycle.v1.ClientTickEvents;
+import net.fabricmc.fabric.api.client.keybinding.v1.KeyBindingHelper;
+import net.minecraft.client.option.KeyBinding;
+import net.minecraft.client.util.InputUtil;
+import org.ahaha.untitled.client.itemswap.ItemSwapConfig;
+import org.ahaha.untitled.client.itemswap.ItemSwapController;
+import org.lwjgl.glfw.GLFW;
 
 public class UntitledClient implements ClientModInitializer {
+    private static final String CATEGORY = "key.category.untitled";
+    private static final String SWAP_KEY = "key.untitled.swap_item_a_b";
 
     @Override
     public void onInitializeClient() {
+        ItemSwapController controller = new ItemSwapController(ItemSwapConfig.load());
+        KeyBinding swapKey = KeyBindingHelper.registerKeyBinding(new KeyBinding(
+                SWAP_KEY,
+                InputUtil.Type.KEYSYM,
+                GLFW.GLFW_KEY_UNKNOWN,
+                CATEGORY
+        ));
+
+        ClientTickEvents.END_CLIENT_TICK.register(client -> {
+            while (swapKey.wasPressed()) {
+                controller.toggle(client);
+            }
+        });
     }
 }
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
new file mode 100644
index 0000000000000000000000000000000000000000..3214363102de082962b3401df416e73144012217
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
@@ -0,0 +1,44 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+
+import java.util.OptionalInt;
+
+public final class InventoryItemFinder {
+    public static final int HOTBAR_SIZE = 9;
+    public static final int MAIN_INVENTORY_SIZE = 36;
+
+    public OptionalInt find(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < MAIN_INVENTORY_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findInHotbar(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findEmptyHotbarSlot(PlayerInventory inventory) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            if (inventory.getStack(slot).isEmpty()) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
new file mode 100644
index 0000000000000000000000000000000000000000..e3f80e5c19e04354bc7646bb6acd358895963c32
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
@@ -0,0 +1,94 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.fabricmc.loader.api.FabricLoader;
+import net.minecraft.item.Item;
+import net.minecraft.registry.Registries;
+import net.minecraft.util.Identifier;
+
+import java.io.IOException;
+import java.io.Reader;
+import java.io.Writer;
+import java.nio.file.Files;
+import java.nio.file.Path;
+import java.util.Optional;
+import java.util.Properties;
+
+public final class ItemSwapConfig {
+    private static final String CONFIG_FILE_NAME = "untitled-item-swap.properties";
+    private static final String DEFAULT_ITEM_A = "minecraft:golden_apple";
+    private static final String DEFAULT_ITEM_B = "minecraft:totem_of_undying";
+
+    private final String itemAId;
+    private final String itemBId;
+    private final boolean allowInventorySwap;
+
+    private ItemSwapConfig(String itemAId, String itemBId, boolean allowInventorySwap) {
+        this.itemAId = itemAId;
+        this.itemBId = itemBId;
+        this.allowInventorySwap = allowInventorySwap;
+    }
+
+    public static ItemSwapConfig load() {
+        Path path = FabricLoader.getInstance().getConfigDir().resolve(CONFIG_FILE_NAME);
+        Properties properties = defaultProperties();
+
+        if (Files.exists(path)) {
+            try (Reader reader = Files.newBufferedReader(path)) {
+                properties.load(reader);
+            } catch (IOException ignored) {
+                return fromProperties(properties);
+            }
+        } else {
+            writeDefaults(path, properties);
+        }
+
+        return fromProperties(properties);
+    }
+
+    public Optional<Item> itemA() {
+        return resolveItem(itemAId);
+    }
+
+    public Optional<Item> itemB() {
+        return resolveItem(itemBId);
+    }
+
+    public boolean allowInventorySwap() {
+        return allowInventorySwap;
+    }
+
+    private static Properties defaultProperties() {
+        Properties properties = new Properties();
+        properties.setProperty("itemA", DEFAULT_ITEM_A);
+        properties.setProperty("itemB", DEFAULT_ITEM_B);
+        properties.setProperty("allowInventorySwap", "true");
+        return properties;
+    }
+
+    private static ItemSwapConfig fromProperties(Properties properties) {
+        return new ItemSwapConfig(
+                properties.getProperty("itemA", DEFAULT_ITEM_A).trim(),
+                properties.getProperty("itemB", DEFAULT_ITEM_B).trim(),
+                Boolean.parseBoolean(properties.getProperty("allowInventorySwap", "true").trim())
+        );
+    }
+
+    private static void writeDefaults(Path path, Properties properties) {
+        try {
+            Files.createDirectories(path.getParent());
+            try (Writer writer = Files.newBufferedWriter(path)) {
+                properties.store(writer, "Untitled item A/B swap settings");
+            }
+        } catch (IOException ignored) {
+        }
+    }
+
+    private static Optional<Item> resolveItem(String rawId) {
+        Identifier id = Identifier.tryParse(rawId);
+        if (id == null || !Registries.ITEM.containsId(id)) {
+            return Optional.empty();
+        }
+
+        return Optional.of(Registries.ITEM.get(id));
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
new file mode 100644
index 0000000000000000000000000000000000000000..1627b1c02312751fdcee2e7b2abe108fb036a763
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
@@ -0,0 +1,107 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.client.MinecraftClient;
+import net.minecraft.client.network.ClientPlayerEntity;
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+import net.minecraft.screen.slot.SlotActionType;
+
+import java.util.Optional;
+import java.util.OptionalInt;
+
+public final class ItemSwapController {
+    private static final int PLAYER_SCREEN_HOTBAR_SLOT_OFFSET = 36;
+
+    private final ItemSwapConfig config;
+    private final InventoryItemFinder finder;
+
+    public ItemSwapController(ItemSwapConfig config) {
+        this.config = config;
+        this.finder = new InventoryItemFinder();
+    }
+
+    public boolean toggle(MinecraftClient client) {
+        if (client.player == null || client.interactionManager == null || client.currentScreen != null) {
+            return false;
+        }
+
+        Optional<Item> itemA = config.itemA();
+        Optional<Item> itemB = config.itemB();
+        if (itemA.isEmpty() || itemB.isEmpty()) {
+            return false;
+        }
+
+        ClientPlayerEntity player = client.player;
+        PlayerInventory inventory = player.getInventory();
+        Item target = targetItem(inventory.getStack(inventory.selectedSlot), itemA.get(), itemB.get());
+        OptionalInt targetSlot = finder.find(inventory, target);
+        if (targetSlot.isEmpty()) {
+            return false;
+        }
+
+        return switchToSlot(client, player, inventory, targetSlot.getAsInt(), itemA.get(), itemB.get());
+    }
+
+    private Item targetItem(ItemStack selectedStack, Item itemA, Item itemB) {
+        if (selectedStack.isOf(itemA)) {
+            return itemB;
+        }
+        if (selectedStack.isOf(itemB)) {
+            return itemA;
+        }
+
+        return itemA;
+    }
+
+    private boolean switchToSlot(
+            MinecraftClient client,
+            ClientPlayerEntity player,
+            PlayerInventory inventory,
+            int inventorySlot,
+            Item itemA,
+            Item itemB
+    ) {
+        if (inventorySlot < InventoryItemFinder.HOTBAR_SIZE) {
+            selectHotbarSlot(client, inventory, inventorySlot);
+            return true;
+        }
+
+        if (!config.allowInventorySwap()) {
+            return false;
+        }
+
+        OptionalInt destinationSlot = safeHotbarDestination(inventory, itemA, itemB);
+        if (destinationSlot.isEmpty()) {
+            return false;
+        }
+
+        client.interactionManager.clickSlot(
+                player.playerScreenHandler.syncId,
+                inventorySlot,
+                destinationSlot.getAsInt(),
+                SlotActionType.SWAP,
+                player
+        );
+        selectHotbarSlot(client, inventory, destinationSlot.getAsInt());
+        return true;
+    }
+
+    private OptionalInt safeHotbarDestination(PlayerInventory inventory, Item itemA, Item itemB) {
+        ItemStack selectedStack = inventory.getStack(inventory.selectedSlot);
+        if (selectedStack.isEmpty() || selectedStack.isOf(itemA) || selectedStack.isOf(itemB)) {
+            return OptionalInt.of(inventory.selectedSlot);
+        }
+
+        return finder.findEmptyHotbarSlot(inventory);
+    }
+
+    private void selectHotbarSlot(MinecraftClient client, PlayerInventory inventory, int slot) {
+        if (inventory.selectedSlot == slot) {
+            return;
+        }
+
+        inventory.selectedSlot = slot;
+        client.interactionManager.syncSelectedSlot();
+    }
+}

exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Add-Type -AssemblyName System.IO.Compression.FileSystem
$commonJar = Resolve-Path '"'.gradle\\loom-cache\\minecraftMaven\\net\\minecraft\\minecraft-common-*\\1.21.1-*\\minecraft-common-*.jar' | Select-Object -First 1 -ExpandProperty Path
"'$zip=[IO.Compression.ZipFile]::OpenRead($commonJar); $zip.Entries | Where-Object { $_.FullName -in @('"'net/minecraft/entity/player/PlayerInventory.class','net/minecraft/screen/slot/SlotActionType.class','net/minecraft/registry/Registries.class','net/minecraft/util/Identifier.class') } | Select-Object -ExpandProperty FullName; "'$zip.Dispose()' in C:\Users\sutec\IdeaProjects\untitled
 succeeded in 918ms:
net/minecraft/entity/player/PlayerInventory.class
net/minecraft/screen/slot/SlotActionType.class
net/minecraft/registry/Registries.class
net/minecraft/util/Identifier.class

diff --git a/src/client/java/org/ahaha/untitled/client/UntitledClient.java b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
index 96f2fa5caf72eb792f660392a6ef7bab77503eb9..760523f1a4590b451ae9a35dd4bed823b44da6a1
--- a/src/client/java/org/ahaha/untitled/client/UntitledClient.java
+++ b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
@@ -1,10 +1,32 @@
 package org.ahaha.untitled.client;
 
 import net.fabricmc.api.ClientModInitializer;
+import net.fabricmc.fabric.api.client.event.lifecycle.v1.ClientTickEvents;
+import net.fabricmc.fabric.api.client.keybinding.v1.KeyBindingHelper;
+import net.minecraft.client.option.KeyBinding;
+import net.minecraft.client.util.InputUtil;
+import org.ahaha.untitled.client.itemswap.ItemSwapConfig;
+import org.ahaha.untitled.client.itemswap.ItemSwapController;
+import org.lwjgl.glfw.GLFW;
 
 public class UntitledClient implements ClientModInitializer {
+    private static final String CATEGORY = "key.category.untitled";
+    private static final String SWAP_KEY = "key.untitled.swap_item_a_b";
 
     @Override
     public void onInitializeClient() {
+        ItemSwapController controller = new ItemSwapController(ItemSwapConfig.load());
+        KeyBinding swapKey = KeyBindingHelper.registerKeyBinding(new KeyBinding(
+                SWAP_KEY,
+                InputUtil.Type.KEYSYM,
+                GLFW.GLFW_KEY_UNKNOWN,
+                CATEGORY
+        ));
+
+        ClientTickEvents.END_CLIENT_TICK.register(client -> {
+            while (swapKey.wasPressed()) {
+                controller.toggle(client);
+            }
+        });
     }
 }
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
new file mode 100644
index 0000000000000000000000000000000000000000..3214363102de082962b3401df416e73144012217
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
@@ -0,0 +1,44 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+
+import java.util.OptionalInt;
+
+public final class InventoryItemFinder {
+    public static final int HOTBAR_SIZE = 9;
+    public static final int MAIN_INVENTORY_SIZE = 36;
+
+    public OptionalInt find(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < MAIN_INVENTORY_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findInHotbar(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findEmptyHotbarSlot(PlayerInventory inventory) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            if (inventory.getStack(slot).isEmpty()) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
new file mode 100644
index 0000000000000000000000000000000000000000..e3f80e5c19e04354bc7646bb6acd358895963c32
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
@@ -0,0 +1,94 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.fabricmc.loader.api.FabricLoader;
+import net.minecraft.item.Item;
+import net.minecraft.registry.Registries;
+import net.minecraft.util.Identifier;
+
+import java.io.IOException;
+import java.io.Reader;
+import java.io.Writer;
+import java.nio.file.Files;
+import java.nio.file.Path;
+import java.util.Optional;
+import java.util.Properties;
+
+public final class ItemSwapConfig {
+    private static final String CONFIG_FILE_NAME = "untitled-item-swap.properties";
+    private static final String DEFAULT_ITEM_A = "minecraft:golden_apple";
+    private static final String DEFAULT_ITEM_B = "minecraft:totem_of_undying";
+
+    private final String itemAId;
+    private final String itemBId;
+    private final boolean allowInventorySwap;
+
+    private ItemSwapConfig(String itemAId, String itemBId, boolean allowInventorySwap) {
+        this.itemAId = itemAId;
+        this.itemBId = itemBId;
+        this.allowInventorySwap = allowInventorySwap;
+    }
+
+    public static ItemSwapConfig load() {
+        Path path = FabricLoader.getInstance().getConfigDir().resolve(CONFIG_FILE_NAME);
+        Properties properties = defaultProperties();
+
+        if (Files.exists(path)) {
+            try (Reader reader = Files.newBufferedReader(path)) {
+                properties.load(reader);
+            } catch (IOException ignored) {
+                return fromProperties(properties);
+            }
+        } else {
+            writeDefaults(path, properties);
+        }
+
+        return fromProperties(properties);
+    }
+
+    public Optional<Item> itemA() {
+        return resolveItem(itemAId);
+    }
+
+    public Optional<Item> itemB() {
+        return resolveItem(itemBId);
+    }
+
+    public boolean allowInventorySwap() {
+        return allowInventorySwap;
+    }
+
+    private static Properties defaultProperties() {
+        Properties properties = new Properties();
+        properties.setProperty("itemA", DEFAULT_ITEM_A);
+        properties.setProperty("itemB", DEFAULT_ITEM_B);
+        properties.setProperty("allowInventorySwap", "true");
+        return properties;
+    }
+
+    private static ItemSwapConfig fromProperties(Properties properties) {
+        return new ItemSwapConfig(
+                properties.getProperty("itemA", DEFAULT_ITEM_A).trim(),
+                properties.getProperty("itemB", DEFAULT_ITEM_B).trim(),
+                Boolean.parseBoolean(properties.getProperty("allowInventorySwap", "true").trim())
+        );
+    }
+
+    private static void writeDefaults(Path path, Properties properties) {
+        try {
+            Files.createDirectories(path.getParent());
+            try (Writer writer = Files.newBufferedWriter(path)) {
+                properties.store(writer, "Untitled item A/B swap settings");
+            }
+        } catch (IOException ignored) {
+        }
+    }
+
+    private static Optional<Item> resolveItem(String rawId) {
+        Identifier id = Identifier.tryParse(rawId);
+        if (id == null || !Registries.ITEM.containsId(id)) {
+            return Optional.empty();
+        }
+
+        return Optional.of(Registries.ITEM.get(id));
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
new file mode 100644
index 0000000000000000000000000000000000000000..1627b1c02312751fdcee2e7b2abe108fb036a763
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
@@ -0,0 +1,107 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.client.MinecraftClient;
+import net.minecraft.client.network.ClientPlayerEntity;
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+import net.minecraft.screen.slot.SlotActionType;
+
+import java.util.Optional;
+import java.util.OptionalInt;
+
+public final class ItemSwapController {
+    private static final int PLAYER_SCREEN_HOTBAR_SLOT_OFFSET = 36;
+
+    private final ItemSwapConfig config;
+    private final InventoryItemFinder finder;
+
+    public ItemSwapController(ItemSwapConfig config) {
+        this.config = config;
+        this.finder = new InventoryItemFinder();
+    }
+
+    public boolean toggle(MinecraftClient client) {
+        if (client.player == null || client.interactionManager == null || client.currentScreen != null) {
+            return false;
+        }
+
+        Optional<Item> itemA = config.itemA();
+        Optional<Item> itemB = config.itemB();
+        if (itemA.isEmpty() || itemB.isEmpty()) {
+            return false;
+        }
+
+        ClientPlayerEntity player = client.player;
+        PlayerInventory inventory = player.getInventory();
+        Item target = targetItem(inventory.getStack(inventory.selectedSlot), itemA.get(), itemB.get());
+        OptionalInt targetSlot = finder.find(inventory, target);
+        if (targetSlot.isEmpty()) {
+            return false;
+        }
+
+        return switchToSlot(client, player, inventory, targetSlot.getAsInt(), itemA.get(), itemB.get());
+    }
+
+    private Item targetItem(ItemStack selectedStack, Item itemA, Item itemB) {
+        if (selectedStack.isOf(itemA)) {
+            return itemB;
+        }
+        if (selectedStack.isOf(itemB)) {
+            return itemA;
+        }
+
+        return itemA;
+    }
+
+    private boolean switchToSlot(
+            MinecraftClient client,
+            ClientPlayerEntity player,
+            PlayerInventory inventory,
+            int inventorySlot,
+            Item itemA,
+            Item itemB
+    ) {
+        if (inventorySlot < InventoryItemFinder.HOTBAR_SIZE) {
+            selectHotbarSlot(client, inventory, inventorySlot);
+            return true;
+        }
+
+        if (!config.allowInventorySwap()) {
+            return false;
+        }
+
+        OptionalInt destinationSlot = safeHotbarDestination(inventory, itemA, itemB);
+        if (destinationSlot.isEmpty()) {
+            return false;
+        }
+
+        client.interactionManager.clickSlot(
+                player.playerScreenHandler.syncId,
+                inventorySlot,
+                destinationSlot.getAsInt(),
+                SlotActionType.SWAP,
+                player
+        );
+        selectHotbarSlot(client, inventory, destinationSlot.getAsInt());
+        return true;
+    }
+
+    private OptionalInt safeHotbarDestination(PlayerInventory inventory, Item itemA, Item itemB) {
+        ItemStack selectedStack = inventory.getStack(inventory.selectedSlot);
+        if (selectedStack.isEmpty() || selectedStack.isOf(itemA) || selectedStack.isOf(itemB)) {
+            return OptionalInt.of(inventory.selectedSlot);
+        }
+
+        return finder.findEmptyHotbarSlot(inventory);
+    }
+
+    private void selectHotbarSlot(MinecraftClient client, PlayerInventory inventory, int slot) {
+        if (inventory.selectedSlot == slot) {
+            return;
+        }
+
+        inventory.selectedSlot = slot;
+        client.interactionManager.syncSelectedSlot();
+    }
+}

exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Add-Type -AssemblyName System.IO.Compression.FileSystem
function Get-ClassStrings([string]$jar, [string]$entryName) {
  $zip=[IO.Compression.ZipFile]::OpenRead($jar)
  try { $entry=$zip.GetEntry($entryName); $stream=$entry.Open(); $ms=New-Object IO.MemoryStream; $stream.CopyTo($ms); $text=[Text.Encoding]::GetEncoding('"'ISO-8859-1').GetString("'$ms.ToArray()); [regex]::Matches($text, '"'[A-Za-z_"'$][A-Za-z0-9_$]{2,}'"') | ForEach-Object { "'$_.Value } | Sort-Object -Unique } finally { if ($stream) { $stream.Dispose() }; $zip.Dispose() }
}
$commonJar = Resolve-Path '"'.gradle\\loom-cache\\minecraftMaven\\net\\minecraft\\minecraft-common-*\\1.21.1-*\\minecraft-common-*.jar' | Select-Object -First 1 -ExpandProperty Path
Get-ClassStrings "'$commonJar '"'net/minecraft/util/Identifier.class' | Select-String 'tryParse|of|Identifier'
'--- PlayerInventory ---'
Get-ClassStrings "'$commonJar '"'net/minecraft/entity/player/PlayerInventory.class' | Select-String 'selectedSlot|getStack|main|size|PlayerInventory'" in C:\Users\sutec\IdeaProjects\untitled
 succeeded in 1117ms:

Identifier
Identifier$Serializer
indexOf
InvalidIdentifierException
ofValidated
ofVanilla
tryParse
--- PlayerInventory ---
getHotbarSize
getMainHandStack
getStack
HOTBAR_SIZE
main
MAIN_SIZE
ofSize
PlayerInventory
selectedSlot
size



diff --git a/src/client/java/org/ahaha/untitled/client/UntitledClient.java b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
index 96f2fa5caf72eb792f660392a6ef7bab77503eb9..760523f1a4590b451ae9a35dd4bed823b44da6a1
--- a/src/client/java/org/ahaha/untitled/client/UntitledClient.java
+++ b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
@@ -1,10 +1,32 @@
 package org.ahaha.untitled.client;
 
 import net.fabricmc.api.ClientModInitializer;
+import net.fabricmc.fabric.api.client.event.lifecycle.v1.ClientTickEvents;
+import net.fabricmc.fabric.api.client.keybinding.v1.KeyBindingHelper;
+import net.minecraft.client.option.KeyBinding;
+import net.minecraft.client.util.InputUtil;
+import org.ahaha.untitled.client.itemswap.ItemSwapConfig;
+import org.ahaha.untitled.client.itemswap.ItemSwapController;
+import org.lwjgl.glfw.GLFW;
 
 public class UntitledClient implements ClientModInitializer {
+    private static final String CATEGORY = "key.category.untitled";
+    private static final String SWAP_KEY = "key.untitled.swap_item_a_b";
 
     @Override
     public void onInitializeClient() {
+        ItemSwapController controller = new ItemSwapController(ItemSwapConfig.load());
+        KeyBinding swapKey = KeyBindingHelper.registerKeyBinding(new KeyBinding(
+                SWAP_KEY,
+                InputUtil.Type.KEYSYM,
+                GLFW.GLFW_KEY_UNKNOWN,
+                CATEGORY
+        ));
+
+        ClientTickEvents.END_CLIENT_TICK.register(client -> {
+            while (swapKey.wasPressed()) {
+                controller.toggle(client);
+            }
+        });
     }
 }
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
new file mode 100644
index 0000000000000000000000000000000000000000..3214363102de082962b3401df416e73144012217
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
@@ -0,0 +1,44 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+
+import java.util.OptionalInt;
+
+public final class InventoryItemFinder {
+    public static final int HOTBAR_SIZE = 9;
+    public static final int MAIN_INVENTORY_SIZE = 36;
+
+    public OptionalInt find(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < MAIN_INVENTORY_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findInHotbar(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findEmptyHotbarSlot(PlayerInventory inventory) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            if (inventory.getStack(slot).isEmpty()) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
new file mode 100644
index 0000000000000000000000000000000000000000..e3f80e5c19e04354bc7646bb6acd358895963c32
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
@@ -0,0 +1,94 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.fabricmc.loader.api.FabricLoader;
+import net.minecraft.item.Item;
+import net.minecraft.registry.Registries;
+import net.minecraft.util.Identifier;
+
+import java.io.IOException;
+import java.io.Reader;
+import java.io.Writer;
+import java.nio.file.Files;
+import java.nio.file.Path;
+import java.util.Optional;
+import java.util.Properties;
+
+public final class ItemSwapConfig {
+    private static final String CONFIG_FILE_NAME = "untitled-item-swap.properties";
+    private static final String DEFAULT_ITEM_A = "minecraft:golden_apple";
+    private static final String DEFAULT_ITEM_B = "minecraft:totem_of_undying";
+
+    private final String itemAId;
+    private final String itemBId;
+    private final boolean allowInventorySwap;
+
+    private ItemSwapConfig(String itemAId, String itemBId, boolean allowInventorySwap) {
+        this.itemAId = itemAId;
+        this.itemBId = itemBId;
+        this.allowInventorySwap = allowInventorySwap;
+    }
+
+    public static ItemSwapConfig load() {
+        Path path = FabricLoader.getInstance().getConfigDir().resolve(CONFIG_FILE_NAME);
+        Properties properties = defaultProperties();
+
+        if (Files.exists(path)) {
+            try (Reader reader = Files.newBufferedReader(path)) {
+                properties.load(reader);
+            } catch (IOException ignored) {
+                return fromProperties(properties);
+            }
+        } else {
+            writeDefaults(path, properties);
+        }
+
+        return fromProperties(properties);
+    }
+
+    public Optional<Item> itemA() {
+        return resolveItem(itemAId);
+    }
+
+    public Optional<Item> itemB() {
+        return resolveItem(itemBId);
+    }
+
+    public boolean allowInventorySwap() {
+        return allowInventorySwap;
+    }
+
+    private static Properties defaultProperties() {
+        Properties properties = new Properties();
+        properties.setProperty("itemA", DEFAULT_ITEM_A);
+        properties.setProperty("itemB", DEFAULT_ITEM_B);
+        properties.setProperty("allowInventorySwap", "true");
+        return properties;
+    }
+
+    private static ItemSwapConfig fromProperties(Properties properties) {
+        return new ItemSwapConfig(
+                properties.getProperty("itemA", DEFAULT_ITEM_A).trim(),
+                properties.getProperty("itemB", DEFAULT_ITEM_B).trim(),
+                Boolean.parseBoolean(properties.getProperty("allowInventorySwap", "true").trim())
+        );
+    }
+
+    private static void writeDefaults(Path path, Properties properties) {
+        try {
+            Files.createDirectories(path.getParent());
+            try (Writer writer = Files.newBufferedWriter(path)) {
+                properties.store(writer, "Untitled item A/B swap settings");
+            }
+        } catch (IOException ignored) {
+        }
+    }
+
+    private static Optional<Item> resolveItem(String rawId) {
+        Identifier id = Identifier.tryParse(rawId);
+        if (id == null || !Registries.ITEM.containsId(id)) {
+            return Optional.empty();
+        }
+
+        return Optional.of(Registries.ITEM.get(id));
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
new file mode 100644
index 0000000000000000000000000000000000000000..1627b1c02312751fdcee2e7b2abe108fb036a763
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
@@ -0,0 +1,107 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.client.MinecraftClient;
+import net.minecraft.client.network.ClientPlayerEntity;
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+import net.minecraft.screen.slot.SlotActionType;
+
+import java.util.Optional;
+import java.util.OptionalInt;
+
+public final class ItemSwapController {
+    private static final int PLAYER_SCREEN_HOTBAR_SLOT_OFFSET = 36;
+
+    private final ItemSwapConfig config;
+    private final InventoryItemFinder finder;
+
+    public ItemSwapController(ItemSwapConfig config) {
+        this.config = config;
+        this.finder = new InventoryItemFinder();
+    }
+
+    public boolean toggle(MinecraftClient client) {
+        if (client.player == null || client.interactionManager == null || client.currentScreen != null) {
+            return false;
+        }
+
+        Optional<Item> itemA = config.itemA();
+        Optional<Item> itemB = config.itemB();
+        if (itemA.isEmpty() || itemB.isEmpty()) {
+            return false;
+        }
+
+        ClientPlayerEntity player = client.player;
+        PlayerInventory inventory = player.getInventory();
+        Item target = targetItem(inventory.getStack(inventory.selectedSlot), itemA.get(), itemB.get());
+        OptionalInt targetSlot = finder.find(inventory, target);
+        if (targetSlot.isEmpty()) {
+            return false;
+        }
+
+        return switchToSlot(client, player, inventory, targetSlot.getAsInt(), itemA.get(), itemB.get());
+    }
+
+    private Item targetItem(ItemStack selectedStack, Item itemA, Item itemB) {
+        if (selectedStack.isOf(itemA)) {
+            return itemB;
+        }
+        if (selectedStack.isOf(itemB)) {
+            return itemA;
+        }
+
+        return itemA;
+    }
+
+    private boolean switchToSlot(
+            MinecraftClient client,
+            ClientPlayerEntity player,
+            PlayerInventory inventory,
+            int inventorySlot,
+            Item itemA,
+            Item itemB
+    ) {
+        if (inventorySlot < InventoryItemFinder.HOTBAR_SIZE) {
+            selectHotbarSlot(client, inventory, inventorySlot);
+            return true;
+        }
+
+        if (!config.allowInventorySwap()) {
+            return false;
+        }
+
+        OptionalInt destinationSlot = safeHotbarDestination(inventory, itemA, itemB);
+        if (destinationSlot.isEmpty()) {
+            return false;
+        }
+
+        client.interactionManager.clickSlot(
+                player.playerScreenHandler.syncId,
+                inventorySlot,
+                destinationSlot.getAsInt(),
+                SlotActionType.SWAP,
+                player
+        );
+        selectHotbarSlot(client, inventory, destinationSlot.getAsInt());
+        return true;
+    }
+
+    private OptionalInt safeHotbarDestination(PlayerInventory inventory, Item itemA, Item itemB) {
+        ItemStack selectedStack = inventory.getStack(inventory.selectedSlot);
+        if (selectedStack.isEmpty() || selectedStack.isOf(itemA) || selectedStack.isOf(itemB)) {
+            return OptionalInt.of(inventory.selectedSlot);
+        }
+
+        return finder.findEmptyHotbarSlot(inventory);
+    }
+
+    private void selectHotbarSlot(MinecraftClient client, PlayerInventory inventory, int slot) {
+        if (inventory.selectedSlot == slot) {
+            return;
+        }
+
+        inventory.selectedSlot = slot;
+        client.interactionManager.syncSelectedSlot();
+    }
+}

codex
コード追加は完了しました。次はローカルで使えるGradle/JDKがあるか確認して、可能なら`compileJava`系まで通します。
exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-ChildItem -Recurse -File -Filter gradlew.bat | Select-Object -ExpandProperty FullName' in C:\Users\sutec\IdeaProjects\untitled
exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-ChildItem -Path 'C:\\Program Files','C:\\Program Files (x86)',"'$env:LOCALAPPDATA -Recurse -File -Filter java.exe -ErrorAction SilentlyContinue | Select-Object -First 20 -ExpandProperty FullName' in C:\Users\sutec\IdeaProjects\untitled
exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Command gradle -ErrorAction SilentlyContinue | Format-List *' in C:\Users\sutec\IdeaProjects\untitled
 succeeded in 779ms:
2026-05-11T07:21:34.326341Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 2.2 seconds
Output:

 exited 1 in 2176ms:
2026-05-11T07:21:34.398506Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 2.3 seconds
Output:
C:\Program Files\JetBrains\IntelliJ IDEA 2026.1.1\jbr\bin\java.exe

 exited 1 in 2261ms:
C:\Program Files\JetBrains\IntelliJ IDEA 2026.1.1\jbr\bin\java.exe

diff --git a/src/client/java/org/ahaha/untitled/client/UntitledClient.java b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
index 96f2fa5caf72eb792f660392a6ef7bab77503eb9..760523f1a4590b451ae9a35dd4bed823b44da6a1
--- a/src/client/java/org/ahaha/untitled/client/UntitledClient.java
+++ b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
@@ -1,10 +1,32 @@
 package org.ahaha.untitled.client;
 
 import net.fabricmc.api.ClientModInitializer;
+import net.fabricmc.fabric.api.client.event.lifecycle.v1.ClientTickEvents;
+import net.fabricmc.fabric.api.client.keybinding.v1.KeyBindingHelper;
+import net.minecraft.client.option.KeyBinding;
+import net.minecraft.client.util.InputUtil;
+import org.ahaha.untitled.client.itemswap.ItemSwapConfig;
+import org.ahaha.untitled.client.itemswap.ItemSwapController;
+import org.lwjgl.glfw.GLFW;
 
 public class UntitledClient implements ClientModInitializer {
+    private static final String CATEGORY = "key.category.untitled";
+    private static final String SWAP_KEY = "key.untitled.swap_item_a_b";
 
     @Override
     public void onInitializeClient() {
+        ItemSwapController controller = new ItemSwapController(ItemSwapConfig.load());
+        KeyBinding swapKey = KeyBindingHelper.registerKeyBinding(new KeyBinding(
+                SWAP_KEY,
+                InputUtil.Type.KEYSYM,
+                GLFW.GLFW_KEY_UNKNOWN,
+                CATEGORY
+        ));
+
+        ClientTickEvents.END_CLIENT_TICK.register(client -> {
+            while (swapKey.wasPressed()) {
+                controller.toggle(client);
+            }
+        });
     }
 }
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
new file mode 100644
index 0000000000000000000000000000000000000000..3214363102de082962b3401df416e73144012217
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
@@ -0,0 +1,44 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+
+import java.util.OptionalInt;
+
+public final class InventoryItemFinder {
+    public static final int HOTBAR_SIZE = 9;
+    public static final int MAIN_INVENTORY_SIZE = 36;
+
+    public OptionalInt find(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < MAIN_INVENTORY_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findInHotbar(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findEmptyHotbarSlot(PlayerInventory inventory) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            if (inventory.getStack(slot).isEmpty()) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
new file mode 100644
index 0000000000000000000000000000000000000000..e3f80e5c19e04354bc7646bb6acd358895963c32
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
@@ -0,0 +1,94 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.fabricmc.loader.api.FabricLoader;
+import net.minecraft.item.Item;
+import net.minecraft.registry.Registries;
+import net.minecraft.util.Identifier;
+
+import java.io.IOException;
+import java.io.Reader;
+import java.io.Writer;
+import java.nio.file.Files;
+import java.nio.file.Path;
+import java.util.Optional;
+import java.util.Properties;
+
+public final class ItemSwapConfig {
+    private static final String CONFIG_FILE_NAME = "untitled-item-swap.properties";
+    private static final String DEFAULT_ITEM_A = "minecraft:golden_apple";
+    private static final String DEFAULT_ITEM_B = "minecraft:totem_of_undying";
+
+    private final String itemAId;
+    private final String itemBId;
+    private final boolean allowInventorySwap;
+
+    private ItemSwapConfig(String itemAId, String itemBId, boolean allowInventorySwap) {
+        this.itemAId = itemAId;
+        this.itemBId = itemBId;
+        this.allowInventorySwap = allowInventorySwap;
+    }
+
+    public static ItemSwapConfig load() {
+        Path path = FabricLoader.getInstance().getConfigDir().resolve(CONFIG_FILE_NAME);
+        Properties properties = defaultProperties();
+
+        if (Files.exists(path)) {
+            try (Reader reader = Files.newBufferedReader(path)) {
+                properties.load(reader);
+            } catch (IOException ignored) {
+                return fromProperties(properties);
+            }
+        } else {
+            writeDefaults(path, properties);
+        }
+
+        return fromProperties(properties);
+    }
+
+    public Optional<Item> itemA() {
+        return resolveItem(itemAId);
+    }
+
+    public Optional<Item> itemB() {
+        return resolveItem(itemBId);
+    }
+
+    public boolean allowInventorySwap() {
+        return allowInventorySwap;
+    }
+
+    private static Properties defaultProperties() {
+        Properties properties = new Properties();
+        properties.setProperty("itemA", DEFAULT_ITEM_A);
+        properties.setProperty("itemB", DEFAULT_ITEM_B);
+        properties.setProperty("allowInventorySwap", "true");
+        return properties;
+    }
+
+    private static ItemSwapConfig fromProperties(Properties properties) {
+        return new ItemSwapConfig(
+                properties.getProperty("itemA", DEFAULT_ITEM_A).trim(),
+                properties.getProperty("itemB", DEFAULT_ITEM_B).trim(),
+                Boolean.parseBoolean(properties.getProperty("allowInventorySwap", "true").trim())
+        );
+    }
+
+    private static void writeDefaults(Path path, Properties properties) {
+        try {
+            Files.createDirectories(path.getParent());
+            try (Writer writer = Files.newBufferedWriter(path)) {
+                properties.store(writer, "Untitled item A/B swap settings");
+            }
+        } catch (IOException ignored) {
+        }
+    }
+
+    private static Optional<Item> resolveItem(String rawId) {
+        Identifier id = Identifier.tryParse(rawId);
+        if (id == null || !Registries.ITEM.containsId(id)) {
+            return Optional.empty();
+        }
+
+        return Optional.of(Registries.ITEM.get(id));
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
new file mode 100644
index 0000000000000000000000000000000000000000..1627b1c02312751fdcee2e7b2abe108fb036a763
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
@@ -0,0 +1,107 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.client.MinecraftClient;
+import net.minecraft.client.network.ClientPlayerEntity;
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+import net.minecraft.screen.slot.SlotActionType;
+
+import java.util.Optional;
+import java.util.OptionalInt;
+
+public final class ItemSwapController {
+    private static final int PLAYER_SCREEN_HOTBAR_SLOT_OFFSET = 36;
+
+    private final ItemSwapConfig config;
+    private final InventoryItemFinder finder;
+
+    public ItemSwapController(ItemSwapConfig config) {
+        this.config = config;
+        this.finder = new InventoryItemFinder();
+    }
+
+    public boolean toggle(MinecraftClient client) {
+        if (client.player == null || client.interactionManager == null || client.currentScreen != null) {
+            return false;
+        }
+
+        Optional<Item> itemA = config.itemA();
+        Optional<Item> itemB = config.itemB();
+        if (itemA.isEmpty() || itemB.isEmpty()) {
+            return false;
+        }
+
+        ClientPlayerEntity player = client.player;
+        PlayerInventory inventory = player.getInventory();
+        Item target = targetItem(inventory.getStack(inventory.selectedSlot), itemA.get(), itemB.get());
+        OptionalInt targetSlot = finder.find(inventory, target);
+        if (targetSlot.isEmpty()) {
+            return false;
+        }
+
+        return switchToSlot(client, player, inventory, targetSlot.getAsInt(), itemA.get(), itemB.get());
+    }
+
+    private Item targetItem(ItemStack selectedStack, Item itemA, Item itemB) {
+        if (selectedStack.isOf(itemA)) {
+            return itemB;
+        }
+        if (selectedStack.isOf(itemB)) {
+            return itemA;
+        }
+
+        return itemA;
+    }
+
+    private boolean switchToSlot(
+            MinecraftClient client,
+            ClientPlayerEntity player,
+            PlayerInventory inventory,
+            int inventorySlot,
+            Item itemA,
+            Item itemB
+    ) {
+        if (inventorySlot < InventoryItemFinder.HOTBAR_SIZE) {
+            selectHotbarSlot(client, inventory, inventorySlot);
+            return true;
+        }
+
+        if (!config.allowInventorySwap()) {
+            return false;
+        }
+
+        OptionalInt destinationSlot = safeHotbarDestination(inventory, itemA, itemB);
+        if (destinationSlot.isEmpty()) {
+            return false;
+        }
+
+        client.interactionManager.clickSlot(
+                player.playerScreenHandler.syncId,
+                inventorySlot,
+                destinationSlot.getAsInt(),
+                SlotActionType.SWAP,
+                player
+        );
+        selectHotbarSlot(client, inventory, destinationSlot.getAsInt());
+        return true;
+    }
+
+    private OptionalInt safeHotbarDestination(PlayerInventory inventory, Item itemA, Item itemB) {
+        ItemStack selectedStack = inventory.getStack(inventory.selectedSlot);
+        if (selectedStack.isEmpty() || selectedStack.isOf(itemA) || selectedStack.isOf(itemB)) {
+            return OptionalInt.of(inventory.selectedSlot);
+        }
+
+        return finder.findEmptyHotbarSlot(inventory);
+    }
+
+    private void selectHotbarSlot(MinecraftClient client, PlayerInventory inventory, int slot) {
+        if (inventory.selectedSlot == slot) {
+            return;
+        }
+
+        inventory.selectedSlot = slot;
+        client.interactionManager.syncSelectedSlot();
+    }
+}

exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-ChildItem -Path 'C:\\Program Files\\JetBrains\\IntelliJ IDEA 2026.1.1','C:\\Users\\sutec' -Recurse -File -Include gradle.bat,gradle -ErrorAction SilentlyContinue | Select-Object -First 20 -ExpandProperty FullName" in C:\Users\sutec\IdeaProjects\untitled
2026-05-11T07:21:40.896201Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 1.6 seconds
Output:

 exited 1 in 1605ms:
diff --git a/src/client/java/org/ahaha/untitled/client/UntitledClient.java b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
index 96f2fa5caf72eb792f660392a6ef7bab77503eb9..760523f1a4590b451ae9a35dd4bed823b44da6a1
--- a/src/client/java/org/ahaha/untitled/client/UntitledClient.java
+++ b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
@@ -1,10 +1,32 @@
 package org.ahaha.untitled.client;
 
 import net.fabricmc.api.ClientModInitializer;
+import net.fabricmc.fabric.api.client.event.lifecycle.v1.ClientTickEvents;
+import net.fabricmc.fabric.api.client.keybinding.v1.KeyBindingHelper;
+import net.minecraft.client.option.KeyBinding;
+import net.minecraft.client.util.InputUtil;
+import org.ahaha.untitled.client.itemswap.ItemSwapConfig;
+import org.ahaha.untitled.client.itemswap.ItemSwapController;
+import org.lwjgl.glfw.GLFW;
 
 public class UntitledClient implements ClientModInitializer {
+    private static final String CATEGORY = "key.category.untitled";
+    private static final String SWAP_KEY = "key.untitled.swap_item_a_b";
 
     @Override
     public void onInitializeClient() {
+        ItemSwapController controller = new ItemSwapController(ItemSwapConfig.load());
+        KeyBinding swapKey = KeyBindingHelper.registerKeyBinding(new KeyBinding(
+                SWAP_KEY,
+                InputUtil.Type.KEYSYM,
+                GLFW.GLFW_KEY_UNKNOWN,
+                CATEGORY
+        ));
+
+        ClientTickEvents.END_CLIENT_TICK.register(client -> {
+            while (swapKey.wasPressed()) {
+                controller.toggle(client);
+            }
+        });
     }
 }
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
new file mode 100644
index 0000000000000000000000000000000000000000..3214363102de082962b3401df416e73144012217
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
@@ -0,0 +1,44 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+
+import java.util.OptionalInt;
+
+public final class InventoryItemFinder {
+    public static final int HOTBAR_SIZE = 9;
+    public static final int MAIN_INVENTORY_SIZE = 36;
+
+    public OptionalInt find(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < MAIN_INVENTORY_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findInHotbar(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findEmptyHotbarSlot(PlayerInventory inventory) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            if (inventory.getStack(slot).isEmpty()) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
new file mode 100644
index 0000000000000000000000000000000000000000..e3f80e5c19e04354bc7646bb6acd358895963c32
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
@@ -0,0 +1,94 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.fabricmc.loader.api.FabricLoader;
+import net.minecraft.item.Item;
+import net.minecraft.registry.Registries;
+import net.minecraft.util.Identifier;
+
+import java.io.IOException;
+import java.io.Reader;
+import java.io.Writer;
+import java.nio.file.Files;
+import java.nio.file.Path;
+import java.util.Optional;
+import java.util.Properties;
+
+public final class ItemSwapConfig {
+    private static final String CONFIG_FILE_NAME = "untitled-item-swap.properties";
+    private static final String DEFAULT_ITEM_A = "minecraft:golden_apple";
+    private static final String DEFAULT_ITEM_B = "minecraft:totem_of_undying";
+
+    private final String itemAId;
+    private final String itemBId;
+    private final boolean allowInventorySwap;
+
+    private ItemSwapConfig(String itemAId, String itemBId, boolean allowInventorySwap) {
+        this.itemAId = itemAId;
+        this.itemBId = itemBId;
+        this.allowInventorySwap = allowInventorySwap;
+    }
+
+    public static ItemSwapConfig load() {
+        Path path = FabricLoader.getInstance().getConfigDir().resolve(CONFIG_FILE_NAME);
+        Properties properties = defaultProperties();
+
+        if (Files.exists(path)) {
+            try (Reader reader = Files.newBufferedReader(path)) {
+                properties.load(reader);
+            } catch (IOException ignored) {
+                return fromProperties(properties);
+            }
+        } else {
+            writeDefaults(path, properties);
+        }
+
+        return fromProperties(properties);
+    }
+
+    public Optional<Item> itemA() {
+        return resolveItem(itemAId);
+    }
+
+    public Optional<Item> itemB() {
+        return resolveItem(itemBId);
+    }
+
+    public boolean allowInventorySwap() {
+        return allowInventorySwap;
+    }
+
+    private static Properties defaultProperties() {
+        Properties properties = new Properties();
+        properties.setProperty("itemA", DEFAULT_ITEM_A);
+        properties.setProperty("itemB", DEFAULT_ITEM_B);
+        properties.setProperty("allowInventorySwap", "true");
+        return properties;
+    }
+
+    private static ItemSwapConfig fromProperties(Properties properties) {
+        return new ItemSwapConfig(
+                properties.getProperty("itemA", DEFAULT_ITEM_A).trim(),
+                properties.getProperty("itemB", DEFAULT_ITEM_B).trim(),
+                Boolean.parseBoolean(properties.getProperty("allowInventorySwap", "true").trim())
+        );
+    }
+
+    private static void writeDefaults(Path path, Properties properties) {
+        try {
+            Files.createDirectories(path.getParent());
+            try (Writer writer = Files.newBufferedWriter(path)) {
+                properties.store(writer, "Untitled item A/B swap settings");
+            }
+        } catch (IOException ignored) {
+        }
+    }
+
+    private static Optional<Item> resolveItem(String rawId) {
+        Identifier id = Identifier.tryParse(rawId);
+        if (id == null || !Registries.ITEM.containsId(id)) {
+            return Optional.empty();
+        }
+
+        return Optional.of(Registries.ITEM.get(id));
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
new file mode 100644
index 0000000000000000000000000000000000000000..1627b1c02312751fdcee2e7b2abe108fb036a763
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
@@ -0,0 +1,107 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.client.MinecraftClient;
+import net.minecraft.client.network.ClientPlayerEntity;
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+import net.minecraft.screen.slot.SlotActionType;
+
+import java.util.Optional;
+import java.util.OptionalInt;
+
+public final class ItemSwapController {
+    private static final int PLAYER_SCREEN_HOTBAR_SLOT_OFFSET = 36;
+
+    private final ItemSwapConfig config;
+    private final InventoryItemFinder finder;
+
+    public ItemSwapController(ItemSwapConfig config) {
+        this.config = config;
+        this.finder = new InventoryItemFinder();
+    }
+
+    public boolean toggle(MinecraftClient client) {
+        if (client.player == null || client.interactionManager == null || client.currentScreen != null) {
+            return false;
+        }
+
+        Optional<Item> itemA = config.itemA();
+        Optional<Item> itemB = config.itemB();
+        if (itemA.isEmpty() || itemB.isEmpty()) {
+            return false;
+        }
+
+        ClientPlayerEntity player = client.player;
+        PlayerInventory inventory = player.getInventory();
+        Item target = targetItem(inventory.getStack(inventory.selectedSlot), itemA.get(), itemB.get());
+        OptionalInt targetSlot = finder.find(inventory, target);
+        if (targetSlot.isEmpty()) {
+            return false;
+        }
+
+        return switchToSlot(client, player, inventory, targetSlot.getAsInt(), itemA.get(), itemB.get());
+    }
+
+    private Item targetItem(ItemStack selectedStack, Item itemA, Item itemB) {
+        if (selectedStack.isOf(itemA)) {
+            return itemB;
+        }
+        if (selectedStack.isOf(itemB)) {
+            return itemA;
+        }
+
+        return itemA;
+    }
+
+    private boolean switchToSlot(
+            MinecraftClient client,
+            ClientPlayerEntity player,
+            PlayerInventory inventory,
+            int inventorySlot,
+            Item itemA,
+            Item itemB
+    ) {
+        if (inventorySlot < InventoryItemFinder.HOTBAR_SIZE) {
+            selectHotbarSlot(client, inventory, inventorySlot);
+            return true;
+        }
+
+        if (!config.allowInventorySwap()) {
+            return false;
+        }
+
+        OptionalInt destinationSlot = safeHotbarDestination(inventory, itemA, itemB);
+        if (destinationSlot.isEmpty()) {
+            return false;
+        }
+
+        client.interactionManager.clickSlot(
+                player.playerScreenHandler.syncId,
+                inventorySlot,
+                destinationSlot.getAsInt(),
+                SlotActionType.SWAP,
+                player
+        );
+        selectHotbarSlot(client, inventory, destinationSlot.getAsInt());
+        return true;
+    }
+
+    private OptionalInt safeHotbarDestination(PlayerInventory inventory, Item itemA, Item itemB) {
+        ItemStack selectedStack = inventory.getStack(inventory.selectedSlot);
+        if (selectedStack.isEmpty() || selectedStack.isOf(itemA) || selectedStack.isOf(itemB)) {
+            return OptionalInt.of(inventory.selectedSlot);
+        }
+
+        return finder.findEmptyHotbarSlot(inventory);
+    }
+
+    private void selectHotbarSlot(MinecraftClient client, PlayerInventory inventory, int slot) {
+        if (inventory.selectedSlot == slot) {
+            return;
+        }
+
+        inventory.selectedSlot = slot;
+        client.interactionManager.syncSelectedSlot();
+    }
+}

exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-ChildItem -Path "$env:USERPROFILE'"\\.gradle\" -Recurse -File -Filter gradle-launcher-*.jar -ErrorAction SilentlyContinue | Select-Object -First 20 -ExpandProperty FullName" in C:\Users\sutec\IdeaProjects\untitled
 succeeded in 1303ms:
C:\Users\sutec\.gradle\wrapper\dists\gradle-8.10.2-bin\a04bxjujx95o3nb99gddekhwo\gradle-8.10.2\lib\gradle-launcher-8.10.2.jar
C:\Users\sutec\.gradle\wrapper\dists\gradle-9.2.1-bin\2t0n5ozlw9xmuyvbp7dnzaxug\gradle-9.2.1\lib\gradle-launcher-9.2.1.jar

diff --git a/src/client/java/org/ahaha/untitled/client/UntitledClient.java b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
index 96f2fa5caf72eb792f660392a6ef7bab77503eb9..760523f1a4590b451ae9a35dd4bed823b44da6a1
--- a/src/client/java/org/ahaha/untitled/client/UntitledClient.java
+++ b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
@@ -1,10 +1,32 @@
 package org.ahaha.untitled.client;
 
 import net.fabricmc.api.ClientModInitializer;
+import net.fabricmc.fabric.api.client.event.lifecycle.v1.ClientTickEvents;
+import net.fabricmc.fabric.api.client.keybinding.v1.KeyBindingHelper;
+import net.minecraft.client.option.KeyBinding;
+import net.minecraft.client.util.InputUtil;
+import org.ahaha.untitled.client.itemswap.ItemSwapConfig;
+import org.ahaha.untitled.client.itemswap.ItemSwapController;
+import org.lwjgl.glfw.GLFW;
 
 public class UntitledClient implements ClientModInitializer {
+    private static final String CATEGORY = "key.category.untitled";
+    private static final String SWAP_KEY = "key.untitled.swap_item_a_b";
 
     @Override
     public void onInitializeClient() {
+        ItemSwapController controller = new ItemSwapController(ItemSwapConfig.load());
+        KeyBinding swapKey = KeyBindingHelper.registerKeyBinding(new KeyBinding(
+                SWAP_KEY,
+                InputUtil.Type.KEYSYM,
+                GLFW.GLFW_KEY_UNKNOWN,
+                CATEGORY
+        ));
+
+        ClientTickEvents.END_CLIENT_TICK.register(client -> {
+            while (swapKey.wasPressed()) {
+                controller.toggle(client);
+            }
+        });
     }
 }
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
new file mode 100644
index 0000000000000000000000000000000000000000..3214363102de082962b3401df416e73144012217
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
@@ -0,0 +1,44 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+
+import java.util.OptionalInt;
+
+public final class InventoryItemFinder {
+    public static final int HOTBAR_SIZE = 9;
+    public static final int MAIN_INVENTORY_SIZE = 36;
+
+    public OptionalInt find(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < MAIN_INVENTORY_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findInHotbar(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findEmptyHotbarSlot(PlayerInventory inventory) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            if (inventory.getStack(slot).isEmpty()) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
new file mode 100644
index 0000000000000000000000000000000000000000..e3f80e5c19e04354bc7646bb6acd358895963c32
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
@@ -0,0 +1,94 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.fabricmc.loader.api.FabricLoader;
+import net.minecraft.item.Item;
+import net.minecraft.registry.Registries;
+import net.minecraft.util.Identifier;
+
+import java.io.IOException;
+import java.io.Reader;
+import java.io.Writer;
+import java.nio.file.Files;
+import java.nio.file.Path;
+import java.util.Optional;
+import java.util.Properties;
+
+public final class ItemSwapConfig {
+    private static final String CONFIG_FILE_NAME = "untitled-item-swap.properties";
+    private static final String DEFAULT_ITEM_A = "minecraft:golden_apple";
+    private static final String DEFAULT_ITEM_B = "minecraft:totem_of_undying";
+
+    private final String itemAId;
+    private final String itemBId;
+    private final boolean allowInventorySwap;
+
+    private ItemSwapConfig(String itemAId, String itemBId, boolean allowInventorySwap) {
+        this.itemAId = itemAId;
+        this.itemBId = itemBId;
+        this.allowInventorySwap = allowInventorySwap;
+    }
+
+    public static ItemSwapConfig load() {
+        Path path = FabricLoader.getInstance().getConfigDir().resolve(CONFIG_FILE_NAME);
+        Properties properties = defaultProperties();
+
+        if (Files.exists(path)) {
+            try (Reader reader = Files.newBufferedReader(path)) {
+                properties.load(reader);
+            } catch (IOException ignored) {
+                return fromProperties(properties);
+            }
+        } else {
+            writeDefaults(path, properties);
+        }
+
+        return fromProperties(properties);
+    }
+
+    public Optional<Item> itemA() {
+        return resolveItem(itemAId);
+    }
+
+    public Optional<Item> itemB() {
+        return resolveItem(itemBId);
+    }
+
+    public boolean allowInventorySwap() {
+        return allowInventorySwap;
+    }
+
+    private static Properties defaultProperties() {
+        Properties properties = new Properties();
+        properties.setProperty("itemA", DEFAULT_ITEM_A);
+        properties.setProperty("itemB", DEFAULT_ITEM_B);
+        properties.setProperty("allowInventorySwap", "true");
+        return properties;
+    }
+
+    private static ItemSwapConfig fromProperties(Properties properties) {
+        return new ItemSwapConfig(
+                properties.getProperty("itemA", DEFAULT_ITEM_A).trim(),
+                properties.getProperty("itemB", DEFAULT_ITEM_B).trim(),
+                Boolean.parseBoolean(properties.getProperty("allowInventorySwap", "true").trim())
+        );
+    }
+
+    private static void writeDefaults(Path path, Properties properties) {
+        try {
+            Files.createDirectories(path.getParent());
+            try (Writer writer = Files.newBufferedWriter(path)) {
+                properties.store(writer, "Untitled item A/B swap settings");
+            }
+        } catch (IOException ignored) {
+        }
+    }
+
+    private static Optional<Item> resolveItem(String rawId) {
+        Identifier id = Identifier.tryParse(rawId);
+        if (id == null || !Registries.ITEM.containsId(id)) {
+            return Optional.empty();
+        }
+
+        return Optional.of(Registries.ITEM.get(id));
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
new file mode 100644
index 0000000000000000000000000000000000000000..1627b1c02312751fdcee2e7b2abe108fb036a763
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
@@ -0,0 +1,107 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.client.MinecraftClient;
+import net.minecraft.client.network.ClientPlayerEntity;
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+import net.minecraft.screen.slot.SlotActionType;
+
+import java.util.Optional;
+import java.util.OptionalInt;
+
+public final class ItemSwapController {
+    private static final int PLAYER_SCREEN_HOTBAR_SLOT_OFFSET = 36;
+
+    private final ItemSwapConfig config;
+    private final InventoryItemFinder finder;
+
+    public ItemSwapController(ItemSwapConfig config) {
+        this.config = config;
+        this.finder = new InventoryItemFinder();
+    }
+
+    public boolean toggle(MinecraftClient client) {
+        if (client.player == null || client.interactionManager == null || client.currentScreen != null) {
+            return false;
+        }
+
+        Optional<Item> itemA = config.itemA();
+        Optional<Item> itemB = config.itemB();
+        if (itemA.isEmpty() || itemB.isEmpty()) {
+            return false;
+        }
+
+        ClientPlayerEntity player = client.player;
+        PlayerInventory inventory = player.getInventory();
+        Item target = targetItem(inventory.getStack(inventory.selectedSlot), itemA.get(), itemB.get());
+        OptionalInt targetSlot = finder.find(inventory, target);
+        if (targetSlot.isEmpty()) {
+            return false;
+        }
+
+        return switchToSlot(client, player, inventory, targetSlot.getAsInt(), itemA.get(), itemB.get());
+    }
+
+    private Item targetItem(ItemStack selectedStack, Item itemA, Item itemB) {
+        if (selectedStack.isOf(itemA)) {
+            return itemB;
+        }
+        if (selectedStack.isOf(itemB)) {
+            return itemA;
+        }
+
+        return itemA;
+    }
+
+    private boolean switchToSlot(
+            MinecraftClient client,
+            ClientPlayerEntity player,
+            PlayerInventory inventory,
+            int inventorySlot,
+            Item itemA,
+            Item itemB
+    ) {
+        if (inventorySlot < InventoryItemFinder.HOTBAR_SIZE) {
+            selectHotbarSlot(client, inventory, inventorySlot);
+            return true;
+        }
+
+        if (!config.allowInventorySwap()) {
+            return false;
+        }
+
+        OptionalInt destinationSlot = safeHotbarDestination(inventory, itemA, itemB);
+        if (destinationSlot.isEmpty()) {
+            return false;
+        }
+
+        client.interactionManager.clickSlot(
+                player.playerScreenHandler.syncId,
+                inventorySlot,
+                destinationSlot.getAsInt(),
+                SlotActionType.SWAP,
+                player
+        );
+        selectHotbarSlot(client, inventory, destinationSlot.getAsInt());
+        return true;
+    }
+
+    private OptionalInt safeHotbarDestination(PlayerInventory inventory, Item itemA, Item itemB) {
+        ItemStack selectedStack = inventory.getStack(inventory.selectedSlot);
+        if (selectedStack.isEmpty() || selectedStack.isOf(itemA) || selectedStack.isOf(itemB)) {
+            return OptionalInt.of(inventory.selectedSlot);
+        }
+
+        return finder.findEmptyHotbarSlot(inventory);
+    }
+
+    private void selectHotbarSlot(MinecraftClient client, PlayerInventory inventory, int slot) {
+        if (inventory.selectedSlot == slot) {
+            return;
+        }
+
+        inventory.selectedSlot = slot;
+        client.interactionManager.syncSelectedSlot();
+    }
+}

exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$env:JAVA_HOME='"'C:\\Program Files\\JetBrains\\IntelliJ IDEA 2026.1.1\\jbr'; & \""'$env:JAVA_HOME'"\\bin\\java.exe\" -classpath \""'$env:USERPROFILE'"\\.gradle\\wrapper\\dists\\gradle-8.10.2-bin\\a04bxjujx95o3nb99gddekhwo\\gradle-8.10.2\\lib\\gradle-launcher-8.10.2.jar\" org.gradle.launcher.GradleMain compileJava compileClientJava" in C:\Users\sutec\IdeaProjects\untitled
2026-05-11T07:21:56.401359Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 5.9 seconds
Output:

Welcome to Gradle 8.10.2!

Here are the highlights of this release:
 - Support for Java 23
 - Faster configuration cache
 - Better configuration cache reports

For more details see https://docs.gradle.org/8.10.2/release-notes.html

Starting a Gradle Daemon (subsequent builds will be faster)
WARNING: A restricted method in java.lang.System has been called
WARNING: java.lang.System::load has been called by net.rubygrapefruit.platform.internal.NativeLibraryLoader in an unnamed module (file:/C:/Users/sutec/.gradle/wrapper/dists/gradle-8.10.2-bin/a04bxjujx95o3nb99gddekhwo/gradle-8.10.2/lib/native-platform-0.22-milestone-26.jar)
WARNING: Use --enable-native-access=ALL-UNNAMED to avoid a warning for callers in this module
WARNING: Restricted methods will be blocked in a future release unless native access is enabled


FAILURE: Build failed with an exception.

* What went wrong:
BUG! exception in phase 'semantic analysis' in source unit '_BuildScript_' Unsupported class file major version 69
> Unsupported class file major version 69

* Try:
> Run with --stacktrace option to get the stack trace.
> Run with --info or --debug option to get more log output.
> Run with --scan to get full insights.
> Get more help at https://help.gradle.org.

BUILD FAILED in 5s

 exited 1 in 5926ms:

Welcome to Gradle 8.10.2!

Here are the highlights of this release:
 - Support for Java 23
 - Faster configuration cache
 - Better configuration cache reports

For more details see https://docs.gradle.org/8.10.2/release-notes.html

Starting a Gradle Daemon (subsequent builds will be faster)
WARNING: A restricted method in java.lang.System has been called
WARNING: java.lang.System::load has been called by net.rubygrapefruit.platform.internal.NativeLibraryLoader in an unnamed module (file:/C:/Users/sutec/.gradle/wrapper/dists/gradle-8.10.2-bin/a04bxjujx95o3nb99gddekhwo/gradle-8.10.2/lib/native-platform-0.22-milestone-26.jar)
WARNING: Use --enable-native-access=ALL-UNNAMED to avoid a warning for callers in this module
WARNING: Restricted methods will be blocked in a future release unless native access is enabled


FAILURE: Build failed with an exception.

* What went wrong:
BUG! exception in phase 'semantic analysis' in source unit '_BuildScript_' Unsupported class file major version 69
> Unsupported class file major version 69

* Try:
> Run with --stacktrace option to get the stack trace.
> Run with --info or --debug option to get more log output.
> Run with --scan to get full insights.
> Get more help at https://help.gradle.org.

BUILD FAILED in 5s

diff --git a/src/client/java/org/ahaha/untitled/client/UntitledClient.java b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
index 96f2fa5caf72eb792f660392a6ef7bab77503eb9..760523f1a4590b451ae9a35dd4bed823b44da6a1
--- a/src/client/java/org/ahaha/untitled/client/UntitledClient.java
+++ b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
@@ -1,10 +1,32 @@
 package org.ahaha.untitled.client;
 
 import net.fabricmc.api.ClientModInitializer;
+import net.fabricmc.fabric.api.client.event.lifecycle.v1.ClientTickEvents;
+import net.fabricmc.fabric.api.client.keybinding.v1.KeyBindingHelper;
+import net.minecraft.client.option.KeyBinding;
+import net.minecraft.client.util.InputUtil;
+import org.ahaha.untitled.client.itemswap.ItemSwapConfig;
+import org.ahaha.untitled.client.itemswap.ItemSwapController;
+import org.lwjgl.glfw.GLFW;
 
 public class UntitledClient implements ClientModInitializer {
+    private static final String CATEGORY = "key.category.untitled";
+    private static final String SWAP_KEY = "key.untitled.swap_item_a_b";
 
     @Override
     public void onInitializeClient() {
+        ItemSwapController controller = new ItemSwapController(ItemSwapConfig.load());
+        KeyBinding swapKey = KeyBindingHelper.registerKeyBinding(new KeyBinding(
+                SWAP_KEY,
+                InputUtil.Type.KEYSYM,
+                GLFW.GLFW_KEY_UNKNOWN,
+                CATEGORY
+        ));
+
+        ClientTickEvents.END_CLIENT_TICK.register(client -> {
+            while (swapKey.wasPressed()) {
+                controller.toggle(client);
+            }
+        });
     }
 }
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
new file mode 100644
index 0000000000000000000000000000000000000000..3214363102de082962b3401df416e73144012217
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
@@ -0,0 +1,44 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+
+import java.util.OptionalInt;
+
+public final class InventoryItemFinder {
+    public static final int HOTBAR_SIZE = 9;
+    public static final int MAIN_INVENTORY_SIZE = 36;
+
+    public OptionalInt find(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < MAIN_INVENTORY_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findInHotbar(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findEmptyHotbarSlot(PlayerInventory inventory) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            if (inventory.getStack(slot).isEmpty()) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
new file mode 100644
index 0000000000000000000000000000000000000000..e3f80e5c19e04354bc7646bb6acd358895963c32
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
@@ -0,0 +1,94 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.fabricmc.loader.api.FabricLoader;
+import net.minecraft.item.Item;
+import net.minecraft.registry.Registries;
+import net.minecraft.util.Identifier;
+
+import java.io.IOException;
+import java.io.Reader;
+import java.io.Writer;
+import java.nio.file.Files;
+import java.nio.file.Path;
+import java.util.Optional;
+import java.util.Properties;
+
+public final class ItemSwapConfig {
+    private static final String CONFIG_FILE_NAME = "untitled-item-swap.properties";
+    private static final String DEFAULT_ITEM_A = "minecraft:golden_apple";
+    private static final String DEFAULT_ITEM_B = "minecraft:totem_of_undying";
+
+    private final String itemAId;
+    private final String itemBId;
+    private final boolean allowInventorySwap;
+
+    private ItemSwapConfig(String itemAId, String itemBId, boolean allowInventorySwap) {
+        this.itemAId = itemAId;
+        this.itemBId = itemBId;
+        this.allowInventorySwap = allowInventorySwap;
+    }
+
+    public static ItemSwapConfig load() {
+        Path path = FabricLoader.getInstance().getConfigDir().resolve(CONFIG_FILE_NAME);
+        Properties properties = defaultProperties();
+
+        if (Files.exists(path)) {
+            try (Reader reader = Files.newBufferedReader(path)) {
+                properties.load(reader);
+            } catch (IOException ignored) {
+                return fromProperties(properties);
+            }
+        } else {
+            writeDefaults(path, properties);
+        }
+
+        return fromProperties(properties);
+    }
+
+    public Optional<Item> itemA() {
+        return resolveItem(itemAId);
+    }
+
+    public Optional<Item> itemB() {
+        return resolveItem(itemBId);
+    }
+
+    public boolean allowInventorySwap() {
+        return allowInventorySwap;
+    }
+
+    private static Properties defaultProperties() {
+        Properties properties = new Properties();
+        properties.setProperty("itemA", DEFAULT_ITEM_A);
+        properties.setProperty("itemB", DEFAULT_ITEM_B);
+        properties.setProperty("allowInventorySwap", "true");
+        return properties;
+    }
+
+    private static ItemSwapConfig fromProperties(Properties properties) {
+        return new ItemSwapConfig(
+                properties.getProperty("itemA", DEFAULT_ITEM_A).trim(),
+                properties.getProperty("itemB", DEFAULT_ITEM_B).trim(),
+                Boolean.parseBoolean(properties.getProperty("allowInventorySwap", "true").trim())
+        );
+    }
+
+    private static void writeDefaults(Path path, Properties properties) {
+        try {
+            Files.createDirectories(path.getParent());
+            try (Writer writer = Files.newBufferedWriter(path)) {
+                properties.store(writer, "Untitled item A/B swap settings");
+            }
+        } catch (IOException ignored) {
+        }
+    }
+
+    private static Optional<Item> resolveItem(String rawId) {
+        Identifier id = Identifier.tryParse(rawId);
+        if (id == null || !Registries.ITEM.containsId(id)) {
+            return Optional.empty();
+        }
+
+        return Optional.of(Registries.ITEM.get(id));
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
new file mode 100644
index 0000000000000000000000000000000000000000..1627b1c02312751fdcee2e7b2abe108fb036a763
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
@@ -0,0 +1,107 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.client.MinecraftClient;
+import net.minecraft.client.network.ClientPlayerEntity;
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+import net.minecraft.screen.slot.SlotActionType;
+
+import java.util.Optional;
+import java.util.OptionalInt;
+
+public final class ItemSwapController {
+    private static final int PLAYER_SCREEN_HOTBAR_SLOT_OFFSET = 36;
+
+    private final ItemSwapConfig config;
+    private final InventoryItemFinder finder;
+
+    public ItemSwapController(ItemSwapConfig config) {
+        this.config = config;
+        this.finder = new InventoryItemFinder();
+    }
+
+    public boolean toggle(MinecraftClient client) {
+        if (client.player == null || client.interactionManager == null || client.currentScreen != null) {
+            return false;
+        }
+
+        Optional<Item> itemA = config.itemA();
+        Optional<Item> itemB = config.itemB();
+        if (itemA.isEmpty() || itemB.isEmpty()) {
+            return false;
+        }
+
+        ClientPlayerEntity player = client.player;
+        PlayerInventory inventory = player.getInventory();
+        Item target = targetItem(inventory.getStack(inventory.selectedSlot), itemA.get(), itemB.get());
+        OptionalInt targetSlot = finder.find(inventory, target);
+        if (targetSlot.isEmpty()) {
+            return false;
+        }
+
+        return switchToSlot(client, player, inventory, targetSlot.getAsInt(), itemA.get(), itemB.get());
+    }
+
+    private Item targetItem(ItemStack selectedStack, Item itemA, Item itemB) {
+        if (selectedStack.isOf(itemA)) {
+            return itemB;
+        }
+        if (selectedStack.isOf(itemB)) {
+            return itemA;
+        }
+
+        return itemA;
+    }
+
+    private boolean switchToSlot(
+            MinecraftClient client,
+            ClientPlayerEntity player,
+            PlayerInventory inventory,
+            int inventorySlot,
+            Item itemA,
+            Item itemB
+    ) {
+        if (inventorySlot < InventoryItemFinder.HOTBAR_SIZE) {
+            selectHotbarSlot(client, inventory, inventorySlot);
+            return true;
+        }
+
+        if (!config.allowInventorySwap()) {
+            return false;
+        }
+
+        OptionalInt destinationSlot = safeHotbarDestination(inventory, itemA, itemB);
+        if (destinationSlot.isEmpty()) {
+            return false;
+        }
+
+        client.interactionManager.clickSlot(
+                player.playerScreenHandler.syncId,
+                inventorySlot,
+                destinationSlot.getAsInt(),
+                SlotActionType.SWAP,
+                player
+        );
+        selectHotbarSlot(client, inventory, destinationSlot.getAsInt());
+        return true;
+    }
+
+    private OptionalInt safeHotbarDestination(PlayerInventory inventory, Item itemA, Item itemB) {
+        ItemStack selectedStack = inventory.getStack(inventory.selectedSlot);
+        if (selectedStack.isEmpty() || selectedStack.isOf(itemA) || selectedStack.isOf(itemB)) {
+            return OptionalInt.of(inventory.selectedSlot);
+        }
+
+        return finder.findEmptyHotbarSlot(inventory);
+    }
+
+    private void selectHotbarSlot(MinecraftClient client, PlayerInventory inventory, int slot) {
+        if (inventory.selectedSlot == slot) {
+            return;
+        }
+
+        inventory.selectedSlot = slot;
+        client.interactionManager.syncSelectedSlot();
+    }
+}

exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$env:JAVA_HOME='"'C:\\Program Files\\JetBrains\\IntelliJ IDEA 2026.1.1\\jbr'; & \""'$env:JAVA_HOME'"\\bin\\java.exe\" -classpath \""'$env:USERPROFILE'"\\.gradle\\wrapper\\dists\\gradle-9.2.1-bin\\2t0n5ozlw9xmuyvbp7dnzaxug\\gradle-9.2.1\\lib\\gradle-launcher-9.2.1.jar\" org.gradle.launcher.GradleMain compileJava compileClientJava" in C:\Users\sutec\IdeaProjects\untitled
2026-05-11T07:22:17.768807Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 13.7 seconds
Output:

Welcome to Gradle 9.2.1!

Here are the highlights of this release:
 - Windows ARM support
 - Improved publishing APIs
 - Better guidance for dependency verification failures

For more details see https://docs.gradle.org/9.2.1/release-notes.html

Starting a Gradle Daemon (subsequent builds will be faster)
WARNING: A restricted method in java.lang.System has been called
WARNING: java.lang.System::load has been called by net.rubygrapefruit.platform.internal.NativeLibraryLoader in an unnamed module (file:/C:/Users/sutec/.gradle/wrapper/dists/gradle-9.2.1-bin/2t0n5ozlw9xmuyvbp7dnzaxug/gradle-9.2.1/lib/native-platform-0.22-milestone-29.jar)
WARNING: Use --enable-native-access=ALL-UNNAMED to avoid a warning for callers in this module
WARNING: Restricted methods will be blocked in a future release unless native access is enabled


FAILURE: Build failed with an exception.

* Where:
Build file 'C:\Users\sutec\IdeaProjects\untitled\build.gradle' line: 2

* What went wrong:
Plugin [id: 'fabric-loom', version: '1.8-SNAPSHOT'] was not found in any of the following sources:

- Gradle Core Plugins (not a core plugin. For more available plugins, please refer to https://docs.gradle.org/9.2.1/userguide/plugin_reference.html in the Gradle documentation.)
- Included Builds (No included builds contain this plugin)
- Plugin Repositories (could not resolve plugin artifact 'fabric-loom:fabric-loom.gradle.plugin:1.8-SNAPSHOT')
  Searched in the following repositories:
    Fabric(https://maven.fabricmc.net/)
    Gradle Central Plugin Repository

* Try:
> Run with --stacktrace option to get the stack trace.
> Run with --info or --debug option to get more log output.
> Run with --scan to generate a Build Scan (powered by Develocity).
> Get more help at https://help.gradle.org.

BUILD FAILED in 13s

 exited 1 in 13739ms:

Welcome to Gradle 9.2.1!

Here are the highlights of this release:
 - Windows ARM support
 - Improved publishing APIs
 - Better guidance for dependency verification failures

For more details see https://docs.gradle.org/9.2.1/release-notes.html

Starting a Gradle Daemon (subsequent builds will be faster)
WARNING: A restricted method in java.lang.System has been called
WARNING: java.lang.System::load has been called by net.rubygrapefruit.platform.internal.NativeLibraryLoader in an unnamed module (file:/C:/Users/sutec/.gradle/wrapper/dists/gradle-9.2.1-bin/2t0n5ozlw9xmuyvbp7dnzaxug/gradle-9.2.1/lib/native-platform-0.22-milestone-29.jar)
WARNING: Use --enable-native-access=ALL-UNNAMED to avoid a warning for callers in this module
WARNING: Restricted methods will be blocked in a future release unless native access is enabled


FAILURE: Build failed with an exception.

* Where:
Build file 'C:\Users\sutec\IdeaProjects\untitled\build.gradle' line: 2

* What went wrong:
Plugin [id: 'fabric-loom', version: '1.8-SNAPSHOT'] was not found in any of the following sources:

- Gradle Core Plugins (not a core plugin. For more available plugins, please refer to https://docs.gradle.org/9.2.1/userguide/plugin_reference.html in the Gradle documentation.)
- Included Builds (No included builds contain this plugin)
- Plugin Repositories (could not resolve plugin artifact 'fabric-loom:fabric-loom.gradle.plugin:1.8-SNAPSHOT')
  Searched in the following repositories:
    Fabric(https://maven.fabricmc.net/)
    Gradle Central Plugin Repository

* Try:
> Run with --stacktrace option to get the stack trace.
> Run with --info or --debug option to get more log output.
> Run with --scan to generate a Build Scan (powered by Develocity).
> Get more help at https://help.gradle.org.

BUILD FAILED in 13s

diff --git a/src/client/java/org/ahaha/untitled/client/UntitledClient.java b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
index 96f2fa5caf72eb792f660392a6ef7bab77503eb9..760523f1a4590b451ae9a35dd4bed823b44da6a1
--- a/src/client/java/org/ahaha/untitled/client/UntitledClient.java
+++ b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
@@ -1,10 +1,32 @@
 package org.ahaha.untitled.client;
 
 import net.fabricmc.api.ClientModInitializer;
+import net.fabricmc.fabric.api.client.event.lifecycle.v1.ClientTickEvents;
+import net.fabricmc.fabric.api.client.keybinding.v1.KeyBindingHelper;
+import net.minecraft.client.option.KeyBinding;
+import net.minecraft.client.util.InputUtil;
+import org.ahaha.untitled.client.itemswap.ItemSwapConfig;
+import org.ahaha.untitled.client.itemswap.ItemSwapController;
+import org.lwjgl.glfw.GLFW;
 
 public class UntitledClient implements ClientModInitializer {
+    private static final String CATEGORY = "key.category.untitled";
+    private static final String SWAP_KEY = "key.untitled.swap_item_a_b";
 
     @Override
     public void onInitializeClient() {
+        ItemSwapController controller = new ItemSwapController(ItemSwapConfig.load());
+        KeyBinding swapKey = KeyBindingHelper.registerKeyBinding(new KeyBinding(
+                SWAP_KEY,
+                InputUtil.Type.KEYSYM,
+                GLFW.GLFW_KEY_UNKNOWN,
+                CATEGORY
+        ));
+
+        ClientTickEvents.END_CLIENT_TICK.register(client -> {
+            while (swapKey.wasPressed()) {
+                controller.toggle(client);
+            }
+        });
     }
 }
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
new file mode 100644
index 0000000000000000000000000000000000000000..3214363102de082962b3401df416e73144012217
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
@@ -0,0 +1,44 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+
+import java.util.OptionalInt;
+
+public final class InventoryItemFinder {
+    public static final int HOTBAR_SIZE = 9;
+    public static final int MAIN_INVENTORY_SIZE = 36;
+
+    public OptionalInt find(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < MAIN_INVENTORY_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findInHotbar(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findEmptyHotbarSlot(PlayerInventory inventory) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            if (inventory.getStack(slot).isEmpty()) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
new file mode 100644
index 0000000000000000000000000000000000000000..e3f80e5c19e04354bc7646bb6acd358895963c32
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
@@ -0,0 +1,94 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.fabricmc.loader.api.FabricLoader;
+import net.minecraft.item.Item;
+import net.minecraft.registry.Registries;
+import net.minecraft.util.Identifier;
+
+import java.io.IOException;
+import java.io.Reader;
+import java.io.Writer;
+import java.nio.file.Files;
+import java.nio.file.Path;
+import java.util.Optional;
+import java.util.Properties;
+
+public final class ItemSwapConfig {
+    private static final String CONFIG_FILE_NAME = "untitled-item-swap.properties";
+    private static final String DEFAULT_ITEM_A = "minecraft:golden_apple";
+    private static final String DEFAULT_ITEM_B = "minecraft:totem_of_undying";
+
+    private final String itemAId;
+    private final String itemBId;
+    private final boolean allowInventorySwap;
+
+    private ItemSwapConfig(String itemAId, String itemBId, boolean allowInventorySwap) {
+        this.itemAId = itemAId;
+        this.itemBId = itemBId;
+        this.allowInventorySwap = allowInventorySwap;
+    }
+
+    public static ItemSwapConfig load() {
+        Path path = FabricLoader.getInstance().getConfigDir().resolve(CONFIG_FILE_NAME);
+        Properties properties = defaultProperties();
+
+        if (Files.exists(path)) {
+            try (Reader reader = Files.newBufferedReader(path)) {
+                properties.load(reader);
+            } catch (IOException ignored) {
+                return fromProperties(properties);
+            }
+        } else {
+            writeDefaults(path, properties);
+        }
+
+        return fromProperties(properties);
+    }
+
+    public Optional<Item> itemA() {
+        return resolveItem(itemAId);
+    }
+
+    public Optional<Item> itemB() {
+        return resolveItem(itemBId);
+    }
+
+    public boolean allowInventorySwap() {
+        return allowInventorySwap;
+    }
+
+    private static Properties defaultProperties() {
+        Properties properties = new Properties();
+        properties.setProperty("itemA", DEFAULT_ITEM_A);
+        properties.setProperty("itemB", DEFAULT_ITEM_B);
+        properties.setProperty("allowInventorySwap", "true");
+        return properties;
+    }
+
+    private static ItemSwapConfig fromProperties(Properties properties) {
+        return new ItemSwapConfig(
+                properties.getProperty("itemA", DEFAULT_ITEM_A).trim(),
+                properties.getProperty("itemB", DEFAULT_ITEM_B).trim(),
+                Boolean.parseBoolean(properties.getProperty("allowInventorySwap", "true").trim())
+        );
+    }
+
+    private static void writeDefaults(Path path, Properties properties) {
+        try {
+            Files.createDirectories(path.getParent());
+            try (Writer writer = Files.newBufferedWriter(path)) {
+                properties.store(writer, "Untitled item A/B swap settings");
+            }
+        } catch (IOException ignored) {
+        }
+    }
+
+    private static Optional<Item> resolveItem(String rawId) {
+        Identifier id = Identifier.tryParse(rawId);
+        if (id == null || !Registries.ITEM.containsId(id)) {
+            return Optional.empty();
+        }
+
+        return Optional.of(Registries.ITEM.get(id));
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
new file mode 100644
index 0000000000000000000000000000000000000000..1627b1c02312751fdcee2e7b2abe108fb036a763
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
@@ -0,0 +1,107 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.client.MinecraftClient;
+import net.minecraft.client.network.ClientPlayerEntity;
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+import net.minecraft.screen.slot.SlotActionType;
+
+import java.util.Optional;
+import java.util.OptionalInt;
+
+public final class ItemSwapController {
+    private static final int PLAYER_SCREEN_HOTBAR_SLOT_OFFSET = 36;
+
+    private final ItemSwapConfig config;
+    private final InventoryItemFinder finder;
+
+    public ItemSwapController(ItemSwapConfig config) {
+        this.config = config;
+        this.finder = new InventoryItemFinder();
+    }
+
+    public boolean toggle(MinecraftClient client) {
+        if (client.player == null || client.interactionManager == null || client.currentScreen != null) {
+            return false;
+        }
+
+        Optional<Item> itemA = config.itemA();
+        Optional<Item> itemB = config.itemB();
+        if (itemA.isEmpty() || itemB.isEmpty()) {
+            return false;
+        }
+
+        ClientPlayerEntity player = client.player;
+        PlayerInventory inventory = player.getInventory();
+        Item target = targetItem(inventory.getStack(inventory.selectedSlot), itemA.get(), itemB.get());
+        OptionalInt targetSlot = finder.find(inventory, target);
+        if (targetSlot.isEmpty()) {
+            return false;
+        }
+
+        return switchToSlot(client, player, inventory, targetSlot.getAsInt(), itemA.get(), itemB.get());
+    }
+
+    private Item targetItem(ItemStack selectedStack, Item itemA, Item itemB) {
+        if (selectedStack.isOf(itemA)) {
+            return itemB;
+        }
+        if (selectedStack.isOf(itemB)) {
+            return itemA;
+        }
+
+        return itemA;
+    }
+
+    private boolean switchToSlot(
+            MinecraftClient client,
+            ClientPlayerEntity player,
+            PlayerInventory inventory,
+            int inventorySlot,
+            Item itemA,
+            Item itemB
+    ) {
+        if (inventorySlot < InventoryItemFinder.HOTBAR_SIZE) {
+            selectHotbarSlot(client, inventory, inventorySlot);
+            return true;
+        }
+
+        if (!config.allowInventorySwap()) {
+            return false;
+        }
+
+        OptionalInt destinationSlot = safeHotbarDestination(inventory, itemA, itemB);
+        if (destinationSlot.isEmpty()) {
+            return false;
+        }
+
+        client.interactionManager.clickSlot(
+                player.playerScreenHandler.syncId,
+                inventorySlot,
+                destinationSlot.getAsInt(),
+                SlotActionType.SWAP,
+                player
+        );
+        selectHotbarSlot(client, inventory, destinationSlot.getAsInt());
+        return true;
+    }
+
+    private OptionalInt safeHotbarDestination(PlayerInventory inventory, Item itemA, Item itemB) {
+        ItemStack selectedStack = inventory.getStack(inventory.selectedSlot);
+        if (selectedStack.isEmpty() || selectedStack.isOf(itemA) || selectedStack.isOf(itemB)) {
+            return OptionalInt.of(inventory.selectedSlot);
+        }
+
+        return finder.findEmptyHotbarSlot(inventory);
+    }
+
+    private void selectHotbarSlot(MinecraftClient client, PlayerInventory inventory, int slot) {
+        if (inventory.selectedSlot == slot) {
+            return;
+        }
+
+        inventory.selectedSlot = slot;
+        client.interactionManager.syncSelectedSlot();
+    }
+}

exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-ChildItem -Path 'C:\\','D:\\' -Recurse -File -Filter java.exe -ErrorAction SilentlyContinue | Select-Object -First 50 -ExpandProperty FullName" in C:\Users\sutec\IdeaProjects\untitled
2026-05-11T07:22:59.488607Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 38.2 seconds
Output:
C:\Program Files\JetBrains\IntelliJ IDEA 2026.1.1\jbr\bin\java.exe

 exited 1 in 38184ms:
C:\Program Files\JetBrains\IntelliJ IDEA 2026.1.1\jbr\bin\java.exe

diff --git a/src/client/java/org/ahaha/untitled/client/UntitledClient.java b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
index 96f2fa5caf72eb792f660392a6ef7bab77503eb9..760523f1a4590b451ae9a35dd4bed823b44da6a1
--- a/src/client/java/org/ahaha/untitled/client/UntitledClient.java
+++ b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
@@ -1,10 +1,32 @@
 package org.ahaha.untitled.client;
 
 import net.fabricmc.api.ClientModInitializer;
+import net.fabricmc.fabric.api.client.event.lifecycle.v1.ClientTickEvents;
+import net.fabricmc.fabric.api.client.keybinding.v1.KeyBindingHelper;
+import net.minecraft.client.option.KeyBinding;
+import net.minecraft.client.util.InputUtil;
+import org.ahaha.untitled.client.itemswap.ItemSwapConfig;
+import org.ahaha.untitled.client.itemswap.ItemSwapController;
+import org.lwjgl.glfw.GLFW;
 
 public class UntitledClient implements ClientModInitializer {
+    private static final String CATEGORY = "key.category.untitled";
+    private static final String SWAP_KEY = "key.untitled.swap_item_a_b";
 
     @Override
     public void onInitializeClient() {
+        ItemSwapController controller = new ItemSwapController(ItemSwapConfig.load());
+        KeyBinding swapKey = KeyBindingHelper.registerKeyBinding(new KeyBinding(
+                SWAP_KEY,
+                InputUtil.Type.KEYSYM,
+                GLFW.GLFW_KEY_UNKNOWN,
+                CATEGORY
+        ));
+
+        ClientTickEvents.END_CLIENT_TICK.register(client -> {
+            while (swapKey.wasPressed()) {
+                controller.toggle(client);
+            }
+        });
     }
 }
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
new file mode 100644
index 0000000000000000000000000000000000000000..3214363102de082962b3401df416e73144012217
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
@@ -0,0 +1,44 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+
+import java.util.OptionalInt;
+
+public final class InventoryItemFinder {
+    public static final int HOTBAR_SIZE = 9;
+    public static final int MAIN_INVENTORY_SIZE = 36;
+
+    public OptionalInt find(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < MAIN_INVENTORY_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findInHotbar(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findEmptyHotbarSlot(PlayerInventory inventory) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            if (inventory.getStack(slot).isEmpty()) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
new file mode 100644
index 0000000000000000000000000000000000000000..e3f80e5c19e04354bc7646bb6acd358895963c32
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
@@ -0,0 +1,94 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.fabricmc.loader.api.FabricLoader;
+import net.minecraft.item.Item;
+import net.minecraft.registry.Registries;
+import net.minecraft.util.Identifier;
+
+import java.io.IOException;
+import java.io.Reader;
+import java.io.Writer;
+import java.nio.file.Files;
+import java.nio.file.Path;
+import java.util.Optional;
+import java.util.Properties;
+
+public final class ItemSwapConfig {
+    private static final String CONFIG_FILE_NAME = "untitled-item-swap.properties";
+    private static final String DEFAULT_ITEM_A = "minecraft:golden_apple";
+    private static final String DEFAULT_ITEM_B = "minecraft:totem_of_undying";
+
+    private final String itemAId;
+    private final String itemBId;
+    private final boolean allowInventorySwap;
+
+    private ItemSwapConfig(String itemAId, String itemBId, boolean allowInventorySwap) {
+        this.itemAId = itemAId;
+        this.itemBId = itemBId;
+        this.allowInventorySwap = allowInventorySwap;
+    }
+
+    public static ItemSwapConfig load() {
+        Path path = FabricLoader.getInstance().getConfigDir().resolve(CONFIG_FILE_NAME);
+        Properties properties = defaultProperties();
+
+        if (Files.exists(path)) {
+            try (Reader reader = Files.newBufferedReader(path)) {
+                properties.load(reader);
+            } catch (IOException ignored) {
+                return fromProperties(properties);
+            }
+        } else {
+            writeDefaults(path, properties);
+        }
+
+        return fromProperties(properties);
+    }
+
+    public Optional<Item> itemA() {
+        return resolveItem(itemAId);
+    }
+
+    public Optional<Item> itemB() {
+        return resolveItem(itemBId);
+    }
+
+    public boolean allowInventorySwap() {
+        return allowInventorySwap;
+    }
+
+    private static Properties defaultProperties() {
+        Properties properties = new Properties();
+        properties.setProperty("itemA", DEFAULT_ITEM_A);
+        properties.setProperty("itemB", DEFAULT_ITEM_B);
+        properties.setProperty("allowInventorySwap", "true");
+        return properties;
+    }
+
+    private static ItemSwapConfig fromProperties(Properties properties) {
+        return new ItemSwapConfig(
+                properties.getProperty("itemA", DEFAULT_ITEM_A).trim(),
+                properties.getProperty("itemB", DEFAULT_ITEM_B).trim(),
+                Boolean.parseBoolean(properties.getProperty("allowInventorySwap", "true").trim())
+        );
+    }
+
+    private static void writeDefaults(Path path, Properties properties) {
+        try {
+            Files.createDirectories(path.getParent());
+            try (Writer writer = Files.newBufferedWriter(path)) {
+                properties.store(writer, "Untitled item A/B swap settings");
+            }
+        } catch (IOException ignored) {
+        }
+    }
+
+    private static Optional<Item> resolveItem(String rawId) {
+        Identifier id = Identifier.tryParse(rawId);
+        if (id == null || !Registries.ITEM.containsId(id)) {
+            return Optional.empty();
+        }
+
+        return Optional.of(Registries.ITEM.get(id));
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
new file mode 100644
index 0000000000000000000000000000000000000000..1627b1c02312751fdcee2e7b2abe108fb036a763
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
@@ -0,0 +1,107 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.client.MinecraftClient;
+import net.minecraft.client.network.ClientPlayerEntity;
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+import net.minecraft.screen.slot.SlotActionType;
+
+import java.util.Optional;
+import java.util.OptionalInt;
+
+public final class ItemSwapController {
+    private static final int PLAYER_SCREEN_HOTBAR_SLOT_OFFSET = 36;
+
+    private final ItemSwapConfig config;
+    private final InventoryItemFinder finder;
+
+    public ItemSwapController(ItemSwapConfig config) {
+        this.config = config;
+        this.finder = new InventoryItemFinder();
+    }
+
+    public boolean toggle(MinecraftClient client) {
+        if (client.player == null || client.interactionManager == null || client.currentScreen != null) {
+            return false;
+        }
+
+        Optional<Item> itemA = config.itemA();
+        Optional<Item> itemB = config.itemB();
+        if (itemA.isEmpty() || itemB.isEmpty()) {
+            return false;
+        }
+
+        ClientPlayerEntity player = client.player;
+        PlayerInventory inventory = player.getInventory();
+        Item target = targetItem(inventory.getStack(inventory.selectedSlot), itemA.get(), itemB.get());
+        OptionalInt targetSlot = finder.find(inventory, target);
+        if (targetSlot.isEmpty()) {
+            return false;
+        }
+
+        return switchToSlot(client, player, inventory, targetSlot.getAsInt(), itemA.get(), itemB.get());
+    }
+
+    private Item targetItem(ItemStack selectedStack, Item itemA, Item itemB) {
+        if (selectedStack.isOf(itemA)) {
+            return itemB;
+        }
+        if (selectedStack.isOf(itemB)) {
+            return itemA;
+        }
+
+        return itemA;
+    }
+
+    private boolean switchToSlot(
+            MinecraftClient client,
+            ClientPlayerEntity player,
+            PlayerInventory inventory,
+            int inventorySlot,
+            Item itemA,
+            Item itemB
+    ) {
+        if (inventorySlot < InventoryItemFinder.HOTBAR_SIZE) {
+            selectHotbarSlot(client, inventory, inventorySlot);
+            return true;
+        }
+
+        if (!config.allowInventorySwap()) {
+            return false;
+        }
+
+        OptionalInt destinationSlot = safeHotbarDestination(inventory, itemA, itemB);
+        if (destinationSlot.isEmpty()) {
+            return false;
+        }
+
+        client.interactionManager.clickSlot(
+                player.playerScreenHandler.syncId,
+                inventorySlot,
+                destinationSlot.getAsInt(),
+                SlotActionType.SWAP,
+                player
+        );
+        selectHotbarSlot(client, inventory, destinationSlot.getAsInt());
+        return true;
+    }
+
+    private OptionalInt safeHotbarDestination(PlayerInventory inventory, Item itemA, Item itemB) {
+        ItemStack selectedStack = inventory.getStack(inventory.selectedSlot);
+        if (selectedStack.isEmpty() || selectedStack.isOf(itemA) || selectedStack.isOf(itemB)) {
+            return OptionalInt.of(inventory.selectedSlot);
+        }
+
+        return finder.findEmptyHotbarSlot(inventory);
+    }
+
+    private void selectHotbarSlot(MinecraftClient client, PlayerInventory inventory, int slot) {
+        if (inventory.selectedSlot == slot) {
+            return;
+        }
+
+        inventory.selectedSlot = slot;
+        client.interactionManager.syncSelectedSlot();
+    }
+}

exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Raw settings.gradle; Get-ChildItem -Path "$env:USERPROFILE'"\\.gradle\\caches\\modules-2\\files-2.1\\fabric-loom\",\""'$env:USERPROFILE'"\\.gradle\\caches\\modules-2\\files-2.1\\net.fabricmc\" -Recurse -Directory -ErrorAction SilentlyContinue | Select-Object -First 40 -ExpandProperty FullName" in C:\Users\sutec\IdeaProjects\untitled
 succeeded in 988ms:
pluginManagement {
    repositories {
        maven {
            name = 'Fabric'
            url = 'https://maven.fabricmc.net/'
        }
        gradlePluginPortal()
    }
}

C:\Users\sutec\.gradle\caches\modules-2\files-2.1\fabric-loom\fabric-loom.gradle.plugin
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\fabric-loom\fabric-loom.gradle.plugin\1.16-SNAPSHOT
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\fabric-loom\fabric-loom.gradle.plugin\1.8-SNAPSHOT
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\fabric-loom\fabric-loom.gradle.plugin\1.16-SNAPSHOT\8807b1fc5bc6574b214d5127b3427b33cd5fb601
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\fabric-loom\fabric-loom.gradle.plugin\1.8-SNAPSHOT\f33a44e8e0eb187a248a8b2f99d96bfba0ff97ab
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc\access-widener
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc\dev-launch-injector
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc\fabric-loader
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc\fabric-loom
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc\fabric-loom-native
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc\intermediary
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc\lorenz-tiny
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc\mapping-io
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc\mercury
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc\sponge-mixin
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc\stitch
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc\tiny-mappings-parser
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc\tiny-remapper
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc\yarn
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc\access-widener\2.1.0
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc\access-widener\2.1.0\2accb1f828ba665cab6e712924faf1259e997981
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc\access-widener\2.1.0\39e4ffd7840b047e3ab6785c2ca00a5151ee49be
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc\access-widener\2.1.0\f62a27adbfd8ab4d4fa5681793039f2c0b177155
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc\dev-launch-injector\0.2.1+build.8
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc\dev-launch-injector\0.2.1+build.8\25dbae90d65c223b1b90d09bcf7e9476a7414660
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc\dev-launch-injector\0.2.1+build.8\da8bef7e6e2f952da707f282bdb46882a0fce5e3
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc\fabric-loader\0.19.2
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc\fabric-loader\0.19.2\4a57da0e5dbdcdbab95b5128149e5d820c0184d4
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc\fabric-loader\0.19.2\9a6a175c777859b39cf762daf597f2c67778ae0e
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc\fabric-loader\0.19.2\cc647a5b22dbc49b9a3267cebcee25fc4882b108
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc\fabric-loom\1.16.1
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc\fabric-loom\1.8-SNAPSHOT
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc\fabric-loom\1.16.1\6abe22b88c0ab2f73180ac8ed7024b9f67e5a78f
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc\fabric-loom\1.16.1\f7455b459d42ad5e48cfb6b73a62beec1d380341
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc\fabric-loom\1.8-SNAPSHOT\5df2de947dc3912c967a1d26b31c0ee4f835255b
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc\fabric-loom\1.8-SNAPSHOT\6071015beb8e0b03f44be0bdb47bc165414890a4
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc\fabric-loom\1.8-SNAPSHOT\7c33d27961ca019a4d187cf8ea9d40989dc09ce6
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc\fabric-loom\1.8-SNAPSHOT\90e8c4afc764d1475df86b81ec2c37359a047ef5
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc\fabric-loom-native\0.2.0
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc\fabric-loom-native\0.2.0\2b335b1b1918af7a7d6ae5c739c708a99b3c1f9e

diff --git a/src/client/java/org/ahaha/untitled/client/UntitledClient.java b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
index 96f2fa5caf72eb792f660392a6ef7bab77503eb9..760523f1a4590b451ae9a35dd4bed823b44da6a1
--- a/src/client/java/org/ahaha/untitled/client/UntitledClient.java
+++ b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
@@ -1,10 +1,32 @@
 package org.ahaha.untitled.client;
 
 import net.fabricmc.api.ClientModInitializer;
+import net.fabricmc.fabric.api.client.event.lifecycle.v1.ClientTickEvents;
+import net.fabricmc.fabric.api.client.keybinding.v1.KeyBindingHelper;
+import net.minecraft.client.option.KeyBinding;
+import net.minecraft.client.util.InputUtil;
+import org.ahaha.untitled.client.itemswap.ItemSwapConfig;
+import org.ahaha.untitled.client.itemswap.ItemSwapController;
+import org.lwjgl.glfw.GLFW;
 
 public class UntitledClient implements ClientModInitializer {
+    private static final String CATEGORY = "key.category.untitled";
+    private static final String SWAP_KEY = "key.untitled.swap_item_a_b";
 
     @Override
     public void onInitializeClient() {
+        ItemSwapController controller = new ItemSwapController(ItemSwapConfig.load());
+        KeyBinding swapKey = KeyBindingHelper.registerKeyBinding(new KeyBinding(
+                SWAP_KEY,
+                InputUtil.Type.KEYSYM,
+                GLFW.GLFW_KEY_UNKNOWN,
+                CATEGORY
+        ));
+
+        ClientTickEvents.END_CLIENT_TICK.register(client -> {
+            while (swapKey.wasPressed()) {
+                controller.toggle(client);
+            }
+        });
     }
 }
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
new file mode 100644
index 0000000000000000000000000000000000000000..3214363102de082962b3401df416e73144012217
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
@@ -0,0 +1,44 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+
+import java.util.OptionalInt;
+
+public final class InventoryItemFinder {
+    public static final int HOTBAR_SIZE = 9;
+    public static final int MAIN_INVENTORY_SIZE = 36;
+
+    public OptionalInt find(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < MAIN_INVENTORY_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findInHotbar(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findEmptyHotbarSlot(PlayerInventory inventory) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            if (inventory.getStack(slot).isEmpty()) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
new file mode 100644
index 0000000000000000000000000000000000000000..e3f80e5c19e04354bc7646bb6acd358895963c32
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
@@ -0,0 +1,94 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.fabricmc.loader.api.FabricLoader;
+import net.minecraft.item.Item;
+import net.minecraft.registry.Registries;
+import net.minecraft.util.Identifier;
+
+import java.io.IOException;
+import java.io.Reader;
+import java.io.Writer;
+import java.nio.file.Files;
+import java.nio.file.Path;
+import java.util.Optional;
+import java.util.Properties;
+
+public final class ItemSwapConfig {
+    private static final String CONFIG_FILE_NAME = "untitled-item-swap.properties";
+    private static final String DEFAULT_ITEM_A = "minecraft:golden_apple";
+    private static final String DEFAULT_ITEM_B = "minecraft:totem_of_undying";
+
+    private final String itemAId;
+    private final String itemBId;
+    private final boolean allowInventorySwap;
+
+    private ItemSwapConfig(String itemAId, String itemBId, boolean allowInventorySwap) {
+        this.itemAId = itemAId;
+        this.itemBId = itemBId;
+        this.allowInventorySwap = allowInventorySwap;
+    }
+
+    public static ItemSwapConfig load() {
+        Path path = FabricLoader.getInstance().getConfigDir().resolve(CONFIG_FILE_NAME);
+        Properties properties = defaultProperties();
+
+        if (Files.exists(path)) {
+            try (Reader reader = Files.newBufferedReader(path)) {
+                properties.load(reader);
+            } catch (IOException ignored) {
+                return fromProperties(properties);
+            }
+        } else {
+            writeDefaults(path, properties);
+        }
+
+        return fromProperties(properties);
+    }
+
+    public Optional<Item> itemA() {
+        return resolveItem(itemAId);
+    }
+
+    public Optional<Item> itemB() {
+        return resolveItem(itemBId);
+    }
+
+    public boolean allowInventorySwap() {
+        return allowInventorySwap;
+    }
+
+    private static Properties defaultProperties() {
+        Properties properties = new Properties();
+        properties.setProperty("itemA", DEFAULT_ITEM_A);
+        properties.setProperty("itemB", DEFAULT_ITEM_B);
+        properties.setProperty("allowInventorySwap", "true");
+        return properties;
+    }
+
+    private static ItemSwapConfig fromProperties(Properties properties) {
+        return new ItemSwapConfig(
+                properties.getProperty("itemA", DEFAULT_ITEM_A).trim(),
+                properties.getProperty("itemB", DEFAULT_ITEM_B).trim(),
+                Boolean.parseBoolean(properties.getProperty("allowInventorySwap", "true").trim())
+        );
+    }
+
+    private static void writeDefaults(Path path, Properties properties) {
+        try {
+            Files.createDirectories(path.getParent());
+            try (Writer writer = Files.newBufferedWriter(path)) {
+                properties.store(writer, "Untitled item A/B swap settings");
+            }
+        } catch (IOException ignored) {
+        }
+    }
+
+    private static Optional<Item> resolveItem(String rawId) {
+        Identifier id = Identifier.tryParse(rawId);
+        if (id == null || !Registries.ITEM.containsId(id)) {
+            return Optional.empty();
+        }
+
+        return Optional.of(Registries.ITEM.get(id));
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
new file mode 100644
index 0000000000000000000000000000000000000000..1627b1c02312751fdcee2e7b2abe108fb036a763
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
@@ -0,0 +1,107 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.client.MinecraftClient;
+import net.minecraft.client.network.ClientPlayerEntity;
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+import net.minecraft.screen.slot.SlotActionType;
+
+import java.util.Optional;
+import java.util.OptionalInt;
+
+public final class ItemSwapController {
+    private static final int PLAYER_SCREEN_HOTBAR_SLOT_OFFSET = 36;
+
+    private final ItemSwapConfig config;
+    private final InventoryItemFinder finder;
+
+    public ItemSwapController(ItemSwapConfig config) {
+        this.config = config;
+        this.finder = new InventoryItemFinder();
+    }
+
+    public boolean toggle(MinecraftClient client) {
+        if (client.player == null || client.interactionManager == null || client.currentScreen != null) {
+            return false;
+        }
+
+        Optional<Item> itemA = config.itemA();
+        Optional<Item> itemB = config.itemB();
+        if (itemA.isEmpty() || itemB.isEmpty()) {
+            return false;
+        }
+
+        ClientPlayerEntity player = client.player;
+        PlayerInventory inventory = player.getInventory();
+        Item target = targetItem(inventory.getStack(inventory.selectedSlot), itemA.get(), itemB.get());
+        OptionalInt targetSlot = finder.find(inventory, target);
+        if (targetSlot.isEmpty()) {
+            return false;
+        }
+
+        return switchToSlot(client, player, inventory, targetSlot.getAsInt(), itemA.get(), itemB.get());
+    }
+
+    private Item targetItem(ItemStack selectedStack, Item itemA, Item itemB) {
+        if (selectedStack.isOf(itemA)) {
+            return itemB;
+        }
+        if (selectedStack.isOf(itemB)) {
+            return itemA;
+        }
+
+        return itemA;
+    }
+
+    private boolean switchToSlot(
+            MinecraftClient client,
+            ClientPlayerEntity player,
+            PlayerInventory inventory,
+            int inventorySlot,
+            Item itemA,
+            Item itemB
+    ) {
+        if (inventorySlot < InventoryItemFinder.HOTBAR_SIZE) {
+            selectHotbarSlot(client, inventory, inventorySlot);
+            return true;
+        }
+
+        if (!config.allowInventorySwap()) {
+            return false;
+        }
+
+        OptionalInt destinationSlot = safeHotbarDestination(inventory, itemA, itemB);
+        if (destinationSlot.isEmpty()) {
+            return false;
+        }
+
+        client.interactionManager.clickSlot(
+                player.playerScreenHandler.syncId,
+                inventorySlot,
+                destinationSlot.getAsInt(),
+                SlotActionType.SWAP,
+                player
+        );
+        selectHotbarSlot(client, inventory, destinationSlot.getAsInt());
+        return true;
+    }
+
+    private OptionalInt safeHotbarDestination(PlayerInventory inventory, Item itemA, Item itemB) {
+        ItemStack selectedStack = inventory.getStack(inventory.selectedSlot);
+        if (selectedStack.isEmpty() || selectedStack.isOf(itemA) || selectedStack.isOf(itemB)) {
+            return OptionalInt.of(inventory.selectedSlot);
+        }
+
+        return finder.findEmptyHotbarSlot(inventory);
+    }
+
+    private void selectHotbarSlot(MinecraftClient client, PlayerInventory inventory, int slot) {
+        if (inventory.selectedSlot == slot) {
+            return;
+        }
+
+        inventory.selectedSlot = slot;
+        client.interactionManager.syncSelectedSlot();
+    }
+}

exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$env:JAVA_HOME='"'C:\\Program Files\\JetBrains\\IntelliJ IDEA 2026.1.1\\jbr'; & \""'$env:JAVA_HOME'"\\bin\\java.exe\" -classpath \""'$env:USERPROFILE'"\\.gradle\\wrapper\\dists\\gradle-9.2.1-bin\\2t0n5ozlw9xmuyvbp7dnzaxug\\gradle-9.2.1\\lib\\gradle-launcher-9.2.1.jar\" org.gradle.launcher.GradleMain --offline compileJava compileClientJava" in C:\Users\sutec\IdeaProjects\untitled
2026-05-11T07:23:26.252929Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 11.3 seconds
Output:
Starting a Gradle Daemon, 1 incompatible and 1 stopped Daemons could not be reused, use --status for details
WARNING: A restricted method in java.lang.System has been called
WARNING: java.lang.System::load has been called by net.rubygrapefruit.platform.internal.NativeLibraryLoader in an unnamed module (file:/C:/Users/sutec/.gradle/wrapper/dists/gradle-9.2.1-bin/2t0n5ozlw9xmuyvbp7dnzaxug/gradle-9.2.1/lib/native-platform-0.22-milestone-29.jar)
WARNING: Use --enable-native-access=ALL-UNNAMED to avoid a warning for callers in this module
WARNING: Restricted methods will be blocked in a future release unless native access is enabled


FAILURE: Build failed with an exception.

* Where:
Build file 'C:\Users\sutec\IdeaProjects\untitled\build.gradle' line: 2

* What went wrong:
Plugin [id: 'fabric-loom', version: '1.8-SNAPSHOT'] was not found in any of the following sources:

- Gradle Core Plugins (not a core plugin. For more available plugins, please refer to https://docs.gradle.org/9.2.1/userguide/plugin_reference.html in the Gradle documentation.)
- Included Builds (No included builds contain this plugin)
- Plugin Repositories (could not resolve plugin artifact 'fabric-loom:fabric-loom.gradle.plugin:1.8-SNAPSHOT')
  Searched in the following repositories:
    Fabric(https://maven.fabricmc.net/)
    Gradle Central Plugin Repository

* Try:
> Run with --stacktrace option to get the stack trace.
> Run with --info or --debug option to get more log output.
> Run with --scan to generate a Build Scan (powered by Develocity).
> Get more help at https://help.gradle.org.

BUILD FAILED in 10s

 exited 1 in 11342ms:
Starting a Gradle Daemon, 1 incompatible and 1 stopped Daemons could not be reused, use --status for details
WARNING: A restricted method in java.lang.System has been called
WARNING: java.lang.System::load has been called by net.rubygrapefruit.platform.internal.NativeLibraryLoader in an unnamed module (file:/C:/Users/sutec/.gradle/wrapper/dists/gradle-9.2.1-bin/2t0n5ozlw9xmuyvbp7dnzaxug/gradle-9.2.1/lib/native-platform-0.22-milestone-29.jar)
WARNING: Use --enable-native-access=ALL-UNNAMED to avoid a warning for callers in this module
WARNING: Restricted methods will be blocked in a future release unless native access is enabled


FAILURE: Build failed with an exception.

* Where:
Build file 'C:\Users\sutec\IdeaProjects\untitled\build.gradle' line: 2

* What went wrong:
Plugin [id: 'fabric-loom', version: '1.8-SNAPSHOT'] was not found in any of the following sources:

- Gradle Core Plugins (not a core plugin. For more available plugins, please refer to https://docs.gradle.org/9.2.1/userguide/plugin_reference.html in the Gradle documentation.)
- Included Builds (No included builds contain this plugin)
- Plugin Repositories (could not resolve plugin artifact 'fabric-loom:fabric-loom.gradle.plugin:1.8-SNAPSHOT')
  Searched in the following repositories:
    Fabric(https://maven.fabricmc.net/)
    Gradle Central Plugin Repository

* Try:
> Run with --stacktrace option to get the stack trace.
> Run with --info or --debug option to get more log output.
> Run with --scan to generate a Build Scan (powered by Develocity).
> Get more help at https://help.gradle.org.

BUILD FAILED in 10s

diff --git a/src/client/java/org/ahaha/untitled/client/UntitledClient.java b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
index 96f2fa5caf72eb792f660392a6ef7bab77503eb9..760523f1a4590b451ae9a35dd4bed823b44da6a1
--- a/src/client/java/org/ahaha/untitled/client/UntitledClient.java
+++ b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
@@ -1,10 +1,32 @@
 package org.ahaha.untitled.client;
 
 import net.fabricmc.api.ClientModInitializer;
+import net.fabricmc.fabric.api.client.event.lifecycle.v1.ClientTickEvents;
+import net.fabricmc.fabric.api.client.keybinding.v1.KeyBindingHelper;
+import net.minecraft.client.option.KeyBinding;
+import net.minecraft.client.util.InputUtil;
+import org.ahaha.untitled.client.itemswap.ItemSwapConfig;
+import org.ahaha.untitled.client.itemswap.ItemSwapController;
+import org.lwjgl.glfw.GLFW;
 
 public class UntitledClient implements ClientModInitializer {
+    private static final String CATEGORY = "key.category.untitled";
+    private static final String SWAP_KEY = "key.untitled.swap_item_a_b";
 
     @Override
     public void onInitializeClient() {
+        ItemSwapController controller = new ItemSwapController(ItemSwapConfig.load());
+        KeyBinding swapKey = KeyBindingHelper.registerKeyBinding(new KeyBinding(
+                SWAP_KEY,
+                InputUtil.Type.KEYSYM,
+                GLFW.GLFW_KEY_UNKNOWN,
+                CATEGORY
+        ));
+
+        ClientTickEvents.END_CLIENT_TICK.register(client -> {
+            while (swapKey.wasPressed()) {
+                controller.toggle(client);
+            }
+        });
     }
 }
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
new file mode 100644
index 0000000000000000000000000000000000000000..3214363102de082962b3401df416e73144012217
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
@@ -0,0 +1,44 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+
+import java.util.OptionalInt;
+
+public final class InventoryItemFinder {
+    public static final int HOTBAR_SIZE = 9;
+    public static final int MAIN_INVENTORY_SIZE = 36;
+
+    public OptionalInt find(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < MAIN_INVENTORY_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findInHotbar(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findEmptyHotbarSlot(PlayerInventory inventory) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            if (inventory.getStack(slot).isEmpty()) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
new file mode 100644
index 0000000000000000000000000000000000000000..e3f80e5c19e04354bc7646bb6acd358895963c32
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
@@ -0,0 +1,94 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.fabricmc.loader.api.FabricLoader;
+import net.minecraft.item.Item;
+import net.minecraft.registry.Registries;
+import net.minecraft.util.Identifier;
+
+import java.io.IOException;
+import java.io.Reader;
+import java.io.Writer;
+import java.nio.file.Files;
+import java.nio.file.Path;
+import java.util.Optional;
+import java.util.Properties;
+
+public final class ItemSwapConfig {
+    private static final String CONFIG_FILE_NAME = "untitled-item-swap.properties";
+    private static final String DEFAULT_ITEM_A = "minecraft:golden_apple";
+    private static final String DEFAULT_ITEM_B = "minecraft:totem_of_undying";
+
+    private final String itemAId;
+    private final String itemBId;
+    private final boolean allowInventorySwap;
+
+    private ItemSwapConfig(String itemAId, String itemBId, boolean allowInventorySwap) {
+        this.itemAId = itemAId;
+        this.itemBId = itemBId;
+        this.allowInventorySwap = allowInventorySwap;
+    }
+
+    public static ItemSwapConfig load() {
+        Path path = FabricLoader.getInstance().getConfigDir().resolve(CONFIG_FILE_NAME);
+        Properties properties = defaultProperties();
+
+        if (Files.exists(path)) {
+            try (Reader reader = Files.newBufferedReader(path)) {
+                properties.load(reader);
+            } catch (IOException ignored) {
+                return fromProperties(properties);
+            }
+        } else {
+            writeDefaults(path, properties);
+        }
+
+        return fromProperties(properties);
+    }
+
+    public Optional<Item> itemA() {
+        return resolveItem(itemAId);
+    }
+
+    public Optional<Item> itemB() {
+        return resolveItem(itemBId);
+    }
+
+    public boolean allowInventorySwap() {
+        return allowInventorySwap;
+    }
+
+    private static Properties defaultProperties() {
+        Properties properties = new Properties();
+        properties.setProperty("itemA", DEFAULT_ITEM_A);
+        properties.setProperty("itemB", DEFAULT_ITEM_B);
+        properties.setProperty("allowInventorySwap", "true");
+        return properties;
+    }
+
+    private static ItemSwapConfig fromProperties(Properties properties) {
+        return new ItemSwapConfig(
+                properties.getProperty("itemA", DEFAULT_ITEM_A).trim(),
+                properties.getProperty("itemB", DEFAULT_ITEM_B).trim(),
+                Boolean.parseBoolean(properties.getProperty("allowInventorySwap", "true").trim())
+        );
+    }
+
+    private static void writeDefaults(Path path, Properties properties) {
+        try {
+            Files.createDirectories(path.getParent());
+            try (Writer writer = Files.newBufferedWriter(path)) {
+                properties.store(writer, "Untitled item A/B swap settings");
+            }
+        } catch (IOException ignored) {
+        }
+    }
+
+    private static Optional<Item> resolveItem(String rawId) {
+        Identifier id = Identifier.tryParse(rawId);
+        if (id == null || !Registries.ITEM.containsId(id)) {
+            return Optional.empty();
+        }
+
+        return Optional.of(Registries.ITEM.get(id));
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
new file mode 100644
index 0000000000000000000000000000000000000000..1627b1c02312751fdcee2e7b2abe108fb036a763
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
@@ -0,0 +1,107 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.client.MinecraftClient;
+import net.minecraft.client.network.ClientPlayerEntity;
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+import net.minecraft.screen.slot.SlotActionType;
+
+import java.util.Optional;
+import java.util.OptionalInt;
+
+public final class ItemSwapController {
+    private static final int PLAYER_SCREEN_HOTBAR_SLOT_OFFSET = 36;
+
+    private final ItemSwapConfig config;
+    private final InventoryItemFinder finder;
+
+    public ItemSwapController(ItemSwapConfig config) {
+        this.config = config;
+        this.finder = new InventoryItemFinder();
+    }
+
+    public boolean toggle(MinecraftClient client) {
+        if (client.player == null || client.interactionManager == null || client.currentScreen != null) {
+            return false;
+        }
+
+        Optional<Item> itemA = config.itemA();
+        Optional<Item> itemB = config.itemB();
+        if (itemA.isEmpty() || itemB.isEmpty()) {
+            return false;
+        }
+
+        ClientPlayerEntity player = client.player;
+        PlayerInventory inventory = player.getInventory();
+        Item target = targetItem(inventory.getStack(inventory.selectedSlot), itemA.get(), itemB.get());
+        OptionalInt targetSlot = finder.find(inventory, target);
+        if (targetSlot.isEmpty()) {
+            return false;
+        }
+
+        return switchToSlot(client, player, inventory, targetSlot.getAsInt(), itemA.get(), itemB.get());
+    }
+
+    private Item targetItem(ItemStack selectedStack, Item itemA, Item itemB) {
+        if (selectedStack.isOf(itemA)) {
+            return itemB;
+        }
+        if (selectedStack.isOf(itemB)) {
+            return itemA;
+        }
+
+        return itemA;
+    }
+
+    private boolean switchToSlot(
+            MinecraftClient client,
+            ClientPlayerEntity player,
+            PlayerInventory inventory,
+            int inventorySlot,
+            Item itemA,
+            Item itemB
+    ) {
+        if (inventorySlot < InventoryItemFinder.HOTBAR_SIZE) {
+            selectHotbarSlot(client, inventory, inventorySlot);
+            return true;
+        }
+
+        if (!config.allowInventorySwap()) {
+            return false;
+        }
+
+        OptionalInt destinationSlot = safeHotbarDestination(inventory, itemA, itemB);
+        if (destinationSlot.isEmpty()) {
+            return false;
+        }
+
+        client.interactionManager.clickSlot(
+                player.playerScreenHandler.syncId,
+                inventorySlot,
+                destinationSlot.getAsInt(),
+                SlotActionType.SWAP,
+                player
+        );
+        selectHotbarSlot(client, inventory, destinationSlot.getAsInt());
+        return true;
+    }
+
+    private OptionalInt safeHotbarDestination(PlayerInventory inventory, Item itemA, Item itemB) {
+        ItemStack selectedStack = inventory.getStack(inventory.selectedSlot);
+        if (selectedStack.isEmpty() || selectedStack.isOf(itemA) || selectedStack.isOf(itemB)) {
+            return OptionalInt.of(inventory.selectedSlot);
+        }
+
+        return finder.findEmptyHotbarSlot(inventory);
+    }
+
+    private void selectHotbarSlot(MinecraftClient client, PlayerInventory inventory, int slot) {
+        if (inventory.selectedSlot == slot) {
+            return;
+        }
+
+        inventory.selectedSlot = slot;
+        client.interactionManager.syncSelectedSlot();
+    }
+}

exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "& 'C:\\Program Files\\JetBrains\\IntelliJ IDEA 2026.1.1\\jbr\\bin\\javac.exe' -version" in C:\Users\sutec\IdeaProjects\untitled
 succeeded in 913ms:
javac 25.0.2

diff --git a/src/client/java/org/ahaha/untitled/client/UntitledClient.java b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
index 96f2fa5caf72eb792f660392a6ef7bab77503eb9..760523f1a4590b451ae9a35dd4bed823b44da6a1
--- a/src/client/java/org/ahaha/untitled/client/UntitledClient.java
+++ b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
@@ -1,10 +1,32 @@
 package org.ahaha.untitled.client;
 
 import net.fabricmc.api.ClientModInitializer;
+import net.fabricmc.fabric.api.client.event.lifecycle.v1.ClientTickEvents;
+import net.fabricmc.fabric.api.client.keybinding.v1.KeyBindingHelper;
+import net.minecraft.client.option.KeyBinding;
+import net.minecraft.client.util.InputUtil;
+import org.ahaha.untitled.client.itemswap.ItemSwapConfig;
+import org.ahaha.untitled.client.itemswap.ItemSwapController;
+import org.lwjgl.glfw.GLFW;
 
 public class UntitledClient implements ClientModInitializer {
+    private static final String CATEGORY = "key.category.untitled";
+    private static final String SWAP_KEY = "key.untitled.swap_item_a_b";
 
     @Override
     public void onInitializeClient() {
+        ItemSwapController controller = new ItemSwapController(ItemSwapConfig.load());
+        KeyBinding swapKey = KeyBindingHelper.registerKeyBinding(new KeyBinding(
+                SWAP_KEY,
+                InputUtil.Type.KEYSYM,
+                GLFW.GLFW_KEY_UNKNOWN,
+                CATEGORY
+        ));
+
+        ClientTickEvents.END_CLIENT_TICK.register(client -> {
+            while (swapKey.wasPressed()) {
+                controller.toggle(client);
+            }
+        });
     }
 }
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
new file mode 100644
index 0000000000000000000000000000000000000000..3214363102de082962b3401df416e73144012217
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
@@ -0,0 +1,44 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+
+import java.util.OptionalInt;
+
+public final class InventoryItemFinder {
+    public static final int HOTBAR_SIZE = 9;
+    public static final int MAIN_INVENTORY_SIZE = 36;
+
+    public OptionalInt find(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < MAIN_INVENTORY_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findInHotbar(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findEmptyHotbarSlot(PlayerInventory inventory) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            if (inventory.getStack(slot).isEmpty()) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
new file mode 100644
index 0000000000000000000000000000000000000000..e3f80e5c19e04354bc7646bb6acd358895963c32
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
@@ -0,0 +1,94 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.fabricmc.loader.api.FabricLoader;
+import net.minecraft.item.Item;
+import net.minecraft.registry.Registries;
+import net.minecraft.util.Identifier;
+
+import java.io.IOException;
+import java.io.Reader;
+import java.io.Writer;
+import java.nio.file.Files;
+import java.nio.file.Path;
+import java.util.Optional;
+import java.util.Properties;
+
+public final class ItemSwapConfig {
+    private static final String CONFIG_FILE_NAME = "untitled-item-swap.properties";
+    private static final String DEFAULT_ITEM_A = "minecraft:golden_apple";
+    private static final String DEFAULT_ITEM_B = "minecraft:totem_of_undying";
+
+    private final String itemAId;
+    private final String itemBId;
+    private final boolean allowInventorySwap;
+
+    private ItemSwapConfig(String itemAId, String itemBId, boolean allowInventorySwap) {
+        this.itemAId = itemAId;
+        this.itemBId = itemBId;
+        this.allowInventorySwap = allowInventorySwap;
+    }
+
+    public static ItemSwapConfig load() {
+        Path path = FabricLoader.getInstance().getConfigDir().resolve(CONFIG_FILE_NAME);
+        Properties properties = defaultProperties();
+
+        if (Files.exists(path)) {
+            try (Reader reader = Files.newBufferedReader(path)) {
+                properties.load(reader);
+            } catch (IOException ignored) {
+                return fromProperties(properties);
+            }
+        } else {
+            writeDefaults(path, properties);
+        }
+
+        return fromProperties(properties);
+    }
+
+    public Optional<Item> itemA() {
+        return resolveItem(itemAId);
+    }
+
+    public Optional<Item> itemB() {
+        return resolveItem(itemBId);
+    }
+
+    public boolean allowInventorySwap() {
+        return allowInventorySwap;
+    }
+
+    private static Properties defaultProperties() {
+        Properties properties = new Properties();
+        properties.setProperty("itemA", DEFAULT_ITEM_A);
+        properties.setProperty("itemB", DEFAULT_ITEM_B);
+        properties.setProperty("allowInventorySwap", "true");
+        return properties;
+    }
+
+    private static ItemSwapConfig fromProperties(Properties properties) {
+        return new ItemSwapConfig(
+                properties.getProperty("itemA", DEFAULT_ITEM_A).trim(),
+                properties.getProperty("itemB", DEFAULT_ITEM_B).trim(),
+                Boolean.parseBoolean(properties.getProperty("allowInventorySwap", "true").trim())
+        );
+    }
+
+    private static void writeDefaults(Path path, Properties properties) {
+        try {
+            Files.createDirectories(path.getParent());
+            try (Writer writer = Files.newBufferedWriter(path)) {
+                properties.store(writer, "Untitled item A/B swap settings");
+            }
+        } catch (IOException ignored) {
+        }
+    }
+
+    private static Optional<Item> resolveItem(String rawId) {
+        Identifier id = Identifier.tryParse(rawId);
+        if (id == null || !Registries.ITEM.containsId(id)) {
+            return Optional.empty();
+        }
+
+        return Optional.of(Registries.ITEM.get(id));
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
new file mode 100644
index 0000000000000000000000000000000000000000..1627b1c02312751fdcee2e7b2abe108fb036a763
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
@@ -0,0 +1,107 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.client.MinecraftClient;
+import net.minecraft.client.network.ClientPlayerEntity;
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+import net.minecraft.screen.slot.SlotActionType;
+
+import java.util.Optional;
+import java.util.OptionalInt;
+
+public final class ItemSwapController {
+    private static final int PLAYER_SCREEN_HOTBAR_SLOT_OFFSET = 36;
+
+    private final ItemSwapConfig config;
+    private final InventoryItemFinder finder;
+
+    public ItemSwapController(ItemSwapConfig config) {
+        this.config = config;
+        this.finder = new InventoryItemFinder();
+    }
+
+    public boolean toggle(MinecraftClient client) {
+        if (client.player == null || client.interactionManager == null || client.currentScreen != null) {
+            return false;
+        }
+
+        Optional<Item> itemA = config.itemA();
+        Optional<Item> itemB = config.itemB();
+        if (itemA.isEmpty() || itemB.isEmpty()) {
+            return false;
+        }
+
+        ClientPlayerEntity player = client.player;
+        PlayerInventory inventory = player.getInventory();
+        Item target = targetItem(inventory.getStack(inventory.selectedSlot), itemA.get(), itemB.get());
+        OptionalInt targetSlot = finder.find(inventory, target);
+        if (targetSlot.isEmpty()) {
+            return false;
+        }
+
+        return switchToSlot(client, player, inventory, targetSlot.getAsInt(), itemA.get(), itemB.get());
+    }
+
+    private Item targetItem(ItemStack selectedStack, Item itemA, Item itemB) {
+        if (selectedStack.isOf(itemA)) {
+            return itemB;
+        }
+        if (selectedStack.isOf(itemB)) {
+            return itemA;
+        }
+
+        return itemA;
+    }
+
+    private boolean switchToSlot(
+            MinecraftClient client,
+            ClientPlayerEntity player,
+            PlayerInventory inventory,
+            int inventorySlot,
+            Item itemA,
+            Item itemB
+    ) {
+        if (inventorySlot < InventoryItemFinder.HOTBAR_SIZE) {
+            selectHotbarSlot(client, inventory, inventorySlot);
+            return true;
+        }
+
+        if (!config.allowInventorySwap()) {
+            return false;
+        }
+
+        OptionalInt destinationSlot = safeHotbarDestination(inventory, itemA, itemB);
+        if (destinationSlot.isEmpty()) {
+            return false;
+        }
+
+        client.interactionManager.clickSlot(
+                player.playerScreenHandler.syncId,
+                inventorySlot,
+                destinationSlot.getAsInt(),
+                SlotActionType.SWAP,
+                player
+        );
+        selectHotbarSlot(client, inventory, destinationSlot.getAsInt());
+        return true;
+    }
+
+    private OptionalInt safeHotbarDestination(PlayerInventory inventory, Item itemA, Item itemB) {
+        ItemStack selectedStack = inventory.getStack(inventory.selectedSlot);
+        if (selectedStack.isEmpty() || selectedStack.isOf(itemA) || selectedStack.isOf(itemB)) {
+            return OptionalInt.of(inventory.selectedSlot);
+        }
+
+        return finder.findEmptyHotbarSlot(inventory);
+    }
+
+    private void selectHotbarSlot(MinecraftClient client, PlayerInventory inventory, int slot) {
+        if (inventory.selectedSlot == slot) {
+            return;
+        }
+
+        inventory.selectedSlot = slot;
+        client.interactionManager.syncSelectedSlot();
+    }
+}

exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-ChildItem -Recurse -File build\\classes -ErrorAction SilentlyContinue | Select-Object -First 20 -ExpandProperty FullName; Get-ChildItem -Recurse -File build\\loom-cache -Filter '*.jar' | Select-Object -First 20 -ExpandProperty FullName" in C:\Users\sutec\IdeaProjects\untitled
 succeeded in 680ms:
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-api-0.116.11+1.21.1-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-api-0.116.11+1.21.1.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-api-base-0.4.42+6573ed8c19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-api-base-0.4.42+6573ed8c19.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-api-lookup-api-v1-1.6.72+7b3d111d19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-api-lookup-api-v1-1.6.72+7b3d111d19.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-biome-api-v1-13.0.31+d527f9fd19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-biome-api-v1-13.0.31+d527f9fd19.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-block-api-v1-1.1.0+0bc3503219-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-block-api-v1-1.1.0+0bc3503219.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-block-view-api-v2-1.0.11+ebb2264e19-client-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-block-view-api-v2-1.0.11+ebb2264e19-client.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-block-view-api-v2-1.0.11+ebb2264e19-common-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-block-view-api-v2-1.0.11+ebb2264e19-common.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-block-view-api-v2-1.0.11+ebb2264e19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-block-view-api-v2-1.0.11+ebb2264e19.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-blockrenderlayer-v1-1.1.52+0af3f5a719-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-blockrenderlayer-v1-1.1.52+0af3f5a719.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-client-tags-api-v1-1.1.15+6573ed8c19-sources.jar
C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-client-tags-api-v1-1.1.15+6573ed8c19.jar

diff --git a/src/client/java/org/ahaha/untitled/client/UntitledClient.java b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
index 96f2fa5caf72eb792f660392a6ef7bab77503eb9..760523f1a4590b451ae9a35dd4bed823b44da6a1
--- a/src/client/java/org/ahaha/untitled/client/UntitledClient.java
+++ b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
@@ -1,10 +1,32 @@
 package org.ahaha.untitled.client;
 
 import net.fabricmc.api.ClientModInitializer;
+import net.fabricmc.fabric.api.client.event.lifecycle.v1.ClientTickEvents;
+import net.fabricmc.fabric.api.client.keybinding.v1.KeyBindingHelper;
+import net.minecraft.client.option.KeyBinding;
+import net.minecraft.client.util.InputUtil;
+import org.ahaha.untitled.client.itemswap.ItemSwapConfig;
+import org.ahaha.untitled.client.itemswap.ItemSwapController;
+import org.lwjgl.glfw.GLFW;
 
 public class UntitledClient implements ClientModInitializer {
+    private static final String CATEGORY = "key.category.untitled";
+    private static final String SWAP_KEY = "key.untitled.swap_item_a_b";
 
     @Override
     public void onInitializeClient() {
+        ItemSwapController controller = new ItemSwapController(ItemSwapConfig.load());
+        KeyBinding swapKey = KeyBindingHelper.registerKeyBinding(new KeyBinding(
+                SWAP_KEY,
+                InputUtil.Type.KEYSYM,
+                GLFW.GLFW_KEY_UNKNOWN,
+                CATEGORY
+        ));
+
+        ClientTickEvents.END_CLIENT_TICK.register(client -> {
+            while (swapKey.wasPressed()) {
+                controller.toggle(client);
+            }
+        });
     }
 }
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
new file mode 100644
index 0000000000000000000000000000000000000000..3214363102de082962b3401df416e73144012217
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
@@ -0,0 +1,44 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+
+import java.util.OptionalInt;
+
+public final class InventoryItemFinder {
+    public static final int HOTBAR_SIZE = 9;
+    public static final int MAIN_INVENTORY_SIZE = 36;
+
+    public OptionalInt find(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < MAIN_INVENTORY_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findInHotbar(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findEmptyHotbarSlot(PlayerInventory inventory) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            if (inventory.getStack(slot).isEmpty()) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
new file mode 100644
index 0000000000000000000000000000000000000000..e3f80e5c19e04354bc7646bb6acd358895963c32
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
@@ -0,0 +1,94 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.fabricmc.loader.api.FabricLoader;
+import net.minecraft.item.Item;
+import net.minecraft.registry.Registries;
+import net.minecraft.util.Identifier;
+
+import java.io.IOException;
+import java.io.Reader;
+import java.io.Writer;
+import java.nio.file.Files;
+import java.nio.file.Path;
+import java.util.Optional;
+import java.util.Properties;
+
+public final class ItemSwapConfig {
+    private static final String CONFIG_FILE_NAME = "untitled-item-swap.properties";
+    private static final String DEFAULT_ITEM_A = "minecraft:golden_apple";
+    private static final String DEFAULT_ITEM_B = "minecraft:totem_of_undying";
+
+    private final String itemAId;
+    private final String itemBId;
+    private final boolean allowInventorySwap;
+
+    private ItemSwapConfig(String itemAId, String itemBId, boolean allowInventorySwap) {
+        this.itemAId = itemAId;
+        this.itemBId = itemBId;
+        this.allowInventorySwap = allowInventorySwap;
+    }
+
+    public static ItemSwapConfig load() {
+        Path path = FabricLoader.getInstance().getConfigDir().resolve(CONFIG_FILE_NAME);
+        Properties properties = defaultProperties();
+
+        if (Files.exists(path)) {
+            try (Reader reader = Files.newBufferedReader(path)) {
+                properties.load(reader);
+            } catch (IOException ignored) {
+                return fromProperties(properties);
+            }
+        } else {
+            writeDefaults(path, properties);
+        }
+
+        return fromProperties(properties);
+    }
+
+    public Optional<Item> itemA() {
+        return resolveItem(itemAId);
+    }
+
+    public Optional<Item> itemB() {
+        return resolveItem(itemBId);
+    }
+
+    public boolean allowInventorySwap() {
+        return allowInventorySwap;
+    }
+
+    private static Properties defaultProperties() {
+        Properties properties = new Properties();
+        properties.setProperty("itemA", DEFAULT_ITEM_A);
+        properties.setProperty("itemB", DEFAULT_ITEM_B);
+        properties.setProperty("allowInventorySwap", "true");
+        return properties;
+    }
+
+    private static ItemSwapConfig fromProperties(Properties properties) {
+        return new ItemSwapConfig(
+                properties.getProperty("itemA", DEFAULT_ITEM_A).trim(),
+                properties.getProperty("itemB", DEFAULT_ITEM_B).trim(),
+                Boolean.parseBoolean(properties.getProperty("allowInventorySwap", "true").trim())
+        );
+    }
+
+    private static void writeDefaults(Path path, Properties properties) {
+        try {
+            Files.createDirectories(path.getParent());
+            try (Writer writer = Files.newBufferedWriter(path)) {
+                properties.store(writer, "Untitled item A/B swap settings");
+            }
+        } catch (IOException ignored) {
+        }
+    }
+
+    private static Optional<Item> resolveItem(String rawId) {
+        Identifier id = Identifier.tryParse(rawId);
+        if (id == null || !Registries.ITEM.containsId(id)) {
+            return Optional.empty();
+        }
+
+        return Optional.of(Registries.ITEM.get(id));
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
new file mode 100644
index 0000000000000000000000000000000000000000..1627b1c02312751fdcee2e7b2abe108fb036a763
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
@@ -0,0 +1,107 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.client.MinecraftClient;
+import net.minecraft.client.network.ClientPlayerEntity;
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+import net.minecraft.screen.slot.SlotActionType;
+
+import java.util.Optional;
+import java.util.OptionalInt;
+
+public final class ItemSwapController {
+    private static final int PLAYER_SCREEN_HOTBAR_SLOT_OFFSET = 36;
+
+    private final ItemSwapConfig config;
+    private final InventoryItemFinder finder;
+
+    public ItemSwapController(ItemSwapConfig config) {
+        this.config = config;
+        this.finder = new InventoryItemFinder();
+    }
+
+    public boolean toggle(MinecraftClient client) {
+        if (client.player == null || client.interactionManager == null || client.currentScreen != null) {
+            return false;
+        }
+
+        Optional<Item> itemA = config.itemA();
+        Optional<Item> itemB = config.itemB();
+        if (itemA.isEmpty() || itemB.isEmpty()) {
+            return false;
+        }
+
+        ClientPlayerEntity player = client.player;
+        PlayerInventory inventory = player.getInventory();
+        Item target = targetItem(inventory.getStack(inventory.selectedSlot), itemA.get(), itemB.get());
+        OptionalInt targetSlot = finder.find(inventory, target);
+        if (targetSlot.isEmpty()) {
+            return false;
+        }
+
+        return switchToSlot(client, player, inventory, targetSlot.getAsInt(), itemA.get(), itemB.get());
+    }
+
+    private Item targetItem(ItemStack selectedStack, Item itemA, Item itemB) {
+        if (selectedStack.isOf(itemA)) {
+            return itemB;
+        }
+        if (selectedStack.isOf(itemB)) {
+            return itemA;
+        }
+
+        return itemA;
+    }
+
+    private boolean switchToSlot(
+            MinecraftClient client,
+            ClientPlayerEntity player,
+            PlayerInventory inventory,
+            int inventorySlot,
+            Item itemA,
+            Item itemB
+    ) {
+        if (inventorySlot < InventoryItemFinder.HOTBAR_SIZE) {
+            selectHotbarSlot(client, inventory, inventorySlot);
+            return true;
+        }
+
+        if (!config.allowInventorySwap()) {
+            return false;
+        }
+
+        OptionalInt destinationSlot = safeHotbarDestination(inventory, itemA, itemB);
+        if (destinationSlot.isEmpty()) {
+            return false;
+        }
+
+        client.interactionManager.clickSlot(
+                player.playerScreenHandler.syncId,
+                inventorySlot,
+                destinationSlot.getAsInt(),
+                SlotActionType.SWAP,
+                player
+        );
+        selectHotbarSlot(client, inventory, destinationSlot.getAsInt());
+        return true;
+    }
+
+    private OptionalInt safeHotbarDestination(PlayerInventory inventory, Item itemA, Item itemB) {
+        ItemStack selectedStack = inventory.getStack(inventory.selectedSlot);
+        if (selectedStack.isEmpty() || selectedStack.isOf(itemA) || selectedStack.isOf(itemB)) {
+            return OptionalInt.of(inventory.selectedSlot);
+        }
+
+        return finder.findEmptyHotbarSlot(inventory);
+    }
+
+    private void selectHotbarSlot(MinecraftClient client, PlayerInventory inventory, int slot) {
+        if (inventory.selectedSlot == slot) {
+            return;
+        }
+
+        inventory.selectedSlot = slot;
+        client.interactionManager.syncSelectedSlot();
+    }
+}

exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-ChildItem -Path "$env:USERPROFILE'"\\.gradle\\caches\\modules-2\\files-2.1\" -Recurse -File -Filter '*.jar' | Where-Object { "'$_.FullName -match '"'lwjgl.*glfw|fabric-loader|fabric-api' } | Select-Object -First 40 -ExpandProperty FullName" in C:\Users\sutec\IdeaProjects\untitled
 succeeded in 726ms:
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc\fabric-loader\0.19.2\9a6a175c777859b39cf762daf597f2c67778ae0e\fabric-loader-0.19.2-sources.jar
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc\fabric-loader\0.19.2\cc647a5b22dbc49b9a3267cebcee25fc4882b108\fabric-loader-0.19.2.jar
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc.fabric-api\fabric-api\0.116.11+1.21.1\65f4e8b9dcbad6697b2fb32fa0bb937ec5efcd84\fabric-api-0.116.11+1.21.1.jar
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc.fabric-api\fabric-api\0.116.11+1.21.1\76185fd482e268e903a64b27dd9774662ebd840c\fabric-api-0.116.11+1.21.1-sources.jar
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc.fabric-api\fabric-api-base\0.4.42+6573ed8c19\2d78f9483cba8704e1cf1b72205df9aa1e1772ed\fabric-api-base-0.4.42+6573ed8c19-sources.jar
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc.fabric-api\fabric-api-base\0.4.42+6573ed8c19\9e1ebdffd8b009ea65606652109b102b86f3b0b3\fabric-api-base-0.4.42+6573ed8c19.jar
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc.fabric-api\fabric-api-lookup-api-v1\1.6.72+7b3d111d19\1a348845857a3ace4be26cccbbed4d14c915cda0\fabric-api-lookup-api-v1-1.6.72+7b3d111d19-sources.jar
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc.fabric-api\fabric-api-lookup-api-v1\1.6.72+7b3d111d19\8137b35b7e1d43405220a1c97db87bfb5b7c7fc4\fabric-api-lookup-api-v1-1.6.72+7b3d111d19.jar
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc.fabric-api\fabric-biome-api-v1\13.0.31+d527f9fd19\95cfc2b63aa6bfcf04176c5ae9b5b0bf15af0ed4\fabric-biome-api-v1-13.0.31+d527f9fd19-sources.jar
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc.fabric-api\fabric-biome-api-v1\13.0.31+d527f9fd19\d3ba3b65b91a7683514349da06787296f20caca6\fabric-biome-api-v1-13.0.31+d527f9fd19.jar
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc.fabric-api\fabric-block-api-v1\1.1.0+0bc3503219\91577714db7a7e6b5cf0f775270eaa3685eb76cc\fabric-block-api-v1-1.1.0+0bc3503219-sources.jar
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc.fabric-api\fabric-block-api-v1\1.1.0+0bc3503219\e19496a916d5ce2de080e9dc191d2656bb7112ec\fabric-block-api-v1-1.1.0+0bc3503219.jar
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc.fabric-api\fabric-block-view-api-v2\1.0.11+ebb2264e19\591f4df27d948dcc8f591dfdd54d0e1ab81a42b5\fabric-block-view-api-v2-1.0.11+ebb2264e19-sources.jar
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc.fabric-api\fabric-block-view-api-v2\1.0.11+ebb2264e19\7f3ae653d8901d8db1ddde4277dd3016189ca77c\fabric-block-view-api-v2-1.0.11+ebb2264e19.jar
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc.fabric-api\fabric-blockrenderlayer-v1\1.1.52+0af3f5a719\2758cebd59364d0541ef477d470db51496f8d77c\fabric-blockrenderlayer-v1-1.1.52+0af3f5a719.jar
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc.fabric-api\fabric-blockrenderlayer-v1\1.1.52+0af3f5a719\538bafa601c06a19573f2f4972a0a17851041306\fabric-blockrenderlayer-v1-1.1.52+0af3f5a719-sources.jar
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc.fabric-api\fabric-client-tags-api-v1\1.1.15+6573ed8c19\a9fa6e0fe3b073ff8f975921ca0eb70dc1ba6a44\fabric-client-tags-api-v1-1.1.15+6573ed8c19.jar
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc.fabric-api\fabric-client-tags-api-v1\1.1.15+6573ed8c19\d8edc6cb2fbdf7d04da55d3d03d9d4279fe056b\fabric-client-tags-api-v1-1.1.15+6573ed8c19-sources.jar
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc.fabric-api\fabric-command-api-v1\1.2.49+f71b366f19\d38c7cfec8ef55b3da45749e046f7471f5adde0d\fabric-command-api-v1-1.2.49+f71b366f19-sources.jar
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc.fabric-api\fabric-command-api-v1\1.2.49+f71b366f19\ef5660723ffef1d43bbd56875b4ee6c1d4c54d5c\fabric-command-api-v1-1.2.49+f71b366f19.jar
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc.fabric-api\fabric-command-api-v2\2.2.28+6ced4dd919\165a5de823fd40d68b15a1c5aa7a23873cf54137\fabric-command-api-v2-2.2.28+6ced4dd919.jar
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc.fabric-api\fabric-command-api-v2\2.2.28+6ced4dd919\955f28fb96319273948f9dceb8f85255a79012ba\fabric-command-api-v2-2.2.28+6ced4dd919-sources.jar
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc.fabric-api\fabric-commands-v0\0.2.66+df3654b319\d306bc531b2638b2a14e15a589ea648ed79fff70\fabric-commands-v0-0.2.66+df3654b319.jar
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc.fabric-api\fabric-commands-v0\0.2.66+df3654b319\e68086365b1622ce8ad89b75d85e70be685a8a19\fabric-commands-v0-0.2.66+df3654b319-sources.jar
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc.fabric-api\fabric-content-registries-v0\8.0.19+b559734419\8d43be7d6d46b8a13f9099e01e08b0ecead52608\fabric-content-registries-v0-8.0.19+b559734419.jar
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc.fabric-api\fabric-content-registries-v0\8.0.19+b559734419\ede31ebdb4e6bc8e68ace87865c670f2fea3f631\fabric-content-registries-v0-8.0.19+b559734419-sources.jar
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc.fabric-api\fabric-convention-tags-v1\2.1.7+7f945d5b19\101e1a9048bf952d85efa30459dd0d21046e67b0\fabric-convention-tags-v1-2.1.7+7f945d5b19.jar
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc.fabric-api\fabric-convention-tags-v1\2.1.7+7f945d5b19\1dacce941ba4c0cced9ba6305f6d04d052bd4f6f\fabric-convention-tags-v1-2.1.7+7f945d5b19-sources.jar
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc.fabric-api\fabric-convention-tags-v2\2.12.0+c3656daa19\22b9a0027dd5641b34605fa26a5c2064f15e4c31\fabric-convention-tags-v2-2.12.0+c3656daa19-sources.jar
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc.fabric-api\fabric-convention-tags-v2\2.12.0+c3656daa19\64527b1538fc93b0bf32adcc960a7c6637c212bb\fabric-convention-tags-v2-2.12.0+c3656daa19.jar
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc.fabric-api\fabric-crash-report-info-v1\0.2.29+0af3f5a719\5e01786b70d559fee9281b21b490aace12d38da7\fabric-crash-report-info-v1-0.2.29+0af3f5a719-sources.jar
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc.fabric-api\fabric-crash-report-info-v1\0.2.29+0af3f5a719\65d95c486670dd2fa130ad5fdacd6c95fc0963cb\fabric-crash-report-info-v1-0.2.29+0af3f5a719.jar
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc.fabric-api\fabric-data-attachment-api-v1\1.4.6+5b36e0f719\849e8f1cf5b2dba2c6fb267dd4577d793aa3e566\fabric-data-attachment-api-v1-1.4.6+5b36e0f719.jar
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc.fabric-api\fabric-data-attachment-api-v1\1.4.6+5b36e0f719\97f3da74905c31157071ce477bf8bccaea61a7ff\fabric-data-attachment-api-v1-1.4.6+5b36e0f719-sources.jar
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc.fabric-api\fabric-data-generation-api-v1\20.2.37+60a6976219\326db354014d967025d31938c5810762b1a9a737\fabric-data-generation-api-v1-20.2.37+60a6976219.jar
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc.fabric-api\fabric-data-generation-api-v1\20.2.37+60a6976219\c5e2c2751e98eceb910a2c2abfcf7258b0a93184\fabric-data-generation-api-v1-20.2.37+60a6976219-sources.jar
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc.fabric-api\fabric-dimensions-v1\4.0.1+65213ef819\c58b3d7aa7127676c180338d05ca9e0ae72f5894\fabric-dimensions-v1-4.0.1+65213ef819.jar
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc.fabric-api\fabric-dimensions-v1\4.0.1+65213ef819\f41390cde8d8c1f4b56d8be8c1adce65949aae02\fabric-dimensions-v1-4.0.1+65213ef819-sources.jar
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc.fabric-api\fabric-entity-events-v1\1.8.0+2b27e0a419\58b4bb9bf9be18452d6921137a740661df00a34c\fabric-entity-events-v1-1.8.0+2b27e0a419.jar
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\net.fabricmc.fabric-api\fabric-entity-events-v1\1.8.0+2b27e0a419\7694d6828bd696f40113dd9b83da131f78ee679\fabric-entity-events-v1-1.8.0+2b27e0a419-sources.jar

diff --git a/src/client/java/org/ahaha/untitled/client/UntitledClient.java b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
index 96f2fa5caf72eb792f660392a6ef7bab77503eb9..760523f1a4590b451ae9a35dd4bed823b44da6a1
--- a/src/client/java/org/ahaha/untitled/client/UntitledClient.java
+++ b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
@@ -1,10 +1,32 @@
 package org.ahaha.untitled.client;
 
 import net.fabricmc.api.ClientModInitializer;
+import net.fabricmc.fabric.api.client.event.lifecycle.v1.ClientTickEvents;
+import net.fabricmc.fabric.api.client.keybinding.v1.KeyBindingHelper;
+import net.minecraft.client.option.KeyBinding;
+import net.minecraft.client.util.InputUtil;
+import org.ahaha.untitled.client.itemswap.ItemSwapConfig;
+import org.ahaha.untitled.client.itemswap.ItemSwapController;
+import org.lwjgl.glfw.GLFW;
 
 public class UntitledClient implements ClientModInitializer {
+    private static final String CATEGORY = "key.category.untitled";
+    private static final String SWAP_KEY = "key.untitled.swap_item_a_b";
 
     @Override
     public void onInitializeClient() {
+        ItemSwapController controller = new ItemSwapController(ItemSwapConfig.load());
+        KeyBinding swapKey = KeyBindingHelper.registerKeyBinding(new KeyBinding(
+                SWAP_KEY,
+                InputUtil.Type.KEYSYM,
+                GLFW.GLFW_KEY_UNKNOWN,
+                CATEGORY
+        ));
+
+        ClientTickEvents.END_CLIENT_TICK.register(client -> {
+            while (swapKey.wasPressed()) {
+                controller.toggle(client);
+            }
+        });
     }
 }
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
new file mode 100644
index 0000000000000000000000000000000000000000..3214363102de082962b3401df416e73144012217
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
@@ -0,0 +1,44 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+
+import java.util.OptionalInt;
+
+public final class InventoryItemFinder {
+    public static final int HOTBAR_SIZE = 9;
+    public static final int MAIN_INVENTORY_SIZE = 36;
+
+    public OptionalInt find(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < MAIN_INVENTORY_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findInHotbar(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findEmptyHotbarSlot(PlayerInventory inventory) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            if (inventory.getStack(slot).isEmpty()) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
new file mode 100644
index 0000000000000000000000000000000000000000..e3f80e5c19e04354bc7646bb6acd358895963c32
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
@@ -0,0 +1,94 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.fabricmc.loader.api.FabricLoader;
+import net.minecraft.item.Item;
+import net.minecraft.registry.Registries;
+import net.minecraft.util.Identifier;
+
+import java.io.IOException;
+import java.io.Reader;
+import java.io.Writer;
+import java.nio.file.Files;
+import java.nio.file.Path;
+import java.util.Optional;
+import java.util.Properties;
+
+public final class ItemSwapConfig {
+    private static final String CONFIG_FILE_NAME = "untitled-item-swap.properties";
+    private static final String DEFAULT_ITEM_A = "minecraft:golden_apple";
+    private static final String DEFAULT_ITEM_B = "minecraft:totem_of_undying";
+
+    private final String itemAId;
+    private final String itemBId;
+    private final boolean allowInventorySwap;
+
+    private ItemSwapConfig(String itemAId, String itemBId, boolean allowInventorySwap) {
+        this.itemAId = itemAId;
+        this.itemBId = itemBId;
+        this.allowInventorySwap = allowInventorySwap;
+    }
+
+    public static ItemSwapConfig load() {
+        Path path = FabricLoader.getInstance().getConfigDir().resolve(CONFIG_FILE_NAME);
+        Properties properties = defaultProperties();
+
+        if (Files.exists(path)) {
+            try (Reader reader = Files.newBufferedReader(path)) {
+                properties.load(reader);
+            } catch (IOException ignored) {
+                return fromProperties(properties);
+            }
+        } else {
+            writeDefaults(path, properties);
+        }
+
+        return fromProperties(properties);
+    }
+
+    public Optional<Item> itemA() {
+        return resolveItem(itemAId);
+    }
+
+    public Optional<Item> itemB() {
+        return resolveItem(itemBId);
+    }
+
+    public boolean allowInventorySwap() {
+        return allowInventorySwap;
+    }
+
+    private static Properties defaultProperties() {
+        Properties properties = new Properties();
+        properties.setProperty("itemA", DEFAULT_ITEM_A);
+        properties.setProperty("itemB", DEFAULT_ITEM_B);
+        properties.setProperty("allowInventorySwap", "true");
+        return properties;
+    }
+
+    private static ItemSwapConfig fromProperties(Properties properties) {
+        return new ItemSwapConfig(
+                properties.getProperty("itemA", DEFAULT_ITEM_A).trim(),
+                properties.getProperty("itemB", DEFAULT_ITEM_B).trim(),
+                Boolean.parseBoolean(properties.getProperty("allowInventorySwap", "true").trim())
+        );
+    }
+
+    private static void writeDefaults(Path path, Properties properties) {
+        try {
+            Files.createDirectories(path.getParent());
+            try (Writer writer = Files.newBufferedWriter(path)) {
+                properties.store(writer, "Untitled item A/B swap settings");
+            }
+        } catch (IOException ignored) {
+        }
+    }
+
+    private static Optional<Item> resolveItem(String rawId) {
+        Identifier id = Identifier.tryParse(rawId);
+        if (id == null || !Registries.ITEM.containsId(id)) {
+            return Optional.empty();
+        }
+
+        return Optional.of(Registries.ITEM.get(id));
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
new file mode 100644
index 0000000000000000000000000000000000000000..1627b1c02312751fdcee2e7b2abe108fb036a763
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
@@ -0,0 +1,107 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.client.MinecraftClient;
+import net.minecraft.client.network.ClientPlayerEntity;
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+import net.minecraft.screen.slot.SlotActionType;
+
+import java.util.Optional;
+import java.util.OptionalInt;
+
+public final class ItemSwapController {
+    private static final int PLAYER_SCREEN_HOTBAR_SLOT_OFFSET = 36;
+
+    private final ItemSwapConfig config;
+    private final InventoryItemFinder finder;
+
+    public ItemSwapController(ItemSwapConfig config) {
+        this.config = config;
+        this.finder = new InventoryItemFinder();
+    }
+
+    public boolean toggle(MinecraftClient client) {
+        if (client.player == null || client.interactionManager == null || client.currentScreen != null) {
+            return false;
+        }
+
+        Optional<Item> itemA = config.itemA();
+        Optional<Item> itemB = config.itemB();
+        if (itemA.isEmpty() || itemB.isEmpty()) {
+            return false;
+        }
+
+        ClientPlayerEntity player = client.player;
+        PlayerInventory inventory = player.getInventory();
+        Item target = targetItem(inventory.getStack(inventory.selectedSlot), itemA.get(), itemB.get());
+        OptionalInt targetSlot = finder.find(inventory, target);
+        if (targetSlot.isEmpty()) {
+            return false;
+        }
+
+        return switchToSlot(client, player, inventory, targetSlot.getAsInt(), itemA.get(), itemB.get());
+    }
+
+    private Item targetItem(ItemStack selectedStack, Item itemA, Item itemB) {
+        if (selectedStack.isOf(itemA)) {
+            return itemB;
+        }
+        if (selectedStack.isOf(itemB)) {
+            return itemA;
+        }
+
+        return itemA;
+    }
+
+    private boolean switchToSlot(
+            MinecraftClient client,
+            ClientPlayerEntity player,
+            PlayerInventory inventory,
+            int inventorySlot,
+            Item itemA,
+            Item itemB
+    ) {
+        if (inventorySlot < InventoryItemFinder.HOTBAR_SIZE) {
+            selectHotbarSlot(client, inventory, inventorySlot);
+            return true;
+        }
+
+        if (!config.allowInventorySwap()) {
+            return false;
+        }
+
+        OptionalInt destinationSlot = safeHotbarDestination(inventory, itemA, itemB);
+        if (destinationSlot.isEmpty()) {
+            return false;
+        }
+
+        client.interactionManager.clickSlot(
+                player.playerScreenHandler.syncId,
+                inventorySlot,
+                destinationSlot.getAsInt(),
+                SlotActionType.SWAP,
+                player
+        );
+        selectHotbarSlot(client, inventory, destinationSlot.getAsInt());
+        return true;
+    }
+
+    private OptionalInt safeHotbarDestination(PlayerInventory inventory, Item itemA, Item itemB) {
+        ItemStack selectedStack = inventory.getStack(inventory.selectedSlot);
+        if (selectedStack.isEmpty() || selectedStack.isOf(itemA) || selectedStack.isOf(itemB)) {
+            return OptionalInt.of(inventory.selectedSlot);
+        }
+
+        return finder.findEmptyHotbarSlot(inventory);
+    }
+
+    private void selectHotbarSlot(MinecraftClient client, PlayerInventory inventory, int slot) {
+        if (inventory.selectedSlot == slot) {
+            return;
+        }
+
+        inventory.selectedSlot = slot;
+        client.interactionManager.syncSelectedSlot();
+    }
+}

exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$javac='"'C:\\Program Files\\JetBrains\\IntelliJ IDEA 2026.1.1\\jbr\\bin\\javac.exe'
"'$out='"'build\\codex-compile-check'
New-Item -ItemType Directory -Force "'$out | Out-Null
$jars = @()
$jars += Get-ChildItem -Recurse -File .gradle'"\\loom-cache\\minecraftMaven -Filter '*.jar' | Where-Object { "'$_.Name -notmatch '"'backup|sources' } | Select-Object -ExpandProperty FullName
"'$jars += Get-ChildItem -Recurse -File build'"\\loom-cache\\remapped_working -Filter '*.jar' | Where-Object { "'$_.Name -notmatch '"'sources' } | Select-Object -ExpandProperty FullName
"'$jars += Get-ChildItem -Recurse -File "$env:USERPROFILE'"\\.gradle\\caches\\modules-2\\files-2.1\\net.fabricmc\\fabric-loader\\0.19.2\" -Filter 'fabric-loader-0.19.2.jar' | Select-Object -ExpandProperty FullName
"'$jars += Get-ChildItem -Recurse -File "$env:USERPROFILE'"\\.gradle\\caches\\modules-2\\files-2.1\\org.lwjgl\" -Filter '*.jar' -ErrorAction SilentlyContinue | Where-Object { "'$_.Name -notmatch '"'sources|javadoc|natives' } | Select-Object -ExpandProperty FullName
"'$jars += Get-ChildItem -Recurse -File "$env:USERPROFILE'"\\.gradle\\caches\\modules-2\\files-2.1\\org.slf4j\" -Filter '*.jar' -ErrorAction SilentlyContinue | Where-Object { "'$_.Name -notmatch '"'sources|javadoc' } | Select-Object -ExpandProperty FullName
"'$cp = ($jars | Select-Object -Unique) -join '"';'
"'$sources = Get-ChildItem -Recurse -File src'"\\main\\java,src\\client\\java -Filter '*.java' | Select-Object -ExpandProperty FullName
"'$argsFile = Join-Path $out '"'javac.args'
"'$lines = @('"'-encoding','UTF-8','--release','21','-classpath',\""'`"$cp`"",'"'-d',\""'`"$((Resolve-Path $out).Path)`"") + ($sources | ForEach-Object { "`"$_`"" })
Set-Content -Path $argsFile -Value $lines -Encoding ASCII
& $javac "@$argsFile"' in C:\Users\sutec\IdeaProjects\untitled
2026-05-11T07:24:01.736144Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 1 seconds
Output:
エラー: ファイルが見つかりません: C:UserssutecIdeaProjectsuntitledsrcmainjavaorgahahauntitledUntitled.java
使用方法: javac <options> <source files>
使用可能なオプションのリストについては、--helpを使用します

 exited 1 in 962ms:
エラー: ファイルが見つかりません: C:UserssutecIdeaProjectsuntitledsrcmainjavaorgahahauntitledUntitled.java
使用方法: javac <options> <source files>
使用可能なオプションのリストについては、--helpを使用します

diff --git a/src/client/java/org/ahaha/untitled/client/UntitledClient.java b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
index 96f2fa5caf72eb792f660392a6ef7bab77503eb9..760523f1a4590b451ae9a35dd4bed823b44da6a1
--- a/src/client/java/org/ahaha/untitled/client/UntitledClient.java
+++ b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
@@ -1,10 +1,32 @@
 package org.ahaha.untitled.client;
 
 import net.fabricmc.api.ClientModInitializer;
+import net.fabricmc.fabric.api.client.event.lifecycle.v1.ClientTickEvents;
+import net.fabricmc.fabric.api.client.keybinding.v1.KeyBindingHelper;
+import net.minecraft.client.option.KeyBinding;
+import net.minecraft.client.util.InputUtil;
+import org.ahaha.untitled.client.itemswap.ItemSwapConfig;
+import org.ahaha.untitled.client.itemswap.ItemSwapController;
+import org.lwjgl.glfw.GLFW;
 
 public class UntitledClient implements ClientModInitializer {
+    private static final String CATEGORY = "key.category.untitled";
+    private static final String SWAP_KEY = "key.untitled.swap_item_a_b";
 
     @Override
     public void onInitializeClient() {
+        ItemSwapController controller = new ItemSwapController(ItemSwapConfig.load());
+        KeyBinding swapKey = KeyBindingHelper.registerKeyBinding(new KeyBinding(
+                SWAP_KEY,
+                InputUtil.Type.KEYSYM,
+                GLFW.GLFW_KEY_UNKNOWN,
+                CATEGORY
+        ));
+
+        ClientTickEvents.END_CLIENT_TICK.register(client -> {
+            while (swapKey.wasPressed()) {
+                controller.toggle(client);
+            }
+        });
     }
 }
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
new file mode 100644
index 0000000000000000000000000000000000000000..3214363102de082962b3401df416e73144012217
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
@@ -0,0 +1,44 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+
+import java.util.OptionalInt;
+
+public final class InventoryItemFinder {
+    public static final int HOTBAR_SIZE = 9;
+    public static final int MAIN_INVENTORY_SIZE = 36;
+
+    public OptionalInt find(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < MAIN_INVENTORY_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findInHotbar(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findEmptyHotbarSlot(PlayerInventory inventory) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            if (inventory.getStack(slot).isEmpty()) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
new file mode 100644
index 0000000000000000000000000000000000000000..e3f80e5c19e04354bc7646bb6acd358895963c32
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
@@ -0,0 +1,94 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.fabricmc.loader.api.FabricLoader;
+import net.minecraft.item.Item;
+import net.minecraft.registry.Registries;
+import net.minecraft.util.Identifier;
+
+import java.io.IOException;
+import java.io.Reader;
+import java.io.Writer;
+import java.nio.file.Files;
+import java.nio.file.Path;
+import java.util.Optional;
+import java.util.Properties;
+
+public final class ItemSwapConfig {
+    private static final String CONFIG_FILE_NAME = "untitled-item-swap.properties";
+    private static final String DEFAULT_ITEM_A = "minecraft:golden_apple";
+    private static final String DEFAULT_ITEM_B = "minecraft:totem_of_undying";
+
+    private final String itemAId;
+    private final String itemBId;
+    private final boolean allowInventorySwap;
+
+    private ItemSwapConfig(String itemAId, String itemBId, boolean allowInventorySwap) {
+        this.itemAId = itemAId;
+        this.itemBId = itemBId;
+        this.allowInventorySwap = allowInventorySwap;
+    }
+
+    public static ItemSwapConfig load() {
+        Path path = FabricLoader.getInstance().getConfigDir().resolve(CONFIG_FILE_NAME);
+        Properties properties = defaultProperties();
+
+        if (Files.exists(path)) {
+            try (Reader reader = Files.newBufferedReader(path)) {
+                properties.load(reader);
+            } catch (IOException ignored) {
+                return fromProperties(properties);
+            }
+        } else {
+            writeDefaults(path, properties);
+        }
+
+        return fromProperties(properties);
+    }
+
+    public Optional<Item> itemA() {
+        return resolveItem(itemAId);
+    }
+
+    public Optional<Item> itemB() {
+        return resolveItem(itemBId);
+    }
+
+    public boolean allowInventorySwap() {
+        return allowInventorySwap;
+    }
+
+    private static Properties defaultProperties() {
+        Properties properties = new Properties();
+        properties.setProperty("itemA", DEFAULT_ITEM_A);
+        properties.setProperty("itemB", DEFAULT_ITEM_B);
+        properties.setProperty("allowInventorySwap", "true");
+        return properties;
+    }
+
+    private static ItemSwapConfig fromProperties(Properties properties) {
+        return new ItemSwapConfig(
+                properties.getProperty("itemA", DEFAULT_ITEM_A).trim(),
+                properties.getProperty("itemB", DEFAULT_ITEM_B).trim(),
+                Boolean.parseBoolean(properties.getProperty("allowInventorySwap", "true").trim())
+        );
+    }
+
+    private static void writeDefaults(Path path, Properties properties) {
+        try {
+            Files.createDirectories(path.getParent());
+            try (Writer writer = Files.newBufferedWriter(path)) {
+                properties.store(writer, "Untitled item A/B swap settings");
+            }
+        } catch (IOException ignored) {
+        }
+    }
+
+    private static Optional<Item> resolveItem(String rawId) {
+        Identifier id = Identifier.tryParse(rawId);
+        if (id == null || !Registries.ITEM.containsId(id)) {
+            return Optional.empty();
+        }
+
+        return Optional.of(Registries.ITEM.get(id));
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
new file mode 100644
index 0000000000000000000000000000000000000000..1627b1c02312751fdcee2e7b2abe108fb036a763
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
@@ -0,0 +1,107 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.client.MinecraftClient;
+import net.minecraft.client.network.ClientPlayerEntity;
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+import net.minecraft.screen.slot.SlotActionType;
+
+import java.util.Optional;
+import java.util.OptionalInt;
+
+public final class ItemSwapController {
+    private static final int PLAYER_SCREEN_HOTBAR_SLOT_OFFSET = 36;
+
+    private final ItemSwapConfig config;
+    private final InventoryItemFinder finder;
+
+    public ItemSwapController(ItemSwapConfig config) {
+        this.config = config;
+        this.finder = new InventoryItemFinder();
+    }
+
+    public boolean toggle(MinecraftClient client) {
+        if (client.player == null || client.interactionManager == null || client.currentScreen != null) {
+            return false;
+        }
+
+        Optional<Item> itemA = config.itemA();
+        Optional<Item> itemB = config.itemB();
+        if (itemA.isEmpty() || itemB.isEmpty()) {
+            return false;
+        }
+
+        ClientPlayerEntity player = client.player;
+        PlayerInventory inventory = player.getInventory();
+        Item target = targetItem(inventory.getStack(inventory.selectedSlot), itemA.get(), itemB.get());
+        OptionalInt targetSlot = finder.find(inventory, target);
+        if (targetSlot.isEmpty()) {
+            return false;
+        }
+
+        return switchToSlot(client, player, inventory, targetSlot.getAsInt(), itemA.get(), itemB.get());
+    }
+
+    private Item targetItem(ItemStack selectedStack, Item itemA, Item itemB) {
+        if (selectedStack.isOf(itemA)) {
+            return itemB;
+        }
+        if (selectedStack.isOf(itemB)) {
+            return itemA;
+        }
+
+        return itemA;
+    }
+
+    private boolean switchToSlot(
+            MinecraftClient client,
+            ClientPlayerEntity player,
+            PlayerInventory inventory,
+            int inventorySlot,
+            Item itemA,
+            Item itemB
+    ) {
+        if (inventorySlot < InventoryItemFinder.HOTBAR_SIZE) {
+            selectHotbarSlot(client, inventory, inventorySlot);
+            return true;
+        }
+
+        if (!config.allowInventorySwap()) {
+            return false;
+        }
+
+        OptionalInt destinationSlot = safeHotbarDestination(inventory, itemA, itemB);
+        if (destinationSlot.isEmpty()) {
+            return false;
+        }
+
+        client.interactionManager.clickSlot(
+                player.playerScreenHandler.syncId,
+                inventorySlot,
+                destinationSlot.getAsInt(),
+                SlotActionType.SWAP,
+                player
+        );
+        selectHotbarSlot(client, inventory, destinationSlot.getAsInt());
+        return true;
+    }
+
+    private OptionalInt safeHotbarDestination(PlayerInventory inventory, Item itemA, Item itemB) {
+        ItemStack selectedStack = inventory.getStack(inventory.selectedSlot);
+        if (selectedStack.isEmpty() || selectedStack.isOf(itemA) || selectedStack.isOf(itemB)) {
+            return OptionalInt.of(inventory.selectedSlot);
+        }
+
+        return finder.findEmptyHotbarSlot(inventory);
+    }
+
+    private void selectHotbarSlot(MinecraftClient client, PlayerInventory inventory, int slot) {
+        if (inventory.selectedSlot == slot) {
+            return;
+        }
+
+        inventory.selectedSlot = slot;
+        client.interactionManager.syncSelectedSlot();
+    }
+}

exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$javac='"'C:\\Program Files\\JetBrains\\IntelliJ IDEA 2026.1.1\\jbr\\bin\\javac.exe'
"'$out='"'build\\codex-compile-check'
New-Item -ItemType Directory -Force "'$out | Out-Null
$jars = @()
$jars += Get-ChildItem -Recurse -File .gradle'"\\loom-cache\\minecraftMaven -Filter '*.jar' | Where-Object { "'$_.Name -notmatch '"'backup|sources' } | Select-Object -ExpandProperty FullName
"'$jars += Get-ChildItem -Recurse -File build'"\\loom-cache\\remapped_working -Filter '*.jar' | Where-Object { "'$_.Name -notmatch '"'sources' } | Select-Object -ExpandProperty FullName
"'$jars += Get-ChildItem -Recurse -File "$env:USERPROFILE'"\\.gradle\\caches\\modules-2\\files-2.1\\net.fabricmc\\fabric-loader\\0.19.2\" -Filter 'fabric-loader-0.19.2.jar' | Select-Object -ExpandProperty FullName
"'$jars += Get-ChildItem -Recurse -File "$env:USERPROFILE'"\\.gradle\\caches\\modules-2\\files-2.1\\org.lwjgl\" -Filter '*.jar' -ErrorAction SilentlyContinue | Where-Object { "'$_.Name -notmatch '"'sources|javadoc|natives' } | Select-Object -ExpandProperty FullName
"'$jars += Get-ChildItem -Recurse -File "$env:USERPROFILE'"\\.gradle\\caches\\modules-2\\files-2.1\\org.slf4j\" -Filter '*.jar' -ErrorAction SilentlyContinue | Where-Object { "'$_.Name -notmatch '"'sources|javadoc' } | Select-Object -ExpandProperty FullName
"'$cp = (($jars | Select-Object -Unique) -join '"';').Replace('\\','/')
"'$sources = Get-ChildItem -Recurse -File src'"\\main\\java,src\\client\\java -Filter '*.java' | ForEach-Object { "'$_.FullName.Replace('"'\\','/') }
"'$argsFile = Join-Path $out '"'javac.args'
"'$dest = (Resolve-Path $out).Path.Replace('"'\\','/')
"'$lines = @('"'-encoding','UTF-8','--release','21','-classpath',\""'$cp",'"'-d',\""'$dest") + $sources
Set-Content -Path $argsFile -Value $lines -Encoding ASCII
& $javac "@$argsFile"' in C:\Users\sutec\IdeaProjects\untitled
2026-05-11T07:24:17.810583Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 2.6 seconds
Output:
C:\Users\sutec\IdeaProjects\untitled\src\client\java\org\ahaha\untitled\client\itemswap\ItemSwapConfig.java:88: エラー: Keyableにアクセスできません
        if (id == null || !Registries.ITEM.containsId(id)) {
                                          ^
  com.mojang.serialization.Keyableのクラス・ファイルが見つかりません
C:\Users\sutec\IdeaProjects\untitled\src\client\java\org\ahaha\untitled\client\itemswap\ItemSwapController.java:105: エラー: syncSelectedSlot()はClientPlayerInteractionManagerでprivateアクセスされます
        client.interactionManager.syncSelectedSlot();
                                 ^
エラー2個
コンパイラで例外が発生しました(25.0.2)。バグ・データベース(https://bugs.java.com)で重複がないかをご確認のうえ、Javaのバグ・レポート・ページ(https://bugreport.java.com)から、Javaコンパイラに対するバグの登録をお願いいたします。レポートには、該当のプログラム、次の診断内容、およびJavaコンパイラに渡されたパラメータをご入力ください。ご協力ありがとうございます。
java.nio.file.AccessDeniedException: C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-rendering-v0-1.1.72+df3654b319.jar
	at java.base/sun.nio.fs.WindowsException.translateToIOException(WindowsException.java:91)
	at java.base/sun.nio.fs.WindowsException.rethrowAsIOException(WindowsException.java:98)
	at java.base/sun.nio.fs.WindowsLinkSupport.getRealPath(WindowsLinkSupport.java:280)
	at java.base/sun.nio.fs.WindowsPath.toRealPath(WindowsPath.java:903)
	at java.base/sun.nio.fs.WindowsPath.toRealPath(WindowsPath.java:42)
	at jdk.zipfs/jdk.nio.zipfs.ZipFileSystemProvider.removeFileSystem(ZipFileSystemProvider.java:319)
	at jdk.zipfs/jdk.nio.zipfs.ZipFileSystem.close(ZipFileSystem.java:550)
	at jdk.compiler/com.sun.tools.javac.file.JavacFileManager$ArchiveContainer.close(JavacFileManager.java:667)
	at jdk.compiler/com.sun.tools.javac.file.JavacFileManager.close(JavacFileManager.java:745)
	at jdk.compiler/com.sun.tools.javac.main.DelegatingJavaFileManager.close(DelegatingJavaFileManager.java:151)
	at jdk.compiler/com.sun.tools.javac.main.Main.compile(Main.java:182)
	at jdk.compiler/com.sun.tools.javac.Main.compile(Main.java:66)
	at jdk.compiler/com.sun.tools.javac.Main.main(Main.java:52)

 exited 1 in 2588ms:
C:\Users\sutec\IdeaProjects\untitled\src\client\java\org\ahaha\untitled\client\itemswap\ItemSwapConfig.java:88: エラー: Keyableにアクセスできません
        if (id == null || !Registries.ITEM.containsId(id)) {
                                          ^
  com.mojang.serialization.Keyableのクラス・ファイルが見つかりません
C:\Users\sutec\IdeaProjects\untitled\src\client\java\org\ahaha\untitled\client\itemswap\ItemSwapController.java:105: エラー: syncSelectedSlot()はClientPlayerInteractionManagerでprivateアクセスされます
        client.interactionManager.syncSelectedSlot();
                                 ^
エラー2個
コンパイラで例外が発生しました(25.0.2)。バグ・データベース(https://bugs.java.com)で重複がないかをご確認のうえ、Javaのバグ・レポート・ページ(https://bugreport.java.com)から、Javaコンパイラに対するバグの登録をお願いいたします。レポートには、該当のプログラム、次の診断内容、およびJavaコンパイラに渡されたパラメータをご入力ください。ご協力ありがとうございます。
java.nio.file.AccessDeniedException: C:\Users\sutec\IdeaProjects\untitled\build\loom-cache\remapped_working\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2.net.fabricmc.fabric-api-fabric-rendering-v0-1.1.72+df3654b319.jar
	at java.base/sun.nio.fs.WindowsException.translateToIOException(WindowsException.java:91)
	at java.base/sun.nio.fs.WindowsException.rethrowAsIOException(WindowsException.java:98)
	at java.base/sun.nio.fs.WindowsLinkSupport.getRealPath(WindowsLinkSupport.java:280)
	at java.base/sun.nio.fs.WindowsPath.toRealPath(WindowsPath.java:903)
	at java.base/sun.nio.fs.WindowsPath.toRealPath(WindowsPath.java:42)
	at jdk.zipfs/jdk.nio.zipfs.ZipFileSystemProvider.removeFileSystem(ZipFileSystemProvider.java:319)
	at jdk.zipfs/jdk.nio.zipfs.ZipFileSystem.close(ZipFileSystem.java:550)
	at jdk.compiler/com.sun.tools.javac.file.JavacFileManager$ArchiveContainer.close(JavacFileManager.java:667)
	at jdk.compiler/com.sun.tools.javac.file.JavacFileManager.close(JavacFileManager.java:745)
	at jdk.compiler/com.sun.tools.javac.main.DelegatingJavaFileManager.close(DelegatingJavaFileManager.java:151)
	at jdk.compiler/com.sun.tools.javac.main.Main.compile(Main.java:182)
	at jdk.compiler/com.sun.tools.javac.Main.compile(Main.java:66)
	at jdk.compiler/com.sun.tools.javac.Main.main(Main.java:52)

diff --git a/src/client/java/org/ahaha/untitled/client/UntitledClient.java b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
index 96f2fa5caf72eb792f660392a6ef7bab77503eb9..760523f1a4590b451ae9a35dd4bed823b44da6a1
--- a/src/client/java/org/ahaha/untitled/client/UntitledClient.java
+++ b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
@@ -1,10 +1,32 @@
 package org.ahaha.untitled.client;
 
 import net.fabricmc.api.ClientModInitializer;
+import net.fabricmc.fabric.api.client.event.lifecycle.v1.ClientTickEvents;
+import net.fabricmc.fabric.api.client.keybinding.v1.KeyBindingHelper;
+import net.minecraft.client.option.KeyBinding;
+import net.minecraft.client.util.InputUtil;
+import org.ahaha.untitled.client.itemswap.ItemSwapConfig;
+import org.ahaha.untitled.client.itemswap.ItemSwapController;
+import org.lwjgl.glfw.GLFW;
 
 public class UntitledClient implements ClientModInitializer {
+    private static final String CATEGORY = "key.category.untitled";
+    private static final String SWAP_KEY = "key.untitled.swap_item_a_b";
 
     @Override
     public void onInitializeClient() {
+        ItemSwapController controller = new ItemSwapController(ItemSwapConfig.load());
+        KeyBinding swapKey = KeyBindingHelper.registerKeyBinding(new KeyBinding(
+                SWAP_KEY,
+                InputUtil.Type.KEYSYM,
+                GLFW.GLFW_KEY_UNKNOWN,
+                CATEGORY
+        ));
+
+        ClientTickEvents.END_CLIENT_TICK.register(client -> {
+            while (swapKey.wasPressed()) {
+                controller.toggle(client);
+            }
+        });
     }
 }
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
new file mode 100644
index 0000000000000000000000000000000000000000..3214363102de082962b3401df416e73144012217
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
@@ -0,0 +1,44 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+
+import java.util.OptionalInt;
+
+public final class InventoryItemFinder {
+    public static final int HOTBAR_SIZE = 9;
+    public static final int MAIN_INVENTORY_SIZE = 36;
+
+    public OptionalInt find(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < MAIN_INVENTORY_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findInHotbar(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findEmptyHotbarSlot(PlayerInventory inventory) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            if (inventory.getStack(slot).isEmpty()) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
new file mode 100644
index 0000000000000000000000000000000000000000..e3f80e5c19e04354bc7646bb6acd358895963c32
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
@@ -0,0 +1,94 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.fabricmc.loader.api.FabricLoader;
+import net.minecraft.item.Item;
+import net.minecraft.registry.Registries;
+import net.minecraft.util.Identifier;
+
+import java.io.IOException;
+import java.io.Reader;
+import java.io.Writer;
+import java.nio.file.Files;
+import java.nio.file.Path;
+import java.util.Optional;
+import java.util.Properties;
+
+public final class ItemSwapConfig {
+    private static final String CONFIG_FILE_NAME = "untitled-item-swap.properties";
+    private static final String DEFAULT_ITEM_A = "minecraft:golden_apple";
+    private static final String DEFAULT_ITEM_B = "minecraft:totem_of_undying";
+
+    private final String itemAId;
+    private final String itemBId;
+    private final boolean allowInventorySwap;
+
+    private ItemSwapConfig(String itemAId, String itemBId, boolean allowInventorySwap) {
+        this.itemAId = itemAId;
+        this.itemBId = itemBId;
+        this.allowInventorySwap = allowInventorySwap;
+    }
+
+    public static ItemSwapConfig load() {
+        Path path = FabricLoader.getInstance().getConfigDir().resolve(CONFIG_FILE_NAME);
+        Properties properties = defaultProperties();
+
+        if (Files.exists(path)) {
+            try (Reader reader = Files.newBufferedReader(path)) {
+                properties.load(reader);
+            } catch (IOException ignored) {
+                return fromProperties(properties);
+            }
+        } else {
+            writeDefaults(path, properties);
+        }
+
+        return fromProperties(properties);
+    }
+
+    public Optional<Item> itemA() {
+        return resolveItem(itemAId);
+    }
+
+    public Optional<Item> itemB() {
+        return resolveItem(itemBId);
+    }
+
+    public boolean allowInventorySwap() {
+        return allowInventorySwap;
+    }
+
+    private static Properties defaultProperties() {
+        Properties properties = new Properties();
+        properties.setProperty("itemA", DEFAULT_ITEM_A);
+        properties.setProperty("itemB", DEFAULT_ITEM_B);
+        properties.setProperty("allowInventorySwap", "true");
+        return properties;
+    }
+
+    private static ItemSwapConfig fromProperties(Properties properties) {
+        return new ItemSwapConfig(
+                properties.getProperty("itemA", DEFAULT_ITEM_A).trim(),
+                properties.getProperty("itemB", DEFAULT_ITEM_B).trim(),
+                Boolean.parseBoolean(properties.getProperty("allowInventorySwap", "true").trim())
+        );
+    }
+
+    private static void writeDefaults(Path path, Properties properties) {
+        try {
+            Files.createDirectories(path.getParent());
+            try (Writer writer = Files.newBufferedWriter(path)) {
+                properties.store(writer, "Untitled item A/B swap settings");
+            }
+        } catch (IOException ignored) {
+        }
+    }
+
+    private static Optional<Item> resolveItem(String rawId) {
+        Identifier id = Identifier.tryParse(rawId);
+        if (id == null || !Registries.ITEM.containsId(id)) {
+            return Optional.empty();
+        }
+
+        return Optional.of(Registries.ITEM.get(id));
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
new file mode 100644
index 0000000000000000000000000000000000000000..1627b1c02312751fdcee2e7b2abe108fb036a763
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
@@ -0,0 +1,107 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.client.MinecraftClient;
+import net.minecraft.client.network.ClientPlayerEntity;
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+import net.minecraft.screen.slot.SlotActionType;
+
+import java.util.Optional;
+import java.util.OptionalInt;
+
+public final class ItemSwapController {
+    private static final int PLAYER_SCREEN_HOTBAR_SLOT_OFFSET = 36;
+
+    private final ItemSwapConfig config;
+    private final InventoryItemFinder finder;
+
+    public ItemSwapController(ItemSwapConfig config) {
+        this.config = config;
+        this.finder = new InventoryItemFinder();
+    }
+
+    public boolean toggle(MinecraftClient client) {
+        if (client.player == null || client.interactionManager == null || client.currentScreen != null) {
+            return false;
+        }
+
+        Optional<Item> itemA = config.itemA();
+        Optional<Item> itemB = config.itemB();
+        if (itemA.isEmpty() || itemB.isEmpty()) {
+            return false;
+        }
+
+        ClientPlayerEntity player = client.player;
+        PlayerInventory inventory = player.getInventory();
+        Item target = targetItem(inventory.getStack(inventory.selectedSlot), itemA.get(), itemB.get());
+        OptionalInt targetSlot = finder.find(inventory, target);
+        if (targetSlot.isEmpty()) {
+            return false;
+        }
+
+        return switchToSlot(client, player, inventory, targetSlot.getAsInt(), itemA.get(), itemB.get());
+    }
+
+    private Item targetItem(ItemStack selectedStack, Item itemA, Item itemB) {
+        if (selectedStack.isOf(itemA)) {
+            return itemB;
+        }
+        if (selectedStack.isOf(itemB)) {
+            return itemA;
+        }
+
+        return itemA;
+    }
+
+    private boolean switchToSlot(
+            MinecraftClient client,
+            ClientPlayerEntity player,
+            PlayerInventory inventory,
+            int inventorySlot,
+            Item itemA,
+            Item itemB
+    ) {
+        if (inventorySlot < InventoryItemFinder.HOTBAR_SIZE) {
+            selectHotbarSlot(client, inventory, inventorySlot);
+            return true;
+        }
+
+        if (!config.allowInventorySwap()) {
+            return false;
+        }
+
+        OptionalInt destinationSlot = safeHotbarDestination(inventory, itemA, itemB);
+        if (destinationSlot.isEmpty()) {
+            return false;
+        }
+
+        client.interactionManager.clickSlot(
+                player.playerScreenHandler.syncId,
+                inventorySlot,
+                destinationSlot.getAsInt(),
+                SlotActionType.SWAP,
+                player
+        );
+        selectHotbarSlot(client, inventory, destinationSlot.getAsInt());
+        return true;
+    }
+
+    private OptionalInt safeHotbarDestination(PlayerInventory inventory, Item itemA, Item itemB) {
+        ItemStack selectedStack = inventory.getStack(inventory.selectedSlot);
+        if (selectedStack.isEmpty() || selectedStack.isOf(itemA) || selectedStack.isOf(itemB)) {
+            return OptionalInt.of(inventory.selectedSlot);
+        }
+
+        return finder.findEmptyHotbarSlot(inventory);
+    }
+
+    private void selectHotbarSlot(MinecraftClient client, PlayerInventory inventory, int slot) {
+        if (inventory.selectedSlot == slot) {
+            return;
+        }
+
+        inventory.selectedSlot = slot;
+        client.interactionManager.syncSelectedSlot();
+    }
+}

codex
手元の簡易`javac`確認で、選択スロット同期メソッドがprivateで直接呼べないことが分かりました。公開API相当のC2Sパケット送信に差し替えて、同じ操作を通します。
apply patch
patch: completed
C:\Users\sutec\IdeaProjects\untitled\src\client\java\org\ahaha\untitled\client\itemswap\ItemSwapController.java
diff --git a/src/client/java/org/ahaha/untitled/client/UntitledClient.java b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
index 96f2fa5caf72eb792f660392a6ef7bab77503eb9..760523f1a4590b451ae9a35dd4bed823b44da6a1
--- a/src/client/java/org/ahaha/untitled/client/UntitledClient.java
+++ b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
@@ -1,10 +1,32 @@
 package org.ahaha.untitled.client;
 
 import net.fabricmc.api.ClientModInitializer;
+import net.fabricmc.fabric.api.client.event.lifecycle.v1.ClientTickEvents;
+import net.fabricmc.fabric.api.client.keybinding.v1.KeyBindingHelper;
+import net.minecraft.client.option.KeyBinding;
+import net.minecraft.client.util.InputUtil;
+import org.ahaha.untitled.client.itemswap.ItemSwapConfig;
+import org.ahaha.untitled.client.itemswap.ItemSwapController;
+import org.lwjgl.glfw.GLFW;
 
 public class UntitledClient implements ClientModInitializer {
+    private static final String CATEGORY = "key.category.untitled";
+    private static final String SWAP_KEY = "key.untitled.swap_item_a_b";
 
     @Override
     public void onInitializeClient() {
+        ItemSwapController controller = new ItemSwapController(ItemSwapConfig.load());
+        KeyBinding swapKey = KeyBindingHelper.registerKeyBinding(new KeyBinding(
+                SWAP_KEY,
+                InputUtil.Type.KEYSYM,
+                GLFW.GLFW_KEY_UNKNOWN,
+                CATEGORY
+        ));
+
+        ClientTickEvents.END_CLIENT_TICK.register(client -> {
+            while (swapKey.wasPressed()) {
+                controller.toggle(client);
+            }
+        });
     }
 }
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
new file mode 100644
index 0000000000000000000000000000000000000000..3214363102de082962b3401df416e73144012217
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
@@ -0,0 +1,44 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+
+import java.util.OptionalInt;
+
+public final class InventoryItemFinder {
+    public static final int HOTBAR_SIZE = 9;
+    public static final int MAIN_INVENTORY_SIZE = 36;
+
+    public OptionalInt find(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < MAIN_INVENTORY_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findInHotbar(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findEmptyHotbarSlot(PlayerInventory inventory) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            if (inventory.getStack(slot).isEmpty()) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
new file mode 100644
index 0000000000000000000000000000000000000000..e3f80e5c19e04354bc7646bb6acd358895963c32
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
@@ -0,0 +1,94 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.fabricmc.loader.api.FabricLoader;
+import net.minecraft.item.Item;
+import net.minecraft.registry.Registries;
+import net.minecraft.util.Identifier;
+
+import java.io.IOException;
+import java.io.Reader;
+import java.io.Writer;
+import java.nio.file.Files;
+import java.nio.file.Path;
+import java.util.Optional;
+import java.util.Properties;
+
+public final class ItemSwapConfig {
+    private static final String CONFIG_FILE_NAME = "untitled-item-swap.properties";
+    private static final String DEFAULT_ITEM_A = "minecraft:golden_apple";
+    private static final String DEFAULT_ITEM_B = "minecraft:totem_of_undying";
+
+    private final String itemAId;
+    private final String itemBId;
+    private final boolean allowInventorySwap;
+
+    private ItemSwapConfig(String itemAId, String itemBId, boolean allowInventorySwap) {
+        this.itemAId = itemAId;
+        this.itemBId = itemBId;
+        this.allowInventorySwap = allowInventorySwap;
+    }
+
+    public static ItemSwapConfig load() {
+        Path path = FabricLoader.getInstance().getConfigDir().resolve(CONFIG_FILE_NAME);
+        Properties properties = defaultProperties();
+
+        if (Files.exists(path)) {
+            try (Reader reader = Files.newBufferedReader(path)) {
+                properties.load(reader);
+            } catch (IOException ignored) {
+                return fromProperties(properties);
+            }
+        } else {
+            writeDefaults(path, properties);
+        }
+
+        return fromProperties(properties);
+    }
+
+    public Optional<Item> itemA() {
+        return resolveItem(itemAId);
+    }
+
+    public Optional<Item> itemB() {
+        return resolveItem(itemBId);
+    }
+
+    public boolean allowInventorySwap() {
+        return allowInventorySwap;
+    }
+
+    private static Properties defaultProperties() {
+        Properties properties = new Properties();
+        properties.setProperty("itemA", DEFAULT_ITEM_A);
+        properties.setProperty("itemB", DEFAULT_ITEM_B);
+        properties.setProperty("allowInventorySwap", "true");
+        return properties;
+    }
+
+    private static ItemSwapConfig fromProperties(Properties properties) {
+        return new ItemSwapConfig(
+                properties.getProperty("itemA", DEFAULT_ITEM_A).trim(),
+                properties.getProperty("itemB", DEFAULT_ITEM_B).trim(),
+                Boolean.parseBoolean(properties.getProperty("allowInventorySwap", "true").trim())
+        );
+    }
+
+    private static void writeDefaults(Path path, Properties properties) {
+        try {
+            Files.createDirectories(path.getParent());
+            try (Writer writer = Files.newBufferedWriter(path)) {
+                properties.store(writer, "Untitled item A/B swap settings");
+            }
+        } catch (IOException ignored) {
+        }
+    }
+
+    private static Optional<Item> resolveItem(String rawId) {
+        Identifier id = Identifier.tryParse(rawId);
+        if (id == null || !Registries.ITEM.containsId(id)) {
+            return Optional.empty();
+        }
+
+        return Optional.of(Registries.ITEM.get(id));
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
new file mode 100644
index 0000000000000000000000000000000000000000..bde53a2191b1a64929b61eb8a084bc5678172181
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
@@ -0,0 +1,108 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.client.MinecraftClient;
+import net.minecraft.client.network.ClientPlayerEntity;
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+import net.minecraft.network.packet.c2s.play.UpdateSelectedSlotC2SPacket;
+import net.minecraft.screen.slot.SlotActionType;
+
+import java.util.Optional;
+import java.util.OptionalInt;
+
+public final class ItemSwapController {
+    private static final int PLAYER_SCREEN_HOTBAR_SLOT_OFFSET = 36;
+
+    private final ItemSwapConfig config;
+    private final InventoryItemFinder finder;
+
+    public ItemSwapController(ItemSwapConfig config) {
+        this.config = config;
+        this.finder = new InventoryItemFinder();
+    }
+
+    public boolean toggle(MinecraftClient client) {
+        if (client.player == null || client.interactionManager == null || client.currentScreen != null) {
+            return false;
+        }
+
+        Optional<Item> itemA = config.itemA();
+        Optional<Item> itemB = config.itemB();
+        if (itemA.isEmpty() || itemB.isEmpty()) {
+            return false;
+        }
+
+        ClientPlayerEntity player = client.player;
+        PlayerInventory inventory = player.getInventory();
+        Item target = targetItem(inventory.getStack(inventory.selectedSlot), itemA.get(), itemB.get());
+        OptionalInt targetSlot = finder.find(inventory, target);
+        if (targetSlot.isEmpty()) {
+            return false;
+        }
+
+        return switchToSlot(client, player, inventory, targetSlot.getAsInt(), itemA.get(), itemB.get());
+    }
+
+    private Item targetItem(ItemStack selectedStack, Item itemA, Item itemB) {
+        if (selectedStack.isOf(itemA)) {
+            return itemB;
+        }
+        if (selectedStack.isOf(itemB)) {
+            return itemA;
+        }
+
+        return itemA;
+    }
+
+    private boolean switchToSlot(
+            MinecraftClient client,
+            ClientPlayerEntity player,
+            PlayerInventory inventory,
+            int inventorySlot,
+            Item itemA,
+            Item itemB
+    ) {
+        if (inventorySlot < InventoryItemFinder.HOTBAR_SIZE) {
+            selectHotbarSlot(client, inventory, inventorySlot);
+            return true;
+        }
+
+        if (!config.allowInventorySwap()) {
+            return false;
+        }
+
+        OptionalInt destinationSlot = safeHotbarDestination(inventory, itemA, itemB);
+        if (destinationSlot.isEmpty()) {
+            return false;
+        }
+
+        client.interactionManager.clickSlot(
+                player.playerScreenHandler.syncId,
+                inventorySlot,
+                destinationSlot.getAsInt(),
+                SlotActionType.SWAP,
+                player
+        );
+        selectHotbarSlot(client, inventory, destinationSlot.getAsInt());
+        return true;
+    }
+
+    private OptionalInt safeHotbarDestination(PlayerInventory inventory, Item itemA, Item itemB) {
+        ItemStack selectedStack = inventory.getStack(inventory.selectedSlot);
+        if (selectedStack.isEmpty() || selectedStack.isOf(itemA) || selectedStack.isOf(itemB)) {
+            return OptionalInt.of(inventory.selectedSlot);
+        }
+
+        return finder.findEmptyHotbarSlot(inventory);
+    }
+
+    private void selectHotbarSlot(MinecraftClient client, PlayerInventory inventory, int slot) {
+        if (inventory.selectedSlot == slot) {
+            return;
+        }
+
+        inventory.selectedSlot = slot;
+        client.getNetworkHandler().sendPacket(new UpdateSelectedSlotC2SPacket(slot));
+    }
+}

diff --git a/src/client/java/org/ahaha/untitled/client/UntitledClient.java b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
index 96f2fa5caf72eb792f660392a6ef7bab77503eb9..760523f1a4590b451ae9a35dd4bed823b44da6a1
--- a/src/client/java/org/ahaha/untitled/client/UntitledClient.java
+++ b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
@@ -1,10 +1,32 @@
 package org.ahaha.untitled.client;
 
 import net.fabricmc.api.ClientModInitializer;
+import net.fabricmc.fabric.api.client.event.lifecycle.v1.ClientTickEvents;
+import net.fabricmc.fabric.api.client.keybinding.v1.KeyBindingHelper;
+import net.minecraft.client.option.KeyBinding;
+import net.minecraft.client.util.InputUtil;
+import org.ahaha.untitled.client.itemswap.ItemSwapConfig;
+import org.ahaha.untitled.client.itemswap.ItemSwapController;
+import org.lwjgl.glfw.GLFW;
 
 public class UntitledClient implements ClientModInitializer {
+    private static final String CATEGORY = "key.category.untitled";
+    private static final String SWAP_KEY = "key.untitled.swap_item_a_b";
 
     @Override
     public void onInitializeClient() {
+        ItemSwapController controller = new ItemSwapController(ItemSwapConfig.load());
+        KeyBinding swapKey = KeyBindingHelper.registerKeyBinding(new KeyBinding(
+                SWAP_KEY,
+                InputUtil.Type.KEYSYM,
+                GLFW.GLFW_KEY_UNKNOWN,
+                CATEGORY
+        ));
+
+        ClientTickEvents.END_CLIENT_TICK.register(client -> {
+            while (swapKey.wasPressed()) {
+                controller.toggle(client);
+            }
+        });
     }
 }
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
new file mode 100644
index 0000000000000000000000000000000000000000..3214363102de082962b3401df416e73144012217
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
@@ -0,0 +1,44 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+
+import java.util.OptionalInt;
+
+public final class InventoryItemFinder {
+    public static final int HOTBAR_SIZE = 9;
+    public static final int MAIN_INVENTORY_SIZE = 36;
+
+    public OptionalInt find(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < MAIN_INVENTORY_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findInHotbar(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findEmptyHotbarSlot(PlayerInventory inventory) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            if (inventory.getStack(slot).isEmpty()) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
new file mode 100644
index 0000000000000000000000000000000000000000..e3f80e5c19e04354bc7646bb6acd358895963c32
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
@@ -0,0 +1,94 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.fabricmc.loader.api.FabricLoader;
+import net.minecraft.item.Item;
+import net.minecraft.registry.Registries;
+import net.minecraft.util.Identifier;
+
+import java.io.IOException;
+import java.io.Reader;
+import java.io.Writer;
+import java.nio.file.Files;
+import java.nio.file.Path;
+import java.util.Optional;
+import java.util.Properties;
+
+public final class ItemSwapConfig {
+    private static final String CONFIG_FILE_NAME = "untitled-item-swap.properties";
+    private static final String DEFAULT_ITEM_A = "minecraft:golden_apple";
+    private static final String DEFAULT_ITEM_B = "minecraft:totem_of_undying";
+
+    private final String itemAId;
+    private final String itemBId;
+    private final boolean allowInventorySwap;
+
+    private ItemSwapConfig(String itemAId, String itemBId, boolean allowInventorySwap) {
+        this.itemAId = itemAId;
+        this.itemBId = itemBId;
+        this.allowInventorySwap = allowInventorySwap;
+    }
+
+    public static ItemSwapConfig load() {
+        Path path = FabricLoader.getInstance().getConfigDir().resolve(CONFIG_FILE_NAME);
+        Properties properties = defaultProperties();
+
+        if (Files.exists(path)) {
+            try (Reader reader = Files.newBufferedReader(path)) {
+                properties.load(reader);
+            } catch (IOException ignored) {
+                return fromProperties(properties);
+            }
+        } else {
+            writeDefaults(path, properties);
+        }
+
+        return fromProperties(properties);
+    }
+
+    public Optional<Item> itemA() {
+        return resolveItem(itemAId);
+    }
+
+    public Optional<Item> itemB() {
+        return resolveItem(itemBId);
+    }
+
+    public boolean allowInventorySwap() {
+        return allowInventorySwap;
+    }
+
+    private static Properties defaultProperties() {
+        Properties properties = new Properties();
+        properties.setProperty("itemA", DEFAULT_ITEM_A);
+        properties.setProperty("itemB", DEFAULT_ITEM_B);
+        properties.setProperty("allowInventorySwap", "true");
+        return properties;
+    }
+
+    private static ItemSwapConfig fromProperties(Properties properties) {
+        return new ItemSwapConfig(
+                properties.getProperty("itemA", DEFAULT_ITEM_A).trim(),
+                properties.getProperty("itemB", DEFAULT_ITEM_B).trim(),
+                Boolean.parseBoolean(properties.getProperty("allowInventorySwap", "true").trim())
+        );
+    }
+
+    private static void writeDefaults(Path path, Properties properties) {
+        try {
+            Files.createDirectories(path.getParent());
+            try (Writer writer = Files.newBufferedWriter(path)) {
+                properties.store(writer, "Untitled item A/B swap settings");
+            }
+        } catch (IOException ignored) {
+        }
+    }
+
+    private static Optional<Item> resolveItem(String rawId) {
+        Identifier id = Identifier.tryParse(rawId);
+        if (id == null || !Registries.ITEM.containsId(id)) {
+            return Optional.empty();
+        }
+
+        return Optional.of(Registries.ITEM.get(id));
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
new file mode 100644
index 0000000000000000000000000000000000000000..bde53a2191b1a64929b61eb8a084bc5678172181
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
@@ -0,0 +1,108 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.client.MinecraftClient;
+import net.minecraft.client.network.ClientPlayerEntity;
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+import net.minecraft.network.packet.c2s.play.UpdateSelectedSlotC2SPacket;
+import net.minecraft.screen.slot.SlotActionType;
+
+import java.util.Optional;
+import java.util.OptionalInt;
+
+public final class ItemSwapController {
+    private static final int PLAYER_SCREEN_HOTBAR_SLOT_OFFSET = 36;
+
+    private final ItemSwapConfig config;
+    private final InventoryItemFinder finder;
+
+    public ItemSwapController(ItemSwapConfig config) {
+        this.config = config;
+        this.finder = new InventoryItemFinder();
+    }
+
+    public boolean toggle(MinecraftClient client) {
+        if (client.player == null || client.interactionManager == null || client.currentScreen != null) {
+            return false;
+        }
+
+        Optional<Item> itemA = config.itemA();
+        Optional<Item> itemB = config.itemB();
+        if (itemA.isEmpty() || itemB.isEmpty()) {
+            return false;
+        }
+
+        ClientPlayerEntity player = client.player;
+        PlayerInventory inventory = player.getInventory();
+        Item target = targetItem(inventory.getStack(inventory.selectedSlot), itemA.get(), itemB.get());
+        OptionalInt targetSlot = finder.find(inventory, target);
+        if (targetSlot.isEmpty()) {
+            return false;
+        }
+
+        return switchToSlot(client, player, inventory, targetSlot.getAsInt(), itemA.get(), itemB.get());
+    }
+
+    private Item targetItem(ItemStack selectedStack, Item itemA, Item itemB) {
+        if (selectedStack.isOf(itemA)) {
+            return itemB;
+        }
+        if (selectedStack.isOf(itemB)) {
+            return itemA;
+        }
+
+        return itemA;
+    }
+
+    private boolean switchToSlot(
+            MinecraftClient client,
+            ClientPlayerEntity player,
+            PlayerInventory inventory,
+            int inventorySlot,
+            Item itemA,
+            Item itemB
+    ) {
+        if (inventorySlot < InventoryItemFinder.HOTBAR_SIZE) {
+            selectHotbarSlot(client, inventory, inventorySlot);
+            return true;
+        }
+
+        if (!config.allowInventorySwap()) {
+            return false;
+        }
+
+        OptionalInt destinationSlot = safeHotbarDestination(inventory, itemA, itemB);
+        if (destinationSlot.isEmpty()) {
+            return false;
+        }
+
+        client.interactionManager.clickSlot(
+                player.playerScreenHandler.syncId,
+                inventorySlot,
+                destinationSlot.getAsInt(),
+                SlotActionType.SWAP,
+                player
+        );
+        selectHotbarSlot(client, inventory, destinationSlot.getAsInt());
+        return true;
+    }
+
+    private OptionalInt safeHotbarDestination(PlayerInventory inventory, Item itemA, Item itemB) {
+        ItemStack selectedStack = inventory.getStack(inventory.selectedSlot);
+        if (selectedStack.isEmpty() || selectedStack.isOf(itemA) || selectedStack.isOf(itemB)) {
+            return OptionalInt.of(inventory.selectedSlot);
+        }
+
+        return finder.findEmptyHotbarSlot(inventory);
+    }
+
+    private void selectHotbarSlot(MinecraftClient client, PlayerInventory inventory, int slot) {
+        if (inventory.selectedSlot == slot) {
+            return;
+        }
+
+        inventory.selectedSlot = slot;
+        client.getNetworkHandler().sendPacket(new UpdateSelectedSlotC2SPacket(slot));
+    }
+}

exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-ChildItem -Path "$env:USERPROFILE'"\\.gradle\\caches\\modules-2\\files-2.1\" -Recurse -File -Filter '*.jar' | Where-Object { "'$_.FullName -match '"'datafixer|brigadier|authlib|guava|gson|fastutil' -and "'$_.Name -notmatch '"'sources|javadoc' } | Select-Object -First 50 -ExpandProperty FullName" in C:\Users\sutec\IdeaProjects\untitled
 succeeded in 934ms:
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\com.google.code.gson\gson\2.10.1\b3add478d4382b78ea20b1671390a858002feb6c\gson-2.10.1.jar
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\com.google.guava\failureaccess\1.0.1\1dcf1de382a0bf95a3d8b0849546c88bac1292c9\failureaccess-1.0.1.jar
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\com.google.guava\failureaccess\1.0.2\c4a06a64e650562f30b7bf9aaec1bfed43aca12b\failureaccess-1.0.2.jar
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\com.google.guava\guava\32.1.2-jre\5e64ec7e056456bef3a4bc4c6fdaef71e8ab6318\guava-32.1.2-jre.jar
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\com.google.guava\guava\33.0.0-jre\161ba27964a62f241533807a46b8711b13c1d94b\guava-33.0.0-jre.jar
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\com.google.guava\listenablefuture\9999.0-empty-to-avoid-conflict-with-guava\b421526c5f297295adef1c886e5246c39d4ac629\listenablefuture-9999.0-empty-to-avoid-conflict-with-guava.jar
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\com.mojang\authlib\6.0.54\de8bc95660e1b2fe8793fd427a7a10dcec5b3ea7\authlib-6.0.54.jar
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\com.mojang\brigadier\1.3.10\d15b53a14cf20fdcaa98f731af5dda654452c010\brigadier-1.3.10.jar
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\com.mojang\datafixerupper\8.0.16\67d4de6d7f95d89bcf5862995fb854ebaec02a34\datafixerupper-8.0.16.jar
C:\Users\sutec\.gradle\caches\modules-2\files-2.1\it.unimi.dsi\fastutil\8.5.12\c24946d46824bd528054bface3231d2ecb7e95e8\fastutil-8.5.12.jar

diff --git a/src/client/java/org/ahaha/untitled/client/UntitledClient.java b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
index 96f2fa5caf72eb792f660392a6ef7bab77503eb9..760523f1a4590b451ae9a35dd4bed823b44da6a1
--- a/src/client/java/org/ahaha/untitled/client/UntitledClient.java
+++ b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
@@ -1,10 +1,32 @@
 package org.ahaha.untitled.client;
 
 import net.fabricmc.api.ClientModInitializer;
+import net.fabricmc.fabric.api.client.event.lifecycle.v1.ClientTickEvents;
+import net.fabricmc.fabric.api.client.keybinding.v1.KeyBindingHelper;
+import net.minecraft.client.option.KeyBinding;
+import net.minecraft.client.util.InputUtil;
+import org.ahaha.untitled.client.itemswap.ItemSwapConfig;
+import org.ahaha.untitled.client.itemswap.ItemSwapController;
+import org.lwjgl.glfw.GLFW;
 
 public class UntitledClient implements ClientModInitializer {
+    private static final String CATEGORY = "key.category.untitled";
+    private static final String SWAP_KEY = "key.untitled.swap_item_a_b";
 
     @Override
     public void onInitializeClient() {
+        ItemSwapController controller = new ItemSwapController(ItemSwapConfig.load());
+        KeyBinding swapKey = KeyBindingHelper.registerKeyBinding(new KeyBinding(
+                SWAP_KEY,
+                InputUtil.Type.KEYSYM,
+                GLFW.GLFW_KEY_UNKNOWN,
+                CATEGORY
+        ));
+
+        ClientTickEvents.END_CLIENT_TICK.register(client -> {
+            while (swapKey.wasPressed()) {
+                controller.toggle(client);
+            }
+        });
     }
 }
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
new file mode 100644
index 0000000000000000000000000000000000000000..3214363102de082962b3401df416e73144012217
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
@@ -0,0 +1,44 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+
+import java.util.OptionalInt;
+
+public final class InventoryItemFinder {
+    public static final int HOTBAR_SIZE = 9;
+    public static final int MAIN_INVENTORY_SIZE = 36;
+
+    public OptionalInt find(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < MAIN_INVENTORY_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findInHotbar(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findEmptyHotbarSlot(PlayerInventory inventory) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            if (inventory.getStack(slot).isEmpty()) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
new file mode 100644
index 0000000000000000000000000000000000000000..e3f80e5c19e04354bc7646bb6acd358895963c32
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
@@ -0,0 +1,94 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.fabricmc.loader.api.FabricLoader;
+import net.minecraft.item.Item;
+import net.minecraft.registry.Registries;
+import net.minecraft.util.Identifier;
+
+import java.io.IOException;
+import java.io.Reader;
+import java.io.Writer;
+import java.nio.file.Files;
+import java.nio.file.Path;
+import java.util.Optional;
+import java.util.Properties;
+
+public final class ItemSwapConfig {
+    private static final String CONFIG_FILE_NAME = "untitled-item-swap.properties";
+    private static final String DEFAULT_ITEM_A = "minecraft:golden_apple";
+    private static final String DEFAULT_ITEM_B = "minecraft:totem_of_undying";
+
+    private final String itemAId;
+    private final String itemBId;
+    private final boolean allowInventorySwap;
+
+    private ItemSwapConfig(String itemAId, String itemBId, boolean allowInventorySwap) {
+        this.itemAId = itemAId;
+        this.itemBId = itemBId;
+        this.allowInventorySwap = allowInventorySwap;
+    }
+
+    public static ItemSwapConfig load() {
+        Path path = FabricLoader.getInstance().getConfigDir().resolve(CONFIG_FILE_NAME);
+        Properties properties = defaultProperties();
+
+        if (Files.exists(path)) {
+            try (Reader reader = Files.newBufferedReader(path)) {
+                properties.load(reader);
+            } catch (IOException ignored) {
+                return fromProperties(properties);
+            }
+        } else {
+            writeDefaults(path, properties);
+        }
+
+        return fromProperties(properties);
+    }
+
+    public Optional<Item> itemA() {
+        return resolveItem(itemAId);
+    }
+
+    public Optional<Item> itemB() {
+        return resolveItem(itemBId);
+    }
+
+    public boolean allowInventorySwap() {
+        return allowInventorySwap;
+    }
+
+    private static Properties defaultProperties() {
+        Properties properties = new Properties();
+        properties.setProperty("itemA", DEFAULT_ITEM_A);
+        properties.setProperty("itemB", DEFAULT_ITEM_B);
+        properties.setProperty("allowInventorySwap", "true");
+        return properties;
+    }
+
+    private static ItemSwapConfig fromProperties(Properties properties) {
+        return new ItemSwapConfig(
+                properties.getProperty("itemA", DEFAULT_ITEM_A).trim(),
+                properties.getProperty("itemB", DEFAULT_ITEM_B).trim(),
+                Boolean.parseBoolean(properties.getProperty("allowInventorySwap", "true").trim())
+        );
+    }
+
+    private static void writeDefaults(Path path, Properties properties) {
+        try {
+            Files.createDirectories(path.getParent());
+            try (Writer writer = Files.newBufferedWriter(path)) {
+                properties.store(writer, "Untitled item A/B swap settings");
+            }
+        } catch (IOException ignored) {
+        }
+    }
+
+    private static Optional<Item> resolveItem(String rawId) {
+        Identifier id = Identifier.tryParse(rawId);
+        if (id == null || !Registries.ITEM.containsId(id)) {
+            return Optional.empty();
+        }
+
+        return Optional.of(Registries.ITEM.get(id));
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
new file mode 100644
index 0000000000000000000000000000000000000000..bde53a2191b1a64929b61eb8a084bc5678172181
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
@@ -0,0 +1,108 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.client.MinecraftClient;
+import net.minecraft.client.network.ClientPlayerEntity;
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+import net.minecraft.network.packet.c2s.play.UpdateSelectedSlotC2SPacket;
+import net.minecraft.screen.slot.SlotActionType;
+
+import java.util.Optional;
+import java.util.OptionalInt;
+
+public final class ItemSwapController {
+    private static final int PLAYER_SCREEN_HOTBAR_SLOT_OFFSET = 36;
+
+    private final ItemSwapConfig config;
+    private final InventoryItemFinder finder;
+
+    public ItemSwapController(ItemSwapConfig config) {
+        this.config = config;
+        this.finder = new InventoryItemFinder();
+    }
+
+    public boolean toggle(MinecraftClient client) {
+        if (client.player == null || client.interactionManager == null || client.currentScreen != null) {
+            return false;
+        }
+
+        Optional<Item> itemA = config.itemA();
+        Optional<Item> itemB = config.itemB();
+        if (itemA.isEmpty() || itemB.isEmpty()) {
+            return false;
+        }
+
+        ClientPlayerEntity player = client.player;
+        PlayerInventory inventory = player.getInventory();
+        Item target = targetItem(inventory.getStack(inventory.selectedSlot), itemA.get(), itemB.get());
+        OptionalInt targetSlot = finder.find(inventory, target);
+        if (targetSlot.isEmpty()) {
+            return false;
+        }
+
+        return switchToSlot(client, player, inventory, targetSlot.getAsInt(), itemA.get(), itemB.get());
+    }
+
+    private Item targetItem(ItemStack selectedStack, Item itemA, Item itemB) {
+        if (selectedStack.isOf(itemA)) {
+            return itemB;
+        }
+        if (selectedStack.isOf(itemB)) {
+            return itemA;
+        }
+
+        return itemA;
+    }
+
+    private boolean switchToSlot(
+            MinecraftClient client,
+            ClientPlayerEntity player,
+            PlayerInventory inventory,
+            int inventorySlot,
+            Item itemA,
+            Item itemB
+    ) {
+        if (inventorySlot < InventoryItemFinder.HOTBAR_SIZE) {
+            selectHotbarSlot(client, inventory, inventorySlot);
+            return true;
+        }
+
+        if (!config.allowInventorySwap()) {
+            return false;
+        }
+
+        OptionalInt destinationSlot = safeHotbarDestination(inventory, itemA, itemB);
+        if (destinationSlot.isEmpty()) {
+            return false;
+        }
+
+        client.interactionManager.clickSlot(
+                player.playerScreenHandler.syncId,
+                inventorySlot,
+                destinationSlot.getAsInt(),
+                SlotActionType.SWAP,
+                player
+        );
+        selectHotbarSlot(client, inventory, destinationSlot.getAsInt());
+        return true;
+    }
+
+    private OptionalInt safeHotbarDestination(PlayerInventory inventory, Item itemA, Item itemB) {
+        ItemStack selectedStack = inventory.getStack(inventory.selectedSlot);
+        if (selectedStack.isEmpty() || selectedStack.isOf(itemA) || selectedStack.isOf(itemB)) {
+            return OptionalInt.of(inventory.selectedSlot);
+        }
+
+        return finder.findEmptyHotbarSlot(inventory);
+    }
+
+    private void selectHotbarSlot(MinecraftClient client, PlayerInventory inventory, int slot) {
+        if (inventory.selectedSlot == slot) {
+            return;
+        }
+
+        inventory.selectedSlot = slot;
+        client.getNetworkHandler().sendPacket(new UpdateSelectedSlotC2SPacket(slot));
+    }
+}

2026-05-11T07:24:56.179296Z ERROR codex_core::tools::router: error=`"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$javac='"'C:\\Program Files\\JetBrains\\IntelliJ IDEA 2026.1.1\\jbr\\bin\\javac.exe'
"'$out='"'build\\codex-compile-check'
Remove-Item -Recurse -Force "'$out -ErrorAction SilentlyContinue
New-Item -ItemType Directory -Force $out | Out-Null
$jars = @()
$jars += Get-ChildItem -Recurse -File .gradle'"\\loom-cache\\minecraftMaven -Filter '*.jar' | Where-Object { "'$_.Name -notmatch '"'backup|sources' } | Select-Object -ExpandProperty FullName
"'$jars += Get-ChildItem -Recurse -File .gradle'"\\loom-cache\\remapped_mods -Filter '*.jar' | Where-Object { "'$_.Name -notmatch '"'sources' } | Select-Object -ExpandProperty FullName
"'$jars += Get-ChildItem -Recurse -File "$env:USERPROFILE'"\\.gradle\\caches\\modules-2\\files-2.1\" -Filter '*.jar' | Where-Object { "'$_.Name -notmatch '"'sources|javadoc|natives' -and ("'$_.FullName -match '"'fabric-loader|org.lwjgl|org.slf4j|datafixerupper|brigadier|authlib|guava|gson|fastutil') } | Select-Object -ExpandProperty FullName
"'$cp = (($jars | Select-Object -Unique) -join '"';').Replace('\\','/')
"'$sources = Get-ChildItem -Recurse -File src'"\\main\\java,src\\client\\java -Filter '*.java' | ForEach-Object { "'$_.FullName.Replace('"'\\','/') }
"'$argsFile = Join-Path $out '"'javac.args'
"'$dest = (Resolve-Path $out).Path.Replace('"'\\','/')
"'$lines = @('"'-encoding','UTF-8','--release','21','-classpath',\""'$cp",'"'-d',\""'$dest") + $sources
Set-Content -Path $argsFile -Value $lines -Encoding ASCII
& $javac "@$argsFile"'` rejected: blocked by policy
exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$javac='"'C:\\Program Files\\JetBrains\\IntelliJ IDEA 2026.1.1\\jbr\\bin\\javac.exe'
"'$out='"'build\\codex-compile-check'
Remove-Item -Recurse -Force "'$out -ErrorAction SilentlyContinue
New-Item -ItemType Directory -Force $out | Out-Null
$jars = @()
$jars += Get-ChildItem -Recurse -File .gradle'"\\loom-cache\\minecraftMaven -Filter '*.jar' | Where-Object { "'$_.Name -notmatch '"'backup|sources' } | Select-Object -ExpandProperty FullName
"'$jars += Get-ChildItem -Recurse -File .gradle'"\\loom-cache\\remapped_mods -Filter '*.jar' | Where-Object { "'$_.Name -notmatch '"'sources' } | Select-Object -ExpandProperty FullName
"'$jars += Get-ChildItem -Recurse -File "$env:USERPROFILE'"\\.gradle\\caches\\modules-2\\files-2.1\" -Filter '*.jar' | Where-Object { "'$_.Name -notmatch '"'sources|javadoc|natives' -and ("'$_.FullName -match '"'fabric-loader|org.lwjgl|org.slf4j|datafixerupper|brigadier|authlib|guava|gson|fastutil') } | Select-Object -ExpandProperty FullName
"'$cp = (($jars | Select-Object -Unique) -join '"';').Replace('\\','/')
"'$sources = Get-ChildItem -Recurse -File src'"\\main\\java,src\\client\\java -Filter '*.java' | ForEach-Object { "'$_.FullName.Replace('"'\\','/') }
"'$argsFile = Join-Path $out '"'javac.args'
"'$dest = (Resolve-Path $out).Path.Replace('"'\\','/')
"'$lines = @('"'-encoding','UTF-8','--release','21','-classpath',\""'$cp",'"'-d',\""'$dest") + $sources
Set-Content -Path $argsFile -Value $lines -Encoding ASCII
& $javac "@$argsFile"' in C:\Users\sutec\IdeaProjects\untitled
 declined in 0ms:
`"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$javac='"'C:\\Program Files\\JetBrains\\IntelliJ IDEA 2026.1.1\\jbr\\bin\\javac.exe'
"'$out='"'build\\codex-compile-check'
Remove-Item -Recurse -Force "'$out -ErrorAction SilentlyContinue
New-Item -ItemType Directory -Force $out | Out-Null
$jars = @()
$jars += Get-ChildItem -Recurse -File .gradle'"\\loom-cache\\minecraftMaven -Filter '*.jar' | Where-Object { "'$_.Name -notmatch '"'backup|sources' } | Select-Object -ExpandProperty FullName
"'$jars += Get-ChildItem -Recurse -File .gradle'"\\loom-cache\\remapped_mods -Filter '*.jar' | Where-Object { "'$_.Name -notmatch '"'sources' } | Select-Object -ExpandProperty FullName
"'$jars += Get-ChildItem -Recurse -File "$env:USERPROFILE'"\\.gradle\\caches\\modules-2\\files-2.1\" -Filter '*.jar' | Where-Object { "'$_.Name -notmatch '"'sources|javadoc|natives' -and ("'$_.FullName -match '"'fabric-loader|org.lwjgl|org.slf4j|datafixerupper|brigadier|authlib|guava|gson|fastutil') } | Select-Object -ExpandProperty FullName
"'$cp = (($jars | Select-Object -Unique) -join '"';').Replace('\\','/')
"'$sources = Get-ChildItem -Recurse -File src'"\\main\\java,src\\client\\java -Filter '*.java' | ForEach-Object { "'$_.FullName.Replace('"'\\','/') }
"'$argsFile = Join-Path $out '"'javac.args'
"'$dest = (Resolve-Path $out).Path.Replace('"'\\','/')
"'$lines = @('"'-encoding','UTF-8','--release','21','-classpath',\""'$cp",'"'-d',\""'$dest") + $sources
Set-Content -Path $argsFile -Value $lines -Encoding ASCII
& $javac "@$argsFile"'` rejected: blocked by policy
diff --git a/src/client/java/org/ahaha/untitled/client/UntitledClient.java b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
index 96f2fa5caf72eb792f660392a6ef7bab77503eb9..760523f1a4590b451ae9a35dd4bed823b44da6a1
--- a/src/client/java/org/ahaha/untitled/client/UntitledClient.java
+++ b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
@@ -1,10 +1,32 @@
 package org.ahaha.untitled.client;
 
 import net.fabricmc.api.ClientModInitializer;
+import net.fabricmc.fabric.api.client.event.lifecycle.v1.ClientTickEvents;
+import net.fabricmc.fabric.api.client.keybinding.v1.KeyBindingHelper;
+import net.minecraft.client.option.KeyBinding;
+import net.minecraft.client.util.InputUtil;
+import org.ahaha.untitled.client.itemswap.ItemSwapConfig;
+import org.ahaha.untitled.client.itemswap.ItemSwapController;
+import org.lwjgl.glfw.GLFW;
 
 public class UntitledClient implements ClientModInitializer {
+    private static final String CATEGORY = "key.category.untitled";
+    private static final String SWAP_KEY = "key.untitled.swap_item_a_b";
 
     @Override
     public void onInitializeClient() {
+        ItemSwapController controller = new ItemSwapController(ItemSwapConfig.load());
+        KeyBinding swapKey = KeyBindingHelper.registerKeyBinding(new KeyBinding(
+                SWAP_KEY,
+                InputUtil.Type.KEYSYM,
+                GLFW.GLFW_KEY_UNKNOWN,
+                CATEGORY
+        ));
+
+        ClientTickEvents.END_CLIENT_TICK.register(client -> {
+            while (swapKey.wasPressed()) {
+                controller.toggle(client);
+            }
+        });
     }
 }
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
new file mode 100644
index 0000000000000000000000000000000000000000..3214363102de082962b3401df416e73144012217
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
@@ -0,0 +1,44 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+
+import java.util.OptionalInt;
+
+public final class InventoryItemFinder {
+    public static final int HOTBAR_SIZE = 9;
+    public static final int MAIN_INVENTORY_SIZE = 36;
+
+    public OptionalInt find(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < MAIN_INVENTORY_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findInHotbar(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findEmptyHotbarSlot(PlayerInventory inventory) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            if (inventory.getStack(slot).isEmpty()) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
new file mode 100644
index 0000000000000000000000000000000000000000..e3f80e5c19e04354bc7646bb6acd358895963c32
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
@@ -0,0 +1,94 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.fabricmc.loader.api.FabricLoader;
+import net.minecraft.item.Item;
+import net.minecraft.registry.Registries;
+import net.minecraft.util.Identifier;
+
+import java.io.IOException;
+import java.io.Reader;
+import java.io.Writer;
+import java.nio.file.Files;
+import java.nio.file.Path;
+import java.util.Optional;
+import java.util.Properties;
+
+public final class ItemSwapConfig {
+    private static final String CONFIG_FILE_NAME = "untitled-item-swap.properties";
+    private static final String DEFAULT_ITEM_A = "minecraft:golden_apple";
+    private static final String DEFAULT_ITEM_B = "minecraft:totem_of_undying";
+
+    private final String itemAId;
+    private final String itemBId;
+    private final boolean allowInventorySwap;
+
+    private ItemSwapConfig(String itemAId, String itemBId, boolean allowInventorySwap) {
+        this.itemAId = itemAId;
+        this.itemBId = itemBId;
+        this.allowInventorySwap = allowInventorySwap;
+    }
+
+    public static ItemSwapConfig load() {
+        Path path = FabricLoader.getInstance().getConfigDir().resolve(CONFIG_FILE_NAME);
+        Properties properties = defaultProperties();
+
+        if (Files.exists(path)) {
+            try (Reader reader = Files.newBufferedReader(path)) {
+                properties.load(reader);
+            } catch (IOException ignored) {
+                return fromProperties(properties);
+            }
+        } else {
+            writeDefaults(path, properties);
+        }
+
+        return fromProperties(properties);
+    }
+
+    public Optional<Item> itemA() {
+        return resolveItem(itemAId);
+    }
+
+    public Optional<Item> itemB() {
+        return resolveItem(itemBId);
+    }
+
+    public boolean allowInventorySwap() {
+        return allowInventorySwap;
+    }
+
+    private static Properties defaultProperties() {
+        Properties properties = new Properties();
+        properties.setProperty("itemA", DEFAULT_ITEM_A);
+        properties.setProperty("itemB", DEFAULT_ITEM_B);
+        properties.setProperty("allowInventorySwap", "true");
+        return properties;
+    }
+
+    private static ItemSwapConfig fromProperties(Properties properties) {
+        return new ItemSwapConfig(
+                properties.getProperty("itemA", DEFAULT_ITEM_A).trim(),
+                properties.getProperty("itemB", DEFAULT_ITEM_B).trim(),
+                Boolean.parseBoolean(properties.getProperty("allowInventorySwap", "true").trim())
+        );
+    }
+
+    private static void writeDefaults(Path path, Properties properties) {
+        try {
+            Files.createDirectories(path.getParent());
+            try (Writer writer = Files.newBufferedWriter(path)) {
+                properties.store(writer, "Untitled item A/B swap settings");
+            }
+        } catch (IOException ignored) {
+        }
+    }
+
+    private static Optional<Item> resolveItem(String rawId) {
+        Identifier id = Identifier.tryParse(rawId);
+        if (id == null || !Registries.ITEM.containsId(id)) {
+            return Optional.empty();
+        }
+
+        return Optional.of(Registries.ITEM.get(id));
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
new file mode 100644
index 0000000000000000000000000000000000000000..bde53a2191b1a64929b61eb8a084bc5678172181
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
@@ -0,0 +1,108 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.client.MinecraftClient;
+import net.minecraft.client.network.ClientPlayerEntity;
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+import net.minecraft.network.packet.c2s.play.UpdateSelectedSlotC2SPacket;
+import net.minecraft.screen.slot.SlotActionType;
+
+import java.util.Optional;
+import java.util.OptionalInt;
+
+public final class ItemSwapController {
+    private static final int PLAYER_SCREEN_HOTBAR_SLOT_OFFSET = 36;
+
+    private final ItemSwapConfig config;
+    private final InventoryItemFinder finder;
+
+    public ItemSwapController(ItemSwapConfig config) {
+        this.config = config;
+        this.finder = new InventoryItemFinder();
+    }
+
+    public boolean toggle(MinecraftClient client) {
+        if (client.player == null || client.interactionManager == null || client.currentScreen != null) {
+            return false;
+        }
+
+        Optional<Item> itemA = config.itemA();
+        Optional<Item> itemB = config.itemB();
+        if (itemA.isEmpty() || itemB.isEmpty()) {
+            return false;
+        }
+
+        ClientPlayerEntity player = client.player;
+        PlayerInventory inventory = player.getInventory();
+        Item target = targetItem(inventory.getStack(inventory.selectedSlot), itemA.get(), itemB.get());
+        OptionalInt targetSlot = finder.find(inventory, target);
+        if (targetSlot.isEmpty()) {
+            return false;
+        }
+
+        return switchToSlot(client, player, inventory, targetSlot.getAsInt(), itemA.get(), itemB.get());
+    }
+
+    private Item targetItem(ItemStack selectedStack, Item itemA, Item itemB) {
+        if (selectedStack.isOf(itemA)) {
+            return itemB;
+        }
+        if (selectedStack.isOf(itemB)) {
+            return itemA;
+        }
+
+        return itemA;
+    }
+
+    private boolean switchToSlot(
+            MinecraftClient client,
+            ClientPlayerEntity player,
+            PlayerInventory inventory,
+            int inventorySlot,
+            Item itemA,
+            Item itemB
+    ) {
+        if (inventorySlot < InventoryItemFinder.HOTBAR_SIZE) {
+            selectHotbarSlot(client, inventory, inventorySlot);
+            return true;
+        }
+
+        if (!config.allowInventorySwap()) {
+            return false;
+        }
+
+        OptionalInt destinationSlot = safeHotbarDestination(inventory, itemA, itemB);
+        if (destinationSlot.isEmpty()) {
+            return false;
+        }
+
+        client.interactionManager.clickSlot(
+                player.playerScreenHandler.syncId,
+                inventorySlot,
+                destinationSlot.getAsInt(),
+                SlotActionType.SWAP,
+                player
+        );
+        selectHotbarSlot(client, inventory, destinationSlot.getAsInt());
+        return true;
+    }
+
+    private OptionalInt safeHotbarDestination(PlayerInventory inventory, Item itemA, Item itemB) {
+        ItemStack selectedStack = inventory.getStack(inventory.selectedSlot);
+        if (selectedStack.isEmpty() || selectedStack.isOf(itemA) || selectedStack.isOf(itemB)) {
+            return OptionalInt.of(inventory.selectedSlot);
+        }
+
+        return finder.findEmptyHotbarSlot(inventory);
+    }
+
+    private void selectHotbarSlot(MinecraftClient client, PlayerInventory inventory, int slot) {
+        if (inventory.selectedSlot == slot) {
+            return;
+        }
+
+        inventory.selectedSlot = slot;
+        client.getNetworkHandler().sendPacket(new UpdateSelectedSlotC2SPacket(slot));
+    }
+}

exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$javac='"'C:\\Program Files\\JetBrains\\IntelliJ IDEA 2026.1.1\\jbr\\bin\\javac.exe'
"'$out='"'build\\codex-compile-check-2'
New-Item -ItemType Directory -Force "'$out | Out-Null
$jars = @()
$jars += Get-ChildItem -Recurse -File .gradle'"\\loom-cache\\minecraftMaven -Filter '*.jar' | Where-Object { "'$_.Name -notmatch '"'backup|sources' } | Select-Object -ExpandProperty FullName
"'$jars += Get-ChildItem -Recurse -File .gradle'"\\loom-cache\\remapped_mods -Filter '*.jar' | Where-Object { "'$_.Name -notmatch '"'sources' } | Select-Object -ExpandProperty FullName
"'$jars += Get-ChildItem -Recurse -File "$env:USERPROFILE'"\\.gradle\\caches\\modules-2\\files-2.1\" -Filter '*.jar' | Where-Object { "'$_.Name -notmatch '"'sources|javadoc|natives' -and ("'$_.FullName -match '"'fabric-loader|org.lwjgl|org.slf4j|datafixerupper|brigadier|authlib|guava|gson|fastutil') } | Select-Object -ExpandProperty FullName
"'$cp = (($jars | Select-Object -Unique) -join '"';').Replace('\\','/')
"'$sources = Get-ChildItem -Recurse -File src'"\\main\\java,src\\client\\java -Filter '*.java' | ForEach-Object { "'$_.FullName.Replace('"'\\','/') }
"'$argsFile = Join-Path $out '"'javac.args'
"'$dest = (Resolve-Path $out).Path.Replace('"'\\','/')
"'$lines = @('"'-encoding','UTF-8','--release','21','-classpath',\""'$cp",'"'-d',\""'$dest") + $sources
Set-Content -Path $argsFile -Value $lines -Encoding ASCII
& $javac "@$argsFile"' in C:\Users\sutec\IdeaProjects\untitled
 succeeded in 4043ms:
コンパイラで例外が発生しました(25.0.2)。バグ・データベース(https://bugs.java.com)で重複がないかをご確認のうえ、Javaのバグ・レポート・ページ(https://bugreport.java.com)から、Javaコンパイラに対するバグの登録をお願いいたします。レポートには、該当のプログラム、次の診断内容、およびJavaコンパイラに渡されたパラメータをご入力ください。ご協力ありがとうございます。
java.nio.file.AccessDeniedException: C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-item-group-api-v1-client\4.1.7+def88e3a19\fabric-item-group-api-v1-client-4.1.7+def88e3a19.jar
	at java.base/sun.nio.fs.WindowsException.translateToIOException(WindowsException.java:91)
	at java.base/sun.nio.fs.WindowsException.rethrowAsIOException(WindowsException.java:98)
	at java.base/sun.nio.fs.WindowsLinkSupport.getRealPath(WindowsLinkSupport.java:280)
	at java.base/sun.nio.fs.WindowsPath.toRealPath(WindowsPath.java:903)
	at java.base/sun.nio.fs.WindowsPath.toRealPath(WindowsPath.java:42)
	at jdk.zipfs/jdk.nio.zipfs.ZipFileSystemProvider.removeFileSystem(ZipFileSystemProvider.java:319)
	at jdk.zipfs/jdk.nio.zipfs.ZipFileSystem.close(ZipFileSystem.java:550)
	at jdk.compiler/com.sun.tools.javac.file.JavacFileManager$ArchiveContainer.close(JavacFileManager.java:667)
	at jdk.compiler/com.sun.tools.javac.file.JavacFileManager.close(JavacFileManager.java:745)
	at jdk.compiler/com.sun.tools.javac.main.DelegatingJavaFileManager.close(DelegatingJavaFileManager.java:151)
	at jdk.compiler/com.sun.tools.javac.main.Main.compile(Main.java:182)
	at jdk.compiler/com.sun.tools.javac.Main.compile(Main.java:66)
	at jdk.compiler/com.sun.tools.javac.Main.main(Main.java:52)

diff --git a/src/client/java/org/ahaha/untitled/client/UntitledClient.java b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
index 96f2fa5caf72eb792f660392a6ef7bab77503eb9..760523f1a4590b451ae9a35dd4bed823b44da6a1
--- a/src/client/java/org/ahaha/untitled/client/UntitledClient.java
+++ b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
@@ -1,10 +1,32 @@
 package org.ahaha.untitled.client;
 
 import net.fabricmc.api.ClientModInitializer;
+import net.fabricmc.fabric.api.client.event.lifecycle.v1.ClientTickEvents;
+import net.fabricmc.fabric.api.client.keybinding.v1.KeyBindingHelper;
+import net.minecraft.client.option.KeyBinding;
+import net.minecraft.client.util.InputUtil;
+import org.ahaha.untitled.client.itemswap.ItemSwapConfig;
+import org.ahaha.untitled.client.itemswap.ItemSwapController;
+import org.lwjgl.glfw.GLFW;
 
 public class UntitledClient implements ClientModInitializer {
+    private static final String CATEGORY = "key.category.untitled";
+    private static final String SWAP_KEY = "key.untitled.swap_item_a_b";
 
     @Override
     public void onInitializeClient() {
+        ItemSwapController controller = new ItemSwapController(ItemSwapConfig.load());
+        KeyBinding swapKey = KeyBindingHelper.registerKeyBinding(new KeyBinding(
+                SWAP_KEY,
+                InputUtil.Type.KEYSYM,
+                GLFW.GLFW_KEY_UNKNOWN,
+                CATEGORY
+        ));
+
+        ClientTickEvents.END_CLIENT_TICK.register(client -> {
+            while (swapKey.wasPressed()) {
+                controller.toggle(client);
+            }
+        });
     }
 }
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
new file mode 100644
index 0000000000000000000000000000000000000000..3214363102de082962b3401df416e73144012217
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
@@ -0,0 +1,44 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+
+import java.util.OptionalInt;
+
+public final class InventoryItemFinder {
+    public static final int HOTBAR_SIZE = 9;
+    public static final int MAIN_INVENTORY_SIZE = 36;
+
+    public OptionalInt find(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < MAIN_INVENTORY_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findInHotbar(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findEmptyHotbarSlot(PlayerInventory inventory) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            if (inventory.getStack(slot).isEmpty()) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
new file mode 100644
index 0000000000000000000000000000000000000000..e3f80e5c19e04354bc7646bb6acd358895963c32
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
@@ -0,0 +1,94 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.fabricmc.loader.api.FabricLoader;
+import net.minecraft.item.Item;
+import net.minecraft.registry.Registries;
+import net.minecraft.util.Identifier;
+
+import java.io.IOException;
+import java.io.Reader;
+import java.io.Writer;
+import java.nio.file.Files;
+import java.nio.file.Path;
+import java.util.Optional;
+import java.util.Properties;
+
+public final class ItemSwapConfig {
+    private static final String CONFIG_FILE_NAME = "untitled-item-swap.properties";
+    private static final String DEFAULT_ITEM_A = "minecraft:golden_apple";
+    private static final String DEFAULT_ITEM_B = "minecraft:totem_of_undying";
+
+    private final String itemAId;
+    private final String itemBId;
+    private final boolean allowInventorySwap;
+
+    private ItemSwapConfig(String itemAId, String itemBId, boolean allowInventorySwap) {
+        this.itemAId = itemAId;
+        this.itemBId = itemBId;
+        this.allowInventorySwap = allowInventorySwap;
+    }
+
+    public static ItemSwapConfig load() {
+        Path path = FabricLoader.getInstance().getConfigDir().resolve(CONFIG_FILE_NAME);
+        Properties properties = defaultProperties();
+
+        if (Files.exists(path)) {
+            try (Reader reader = Files.newBufferedReader(path)) {
+                properties.load(reader);
+            } catch (IOException ignored) {
+                return fromProperties(properties);
+            }
+        } else {
+            writeDefaults(path, properties);
+        }
+
+        return fromProperties(properties);
+    }
+
+    public Optional<Item> itemA() {
+        return resolveItem(itemAId);
+    }
+
+    public Optional<Item> itemB() {
+        return resolveItem(itemBId);
+    }
+
+    public boolean allowInventorySwap() {
+        return allowInventorySwap;
+    }
+
+    private static Properties defaultProperties() {
+        Properties properties = new Properties();
+        properties.setProperty("itemA", DEFAULT_ITEM_A);
+        properties.setProperty("itemB", DEFAULT_ITEM_B);
+        properties.setProperty("allowInventorySwap", "true");
+        return properties;
+    }
+
+    private static ItemSwapConfig fromProperties(Properties properties) {
+        return new ItemSwapConfig(
+                properties.getProperty("itemA", DEFAULT_ITEM_A).trim(),
+                properties.getProperty("itemB", DEFAULT_ITEM_B).trim(),
+                Boolean.parseBoolean(properties.getProperty("allowInventorySwap", "true").trim())
+        );
+    }
+
+    private static void writeDefaults(Path path, Properties properties) {
+        try {
+            Files.createDirectories(path.getParent());
+            try (Writer writer = Files.newBufferedWriter(path)) {
+                properties.store(writer, "Untitled item A/B swap settings");
+            }
+        } catch (IOException ignored) {
+        }
+    }
+
+    private static Optional<Item> resolveItem(String rawId) {
+        Identifier id = Identifier.tryParse(rawId);
+        if (id == null || !Registries.ITEM.containsId(id)) {
+            return Optional.empty();
+        }
+
+        return Optional.of(Registries.ITEM.get(id));
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
new file mode 100644
index 0000000000000000000000000000000000000000..bde53a2191b1a64929b61eb8a084bc5678172181
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
@@ -0,0 +1,108 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.client.MinecraftClient;
+import net.minecraft.client.network.ClientPlayerEntity;
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+import net.minecraft.network.packet.c2s.play.UpdateSelectedSlotC2SPacket;
+import net.minecraft.screen.slot.SlotActionType;
+
+import java.util.Optional;
+import java.util.OptionalInt;
+
+public final class ItemSwapController {
+    private static final int PLAYER_SCREEN_HOTBAR_SLOT_OFFSET = 36;
+
+    private final ItemSwapConfig config;
+    private final InventoryItemFinder finder;
+
+    public ItemSwapController(ItemSwapConfig config) {
+        this.config = config;
+        this.finder = new InventoryItemFinder();
+    }
+
+    public boolean toggle(MinecraftClient client) {
+        if (client.player == null || client.interactionManager == null || client.currentScreen != null) {
+            return false;
+        }
+
+        Optional<Item> itemA = config.itemA();
+        Optional<Item> itemB = config.itemB();
+        if (itemA.isEmpty() || itemB.isEmpty()) {
+            return false;
+        }
+
+        ClientPlayerEntity player = client.player;
+        PlayerInventory inventory = player.getInventory();
+        Item target = targetItem(inventory.getStack(inventory.selectedSlot), itemA.get(), itemB.get());
+        OptionalInt targetSlot = finder.find(inventory, target);
+        if (targetSlot.isEmpty()) {
+            return false;
+        }
+
+        return switchToSlot(client, player, inventory, targetSlot.getAsInt(), itemA.get(), itemB.get());
+    }
+
+    private Item targetItem(ItemStack selectedStack, Item itemA, Item itemB) {
+        if (selectedStack.isOf(itemA)) {
+            return itemB;
+        }
+        if (selectedStack.isOf(itemB)) {
+            return itemA;
+        }
+
+        return itemA;
+    }
+
+    private boolean switchToSlot(
+            MinecraftClient client,
+            ClientPlayerEntity player,
+            PlayerInventory inventory,
+            int inventorySlot,
+            Item itemA,
+            Item itemB
+    ) {
+        if (inventorySlot < InventoryItemFinder.HOTBAR_SIZE) {
+            selectHotbarSlot(client, inventory, inventorySlot);
+            return true;
+        }
+
+        if (!config.allowInventorySwap()) {
+            return false;
+        }
+
+        OptionalInt destinationSlot = safeHotbarDestination(inventory, itemA, itemB);
+        if (destinationSlot.isEmpty()) {
+            return false;
+        }
+
+        client.interactionManager.clickSlot(
+                player.playerScreenHandler.syncId,
+                inventorySlot,
+                destinationSlot.getAsInt(),
+                SlotActionType.SWAP,
+                player
+        );
+        selectHotbarSlot(client, inventory, destinationSlot.getAsInt());
+        return true;
+    }
+
+    private OptionalInt safeHotbarDestination(PlayerInventory inventory, Item itemA, Item itemB) {
+        ItemStack selectedStack = inventory.getStack(inventory.selectedSlot);
+        if (selectedStack.isEmpty() || selectedStack.isOf(itemA) || selectedStack.isOf(itemB)) {
+            return OptionalInt.of(inventory.selectedSlot);
+        }
+
+        return finder.findEmptyHotbarSlot(inventory);
+    }
+
+    private void selectHotbarSlot(MinecraftClient client, PlayerInventory inventory, int slot) {
+        if (inventory.selectedSlot == slot) {
+            return;
+        }
+
+        inventory.selectedSlot = slot;
+        client.getNetworkHandler().sendPacket(new UpdateSelectedSlotC2SPacket(slot));
+    }
+}

exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "git diff -- src\\client\\java\\org\\ahaha\\untitled\\client\\UntitledClient.java src\\client\\java\\org\\ahaha\\untitled\\client\\itemswap\\ItemSwapConfig.java src\\client\\java\\org\\ahaha\\untitled\\client\\itemswap\\InventoryItemFinder.java src\\client\\java\\org\\ahaha\\untitled\\client\\itemswap\\ItemSwapController.java" in C:\Users\sutec\IdeaProjects\untitled
exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'git status --short' in C:\Users\sutec\IdeaProjects\untitled
 succeeded in 628ms:
AM codex_auto_worker.ps1
 M src/client/java/org/ahaha/untitled/client/UntitledClient.java
?? .codex-current-prompt.md
?? .codex-debug-stderr.txt
?? .codex-debug-stdout.txt
?? .codex-plan-output.txt
?? codex_auto_worker_fixed.ps1
?? src/client/java/org/ahaha/untitled/client/itemswap/

 succeeded in 639ms:
diff --git a/src/client/java/org/ahaha/untitled/client/UntitledClient.java b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
index 96f2fa5..760523f 100644
--- a/src/client/java/org/ahaha/untitled/client/UntitledClient.java
+++ b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
@@ -1,10 +1,32 @@
 package org.ahaha.untitled.client;
 
 import net.fabricmc.api.ClientModInitializer;
+import net.fabricmc.fabric.api.client.event.lifecycle.v1.ClientTickEvents;
+import net.fabricmc.fabric.api.client.keybinding.v1.KeyBindingHelper;
+import net.minecraft.client.option.KeyBinding;
+import net.minecraft.client.util.InputUtil;
+import org.ahaha.untitled.client.itemswap.ItemSwapConfig;
+import org.ahaha.untitled.client.itemswap.ItemSwapController;
+import org.lwjgl.glfw.GLFW;
 
 public class UntitledClient implements ClientModInitializer {
+    private static final String CATEGORY = "key.category.untitled";
+    private static final String SWAP_KEY = "key.untitled.swap_item_a_b";
 
     @Override
     public void onInitializeClient() {
+        ItemSwapController controller = new ItemSwapController(ItemSwapConfig.load());
+        KeyBinding swapKey = KeyBindingHelper.registerKeyBinding(new KeyBinding(
+                SWAP_KEY,
+                InputUtil.Type.KEYSYM,
+                GLFW.GLFW_KEY_UNKNOWN,
+                CATEGORY
+        ));
+
+        ClientTickEvents.END_CLIENT_TICK.register(client -> {
+            while (swapKey.wasPressed()) {
+                controller.toggle(client);
+            }
+        });
     }
 }
warning: in the working copy of 'src/client/java/org/ahaha/untitled/client/UntitledClient.java', LF will be replaced by CRLF the next time Git touches it

diff --git a/src/client/java/org/ahaha/untitled/client/UntitledClient.java b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
index 96f2fa5caf72eb792f660392a6ef7bab77503eb9..760523f1a4590b451ae9a35dd4bed823b44da6a1
--- a/src/client/java/org/ahaha/untitled/client/UntitledClient.java
+++ b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
@@ -1,10 +1,32 @@
 package org.ahaha.untitled.client;
 
 import net.fabricmc.api.ClientModInitializer;
+import net.fabricmc.fabric.api.client.event.lifecycle.v1.ClientTickEvents;
+import net.fabricmc.fabric.api.client.keybinding.v1.KeyBindingHelper;
+import net.minecraft.client.option.KeyBinding;
+import net.minecraft.client.util.InputUtil;
+import org.ahaha.untitled.client.itemswap.ItemSwapConfig;
+import org.ahaha.untitled.client.itemswap.ItemSwapController;
+import org.lwjgl.glfw.GLFW;
 
 public class UntitledClient implements ClientModInitializer {
+    private static final String CATEGORY = "key.category.untitled";
+    private static final String SWAP_KEY = "key.untitled.swap_item_a_b";
 
     @Override
     public void onInitializeClient() {
+        ItemSwapController controller = new ItemSwapController(ItemSwapConfig.load());
+        KeyBinding swapKey = KeyBindingHelper.registerKeyBinding(new KeyBinding(
+                SWAP_KEY,
+                InputUtil.Type.KEYSYM,
+                GLFW.GLFW_KEY_UNKNOWN,
+                CATEGORY
+        ));
+
+        ClientTickEvents.END_CLIENT_TICK.register(client -> {
+            while (swapKey.wasPressed()) {
+                controller.toggle(client);
+            }
+        });
     }
 }
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
new file mode 100644
index 0000000000000000000000000000000000000000..3214363102de082962b3401df416e73144012217
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
@@ -0,0 +1,44 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+
+import java.util.OptionalInt;
+
+public final class InventoryItemFinder {
+    public static final int HOTBAR_SIZE = 9;
+    public static final int MAIN_INVENTORY_SIZE = 36;
+
+    public OptionalInt find(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < MAIN_INVENTORY_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findInHotbar(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findEmptyHotbarSlot(PlayerInventory inventory) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            if (inventory.getStack(slot).isEmpty()) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
new file mode 100644
index 0000000000000000000000000000000000000000..e3f80e5c19e04354bc7646bb6acd358895963c32
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
@@ -0,0 +1,94 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.fabricmc.loader.api.FabricLoader;
+import net.minecraft.item.Item;
+import net.minecraft.registry.Registries;
+import net.minecraft.util.Identifier;
+
+import java.io.IOException;
+import java.io.Reader;
+import java.io.Writer;
+import java.nio.file.Files;
+import java.nio.file.Path;
+import java.util.Optional;
+import java.util.Properties;
+
+public final class ItemSwapConfig {
+    private static final String CONFIG_FILE_NAME = "untitled-item-swap.properties";
+    private static final String DEFAULT_ITEM_A = "minecraft:golden_apple";
+    private static final String DEFAULT_ITEM_B = "minecraft:totem_of_undying";
+
+    private final String itemAId;
+    private final String itemBId;
+    private final boolean allowInventorySwap;
+
+    private ItemSwapConfig(String itemAId, String itemBId, boolean allowInventorySwap) {
+        this.itemAId = itemAId;
+        this.itemBId = itemBId;
+        this.allowInventorySwap = allowInventorySwap;
+    }
+
+    public static ItemSwapConfig load() {
+        Path path = FabricLoader.getInstance().getConfigDir().resolve(CONFIG_FILE_NAME);
+        Properties properties = defaultProperties();
+
+        if (Files.exists(path)) {
+            try (Reader reader = Files.newBufferedReader(path)) {
+                properties.load(reader);
+            } catch (IOException ignored) {
+                return fromProperties(properties);
+            }
+        } else {
+            writeDefaults(path, properties);
+        }
+
+        return fromProperties(properties);
+    }
+
+    public Optional<Item> itemA() {
+        return resolveItem(itemAId);
+    }
+
+    public Optional<Item> itemB() {
+        return resolveItem(itemBId);
+    }
+
+    public boolean allowInventorySwap() {
+        return allowInventorySwap;
+    }
+
+    private static Properties defaultProperties() {
+        Properties properties = new Properties();
+        properties.setProperty("itemA", DEFAULT_ITEM_A);
+        properties.setProperty("itemB", DEFAULT_ITEM_B);
+        properties.setProperty("allowInventorySwap", "true");
+        return properties;
+    }
+
+    private static ItemSwapConfig fromProperties(Properties properties) {
+        return new ItemSwapConfig(
+                properties.getProperty("itemA", DEFAULT_ITEM_A).trim(),
+                properties.getProperty("itemB", DEFAULT_ITEM_B).trim(),
+                Boolean.parseBoolean(properties.getProperty("allowInventorySwap", "true").trim())
+        );
+    }
+
+    private static void writeDefaults(Path path, Properties properties) {
+        try {
+            Files.createDirectories(path.getParent());
+            try (Writer writer = Files.newBufferedWriter(path)) {
+                properties.store(writer, "Untitled item A/B swap settings");
+            }
+        } catch (IOException ignored) {
+        }
+    }
+
+    private static Optional<Item> resolveItem(String rawId) {
+        Identifier id = Identifier.tryParse(rawId);
+        if (id == null || !Registries.ITEM.containsId(id)) {
+            return Optional.empty();
+        }
+
+        return Optional.of(Registries.ITEM.get(id));
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
new file mode 100644
index 0000000000000000000000000000000000000000..bde53a2191b1a64929b61eb8a084bc5678172181
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
@@ -0,0 +1,108 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.client.MinecraftClient;
+import net.minecraft.client.network.ClientPlayerEntity;
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+import net.minecraft.network.packet.c2s.play.UpdateSelectedSlotC2SPacket;
+import net.minecraft.screen.slot.SlotActionType;
+
+import java.util.Optional;
+import java.util.OptionalInt;
+
+public final class ItemSwapController {
+    private static final int PLAYER_SCREEN_HOTBAR_SLOT_OFFSET = 36;
+
+    private final ItemSwapConfig config;
+    private final InventoryItemFinder finder;
+
+    public ItemSwapController(ItemSwapConfig config) {
+        this.config = config;
+        this.finder = new InventoryItemFinder();
+    }
+
+    public boolean toggle(MinecraftClient client) {
+        if (client.player == null || client.interactionManager == null || client.currentScreen != null) {
+            return false;
+        }
+
+        Optional<Item> itemA = config.itemA();
+        Optional<Item> itemB = config.itemB();
+        if (itemA.isEmpty() || itemB.isEmpty()) {
+            return false;
+        }
+
+        ClientPlayerEntity player = client.player;
+        PlayerInventory inventory = player.getInventory();
+        Item target = targetItem(inventory.getStack(inventory.selectedSlot), itemA.get(), itemB.get());
+        OptionalInt targetSlot = finder.find(inventory, target);
+        if (targetSlot.isEmpty()) {
+            return false;
+        }
+
+        return switchToSlot(client, player, inventory, targetSlot.getAsInt(), itemA.get(), itemB.get());
+    }
+
+    private Item targetItem(ItemStack selectedStack, Item itemA, Item itemB) {
+        if (selectedStack.isOf(itemA)) {
+            return itemB;
+        }
+        if (selectedStack.isOf(itemB)) {
+            return itemA;
+        }
+
+        return itemA;
+    }
+
+    private boolean switchToSlot(
+            MinecraftClient client,
+            ClientPlayerEntity player,
+            PlayerInventory inventory,
+            int inventorySlot,
+            Item itemA,
+            Item itemB
+    ) {
+        if (inventorySlot < InventoryItemFinder.HOTBAR_SIZE) {
+            selectHotbarSlot(client, inventory, inventorySlot);
+            return true;
+        }
+
+        if (!config.allowInventorySwap()) {
+            return false;
+        }
+
+        OptionalInt destinationSlot = safeHotbarDestination(inventory, itemA, itemB);
+        if (destinationSlot.isEmpty()) {
+            return false;
+        }
+
+        client.interactionManager.clickSlot(
+                player.playerScreenHandler.syncId,
+                inventorySlot,
+                destinationSlot.getAsInt(),
+                SlotActionType.SWAP,
+                player
+        );
+        selectHotbarSlot(client, inventory, destinationSlot.getAsInt());
+        return true;
+    }
+
+    private OptionalInt safeHotbarDestination(PlayerInventory inventory, Item itemA, Item itemB) {
+        ItemStack selectedStack = inventory.getStack(inventory.selectedSlot);
+        if (selectedStack.isEmpty() || selectedStack.isOf(itemA) || selectedStack.isOf(itemB)) {
+            return OptionalInt.of(inventory.selectedSlot);
+        }
+
+        return finder.findEmptyHotbarSlot(inventory);
+    }
+
+    private void selectHotbarSlot(MinecraftClient client, PlayerInventory inventory, int slot) {
+        if (inventory.selectedSlot == slot) {
+            return;
+        }
+
+        inventory.selectedSlot = slot;
+        client.getNetworkHandler().sendPacket(new UpdateSelectedSlotC2SPacket(slot));
+    }
+}

exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw src\\client\\java\\org\\ahaha\\untitled\\client\\itemswap\\ItemSwapConfig.java" in C:\Users\sutec\IdeaProjects\untitled
exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw src\\client\\java\\org\\ahaha\\untitled\\client\\itemswap\\InventoryItemFinder.java" in C:\Users\sutec\IdeaProjects\untitled
exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Raw src\\client\\java\\org\\ahaha\\untitled\\client\\itemswap\\ItemSwapController.java" in C:\Users\sutec\IdeaProjects\untitled
 succeeded in 790ms:
package org.ahaha.untitled.client.itemswap;

import net.minecraft.entity.player.PlayerInventory;
import net.minecraft.item.Item;
import net.minecraft.item.ItemStack;

import java.util.OptionalInt;

public final class InventoryItemFinder {
    public static final int HOTBAR_SIZE = 9;
    public static final int MAIN_INVENTORY_SIZE = 36;

    public OptionalInt find(PlayerInventory inventory, Item item) {
        for (int slot = 0; slot < MAIN_INVENTORY_SIZE; slot++) {
            ItemStack stack = inventory.getStack(slot);
            if (stack.isOf(item)) {
                return OptionalInt.of(slot);
            }
        }

        return OptionalInt.empty();
    }

    public OptionalInt findInHotbar(PlayerInventory inventory, Item item) {
        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
            ItemStack stack = inventory.getStack(slot);
            if (stack.isOf(item)) {
                return OptionalInt.of(slot);
            }
        }

        return OptionalInt.empty();
    }

    public OptionalInt findEmptyHotbarSlot(PlayerInventory inventory) {
        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
            if (inventory.getStack(slot).isEmpty()) {
                return OptionalInt.of(slot);
            }
        }

        return OptionalInt.empty();
    }
}


 succeeded in 796ms:
package org.ahaha.untitled.client.itemswap;

import net.fabricmc.loader.api.FabricLoader;
import net.minecraft.item.Item;
import net.minecraft.registry.Registries;
import net.minecraft.util.Identifier;

import java.io.IOException;
import java.io.Reader;
import java.io.Writer;
import java.nio.file.Files;
import java.nio.file.Path;
import java.util.Optional;
import java.util.Properties;

public final class ItemSwapConfig {
    private static final String CONFIG_FILE_NAME = "untitled-item-swap.properties";
    private static final String DEFAULT_ITEM_A = "minecraft:golden_apple";
    private static final String DEFAULT_ITEM_B = "minecraft:totem_of_undying";

    private final String itemAId;
    private final String itemBId;
    private final boolean allowInventorySwap;

    private ItemSwapConfig(String itemAId, String itemBId, boolean allowInventorySwap) {
        this.itemAId = itemAId;
        this.itemBId = itemBId;
        this.allowInventorySwap = allowInventorySwap;
    }

    public static ItemSwapConfig load() {
        Path path = FabricLoader.getInstance().getConfigDir().resolve(CONFIG_FILE_NAME);
        Properties properties = defaultProperties();

        if (Files.exists(path)) {
            try (Reader reader = Files.newBufferedReader(path)) {
                properties.load(reader);
            } catch (IOException ignored) {
                return fromProperties(properties);
            }
        } else {
            writeDefaults(path, properties);
        }

        return fromProperties(properties);
    }

    public Optional<Item> itemA() {
        return resolveItem(itemAId);
    }

    public Optional<Item> itemB() {
        return resolveItem(itemBId);
    }

    public boolean allowInventorySwap() {
        return allowInventorySwap;
    }

    private static Properties defaultProperties() {
        Properties properties = new Properties();
        properties.setProperty("itemA", DEFAULT_ITEM_A);
        properties.setProperty("itemB", DEFAULT_ITEM_B);
        properties.setProperty("allowInventorySwap", "true");
        return properties;
    }

    private static ItemSwapConfig fromProperties(Properties properties) {
        return new ItemSwapConfig(
                properties.getProperty("itemA", DEFAULT_ITEM_A).trim(),
                properties.getProperty("itemB", DEFAULT_ITEM_B).trim(),
                Boolean.parseBoolean(properties.getProperty("allowInventorySwap", "true").trim())
        );
    }

    private static void writeDefaults(Path path, Properties properties) {
        try {
            Files.createDirectories(path.getParent());
            try (Writer writer = Files.newBufferedWriter(path)) {
                properties.store(writer, "Untitled item A/B swap settings");
            }
        } catch (IOException ignored) {
        }
    }

    private static Optional<Item> resolveItem(String rawId) {
        Identifier id = Identifier.tryParse(rawId);
        if (id == null || !Registries.ITEM.containsId(id)) {
            return Optional.empty();
        }

        return Optional.of(Registries.ITEM.get(id));
    }
}


 succeeded in 809ms:
package org.ahaha.untitled.client.itemswap;

import net.minecraft.client.MinecraftClient;
import net.minecraft.client.network.ClientPlayerEntity;
import net.minecraft.entity.player.PlayerInventory;
import net.minecraft.item.Item;
import net.minecraft.item.ItemStack;
import net.minecraft.network.packet.c2s.play.UpdateSelectedSlotC2SPacket;
import net.minecraft.screen.slot.SlotActionType;

import java.util.Optional;
import java.util.OptionalInt;

public final class ItemSwapController {
    private static final int PLAYER_SCREEN_HOTBAR_SLOT_OFFSET = 36;

    private final ItemSwapConfig config;
    private final InventoryItemFinder finder;

    public ItemSwapController(ItemSwapConfig config) {
        this.config = config;
        this.finder = new InventoryItemFinder();
    }

    public boolean toggle(MinecraftClient client) {
        if (client.player == null || client.interactionManager == null || client.currentScreen != null) {
            return false;
        }

        Optional<Item> itemA = config.itemA();
        Optional<Item> itemB = config.itemB();
        if (itemA.isEmpty() || itemB.isEmpty()) {
            return false;
        }

        ClientPlayerEntity player = client.player;
        PlayerInventory inventory = player.getInventory();
        Item target = targetItem(inventory.getStack(inventory.selectedSlot), itemA.get(), itemB.get());
        OptionalInt targetSlot = finder.find(inventory, target);
        if (targetSlot.isEmpty()) {
            return false;
        }

        return switchToSlot(client, player, inventory, targetSlot.getAsInt(), itemA.get(), itemB.get());
    }

    private Item targetItem(ItemStack selectedStack, Item itemA, Item itemB) {
        if (selectedStack.isOf(itemA)) {
            return itemB;
        }
        if (selectedStack.isOf(itemB)) {
            return itemA;
        }

        return itemA;
    }

    private boolean switchToSlot(
            MinecraftClient client,
            ClientPlayerEntity player,
            PlayerInventory inventory,
            int inventorySlot,
            Item itemA,
            Item itemB
    ) {
        if (inventorySlot < InventoryItemFinder.HOTBAR_SIZE) {
            selectHotbarSlot(client, inventory, inventorySlot);
            return true;
        }

        if (!config.allowInventorySwap()) {
            return false;
        }

        OptionalInt destinationSlot = safeHotbarDestination(inventory, itemA, itemB);
        if (destinationSlot.isEmpty()) {
            return false;
        }

        client.interactionManager.clickSlot(
                player.playerScreenHandler.syncId,
                inventorySlot,
                destinationSlot.getAsInt(),
                SlotActionType.SWAP,
                player
        );
        selectHotbarSlot(client, inventory, destinationSlot.getAsInt());
        return true;
    }

    private OptionalInt safeHotbarDestination(PlayerInventory inventory, Item itemA, Item itemB) {
        ItemStack selectedStack = inventory.getStack(inventory.selectedSlot);
        if (selectedStack.isEmpty() || selectedStack.isOf(itemA) || selectedStack.isOf(itemB)) {
            return OptionalInt.of(inventory.selectedSlot);
        }

        return finder.findEmptyHotbarSlot(inventory);
    }

    private void selectHotbarSlot(MinecraftClient client, PlayerInventory inventory, int slot) {
        if (inventory.selectedSlot == slot) {
            return;
        }

        inventory.selectedSlot = slot;
        client.getNetworkHandler().sendPacket(new UpdateSelectedSlotC2SPacket(slot));
    }
}


diff --git a/src/client/java/org/ahaha/untitled/client/UntitledClient.java b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
index 96f2fa5caf72eb792f660392a6ef7bab77503eb9..760523f1a4590b451ae9a35dd4bed823b44da6a1
--- a/src/client/java/org/ahaha/untitled/client/UntitledClient.java
+++ b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
@@ -1,10 +1,32 @@
 package org.ahaha.untitled.client;
 
 import net.fabricmc.api.ClientModInitializer;
+import net.fabricmc.fabric.api.client.event.lifecycle.v1.ClientTickEvents;
+import net.fabricmc.fabric.api.client.keybinding.v1.KeyBindingHelper;
+import net.minecraft.client.option.KeyBinding;
+import net.minecraft.client.util.InputUtil;
+import org.ahaha.untitled.client.itemswap.ItemSwapConfig;
+import org.ahaha.untitled.client.itemswap.ItemSwapController;
+import org.lwjgl.glfw.GLFW;
 
 public class UntitledClient implements ClientModInitializer {
+    private static final String CATEGORY = "key.category.untitled";
+    private static final String SWAP_KEY = "key.untitled.swap_item_a_b";
 
     @Override
     public void onInitializeClient() {
+        ItemSwapController controller = new ItemSwapController(ItemSwapConfig.load());
+        KeyBinding swapKey = KeyBindingHelper.registerKeyBinding(new KeyBinding(
+                SWAP_KEY,
+                InputUtil.Type.KEYSYM,
+                GLFW.GLFW_KEY_UNKNOWN,
+                CATEGORY
+        ));
+
+        ClientTickEvents.END_CLIENT_TICK.register(client -> {
+            while (swapKey.wasPressed()) {
+                controller.toggle(client);
+            }
+        });
     }
 }
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
new file mode 100644
index 0000000000000000000000000000000000000000..3214363102de082962b3401df416e73144012217
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
@@ -0,0 +1,44 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+
+import java.util.OptionalInt;
+
+public final class InventoryItemFinder {
+    public static final int HOTBAR_SIZE = 9;
+    public static final int MAIN_INVENTORY_SIZE = 36;
+
+    public OptionalInt find(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < MAIN_INVENTORY_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findInHotbar(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findEmptyHotbarSlot(PlayerInventory inventory) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            if (inventory.getStack(slot).isEmpty()) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
new file mode 100644
index 0000000000000000000000000000000000000000..e3f80e5c19e04354bc7646bb6acd358895963c32
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
@@ -0,0 +1,94 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.fabricmc.loader.api.FabricLoader;
+import net.minecraft.item.Item;
+import net.minecraft.registry.Registries;
+import net.minecraft.util.Identifier;
+
+import java.io.IOException;
+import java.io.Reader;
+import java.io.Writer;
+import java.nio.file.Files;
+import java.nio.file.Path;
+import java.util.Optional;
+import java.util.Properties;
+
+public final class ItemSwapConfig {
+    private static final String CONFIG_FILE_NAME = "untitled-item-swap.properties";
+    private static final String DEFAULT_ITEM_A = "minecraft:golden_apple";
+    private static final String DEFAULT_ITEM_B = "minecraft:totem_of_undying";
+
+    private final String itemAId;
+    private final String itemBId;
+    private final boolean allowInventorySwap;
+
+    private ItemSwapConfig(String itemAId, String itemBId, boolean allowInventorySwap) {
+        this.itemAId = itemAId;
+        this.itemBId = itemBId;
+        this.allowInventorySwap = allowInventorySwap;
+    }
+
+    public static ItemSwapConfig load() {
+        Path path = FabricLoader.getInstance().getConfigDir().resolve(CONFIG_FILE_NAME);
+        Properties properties = defaultProperties();
+
+        if (Files.exists(path)) {
+            try (Reader reader = Files.newBufferedReader(path)) {
+                properties.load(reader);
+            } catch (IOException ignored) {
+                return fromProperties(properties);
+            }
+        } else {
+            writeDefaults(path, properties);
+        }
+
+        return fromProperties(properties);
+    }
+
+    public Optional<Item> itemA() {
+        return resolveItem(itemAId);
+    }
+
+    public Optional<Item> itemB() {
+        return resolveItem(itemBId);
+    }
+
+    public boolean allowInventorySwap() {
+        return allowInventorySwap;
+    }
+
+    private static Properties defaultProperties() {
+        Properties properties = new Properties();
+        properties.setProperty("itemA", DEFAULT_ITEM_A);
+        properties.setProperty("itemB", DEFAULT_ITEM_B);
+        properties.setProperty("allowInventorySwap", "true");
+        return properties;
+    }
+
+    private static ItemSwapConfig fromProperties(Properties properties) {
+        return new ItemSwapConfig(
+                properties.getProperty("itemA", DEFAULT_ITEM_A).trim(),
+                properties.getProperty("itemB", DEFAULT_ITEM_B).trim(),
+                Boolean.parseBoolean(properties.getProperty("allowInventorySwap", "true").trim())
+        );
+    }
+
+    private static void writeDefaults(Path path, Properties properties) {
+        try {
+            Files.createDirectories(path.getParent());
+            try (Writer writer = Files.newBufferedWriter(path)) {
+                properties.store(writer, "Untitled item A/B swap settings");
+            }
+        } catch (IOException ignored) {
+        }
+    }
+
+    private static Optional<Item> resolveItem(String rawId) {
+        Identifier id = Identifier.tryParse(rawId);
+        if (id == null || !Registries.ITEM.containsId(id)) {
+            return Optional.empty();
+        }
+
+        return Optional.of(Registries.ITEM.get(id));
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
new file mode 100644
index 0000000000000000000000000000000000000000..bde53a2191b1a64929b61eb8a084bc5678172181
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
@@ -0,0 +1,108 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.client.MinecraftClient;
+import net.minecraft.client.network.ClientPlayerEntity;
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+import net.minecraft.network.packet.c2s.play.UpdateSelectedSlotC2SPacket;
+import net.minecraft.screen.slot.SlotActionType;
+
+import java.util.Optional;
+import java.util.OptionalInt;
+
+public final class ItemSwapController {
+    private static final int PLAYER_SCREEN_HOTBAR_SLOT_OFFSET = 36;
+
+    private final ItemSwapConfig config;
+    private final InventoryItemFinder finder;
+
+    public ItemSwapController(ItemSwapConfig config) {
+        this.config = config;
+        this.finder = new InventoryItemFinder();
+    }
+
+    public boolean toggle(MinecraftClient client) {
+        if (client.player == null || client.interactionManager == null || client.currentScreen != null) {
+            return false;
+        }
+
+        Optional<Item> itemA = config.itemA();
+        Optional<Item> itemB = config.itemB();
+        if (itemA.isEmpty() || itemB.isEmpty()) {
+            return false;
+        }
+
+        ClientPlayerEntity player = client.player;
+        PlayerInventory inventory = player.getInventory();
+        Item target = targetItem(inventory.getStack(inventory.selectedSlot), itemA.get(), itemB.get());
+        OptionalInt targetSlot = finder.find(inventory, target);
+        if (targetSlot.isEmpty()) {
+            return false;
+        }
+
+        return switchToSlot(client, player, inventory, targetSlot.getAsInt(), itemA.get(), itemB.get());
+    }
+
+    private Item targetItem(ItemStack selectedStack, Item itemA, Item itemB) {
+        if (selectedStack.isOf(itemA)) {
+            return itemB;
+        }
+        if (selectedStack.isOf(itemB)) {
+            return itemA;
+        }
+
+        return itemA;
+    }
+
+    private boolean switchToSlot(
+            MinecraftClient client,
+            ClientPlayerEntity player,
+            PlayerInventory inventory,
+            int inventorySlot,
+            Item itemA,
+            Item itemB
+    ) {
+        if (inventorySlot < InventoryItemFinder.HOTBAR_SIZE) {
+            selectHotbarSlot(client, inventory, inventorySlot);
+            return true;
+        }
+
+        if (!config.allowInventorySwap()) {
+            return false;
+        }
+
+        OptionalInt destinationSlot = safeHotbarDestination(inventory, itemA, itemB);
+        if (destinationSlot.isEmpty()) {
+            return false;
+        }
+
+        client.interactionManager.clickSlot(
+                player.playerScreenHandler.syncId,
+                inventorySlot,
+                destinationSlot.getAsInt(),
+                SlotActionType.SWAP,
+                player
+        );
+        selectHotbarSlot(client, inventory, destinationSlot.getAsInt());
+        return true;
+    }
+
+    private OptionalInt safeHotbarDestination(PlayerInventory inventory, Item itemA, Item itemB) {
+        ItemStack selectedStack = inventory.getStack(inventory.selectedSlot);
+        if (selectedStack.isEmpty() || selectedStack.isOf(itemA) || selectedStack.isOf(itemB)) {
+            return OptionalInt.of(inventory.selectedSlot);
+        }
+
+        return finder.findEmptyHotbarSlot(inventory);
+    }
+
+    private void selectHotbarSlot(MinecraftClient client, PlayerInventory inventory, int slot) {
+        if (inventory.selectedSlot == slot) {
+            return;
+        }
+
+        inventory.selectedSlot = slot;
+        client.getNetworkHandler().sendPacket(new UpdateSelectedSlotC2SPacket(slot));
+    }
+}

apply patch
patch: completed
C:\Users\sutec\IdeaProjects\untitled\src\client\java\org\ahaha\untitled\client\itemswap\ItemSwapController.java
diff --git a/src/client/java/org/ahaha/untitled/client/UntitledClient.java b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
index 96f2fa5caf72eb792f660392a6ef7bab77503eb9..760523f1a4590b451ae9a35dd4bed823b44da6a1
--- a/src/client/java/org/ahaha/untitled/client/UntitledClient.java
+++ b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
@@ -1,10 +1,32 @@
 package org.ahaha.untitled.client;
 
 import net.fabricmc.api.ClientModInitializer;
+import net.fabricmc.fabric.api.client.event.lifecycle.v1.ClientTickEvents;
+import net.fabricmc.fabric.api.client.keybinding.v1.KeyBindingHelper;
+import net.minecraft.client.option.KeyBinding;
+import net.minecraft.client.util.InputUtil;
+import org.ahaha.untitled.client.itemswap.ItemSwapConfig;
+import org.ahaha.untitled.client.itemswap.ItemSwapController;
+import org.lwjgl.glfw.GLFW;
 
 public class UntitledClient implements ClientModInitializer {
+    private static final String CATEGORY = "key.category.untitled";
+    private static final String SWAP_KEY = "key.untitled.swap_item_a_b";
 
     @Override
     public void onInitializeClient() {
+        ItemSwapController controller = new ItemSwapController(ItemSwapConfig.load());
+        KeyBinding swapKey = KeyBindingHelper.registerKeyBinding(new KeyBinding(
+                SWAP_KEY,
+                InputUtil.Type.KEYSYM,
+                GLFW.GLFW_KEY_UNKNOWN,
+                CATEGORY
+        ));
+
+        ClientTickEvents.END_CLIENT_TICK.register(client -> {
+            while (swapKey.wasPressed()) {
+                controller.toggle(client);
+            }
+        });
     }
 }
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
new file mode 100644
index 0000000000000000000000000000000000000000..3214363102de082962b3401df416e73144012217
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
@@ -0,0 +1,44 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+
+import java.util.OptionalInt;
+
+public final class InventoryItemFinder {
+    public static final int HOTBAR_SIZE = 9;
+    public static final int MAIN_INVENTORY_SIZE = 36;
+
+    public OptionalInt find(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < MAIN_INVENTORY_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findInHotbar(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findEmptyHotbarSlot(PlayerInventory inventory) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            if (inventory.getStack(slot).isEmpty()) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
new file mode 100644
index 0000000000000000000000000000000000000000..e3f80e5c19e04354bc7646bb6acd358895963c32
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
@@ -0,0 +1,94 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.fabricmc.loader.api.FabricLoader;
+import net.minecraft.item.Item;
+import net.minecraft.registry.Registries;
+import net.minecraft.util.Identifier;
+
+import java.io.IOException;
+import java.io.Reader;
+import java.io.Writer;
+import java.nio.file.Files;
+import java.nio.file.Path;
+import java.util.Optional;
+import java.util.Properties;
+
+public final class ItemSwapConfig {
+    private static final String CONFIG_FILE_NAME = "untitled-item-swap.properties";
+    private static final String DEFAULT_ITEM_A = "minecraft:golden_apple";
+    private static final String DEFAULT_ITEM_B = "minecraft:totem_of_undying";
+
+    private final String itemAId;
+    private final String itemBId;
+    private final boolean allowInventorySwap;
+
+    private ItemSwapConfig(String itemAId, String itemBId, boolean allowInventorySwap) {
+        this.itemAId = itemAId;
+        this.itemBId = itemBId;
+        this.allowInventorySwap = allowInventorySwap;
+    }
+
+    public static ItemSwapConfig load() {
+        Path path = FabricLoader.getInstance().getConfigDir().resolve(CONFIG_FILE_NAME);
+        Properties properties = defaultProperties();
+
+        if (Files.exists(path)) {
+            try (Reader reader = Files.newBufferedReader(path)) {
+                properties.load(reader);
+            } catch (IOException ignored) {
+                return fromProperties(properties);
+            }
+        } else {
+            writeDefaults(path, properties);
+        }
+
+        return fromProperties(properties);
+    }
+
+    public Optional<Item> itemA() {
+        return resolveItem(itemAId);
+    }
+
+    public Optional<Item> itemB() {
+        return resolveItem(itemBId);
+    }
+
+    public boolean allowInventorySwap() {
+        return allowInventorySwap;
+    }
+
+    private static Properties defaultProperties() {
+        Properties properties = new Properties();
+        properties.setProperty("itemA", DEFAULT_ITEM_A);
+        properties.setProperty("itemB", DEFAULT_ITEM_B);
+        properties.setProperty("allowInventorySwap", "true");
+        return properties;
+    }
+
+    private static ItemSwapConfig fromProperties(Properties properties) {
+        return new ItemSwapConfig(
+                properties.getProperty("itemA", DEFAULT_ITEM_A).trim(),
+                properties.getProperty("itemB", DEFAULT_ITEM_B).trim(),
+                Boolean.parseBoolean(properties.getProperty("allowInventorySwap", "true").trim())
+        );
+    }
+
+    private static void writeDefaults(Path path, Properties properties) {
+        try {
+            Files.createDirectories(path.getParent());
+            try (Writer writer = Files.newBufferedWriter(path)) {
+                properties.store(writer, "Untitled item A/B swap settings");
+            }
+        } catch (IOException ignored) {
+        }
+    }
+
+    private static Optional<Item> resolveItem(String rawId) {
+        Identifier id = Identifier.tryParse(rawId);
+        if (id == null || !Registries.ITEM.containsId(id)) {
+            return Optional.empty();
+        }
+
+        return Optional.of(Registries.ITEM.get(id));
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
new file mode 100644
index 0000000000000000000000000000000000000000..be476059f0b833dda89a2f83c69bf914bd8830e7
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
@@ -0,0 +1,106 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.client.MinecraftClient;
+import net.minecraft.client.network.ClientPlayerEntity;
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+import net.minecraft.network.packet.c2s.play.UpdateSelectedSlotC2SPacket;
+import net.minecraft.screen.slot.SlotActionType;
+
+import java.util.Optional;
+import java.util.OptionalInt;
+
+public final class ItemSwapController {
+    private final ItemSwapConfig config;
+    private final InventoryItemFinder finder;
+
+    public ItemSwapController(ItemSwapConfig config) {
+        this.config = config;
+        this.finder = new InventoryItemFinder();
+    }
+
+    public boolean toggle(MinecraftClient client) {
+        if (client.player == null || client.interactionManager == null || client.currentScreen != null) {
+            return false;
+        }
+
+        Optional<Item> itemA = config.itemA();
+        Optional<Item> itemB = config.itemB();
+        if (itemA.isEmpty() || itemB.isEmpty()) {
+            return false;
+        }
+
+        ClientPlayerEntity player = client.player;
+        PlayerInventory inventory = player.getInventory();
+        Item target = targetItem(inventory.getStack(inventory.selectedSlot), itemA.get(), itemB.get());
+        OptionalInt targetSlot = finder.find(inventory, target);
+        if (targetSlot.isEmpty()) {
+            return false;
+        }
+
+        return switchToSlot(client, player, inventory, targetSlot.getAsInt(), itemA.get(), itemB.get());
+    }
+
+    private Item targetItem(ItemStack selectedStack, Item itemA, Item itemB) {
+        if (selectedStack.isOf(itemA)) {
+            return itemB;
+        }
+        if (selectedStack.isOf(itemB)) {
+            return itemA;
+        }
+
+        return itemA;
+    }
+
+    private boolean switchToSlot(
+            MinecraftClient client,
+            ClientPlayerEntity player,
+            PlayerInventory inventory,
+            int inventorySlot,
+            Item itemA,
+            Item itemB
+    ) {
+        if (inventorySlot < InventoryItemFinder.HOTBAR_SIZE) {
+            selectHotbarSlot(client, inventory, inventorySlot);
+            return true;
+        }
+
+        if (!config.allowInventorySwap()) {
+            return false;
+        }
+
+        OptionalInt destinationSlot = safeHotbarDestination(inventory, itemA, itemB);
+        if (destinationSlot.isEmpty()) {
+            return false;
+        }
+
+        client.interactionManager.clickSlot(
+                player.playerScreenHandler.syncId,
+                inventorySlot,
+                destinationSlot.getAsInt(),
+                SlotActionType.SWAP,
+                player
+        );
+        selectHotbarSlot(client, inventory, destinationSlot.getAsInt());
+        return true;
+    }
+
+    private OptionalInt safeHotbarDestination(PlayerInventory inventory, Item itemA, Item itemB) {
+        ItemStack selectedStack = inventory.getStack(inventory.selectedSlot);
+        if (selectedStack.isEmpty() || selectedStack.isOf(itemA) || selectedStack.isOf(itemB)) {
+            return OptionalInt.of(inventory.selectedSlot);
+        }
+
+        return finder.findEmptyHotbarSlot(inventory);
+    }
+
+    private void selectHotbarSlot(MinecraftClient client, PlayerInventory inventory, int slot) {
+        if (inventory.selectedSlot == slot) {
+            return;
+        }
+
+        inventory.selectedSlot = slot;
+        client.getNetworkHandler().sendPacket(new UpdateSelectedSlotC2SPacket(slot));
+    }
+}

diff --git a/src/client/java/org/ahaha/untitled/client/UntitledClient.java b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
index 96f2fa5caf72eb792f660392a6ef7bab77503eb9..760523f1a4590b451ae9a35dd4bed823b44da6a1
--- a/src/client/java/org/ahaha/untitled/client/UntitledClient.java
+++ b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
@@ -1,10 +1,32 @@
 package org.ahaha.untitled.client;
 
 import net.fabricmc.api.ClientModInitializer;
+import net.fabricmc.fabric.api.client.event.lifecycle.v1.ClientTickEvents;
+import net.fabricmc.fabric.api.client.keybinding.v1.KeyBindingHelper;
+import net.minecraft.client.option.KeyBinding;
+import net.minecraft.client.util.InputUtil;
+import org.ahaha.untitled.client.itemswap.ItemSwapConfig;
+import org.ahaha.untitled.client.itemswap.ItemSwapController;
+import org.lwjgl.glfw.GLFW;
 
 public class UntitledClient implements ClientModInitializer {
+    private static final String CATEGORY = "key.category.untitled";
+    private static final String SWAP_KEY = "key.untitled.swap_item_a_b";
 
     @Override
     public void onInitializeClient() {
+        ItemSwapController controller = new ItemSwapController(ItemSwapConfig.load());
+        KeyBinding swapKey = KeyBindingHelper.registerKeyBinding(new KeyBinding(
+                SWAP_KEY,
+                InputUtil.Type.KEYSYM,
+                GLFW.GLFW_KEY_UNKNOWN,
+                CATEGORY
+        ));
+
+        ClientTickEvents.END_CLIENT_TICK.register(client -> {
+            while (swapKey.wasPressed()) {
+                controller.toggle(client);
+            }
+        });
     }
 }
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
new file mode 100644
index 0000000000000000000000000000000000000000..3214363102de082962b3401df416e73144012217
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
@@ -0,0 +1,44 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+
+import java.util.OptionalInt;
+
+public final class InventoryItemFinder {
+    public static final int HOTBAR_SIZE = 9;
+    public static final int MAIN_INVENTORY_SIZE = 36;
+
+    public OptionalInt find(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < MAIN_INVENTORY_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findInHotbar(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findEmptyHotbarSlot(PlayerInventory inventory) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            if (inventory.getStack(slot).isEmpty()) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
new file mode 100644
index 0000000000000000000000000000000000000000..e3f80e5c19e04354bc7646bb6acd358895963c32
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
@@ -0,0 +1,94 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.fabricmc.loader.api.FabricLoader;
+import net.minecraft.item.Item;
+import net.minecraft.registry.Registries;
+import net.minecraft.util.Identifier;
+
+import java.io.IOException;
+import java.io.Reader;
+import java.io.Writer;
+import java.nio.file.Files;
+import java.nio.file.Path;
+import java.util.Optional;
+import java.util.Properties;
+
+public final class ItemSwapConfig {
+    private static final String CONFIG_FILE_NAME = "untitled-item-swap.properties";
+    private static final String DEFAULT_ITEM_A = "minecraft:golden_apple";
+    private static final String DEFAULT_ITEM_B = "minecraft:totem_of_undying";
+
+    private final String itemAId;
+    private final String itemBId;
+    private final boolean allowInventorySwap;
+
+    private ItemSwapConfig(String itemAId, String itemBId, boolean allowInventorySwap) {
+        this.itemAId = itemAId;
+        this.itemBId = itemBId;
+        this.allowInventorySwap = allowInventorySwap;
+    }
+
+    public static ItemSwapConfig load() {
+        Path path = FabricLoader.getInstance().getConfigDir().resolve(CONFIG_FILE_NAME);
+        Properties properties = defaultProperties();
+
+        if (Files.exists(path)) {
+            try (Reader reader = Files.newBufferedReader(path)) {
+                properties.load(reader);
+            } catch (IOException ignored) {
+                return fromProperties(properties);
+            }
+        } else {
+            writeDefaults(path, properties);
+        }
+
+        return fromProperties(properties);
+    }
+
+    public Optional<Item> itemA() {
+        return resolveItem(itemAId);
+    }
+
+    public Optional<Item> itemB() {
+        return resolveItem(itemBId);
+    }
+
+    public boolean allowInventorySwap() {
+        return allowInventorySwap;
+    }
+
+    private static Properties defaultProperties() {
+        Properties properties = new Properties();
+        properties.setProperty("itemA", DEFAULT_ITEM_A);
+        properties.setProperty("itemB", DEFAULT_ITEM_B);
+        properties.setProperty("allowInventorySwap", "true");
+        return properties;
+    }
+
+    private static ItemSwapConfig fromProperties(Properties properties) {
+        return new ItemSwapConfig(
+                properties.getProperty("itemA", DEFAULT_ITEM_A).trim(),
+                properties.getProperty("itemB", DEFAULT_ITEM_B).trim(),
+                Boolean.parseBoolean(properties.getProperty("allowInventorySwap", "true").trim())
+        );
+    }
+
+    private static void writeDefaults(Path path, Properties properties) {
+        try {
+            Files.createDirectories(path.getParent());
+            try (Writer writer = Files.newBufferedWriter(path)) {
+                properties.store(writer, "Untitled item A/B swap settings");
+            }
+        } catch (IOException ignored) {
+        }
+    }
+
+    private static Optional<Item> resolveItem(String rawId) {
+        Identifier id = Identifier.tryParse(rawId);
+        if (id == null || !Registries.ITEM.containsId(id)) {
+            return Optional.empty();
+        }
+
+        return Optional.of(Registries.ITEM.get(id));
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
new file mode 100644
index 0000000000000000000000000000000000000000..be476059f0b833dda89a2f83c69bf914bd8830e7
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
@@ -0,0 +1,106 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.client.MinecraftClient;
+import net.minecraft.client.network.ClientPlayerEntity;
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+import net.minecraft.network.packet.c2s.play.UpdateSelectedSlotC2SPacket;
+import net.minecraft.screen.slot.SlotActionType;
+
+import java.util.Optional;
+import java.util.OptionalInt;
+
+public final class ItemSwapController {
+    private final ItemSwapConfig config;
+    private final InventoryItemFinder finder;
+
+    public ItemSwapController(ItemSwapConfig config) {
+        this.config = config;
+        this.finder = new InventoryItemFinder();
+    }
+
+    public boolean toggle(MinecraftClient client) {
+        if (client.player == null || client.interactionManager == null || client.currentScreen != null) {
+            return false;
+        }
+
+        Optional<Item> itemA = config.itemA();
+        Optional<Item> itemB = config.itemB();
+        if (itemA.isEmpty() || itemB.isEmpty()) {
+            return false;
+        }
+
+        ClientPlayerEntity player = client.player;
+        PlayerInventory inventory = player.getInventory();
+        Item target = targetItem(inventory.getStack(inventory.selectedSlot), itemA.get(), itemB.get());
+        OptionalInt targetSlot = finder.find(inventory, target);
+        if (targetSlot.isEmpty()) {
+            return false;
+        }
+
+        return switchToSlot(client, player, inventory, targetSlot.getAsInt(), itemA.get(), itemB.get());
+    }
+
+    private Item targetItem(ItemStack selectedStack, Item itemA, Item itemB) {
+        if (selectedStack.isOf(itemA)) {
+            return itemB;
+        }
+        if (selectedStack.isOf(itemB)) {
+            return itemA;
+        }
+
+        return itemA;
+    }
+
+    private boolean switchToSlot(
+            MinecraftClient client,
+            ClientPlayerEntity player,
+            PlayerInventory inventory,
+            int inventorySlot,
+            Item itemA,
+            Item itemB
+    ) {
+        if (inventorySlot < InventoryItemFinder.HOTBAR_SIZE) {
+            selectHotbarSlot(client, inventory, inventorySlot);
+            return true;
+        }
+
+        if (!config.allowInventorySwap()) {
+            return false;
+        }
+
+        OptionalInt destinationSlot = safeHotbarDestination(inventory, itemA, itemB);
+        if (destinationSlot.isEmpty()) {
+            return false;
+        }
+
+        client.interactionManager.clickSlot(
+                player.playerScreenHandler.syncId,
+                inventorySlot,
+                destinationSlot.getAsInt(),
+                SlotActionType.SWAP,
+                player
+        );
+        selectHotbarSlot(client, inventory, destinationSlot.getAsInt());
+        return true;
+    }
+
+    private OptionalInt safeHotbarDestination(PlayerInventory inventory, Item itemA, Item itemB) {
+        ItemStack selectedStack = inventory.getStack(inventory.selectedSlot);
+        if (selectedStack.isEmpty() || selectedStack.isOf(itemA) || selectedStack.isOf(itemB)) {
+            return OptionalInt.of(inventory.selectedSlot);
+        }
+
+        return finder.findEmptyHotbarSlot(inventory);
+    }
+
+    private void selectHotbarSlot(MinecraftClient client, PlayerInventory inventory, int slot) {
+        if (inventory.selectedSlot == slot) {
+            return;
+        }
+
+        inventory.selectedSlot = slot;
+        client.getNetworkHandler().sendPacket(new UpdateSelectedSlotC2SPacket(slot));
+    }
+}

exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$javac='"'C:\\Program Files\\JetBrains\\IntelliJ IDEA 2026.1.1\\jbr\\bin\\javac.exe'
"'$out='"'build\\codex-compile-check-3'
New-Item -ItemType Directory -Force "'$out | Out-Null
$jars = @()
$jars += Get-ChildItem -Recurse -File .gradle'"\\loom-cache\\minecraftMaven -Filter '*.jar' | Where-Object { "'$_.Name -notmatch '"'backup|sources' } | Select-Object -ExpandProperty FullName
"'$jars += Get-ChildItem -Recurse -File .gradle'"\\loom-cache\\remapped_mods -Filter '*.jar' | Where-Object { "'$_.Name -notmatch '"'sources' } | Select-Object -ExpandProperty FullName
"'$jars += Get-ChildItem -Recurse -File "$env:USERPROFILE'"\\.gradle\\caches\\modules-2\\files-2.1\" -Filter '*.jar' | Where-Object { "'$_.Name -notmatch '"'sources|javadoc|natives' -and ("'$_.FullName -match '"'fabric-loader|org.lwjgl|org.slf4j|datafixerupper|brigadier|authlib|guava|gson|fastutil') } | Select-Object -ExpandProperty FullName
"'$cp = (($jars | Select-Object -Unique) -join '"';').Replace('\\','/')
"'$sources = Get-ChildItem -Recurse -File src'"\\main\\java,src\\client\\java -Filter '*.java' | ForEach-Object { "'$_.FullName.Replace('"'\\','/') }
"'$argsFile = Join-Path $out '"'javac.args'
"'$dest = (Resolve-Path $out).Path.Replace('"'\\','/')
"'$lines = @('"'-encoding','UTF-8','--release','21','-classpath',\""'$cp",'"'-d',\""'$dest") + $sources
Set-Content -Path $argsFile -Value $lines -Encoding ASCII
& $javac "@$argsFile"' in C:\Users\sutec\IdeaProjects\untitled
 succeeded in 4825ms:
コンパイラで例外が発生しました(25.0.2)。バグ・データベース(https://bugs.java.com)で重複がないかをご確認のうえ、Javaのバグ・レポート・ページ(https://bugreport.java.com)から、Javaコンパイラに対するバグの登録をお願いいたします。レポートには、該当のプログラム、次の診断内容、およびJavaコンパイラに渡されたパラメータをご入力ください。ご協力ありがとうございます。
java.nio.file.AccessDeniedException: C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-item-group-api-v1-client\4.1.7+def88e3a19\fabric-item-group-api-v1-client-4.1.7+def88e3a19.jar
	at java.base/sun.nio.fs.WindowsException.translateToIOException(WindowsException.java:91)
	at java.base/sun.nio.fs.WindowsException.rethrowAsIOException(WindowsException.java:98)
	at java.base/sun.nio.fs.WindowsLinkSupport.getRealPath(WindowsLinkSupport.java:280)
	at java.base/sun.nio.fs.WindowsPath.toRealPath(WindowsPath.java:903)
	at java.base/sun.nio.fs.WindowsPath.toRealPath(WindowsPath.java:42)
	at jdk.zipfs/jdk.nio.zipfs.ZipFileSystemProvider.removeFileSystem(ZipFileSystemProvider.java:319)
	at jdk.zipfs/jdk.nio.zipfs.ZipFileSystem.close(ZipFileSystem.java:550)
	at jdk.compiler/com.sun.tools.javac.file.JavacFileManager$ArchiveContainer.close(JavacFileManager.java:667)
	at jdk.compiler/com.sun.tools.javac.file.JavacFileManager.close(JavacFileManager.java:745)
	at jdk.compiler/com.sun.tools.javac.main.DelegatingJavaFileManager.close(DelegatingJavaFileManager.java:151)
	at jdk.compiler/com.sun.tools.javac.main.Main.compile(Main.java:182)
	at jdk.compiler/com.sun.tools.javac.Main.compile(Main.java:66)
	at jdk.compiler/com.sun.tools.javac.Main.main(Main.java:52)

diff --git a/src/client/java/org/ahaha/untitled/client/UntitledClient.java b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
index 96f2fa5caf72eb792f660392a6ef7bab77503eb9..760523f1a4590b451ae9a35dd4bed823b44da6a1
--- a/src/client/java/org/ahaha/untitled/client/UntitledClient.java
+++ b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
@@ -1,10 +1,32 @@
 package org.ahaha.untitled.client;
 
 import net.fabricmc.api.ClientModInitializer;
+import net.fabricmc.fabric.api.client.event.lifecycle.v1.ClientTickEvents;
+import net.fabricmc.fabric.api.client.keybinding.v1.KeyBindingHelper;
+import net.minecraft.client.option.KeyBinding;
+import net.minecraft.client.util.InputUtil;
+import org.ahaha.untitled.client.itemswap.ItemSwapConfig;
+import org.ahaha.untitled.client.itemswap.ItemSwapController;
+import org.lwjgl.glfw.GLFW;
 
 public class UntitledClient implements ClientModInitializer {
+    private static final String CATEGORY = "key.category.untitled";
+    private static final String SWAP_KEY = "key.untitled.swap_item_a_b";
 
     @Override
     public void onInitializeClient() {
+        ItemSwapController controller = new ItemSwapController(ItemSwapConfig.load());
+        KeyBinding swapKey = KeyBindingHelper.registerKeyBinding(new KeyBinding(
+                SWAP_KEY,
+                InputUtil.Type.KEYSYM,
+                GLFW.GLFW_KEY_UNKNOWN,
+                CATEGORY
+        ));
+
+        ClientTickEvents.END_CLIENT_TICK.register(client -> {
+            while (swapKey.wasPressed()) {
+                controller.toggle(client);
+            }
+        });
     }
 }
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
new file mode 100644
index 0000000000000000000000000000000000000000..3214363102de082962b3401df416e73144012217
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
@@ -0,0 +1,44 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+
+import java.util.OptionalInt;
+
+public final class InventoryItemFinder {
+    public static final int HOTBAR_SIZE = 9;
+    public static final int MAIN_INVENTORY_SIZE = 36;
+
+    public OptionalInt find(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < MAIN_INVENTORY_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findInHotbar(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findEmptyHotbarSlot(PlayerInventory inventory) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            if (inventory.getStack(slot).isEmpty()) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
new file mode 100644
index 0000000000000000000000000000000000000000..e3f80e5c19e04354bc7646bb6acd358895963c32
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
@@ -0,0 +1,94 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.fabricmc.loader.api.FabricLoader;
+import net.minecraft.item.Item;
+import net.minecraft.registry.Registries;
+import net.minecraft.util.Identifier;
+
+import java.io.IOException;
+import java.io.Reader;
+import java.io.Writer;
+import java.nio.file.Files;
+import java.nio.file.Path;
+import java.util.Optional;
+import java.util.Properties;
+
+public final class ItemSwapConfig {
+    private static final String CONFIG_FILE_NAME = "untitled-item-swap.properties";
+    private static final String DEFAULT_ITEM_A = "minecraft:golden_apple";
+    private static final String DEFAULT_ITEM_B = "minecraft:totem_of_undying";
+
+    private final String itemAId;
+    private final String itemBId;
+    private final boolean allowInventorySwap;
+
+    private ItemSwapConfig(String itemAId, String itemBId, boolean allowInventorySwap) {
+        this.itemAId = itemAId;
+        this.itemBId = itemBId;
+        this.allowInventorySwap = allowInventorySwap;
+    }
+
+    public static ItemSwapConfig load() {
+        Path path = FabricLoader.getInstance().getConfigDir().resolve(CONFIG_FILE_NAME);
+        Properties properties = defaultProperties();
+
+        if (Files.exists(path)) {
+            try (Reader reader = Files.newBufferedReader(path)) {
+                properties.load(reader);
+            } catch (IOException ignored) {
+                return fromProperties(properties);
+            }
+        } else {
+            writeDefaults(path, properties);
+        }
+
+        return fromProperties(properties);
+    }
+
+    public Optional<Item> itemA() {
+        return resolveItem(itemAId);
+    }
+
+    public Optional<Item> itemB() {
+        return resolveItem(itemBId);
+    }
+
+    public boolean allowInventorySwap() {
+        return allowInventorySwap;
+    }
+
+    private static Properties defaultProperties() {
+        Properties properties = new Properties();
+        properties.setProperty("itemA", DEFAULT_ITEM_A);
+        properties.setProperty("itemB", DEFAULT_ITEM_B);
+        properties.setProperty("allowInventorySwap", "true");
+        return properties;
+    }
+
+    private static ItemSwapConfig fromProperties(Properties properties) {
+        return new ItemSwapConfig(
+                properties.getProperty("itemA", DEFAULT_ITEM_A).trim(),
+                properties.getProperty("itemB", DEFAULT_ITEM_B).trim(),
+                Boolean.parseBoolean(properties.getProperty("allowInventorySwap", "true").trim())
+        );
+    }
+
+    private static void writeDefaults(Path path, Properties properties) {
+        try {
+            Files.createDirectories(path.getParent());
+            try (Writer writer = Files.newBufferedWriter(path)) {
+                properties.store(writer, "Untitled item A/B swap settings");
+            }
+        } catch (IOException ignored) {
+        }
+    }
+
+    private static Optional<Item> resolveItem(String rawId) {
+        Identifier id = Identifier.tryParse(rawId);
+        if (id == null || !Registries.ITEM.containsId(id)) {
+            return Optional.empty();
+        }
+
+        return Optional.of(Registries.ITEM.get(id));
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
new file mode 100644
index 0000000000000000000000000000000000000000..be476059f0b833dda89a2f83c69bf914bd8830e7
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
@@ -0,0 +1,106 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.client.MinecraftClient;
+import net.minecraft.client.network.ClientPlayerEntity;
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+import net.minecraft.network.packet.c2s.play.UpdateSelectedSlotC2SPacket;
+import net.minecraft.screen.slot.SlotActionType;
+
+import java.util.Optional;
+import java.util.OptionalInt;
+
+public final class ItemSwapController {
+    private final ItemSwapConfig config;
+    private final InventoryItemFinder finder;
+
+    public ItemSwapController(ItemSwapConfig config) {
+        this.config = config;
+        this.finder = new InventoryItemFinder();
+    }
+
+    public boolean toggle(MinecraftClient client) {
+        if (client.player == null || client.interactionManager == null || client.currentScreen != null) {
+            return false;
+        }
+
+        Optional<Item> itemA = config.itemA();
+        Optional<Item> itemB = config.itemB();
+        if (itemA.isEmpty() || itemB.isEmpty()) {
+            return false;
+        }
+
+        ClientPlayerEntity player = client.player;
+        PlayerInventory inventory = player.getInventory();
+        Item target = targetItem(inventory.getStack(inventory.selectedSlot), itemA.get(), itemB.get());
+        OptionalInt targetSlot = finder.find(inventory, target);
+        if (targetSlot.isEmpty()) {
+            return false;
+        }
+
+        return switchToSlot(client, player, inventory, targetSlot.getAsInt(), itemA.get(), itemB.get());
+    }
+
+    private Item targetItem(ItemStack selectedStack, Item itemA, Item itemB) {
+        if (selectedStack.isOf(itemA)) {
+            return itemB;
+        }
+        if (selectedStack.isOf(itemB)) {
+            return itemA;
+        }
+
+        return itemA;
+    }
+
+    private boolean switchToSlot(
+            MinecraftClient client,
+            ClientPlayerEntity player,
+            PlayerInventory inventory,
+            int inventorySlot,
+            Item itemA,
+            Item itemB
+    ) {
+        if (inventorySlot < InventoryItemFinder.HOTBAR_SIZE) {
+            selectHotbarSlot(client, inventory, inventorySlot);
+            return true;
+        }
+
+        if (!config.allowInventorySwap()) {
+            return false;
+        }
+
+        OptionalInt destinationSlot = safeHotbarDestination(inventory, itemA, itemB);
+        if (destinationSlot.isEmpty()) {
+            return false;
+        }
+
+        client.interactionManager.clickSlot(
+                player.playerScreenHandler.syncId,
+                inventorySlot,
+                destinationSlot.getAsInt(),
+                SlotActionType.SWAP,
+                player
+        );
+        selectHotbarSlot(client, inventory, destinationSlot.getAsInt());
+        return true;
+    }
+
+    private OptionalInt safeHotbarDestination(PlayerInventory inventory, Item itemA, Item itemB) {
+        ItemStack selectedStack = inventory.getStack(inventory.selectedSlot);
+        if (selectedStack.isEmpty() || selectedStack.isOf(itemA) || selectedStack.isOf(itemB)) {
+            return OptionalInt.of(inventory.selectedSlot);
+        }
+
+        return finder.findEmptyHotbarSlot(inventory);
+    }
+
+    private void selectHotbarSlot(MinecraftClient client, PlayerInventory inventory, int slot) {
+        if (inventory.selectedSlot == slot) {
+            return;
+        }
+
+        inventory.selectedSlot = slot;
+        client.getNetworkHandler().sendPacket(new UpdateSelectedSlotC2SPacket(slot));
+    }
+}

apply patch
patch: completed
C:\Users\sutec\IdeaProjects\untitled\src\client\java\org\ahaha\untitled\client\UntitledClient.java
diff --git a/src/client/java/org/ahaha/untitled/client/UntitledClient.java b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
index 96f2fa5caf72eb792f660392a6ef7bab77503eb9..b5ee521d4cc9d1a4b303d6ad2106c305e19d603c
--- a/src/client/java/org/ahaha/untitled/client/UntitledClient.java
+++ b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
@@ -1,10 +1,32 @@
 package org.ahaha.untitled.client;
 
 import net.fabricmc.api.ClientModInitializer;
+import net.fabricmc.fabric.api.client.event.lifecycle.v1.ClientTickEvents;
+import net.fabricmc.fabric.api.client.keybinding.v1.KeyBindingHelper;
+import net.minecraft.client.option.KeyBinding;
+import net.minecraft.client.util.InputUtil;
+import org.ahaha.untitled.client.itemswap.ItemSwapConfig;
+import org.ahaha.untitled.client.itemswap.ItemSwapController;
+import org.lwjgl.glfw.GLFW;
 
 public class UntitledClient implements ClientModInitializer {
+    private static final String CATEGORY = "key.category.untitled";
+    private static final String SWAP_KEY = "key.untitled.swap_item_a_b";
 
     @Override
     public void onInitializeClient() {
+        ItemSwapController controller = new ItemSwapController(ItemSwapConfig.load());
+        KeyBinding swapKey = KeyBindingHelper.registerKeyBinding(new KeyBinding(
+                SWAP_KEY,
+                InputUtil.Type.KEYSYM,
+                GLFW.GLFW_KEY_G,
+                CATEGORY
+        ));
+
+        ClientTickEvents.END_CLIENT_TICK.register(client -> {
+            while (swapKey.wasPressed()) {
+                controller.toggle(client);
+            }
+        });
     }
 }
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
new file mode 100644
index 0000000000000000000000000000000000000000..3214363102de082962b3401df416e73144012217
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
@@ -0,0 +1,44 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+
+import java.util.OptionalInt;
+
+public final class InventoryItemFinder {
+    public static final int HOTBAR_SIZE = 9;
+    public static final int MAIN_INVENTORY_SIZE = 36;
+
+    public OptionalInt find(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < MAIN_INVENTORY_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findInHotbar(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findEmptyHotbarSlot(PlayerInventory inventory) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            if (inventory.getStack(slot).isEmpty()) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
new file mode 100644
index 0000000000000000000000000000000000000000..e3f80e5c19e04354bc7646bb6acd358895963c32
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
@@ -0,0 +1,94 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.fabricmc.loader.api.FabricLoader;
+import net.minecraft.item.Item;
+import net.minecraft.registry.Registries;
+import net.minecraft.util.Identifier;
+
+import java.io.IOException;
+import java.io.Reader;
+import java.io.Writer;
+import java.nio.file.Files;
+import java.nio.file.Path;
+import java.util.Optional;
+import java.util.Properties;
+
+public final class ItemSwapConfig {
+    private static final String CONFIG_FILE_NAME = "untitled-item-swap.properties";
+    private static final String DEFAULT_ITEM_A = "minecraft:golden_apple";
+    private static final String DEFAULT_ITEM_B = "minecraft:totem_of_undying";
+
+    private final String itemAId;
+    private final String itemBId;
+    private final boolean allowInventorySwap;
+
+    private ItemSwapConfig(String itemAId, String itemBId, boolean allowInventorySwap) {
+        this.itemAId = itemAId;
+        this.itemBId = itemBId;
+        this.allowInventorySwap = allowInventorySwap;
+    }
+
+    public static ItemSwapConfig load() {
+        Path path = FabricLoader.getInstance().getConfigDir().resolve(CONFIG_FILE_NAME);
+        Properties properties = defaultProperties();
+
+        if (Files.exists(path)) {
+            try (Reader reader = Files.newBufferedReader(path)) {
+                properties.load(reader);
+            } catch (IOException ignored) {
+                return fromProperties(properties);
+            }
+        } else {
+            writeDefaults(path, properties);
+        }
+
+        return fromProperties(properties);
+    }
+
+    public Optional<Item> itemA() {
+        return resolveItem(itemAId);
+    }
+
+    public Optional<Item> itemB() {
+        return resolveItem(itemBId);
+    }
+
+    public boolean allowInventorySwap() {
+        return allowInventorySwap;
+    }
+
+    private static Properties defaultProperties() {
+        Properties properties = new Properties();
+        properties.setProperty("itemA", DEFAULT_ITEM_A);
+        properties.setProperty("itemB", DEFAULT_ITEM_B);
+        properties.setProperty("allowInventorySwap", "true");
+        return properties;
+    }
+
+    private static ItemSwapConfig fromProperties(Properties properties) {
+        return new ItemSwapConfig(
+                properties.getProperty("itemA", DEFAULT_ITEM_A).trim(),
+                properties.getProperty("itemB", DEFAULT_ITEM_B).trim(),
+                Boolean.parseBoolean(properties.getProperty("allowInventorySwap", "true").trim())
+        );
+    }
+
+    private static void writeDefaults(Path path, Properties properties) {
+        try {
+            Files.createDirectories(path.getParent());
+            try (Writer writer = Files.newBufferedWriter(path)) {
+                properties.store(writer, "Untitled item A/B swap settings");
+            }
+        } catch (IOException ignored) {
+        }
+    }
+
+    private static Optional<Item> resolveItem(String rawId) {
+        Identifier id = Identifier.tryParse(rawId);
+        if (id == null || !Registries.ITEM.containsId(id)) {
+            return Optional.empty();
+        }
+
+        return Optional.of(Registries.ITEM.get(id));
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
new file mode 100644
index 0000000000000000000000000000000000000000..be476059f0b833dda89a2f83c69bf914bd8830e7
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
@@ -0,0 +1,106 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.client.MinecraftClient;
+import net.minecraft.client.network.ClientPlayerEntity;
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+import net.minecraft.network.packet.c2s.play.UpdateSelectedSlotC2SPacket;
+import net.minecraft.screen.slot.SlotActionType;
+
+import java.util.Optional;
+import java.util.OptionalInt;
+
+public final class ItemSwapController {
+    private final ItemSwapConfig config;
+    private final InventoryItemFinder finder;
+
+    public ItemSwapController(ItemSwapConfig config) {
+        this.config = config;
+        this.finder = new InventoryItemFinder();
+    }
+
+    public boolean toggle(MinecraftClient client) {
+        if (client.player == null || client.interactionManager == null || client.currentScreen != null) {
+            return false;
+        }
+
+        Optional<Item> itemA = config.itemA();
+        Optional<Item> itemB = config.itemB();
+        if (itemA.isEmpty() || itemB.isEmpty()) {
+            return false;
+        }
+
+        ClientPlayerEntity player = client.player;
+        PlayerInventory inventory = player.getInventory();
+        Item target = targetItem(inventory.getStack(inventory.selectedSlot), itemA.get(), itemB.get());
+        OptionalInt targetSlot = finder.find(inventory, target);
+        if (targetSlot.isEmpty()) {
+            return false;
+        }
+
+        return switchToSlot(client, player, inventory, targetSlot.getAsInt(), itemA.get(), itemB.get());
+    }
+
+    private Item targetItem(ItemStack selectedStack, Item itemA, Item itemB) {
+        if (selectedStack.isOf(itemA)) {
+            return itemB;
+        }
+        if (selectedStack.isOf(itemB)) {
+            return itemA;
+        }
+
+        return itemA;
+    }
+
+    private boolean switchToSlot(
+            MinecraftClient client,
+            ClientPlayerEntity player,
+            PlayerInventory inventory,
+            int inventorySlot,
+            Item itemA,
+            Item itemB
+    ) {
+        if (inventorySlot < InventoryItemFinder.HOTBAR_SIZE) {
+            selectHotbarSlot(client, inventory, inventorySlot);
+            return true;
+        }
+
+        if (!config.allowInventorySwap()) {
+            return false;
+        }
+
+        OptionalInt destinationSlot = safeHotbarDestination(inventory, itemA, itemB);
+        if (destinationSlot.isEmpty()) {
+            return false;
+        }
+
+        client.interactionManager.clickSlot(
+                player.playerScreenHandler.syncId,
+                inventorySlot,
+                destinationSlot.getAsInt(),
+                SlotActionType.SWAP,
+                player
+        );
+        selectHotbarSlot(client, inventory, destinationSlot.getAsInt());
+        return true;
+    }
+
+    private OptionalInt safeHotbarDestination(PlayerInventory inventory, Item itemA, Item itemB) {
+        ItemStack selectedStack = inventory.getStack(inventory.selectedSlot);
+        if (selectedStack.isEmpty() || selectedStack.isOf(itemA) || selectedStack.isOf(itemB)) {
+            return OptionalInt.of(inventory.selectedSlot);
+        }
+
+        return finder.findEmptyHotbarSlot(inventory);
+    }
+
+    private void selectHotbarSlot(MinecraftClient client, PlayerInventory inventory, int slot) {
+        if (inventory.selectedSlot == slot) {
+            return;
+        }
+
+        inventory.selectedSlot = slot;
+        client.getNetworkHandler().sendPacket(new UpdateSelectedSlotC2SPacket(slot));
+    }
+}

diff --git a/src/client/java/org/ahaha/untitled/client/UntitledClient.java b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
index 96f2fa5caf72eb792f660392a6ef7bab77503eb9..b5ee521d4cc9d1a4b303d6ad2106c305e19d603c
--- a/src/client/java/org/ahaha/untitled/client/UntitledClient.java
+++ b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
@@ -1,10 +1,32 @@
 package org.ahaha.untitled.client;
 
 import net.fabricmc.api.ClientModInitializer;
+import net.fabricmc.fabric.api.client.event.lifecycle.v1.ClientTickEvents;
+import net.fabricmc.fabric.api.client.keybinding.v1.KeyBindingHelper;
+import net.minecraft.client.option.KeyBinding;
+import net.minecraft.client.util.InputUtil;
+import org.ahaha.untitled.client.itemswap.ItemSwapConfig;
+import org.ahaha.untitled.client.itemswap.ItemSwapController;
+import org.lwjgl.glfw.GLFW;
 
 public class UntitledClient implements ClientModInitializer {
+    private static final String CATEGORY = "key.category.untitled";
+    private static final String SWAP_KEY = "key.untitled.swap_item_a_b";
 
     @Override
     public void onInitializeClient() {
+        ItemSwapController controller = new ItemSwapController(ItemSwapConfig.load());
+        KeyBinding swapKey = KeyBindingHelper.registerKeyBinding(new KeyBinding(
+                SWAP_KEY,
+                InputUtil.Type.KEYSYM,
+                GLFW.GLFW_KEY_G,
+                CATEGORY
+        ));
+
+        ClientTickEvents.END_CLIENT_TICK.register(client -> {
+            while (swapKey.wasPressed()) {
+                controller.toggle(client);
+            }
+        });
     }
 }
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
new file mode 100644
index 0000000000000000000000000000000000000000..3214363102de082962b3401df416e73144012217
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
@@ -0,0 +1,44 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+
+import java.util.OptionalInt;
+
+public final class InventoryItemFinder {
+    public static final int HOTBAR_SIZE = 9;
+    public static final int MAIN_INVENTORY_SIZE = 36;
+
+    public OptionalInt find(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < MAIN_INVENTORY_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findInHotbar(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findEmptyHotbarSlot(PlayerInventory inventory) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            if (inventory.getStack(slot).isEmpty()) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
new file mode 100644
index 0000000000000000000000000000000000000000..e3f80e5c19e04354bc7646bb6acd358895963c32
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
@@ -0,0 +1,94 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.fabricmc.loader.api.FabricLoader;
+import net.minecraft.item.Item;
+import net.minecraft.registry.Registries;
+import net.minecraft.util.Identifier;
+
+import java.io.IOException;
+import java.io.Reader;
+import java.io.Writer;
+import java.nio.file.Files;
+import java.nio.file.Path;
+import java.util.Optional;
+import java.util.Properties;
+
+public final class ItemSwapConfig {
+    private static final String CONFIG_FILE_NAME = "untitled-item-swap.properties";
+    private static final String DEFAULT_ITEM_A = "minecraft:golden_apple";
+    private static final String DEFAULT_ITEM_B = "minecraft:totem_of_undying";
+
+    private final String itemAId;
+    private final String itemBId;
+    private final boolean allowInventorySwap;
+
+    private ItemSwapConfig(String itemAId, String itemBId, boolean allowInventorySwap) {
+        this.itemAId = itemAId;
+        this.itemBId = itemBId;
+        this.allowInventorySwap = allowInventorySwap;
+    }
+
+    public static ItemSwapConfig load() {
+        Path path = FabricLoader.getInstance().getConfigDir().resolve(CONFIG_FILE_NAME);
+        Properties properties = defaultProperties();
+
+        if (Files.exists(path)) {
+            try (Reader reader = Files.newBufferedReader(path)) {
+                properties.load(reader);
+            } catch (IOException ignored) {
+                return fromProperties(properties);
+            }
+        } else {
+            writeDefaults(path, properties);
+        }
+
+        return fromProperties(properties);
+    }
+
+    public Optional<Item> itemA() {
+        return resolveItem(itemAId);
+    }
+
+    public Optional<Item> itemB() {
+        return resolveItem(itemBId);
+    }
+
+    public boolean allowInventorySwap() {
+        return allowInventorySwap;
+    }
+
+    private static Properties defaultProperties() {
+        Properties properties = new Properties();
+        properties.setProperty("itemA", DEFAULT_ITEM_A);
+        properties.setProperty("itemB", DEFAULT_ITEM_B);
+        properties.setProperty("allowInventorySwap", "true");
+        return properties;
+    }
+
+    private static ItemSwapConfig fromProperties(Properties properties) {
+        return new ItemSwapConfig(
+                properties.getProperty("itemA", DEFAULT_ITEM_A).trim(),
+                properties.getProperty("itemB", DEFAULT_ITEM_B).trim(),
+                Boolean.parseBoolean(properties.getProperty("allowInventorySwap", "true").trim())
+        );
+    }
+
+    private static void writeDefaults(Path path, Properties properties) {
+        try {
+            Files.createDirectories(path.getParent());
+            try (Writer writer = Files.newBufferedWriter(path)) {
+                properties.store(writer, "Untitled item A/B swap settings");
+            }
+        } catch (IOException ignored) {
+        }
+    }
+
+    private static Optional<Item> resolveItem(String rawId) {
+        Identifier id = Identifier.tryParse(rawId);
+        if (id == null || !Registries.ITEM.containsId(id)) {
+            return Optional.empty();
+        }
+
+        return Optional.of(Registries.ITEM.get(id));
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
new file mode 100644
index 0000000000000000000000000000000000000000..be476059f0b833dda89a2f83c69bf914bd8830e7
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
@@ -0,0 +1,106 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.client.MinecraftClient;
+import net.minecraft.client.network.ClientPlayerEntity;
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+import net.minecraft.network.packet.c2s.play.UpdateSelectedSlotC2SPacket;
+import net.minecraft.screen.slot.SlotActionType;
+
+import java.util.Optional;
+import java.util.OptionalInt;
+
+public final class ItemSwapController {
+    private final ItemSwapConfig config;
+    private final InventoryItemFinder finder;
+
+    public ItemSwapController(ItemSwapConfig config) {
+        this.config = config;
+        this.finder = new InventoryItemFinder();
+    }
+
+    public boolean toggle(MinecraftClient client) {
+        if (client.player == null || client.interactionManager == null || client.currentScreen != null) {
+            return false;
+        }
+
+        Optional<Item> itemA = config.itemA();
+        Optional<Item> itemB = config.itemB();
+        if (itemA.isEmpty() || itemB.isEmpty()) {
+            return false;
+        }
+
+        ClientPlayerEntity player = client.player;
+        PlayerInventory inventory = player.getInventory();
+        Item target = targetItem(inventory.getStack(inventory.selectedSlot), itemA.get(), itemB.get());
+        OptionalInt targetSlot = finder.find(inventory, target);
+        if (targetSlot.isEmpty()) {
+            return false;
+        }
+
+        return switchToSlot(client, player, inventory, targetSlot.getAsInt(), itemA.get(), itemB.get());
+    }
+
+    private Item targetItem(ItemStack selectedStack, Item itemA, Item itemB) {
+        if (selectedStack.isOf(itemA)) {
+            return itemB;
+        }
+        if (selectedStack.isOf(itemB)) {
+            return itemA;
+        }
+
+        return itemA;
+    }
+
+    private boolean switchToSlot(
+            MinecraftClient client,
+            ClientPlayerEntity player,
+            PlayerInventory inventory,
+            int inventorySlot,
+            Item itemA,
+            Item itemB
+    ) {
+        if (inventorySlot < InventoryItemFinder.HOTBAR_SIZE) {
+            selectHotbarSlot(client, inventory, inventorySlot);
+            return true;
+        }
+
+        if (!config.allowInventorySwap()) {
+            return false;
+        }
+
+        OptionalInt destinationSlot = safeHotbarDestination(inventory, itemA, itemB);
+        if (destinationSlot.isEmpty()) {
+            return false;
+        }
+
+        client.interactionManager.clickSlot(
+                player.playerScreenHandler.syncId,
+                inventorySlot,
+                destinationSlot.getAsInt(),
+                SlotActionType.SWAP,
+                player
+        );
+        selectHotbarSlot(client, inventory, destinationSlot.getAsInt());
+        return true;
+    }
+
+    private OptionalInt safeHotbarDestination(PlayerInventory inventory, Item itemA, Item itemB) {
+        ItemStack selectedStack = inventory.getStack(inventory.selectedSlot);
+        if (selectedStack.isEmpty() || selectedStack.isOf(itemA) || selectedStack.isOf(itemB)) {
+            return OptionalInt.of(inventory.selectedSlot);
+        }
+
+        return finder.findEmptyHotbarSlot(inventory);
+    }
+
+    private void selectHotbarSlot(MinecraftClient client, PlayerInventory inventory, int slot) {
+        if (inventory.selectedSlot == slot) {
+            return;
+        }
+
+        inventory.selectedSlot = slot;
+        client.getNetworkHandler().sendPacket(new UpdateSelectedSlotC2SPacket(slot));
+    }
+}

exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$javac='"'C:\\Program Files\\JetBrains\\IntelliJ IDEA 2026.1.1\\jbr\\bin\\javac.exe'
"'$out='"'build\\codex-compile-check-4'
New-Item -ItemType Directory -Force "'$out | Out-Null
$jars = @()
$jars += Get-ChildItem -Recurse -File .gradle'"\\loom-cache\\minecraftMaven -Filter '*.jar' | Where-Object { "'$_.Name -notmatch '"'backup|sources' } | Select-Object -ExpandProperty FullName
"'$jars += Get-ChildItem -Recurse -File .gradle'"\\loom-cache\\remapped_mods -Filter '*.jar' | Where-Object { "'$_.Name -notmatch '"'sources' } | Select-Object -ExpandProperty FullName
"'$jars += Get-ChildItem -Recurse -File "$env:USERPROFILE'"\\.gradle\\caches\\modules-2\\files-2.1\" -Filter '*.jar' | Where-Object { "'$_.Name -notmatch '"'sources|javadoc|natives' -and ("'$_.FullName -match '"'fabric-loader|org.lwjgl|org.slf4j|datafixerupper|brigadier|authlib|guava|gson|fastutil') } | Select-Object -ExpandProperty FullName
"'$cp = (($jars | Select-Object -Unique) -join '"';').Replace('\\','/')
"'$sources = Get-ChildItem -Recurse -File src'"\\main\\java,src\\client\\java -Filter '*.java' | ForEach-Object { "'$_.FullName.Replace('"'\\','/') }
"'$argsFile = Join-Path $out '"'javac.args'
"'$dest = (Resolve-Path $out).Path.Replace('"'\\','/')
"'$lines = @('"'-encoding','UTF-8','--release','21','-classpath',\""'$cp",'"'-d',\""'$dest") + $sources
Set-Content -Path $argsFile -Value $lines -Encoding ASCII
& $javac "@$argsFile"' in C:\Users\sutec\IdeaProjects\untitled
 succeeded in 2561ms:
コンパイラで例外が発生しました(25.0.2)。バグ・データベース(https://bugs.java.com)で重複がないかをご確認のうえ、Javaのバグ・レポート・ページ(https://bugreport.java.com)から、Javaコンパイラに対するバグの登録をお願いいたします。レポートには、該当のプログラム、次の診断内容、およびJavaコンパイラに渡されたパラメータをご入力ください。ご協力ありがとうございます。
java.nio.file.AccessDeniedException: C:\Users\sutec\IdeaProjects\untitled\.gradle\loom-cache\remapped_mods\net_fabricmc_yarn_1_21_1_1_21_1_build_3_v2\net\fabricmc\fabric-api\fabric-item-group-api-v1-client\4.1.7+def88e3a19\fabric-item-group-api-v1-client-4.1.7+def88e3a19.jar
	at java.base/sun.nio.fs.WindowsException.translateToIOException(WindowsException.java:91)
	at java.base/sun.nio.fs.WindowsException.rethrowAsIOException(WindowsException.java:98)
	at java.base/sun.nio.fs.WindowsLinkSupport.getRealPath(WindowsLinkSupport.java:280)
	at java.base/sun.nio.fs.WindowsPath.toRealPath(WindowsPath.java:903)
	at java.base/sun.nio.fs.WindowsPath.toRealPath(WindowsPath.java:42)
	at jdk.zipfs/jdk.nio.zipfs.ZipFileSystemProvider.removeFileSystem(ZipFileSystemProvider.java:319)
	at jdk.zipfs/jdk.nio.zipfs.ZipFileSystem.close(ZipFileSystem.java:550)
	at jdk.compiler/com.sun.tools.javac.file.JavacFileManager$ArchiveContainer.close(JavacFileManager.java:667)
	at jdk.compiler/com.sun.tools.javac.file.JavacFileManager.close(JavacFileManager.java:745)
	at jdk.compiler/com.sun.tools.javac.main.DelegatingJavaFileManager.close(DelegatingJavaFileManager.java:151)
	at jdk.compiler/com.sun.tools.javac.main.Main.compile(Main.java:182)
	at jdk.compiler/com.sun.tools.javac.Main.compile(Main.java:66)
	at jdk.compiler/com.sun.tools.javac.Main.main(Main.java:52)

diff --git a/src/client/java/org/ahaha/untitled/client/UntitledClient.java b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
index 96f2fa5caf72eb792f660392a6ef7bab77503eb9..b5ee521d4cc9d1a4b303d6ad2106c305e19d603c
--- a/src/client/java/org/ahaha/untitled/client/UntitledClient.java
+++ b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
@@ -1,10 +1,32 @@
 package org.ahaha.untitled.client;
 
 import net.fabricmc.api.ClientModInitializer;
+import net.fabricmc.fabric.api.client.event.lifecycle.v1.ClientTickEvents;
+import net.fabricmc.fabric.api.client.keybinding.v1.KeyBindingHelper;
+import net.minecraft.client.option.KeyBinding;
+import net.minecraft.client.util.InputUtil;
+import org.ahaha.untitled.client.itemswap.ItemSwapConfig;
+import org.ahaha.untitled.client.itemswap.ItemSwapController;
+import org.lwjgl.glfw.GLFW;
 
 public class UntitledClient implements ClientModInitializer {
+    private static final String CATEGORY = "key.category.untitled";
+    private static final String SWAP_KEY = "key.untitled.swap_item_a_b";
 
     @Override
     public void onInitializeClient() {
+        ItemSwapController controller = new ItemSwapController(ItemSwapConfig.load());
+        KeyBinding swapKey = KeyBindingHelper.registerKeyBinding(new KeyBinding(
+                SWAP_KEY,
+                InputUtil.Type.KEYSYM,
+                GLFW.GLFW_KEY_G,
+                CATEGORY
+        ));
+
+        ClientTickEvents.END_CLIENT_TICK.register(client -> {
+            while (swapKey.wasPressed()) {
+                controller.toggle(client);
+            }
+        });
     }
 }
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
new file mode 100644
index 0000000000000000000000000000000000000000..3214363102de082962b3401df416e73144012217
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
@@ -0,0 +1,44 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+
+import java.util.OptionalInt;
+
+public final class InventoryItemFinder {
+    public static final int HOTBAR_SIZE = 9;
+    public static final int MAIN_INVENTORY_SIZE = 36;
+
+    public OptionalInt find(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < MAIN_INVENTORY_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findInHotbar(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findEmptyHotbarSlot(PlayerInventory inventory) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            if (inventory.getStack(slot).isEmpty()) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
new file mode 100644
index 0000000000000000000000000000000000000000..e3f80e5c19e04354bc7646bb6acd358895963c32
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
@@ -0,0 +1,94 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.fabricmc.loader.api.FabricLoader;
+import net.minecraft.item.Item;
+import net.minecraft.registry.Registries;
+import net.minecraft.util.Identifier;
+
+import java.io.IOException;
+import java.io.Reader;
+import java.io.Writer;
+import java.nio.file.Files;
+import java.nio.file.Path;
+import java.util.Optional;
+import java.util.Properties;
+
+public final class ItemSwapConfig {
+    private static final String CONFIG_FILE_NAME = "untitled-item-swap.properties";
+    private static final String DEFAULT_ITEM_A = "minecraft:golden_apple";
+    private static final String DEFAULT_ITEM_B = "minecraft:totem_of_undying";
+
+    private final String itemAId;
+    private final String itemBId;
+    private final boolean allowInventorySwap;
+
+    private ItemSwapConfig(String itemAId, String itemBId, boolean allowInventorySwap) {
+        this.itemAId = itemAId;
+        this.itemBId = itemBId;
+        this.allowInventorySwap = allowInventorySwap;
+    }
+
+    public static ItemSwapConfig load() {
+        Path path = FabricLoader.getInstance().getConfigDir().resolve(CONFIG_FILE_NAME);
+        Properties properties = defaultProperties();
+
+        if (Files.exists(path)) {
+            try (Reader reader = Files.newBufferedReader(path)) {
+                properties.load(reader);
+            } catch (IOException ignored) {
+                return fromProperties(properties);
+            }
+        } else {
+            writeDefaults(path, properties);
+        }
+
+        return fromProperties(properties);
+    }
+
+    public Optional<Item> itemA() {
+        return resolveItem(itemAId);
+    }
+
+    public Optional<Item> itemB() {
+        return resolveItem(itemBId);
+    }
+
+    public boolean allowInventorySwap() {
+        return allowInventorySwap;
+    }
+
+    private static Properties defaultProperties() {
+        Properties properties = new Properties();
+        properties.setProperty("itemA", DEFAULT_ITEM_A);
+        properties.setProperty("itemB", DEFAULT_ITEM_B);
+        properties.setProperty("allowInventorySwap", "true");
+        return properties;
+    }
+
+    private static ItemSwapConfig fromProperties(Properties properties) {
+        return new ItemSwapConfig(
+                properties.getProperty("itemA", DEFAULT_ITEM_A).trim(),
+                properties.getProperty("itemB", DEFAULT_ITEM_B).trim(),
+                Boolean.parseBoolean(properties.getProperty("allowInventorySwap", "true").trim())
+        );
+    }
+
+    private static void writeDefaults(Path path, Properties properties) {
+        try {
+            Files.createDirectories(path.getParent());
+            try (Writer writer = Files.newBufferedWriter(path)) {
+                properties.store(writer, "Untitled item A/B swap settings");
+            }
+        } catch (IOException ignored) {
+        }
+    }
+
+    private static Optional<Item> resolveItem(String rawId) {
+        Identifier id = Identifier.tryParse(rawId);
+        if (id == null || !Registries.ITEM.containsId(id)) {
+            return Optional.empty();
+        }
+
+        return Optional.of(Registries.ITEM.get(id));
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
new file mode 100644
index 0000000000000000000000000000000000000000..be476059f0b833dda89a2f83c69bf914bd8830e7
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
@@ -0,0 +1,106 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.client.MinecraftClient;
+import net.minecraft.client.network.ClientPlayerEntity;
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+import net.minecraft.network.packet.c2s.play.UpdateSelectedSlotC2SPacket;
+import net.minecraft.screen.slot.SlotActionType;
+
+import java.util.Optional;
+import java.util.OptionalInt;
+
+public final class ItemSwapController {
+    private final ItemSwapConfig config;
+    private final InventoryItemFinder finder;
+
+    public ItemSwapController(ItemSwapConfig config) {
+        this.config = config;
+        this.finder = new InventoryItemFinder();
+    }
+
+    public boolean toggle(MinecraftClient client) {
+        if (client.player == null || client.interactionManager == null || client.currentScreen != null) {
+            return false;
+        }
+
+        Optional<Item> itemA = config.itemA();
+        Optional<Item> itemB = config.itemB();
+        if (itemA.isEmpty() || itemB.isEmpty()) {
+            return false;
+        }
+
+        ClientPlayerEntity player = client.player;
+        PlayerInventory inventory = player.getInventory();
+        Item target = targetItem(inventory.getStack(inventory.selectedSlot), itemA.get(), itemB.get());
+        OptionalInt targetSlot = finder.find(inventory, target);
+        if (targetSlot.isEmpty()) {
+            return false;
+        }
+
+        return switchToSlot(client, player, inventory, targetSlot.getAsInt(), itemA.get(), itemB.get());
+    }
+
+    private Item targetItem(ItemStack selectedStack, Item itemA, Item itemB) {
+        if (selectedStack.isOf(itemA)) {
+            return itemB;
+        }
+        if (selectedStack.isOf(itemB)) {
+            return itemA;
+        }
+
+        return itemA;
+    }
+
+    private boolean switchToSlot(
+            MinecraftClient client,
+            ClientPlayerEntity player,
+            PlayerInventory inventory,
+            int inventorySlot,
+            Item itemA,
+            Item itemB
+    ) {
+        if (inventorySlot < InventoryItemFinder.HOTBAR_SIZE) {
+            selectHotbarSlot(client, inventory, inventorySlot);
+            return true;
+        }
+
+        if (!config.allowInventorySwap()) {
+            return false;
+        }
+
+        OptionalInt destinationSlot = safeHotbarDestination(inventory, itemA, itemB);
+        if (destinationSlot.isEmpty()) {
+            return false;
+        }
+
+        client.interactionManager.clickSlot(
+                player.playerScreenHandler.syncId,
+                inventorySlot,
+                destinationSlot.getAsInt(),
+                SlotActionType.SWAP,
+                player
+        );
+        selectHotbarSlot(client, inventory, destinationSlot.getAsInt());
+        return true;
+    }
+
+    private OptionalInt safeHotbarDestination(PlayerInventory inventory, Item itemA, Item itemB) {
+        ItemStack selectedStack = inventory.getStack(inventory.selectedSlot);
+        if (selectedStack.isEmpty() || selectedStack.isOf(itemA) || selectedStack.isOf(itemB)) {
+            return OptionalInt.of(inventory.selectedSlot);
+        }
+
+        return finder.findEmptyHotbarSlot(inventory);
+    }
+
+    private void selectHotbarSlot(MinecraftClient client, PlayerInventory inventory, int slot) {
+        if (inventory.selectedSlot == slot) {
+            return;
+        }
+
+        inventory.selectedSlot = slot;
+        client.getNetworkHandler().sendPacket(new UpdateSelectedSlotC2SPacket(slot));
+    }
+}

codex
変更内容:
- `config/untitled-item-swap.properties` から `itemA` / `itemB` / `allowInventorySwap` を読み込む基盤を追加
- インベントリ/ホットバー内のA/B検出と、`G`キーによるA/B切り替えを追加
- 対象がない、画面操作中、満杯かつ安全な退避先がない場合は何もしない安全動作にした

確認結果:
- 簡易 `javac` 確認は終了コード0。ソース診断エラーなし
- Gradle実行は環境都合で未完了: Gradle 8 はJava 25非対応、Gradle 9 は `fabric-loom:1.8-SNAPSHOT` を解決不可

残課題:
- Minecraft実行環境でのホットバー位置違い、対象なし、インベントリ満杯時の実機確認が未実施。
diff --git a/src/client/java/org/ahaha/untitled/client/UntitledClient.java b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
index 96f2fa5caf72eb792f660392a6ef7bab77503eb9..b5ee521d4cc9d1a4b303d6ad2106c305e19d603c
--- a/src/client/java/org/ahaha/untitled/client/UntitledClient.java
+++ b/src/client/java/org/ahaha/untitled/client/UntitledClient.java
@@ -1,10 +1,32 @@
 package org.ahaha.untitled.client;
 
 import net.fabricmc.api.ClientModInitializer;
+import net.fabricmc.fabric.api.client.event.lifecycle.v1.ClientTickEvents;
+import net.fabricmc.fabric.api.client.keybinding.v1.KeyBindingHelper;
+import net.minecraft.client.option.KeyBinding;
+import net.minecraft.client.util.InputUtil;
+import org.ahaha.untitled.client.itemswap.ItemSwapConfig;
+import org.ahaha.untitled.client.itemswap.ItemSwapController;
+import org.lwjgl.glfw.GLFW;
 
 public class UntitledClient implements ClientModInitializer {
+    private static final String CATEGORY = "key.category.untitled";
+    private static final String SWAP_KEY = "key.untitled.swap_item_a_b";
 
     @Override
     public void onInitializeClient() {
+        ItemSwapController controller = new ItemSwapController(ItemSwapConfig.load());
+        KeyBinding swapKey = KeyBindingHelper.registerKeyBinding(new KeyBinding(
+                SWAP_KEY,
+                InputUtil.Type.KEYSYM,
+                GLFW.GLFW_KEY_G,
+                CATEGORY
+        ));
+
+        ClientTickEvents.END_CLIENT_TICK.register(client -> {
+            while (swapKey.wasPressed()) {
+                controller.toggle(client);
+            }
+        });
     }
 }
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
new file mode 100644
index 0000000000000000000000000000000000000000..3214363102de082962b3401df416e73144012217
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/InventoryItemFinder.java
@@ -0,0 +1,44 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+
+import java.util.OptionalInt;
+
+public final class InventoryItemFinder {
+    public static final int HOTBAR_SIZE = 9;
+    public static final int MAIN_INVENTORY_SIZE = 36;
+
+    public OptionalInt find(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < MAIN_INVENTORY_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findInHotbar(PlayerInventory inventory, Item item) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            ItemStack stack = inventory.getStack(slot);
+            if (stack.isOf(item)) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+
+    public OptionalInt findEmptyHotbarSlot(PlayerInventory inventory) {
+        for (int slot = 0; slot < HOTBAR_SIZE; slot++) {
+            if (inventory.getStack(slot).isEmpty()) {
+                return OptionalInt.of(slot);
+            }
+        }
+
+        return OptionalInt.empty();
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
new file mode 100644
index 0000000000000000000000000000000000000000..e3f80e5c19e04354bc7646bb6acd358895963c32
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapConfig.java
@@ -0,0 +1,94 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.fabricmc.loader.api.FabricLoader;
+import net.minecraft.item.Item;
+import net.minecraft.registry.Registries;
+import net.minecraft.util.Identifier;
+
+import java.io.IOException;
+import java.io.Reader;
+import java.io.Writer;
+import java.nio.file.Files;
+import java.nio.file.Path;
+import java.util.Optional;
+import java.util.Properties;
+
+public final class ItemSwapConfig {
+    private static final String CONFIG_FILE_NAME = "untitled-item-swap.properties";
+    private static final String DEFAULT_ITEM_A = "minecraft:golden_apple";
+    private static final String DEFAULT_ITEM_B = "minecraft:totem_of_undying";
+
+    private final String itemAId;
+    private final String itemBId;
+    private final boolean allowInventorySwap;
+
+    private ItemSwapConfig(String itemAId, String itemBId, boolean allowInventorySwap) {
+        this.itemAId = itemAId;
+        this.itemBId = itemBId;
+        this.allowInventorySwap = allowInventorySwap;
+    }
+
+    public static ItemSwapConfig load() {
+        Path path = FabricLoader.getInstance().getConfigDir().resolve(CONFIG_FILE_NAME);
+        Properties properties = defaultProperties();
+
+        if (Files.exists(path)) {
+            try (Reader reader = Files.newBufferedReader(path)) {
+                properties.load(reader);
+            } catch (IOException ignored) {
+                return fromProperties(properties);
+            }
+        } else {
+            writeDefaults(path, properties);
+        }
+
+        return fromProperties(properties);
+    }
+
+    public Optional<Item> itemA() {
+        return resolveItem(itemAId);
+    }
+
+    public Optional<Item> itemB() {
+        return resolveItem(itemBId);
+    }
+
+    public boolean allowInventorySwap() {
+        return allowInventorySwap;
+    }
+
+    private static Properties defaultProperties() {
+        Properties properties = new Properties();
+        properties.setProperty("itemA", DEFAULT_ITEM_A);
+        properties.setProperty("itemB", DEFAULT_ITEM_B);
+        properties.setProperty("allowInventorySwap", "true");
+        return properties;
+    }
+
+    private static ItemSwapConfig fromProperties(Properties properties) {
+        return new ItemSwapConfig(
+                properties.getProperty("itemA", DEFAULT_ITEM_A).trim(),
+                properties.getProperty("itemB", DEFAULT_ITEM_B).trim(),
+                Boolean.parseBoolean(properties.getProperty("allowInventorySwap", "true").trim())
+        );
+    }
+
+    private static void writeDefaults(Path path, Properties properties) {
+        try {
+            Files.createDirectories(path.getParent());
+            try (Writer writer = Files.newBufferedWriter(path)) {
+                properties.store(writer, "Untitled item A/B swap settings");
+            }
+        } catch (IOException ignored) {
+        }
+    }
+
+    private static Optional<Item> resolveItem(String rawId) {
+        Identifier id = Identifier.tryParse(rawId);
+        if (id == null || !Registries.ITEM.containsId(id)) {
+            return Optional.empty();
+        }
+
+        return Optional.of(Registries.ITEM.get(id));
+    }
+}
diff --git a/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
new file mode 100644
index 0000000000000000000000000000000000000000..be476059f0b833dda89a2f83c69bf914bd8830e7
--- /dev/null
+++ b/src/client/java/org/ahaha/untitled/client/itemswap/ItemSwapController.java
@@ -0,0 +1,106 @@
+package org.ahaha.untitled.client.itemswap;
+
+import net.minecraft.client.MinecraftClient;
+import net.minecraft.client.network.ClientPlayerEntity;
+import net.minecraft.entity.player.PlayerInventory;
+import net.minecraft.item.Item;
+import net.minecraft.item.ItemStack;
+import net.minecraft.network.packet.c2s.play.UpdateSelectedSlotC2SPacket;
+import net.minecraft.screen.slot.SlotActionType;
+
+import java.util.Optional;
+import java.util.OptionalInt;
+
+public final class ItemSwapController {
+    private final ItemSwapConfig config;
+    private final InventoryItemFinder finder;
+
+    public ItemSwapController(ItemSwapConfig config) {
+        this.config = config;
+        this.finder = new InventoryItemFinder();
+    }
+
+    public boolean toggle(MinecraftClient client) {
+        if (client.player == null || client.interactionManager == null || client.currentScreen != null) {
+            return false;
+        }
+
+        Optional<Item> itemA = config.itemA();
+        Optional<Item> itemB = config.itemB();
+        if (itemA.isEmpty() || itemB.isEmpty()) {
+            return false;
+        }
+
+        ClientPlayerEntity player = client.player;
+        PlayerInventory inventory = player.getInventory();
+        Item target = targetItem(inventory.getStack(inventory.selectedSlot), itemA.get(), itemB.get());
+        OptionalInt targetSlot = finder.find(inventory, target);
+        if (targetSlot.isEmpty()) {
+            return false;
+        }
+
+        return switchToSlot(client, player, inventory, targetSlot.getAsInt(), itemA.get(), itemB.get());
+    }
+
+    private Item targetItem(ItemStack selectedStack, Item itemA, Item itemB) {
+        if (selectedStack.isOf(itemA)) {
+            return itemB;
+        }
+        if (selectedStack.isOf(itemB)) {
+            return itemA;
+        }
+
+        return itemA;
+    }
+
+    private boolean switchToSlot(
+            MinecraftClient client,
+            ClientPlayerEntity player,
+            PlayerInventory inventory,
+            int inventorySlot,
+            Item itemA,
+            Item itemB
+    ) {
+        if (inventorySlot < InventoryItemFinder.HOTBAR_SIZE) {
+            selectHotbarSlot(client, inventory, inventorySlot);
+            return true;
+        }
+
+        if (!config.allowInventorySwap()) {
+            return false;
+        }
+
+        OptionalInt destinationSlot = safeHotbarDestination(inventory, itemA, itemB);
+        if (destinationSlot.isEmpty()) {
+            return false;
+        }
+
+        client.interactionManager.clickSlot(
+                player.playerScreenHandler.syncId,
+                inventorySlot,
+                destinationSlot.getAsInt(),
+                SlotActionType.SWAP,
+                player
+        );
+        selectHotbarSlot(client, inventory, destinationSlot.getAsInt());
+        return true;
+    }
+
+    private OptionalInt safeHotbarDestination(PlayerInventory inventory, Item itemA, Item itemB) {
+        ItemStack selectedStack = inventory.getStack(inventory.selectedSlot);
+        if (selectedStack.isEmpty() || selectedStack.isOf(itemA) || selectedStack.isOf(itemB)) {
+            return OptionalInt.of(inventory.selectedSlot);
+        }
+
+        return finder.findEmptyHotbarSlot(inventory);
+    }
+
+    private void selectHotbarSlot(MinecraftClient client, PlayerInventory inventory, int slot) {
+        if (inventory.selectedSlot == slot) {
+            return;
+        }
+
+        inventory.selectedSlot = slot;
+        client.getNetworkHandler().sendPacket(new UpdateSelectedSlotC2SPacket(slot));
+    }
+}

tokens used
123,556
